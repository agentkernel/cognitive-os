# Rust
/target/

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Downloaded campaign packages, installers, runtime archives, and execution
# evidence are local staging material. Commit only their verified digest,
# attestation reference, and sanitized facts in the task evidence records.
/artifacts/

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Local personal notes (not Cos deliverables)
docs/_local/


# Nested CognitiveOS Research blog (independent git; never push to Cos origin/main)
/personal-blog/
