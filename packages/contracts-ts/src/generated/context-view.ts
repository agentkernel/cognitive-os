// @generated by contracts-codegen v0.2.0. DO NOT EDIT.
// source: specs/schemas/context-view.schema.json
// schema_digest: sha256:4051718506d03fdbaf4fc6ef472567a4ff5544b0267dfec25710cde398bb88a3 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { GovernedObjectHeader } from "./governed-object-header.js";

import type { StrongReference } from "./object-reference.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "context-view.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:4051718506d03fdbaf4fc6ef472567a4ff5544b0267dfec25710cde398bb88a3";

export interface ResolutionCost {
  bytes: number;
  money_microunits?: number;
  resolve_ms: number;
  tokens?: number;
}

export interface ItemCost {
  bytes: number;
  tokens?: number;
}

export type LoadedContextItemRepresentation = "structured" | "text" | "binary_ref";

export type LoadedContextItemRole = "control" | "authoritative_state" | "evidence" | "working" | "untrusted_input";

export type LoadedContextItemTrustLevel = "control" | "authoritative" | "verified" | "untrusted";

export interface LoadedContextItem {
  cost: ItemCost;
  item_id: string;
  object_ref: StrongReference;
  representation: LoadedContextItemRepresentation;
  role: LoadedContextItemRole;
  trust_level: LoadedContextItemTrustLevel;
}

export interface LossDeclaration {
  omitted_classes: string[];
  source: string;
  transform: string;
  verification: string;
}

export type ContextViewPinnedVersionsValue = number | string;

export interface RejectedCandidate {
  candidate_ref: string;
  reason: string;
}

export interface ContextView {
  activity_bound: string;
  complete: boolean;
  cost: ResolutionCost;
  header: GovernedObjectHeader;
  loaded: LoadedContextItem[];
  loss_declaration: LossDeclaration[];
  missing?: string[];
  pinned_versions: Record<string, ContextViewPinnedVersionsValue>;
  rejected: RejectedCandidate[];
  request_ref: StrongReference;
}
