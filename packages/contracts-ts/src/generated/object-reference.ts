// @generated by contracts-codegen v0.2.0. DO NOT EDIT.
// source: specs/schemas/object-reference.schema.json
// schema_digest: sha256:1e1910c8e829a9beee36d7c86aabbbb7f5d0fefb1abc3f80629f4302754424a5 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Digest, UriRef } from "./common-defs.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "object-reference.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:1e1910c8e829a9beee36d7c86aabbbb7f5d0fefb1abc3f80629f4302754424a5";

export type FreshnessMode = "latest_authorized" | "max_age" | "not_older_than" | "minimum_version";

export interface Freshness {
  max_age_ms?: number;
  minimum_object_version?: number;
  mode: FreshnessMode;
  not_older_than?: string;
}

export type ResolutionOnUnavailable = "fail_closed" | "use_fresh_authorized_cache";

export type ResolutionResolveAt = "read" | "activity_start" | "authorization" | "commit";

export type ResolutionStrategy = "authority_current" | "authority_as_of" | "local_authorized_cache";

export interface Resolution {
  as_of?: string;
  on_unavailable: ResolutionOnUnavailable;
  pin_result: boolean;
  resolve_at: ResolutionResolveAt;
  resolver_policy_ref?: UriRef;
  strategy: ResolutionStrategy;
}

/** Pattern: `^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$` */
export type UuidV7 = string;

export type StrongReferenceKind = "strong";

export interface StrongReference {
  content_digest: Digest;
  id: UuidV7;
  kind: StrongReferenceKind;
  object_version: number;
}

export type WeakReferenceKind = "weak";

export interface WeakReference {
  freshness: Freshness;
  id: UuidV7;
  kind: WeakReferenceKind;
  resolution: Resolution;
}

/** CognitiveOS v0.1 Draft normative strong and weak object reference contract. */
export type ObjectReference = StrongReference | WeakReference;
