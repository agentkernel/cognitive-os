// @generated by contracts-codegen v0.2.0. DO NOT EDIT.
// source: specs/schemas/event.schema.json
// schema_digest: sha256:e9e0b36dd588c730b5081bf35030f892461197a39410dff1718fcf81bbc0be84 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Digest } from "./common-defs.js";

import type { GovernedObjectHeader } from "./governed-object-header.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "event.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:e9e0b36dd588c730b5081bf35030f892461197a39410dff1718fcf81bbc0be84";

export type AckPolicyConsumerOffset = "none" | "optional" | "required";

export type AckPolicyMode = "none" | "explicit";

export interface AckPolicy {
  consumer_offset?: AckPolicyConsumerOffset;
  mode: AckPolicyMode;
}

export type BackpressurePolicyMode = "bounded_block" | "reject" | "spill";

export type BackpressurePolicyOverflow = "reject" | "spill" | "drop_telemetry";

export interface BackpressurePolicy {
  mode: BackpressurePolicyMode;
  overflow: BackpressurePolicyOverflow;
}

export type EventDeliveryClass = "at_most_once" | "at_least_once";

export interface Event {
  ack: AckPolicy;
  backpressure: BackpressurePolicy;
  causation_id: string | null;
  correlation_id: string;
  deadline: string | null;
  delivery_class: EventDeliveryClass;
  event_time: string;
  event_type: string;
  header: GovernedObjectHeader;
  immutable: boolean;
  ingest_time: string;
  payload?: unknown;
  payload_ref?: string;
  schema_digest: Digest;
  source: string;
  subject: string;
}
