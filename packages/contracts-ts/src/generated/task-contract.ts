// @generated by contracts-codegen v0.2.1. DO NOT EDIT.
// source: specs/schemas/task-contract.schema.json
// schema_digest: sha256:a5f8b7ecd3bc78aeede571e24f9c4103cc5cbb2d73accf44d791b58ed532a072 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Budget } from "./common-defs.js";

import type { GovernedObjectHeader } from "./governed-object-header.js";

import type { StrongReference } from "./object-reference.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "task-contract.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:a5f8b7ecd3bc78aeede571e24f9c4103cc5cbb2d73accf44d791b58ed532a072";

export type ContractConditionKind = "acceptance" | "stop" | "escalation" | "wait" | "constraint";

export interface ContractCondition {
  description: string;
  id: string;
  kind: ContractConditionKind;
  machine_expression?: Record<string, unknown>;
  verifier_ref?: string;
}

export interface HumanGate {
  approver?: string;
  id: string;
  trigger: string;
}

export interface TaskScope {
  in_scope: string[];
  out_of_scope: string[];
}

export interface TaskContract {
  /** Authority-managed state domain names; the domain registry (specs/registry/state-domains.yaml) is open (domain_name_closed_enum: false), so values are registry domain identifiers, not a closed enum. */
  allowed_state_domains: string[];
  allowed_tools: string[];
  budget: Budget;
  conditions: ContractCondition[];
  contract_epoch: number;
  header: GovernedObjectHeader;
  human_gates?: HumanGate[];
  intent_acceptance_ref: StrongReference;
  intent_interpretation_ref: StrongReference;
  max_iterations: number;
  max_retries: number;
  objective: string;
  scope: TaskScope;
  task_ref: string;
  user_intent_ref: StrongReference;
}
