// @generated by contracts-codegen v0.2.0. DO NOT EDIT.
// source: specs/schemas/akp-request-envelope.schema.json
// schema_digest: sha256:feeaeb0942ce2796d0155b4b9c316a87cca94eccbf7b0fd7b031a2135dd7ee7b (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Budget, Digest, UriRef } from "./common-defs.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "akp-request-envelope.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:feeaeb0942ce2796d0155b4b9c316a87cca94eccbf7b0fd7b031a2135dd7ee7b";

/** An extension entry is `{id, critical}`; receivers reject unknown critical extensions before any payload processing (REQ-AKP-ENV-002) and treat malformed entries as critical. */
export interface Extension {
  critical: boolean;
  id: string;
}

export type AkpRequestEnvelopeProtocolVersion = "cognitiveos.akp/0.2";

/** AKP request envelope wire shape (specs/akp/README.md sections 3, 6 and 10.1; REQ-AKP-ENV-001, REQ-AKP-ENV-002, REQ-AKP-MGMT-001). Registers the machine schema for the envelope members the companion describes. Behavioral gates stay runtime contracts: version negotiation, unknown-critical-extension rejection before payload processing, payload-digest verification, and the idempotency-key requirement for effecting operations. */
export interface AkpRequestEnvelope {
  activity_context_ref?: UriRef;
  actor_chain_digest?: Digest;
  approval_refs?: UriRef[];
  audience: UriRef;
  authorization_ref?: UriRef;
  budget?: Budget;
  causation_id?: string;
  correlation_id: string;
  deadline: string;
  expected_versions?: Record<string, number>;
  extensions?: Extension[];
  idempotency_key?: string;
  management_session_ref?: UriRef;
  message_id: string;
  operation: string;
  payload?: unknown;
  payload_digest?: Digest;
  payload_ref?: UriRef;
  policy_version?: number;
  proposal_ref?: UriRef;
  protocol_version: AkpRequestEnvelopeProtocolVersion;
  revocation_epoch?: number;
  schema_digest: Digest;
  sender: UriRef;
}
