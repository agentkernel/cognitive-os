// @generated by contracts-codegen v0.2.1. DO NOT EDIT.
// source: specs/schemas/management-approval-request.schema.json
// schema_digest: sha256:92c34b321b6179fd9f6968deca9460553b392d27ba837b391f35d7757c93eb4e (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Digest, UriRef } from "./common-defs.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "management-approval-request.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:92c34b321b6179fd9f6968deca9460553b392d27ba837b391f35d7757c93eb4e";

/** Tiered completion surface (whitepaper section 12.12 matrix): policy_auto (R0), chat_structured (R1 in-conversation structured confirmation), trusted_surface (R2 credential-signed trusted surface), dual_independent (R3 dual-principal with step-up). */
export type ManagementApprovalRequestConfirmationSurface = "policy_auto" | "chat_structured" | "trusted_surface" | "dual_independent";

export type ManagementApprovalRequestRiskClass = "R0" | "R1" | "R2" | "R3";

export type ManagementApprovalRequestSchemaVersion = "cognitiveos.management-approval-request/0.1";

/** OS-issued approval request/challenge card (F-011 R1 registration). Registers the structured exchange described by RFC-0001 section 7.6 (natural language only triggers a structured challenge; it is never itself an approval), REQ-AKP-MGMT-002 (a challenge binds proposal digest, session, human principal, method, expiry and a single/limited-use policy) and the whitepaper section 12.12 tiered-confirmation baseline (IMP-05). The confirmation surface is tiered by risk class and fails closed: a chat channel is a notification and low-risk confirmation surface only; R2/R3 completion surfaces leave the conversation channel. Channel-identity independence from the workload under review, one-time challenge freshness, per-source rate limiting and aggregation remain behavioral gates. */
export interface ManagementApprovalRequest {
  aggregation_key?: string;
  challenge_digest: Digest;
  /** OS-held channel identity independent of the workload under review (section 12.12 mechanism constraint; independence itself is verified behaviorally). */
  channel_identity: UriRef;
  /** Tiered completion surface (whitepaper section 12.12 matrix): policy_auto (R0), chat_structured (R1 in-conversation structured confirmation), trusted_surface (R2 credential-signed trusted surface), dual_independent (R3 dual-principal with step-up). */
  confirmation_surface: ManagementApprovalRequestConfirmationSurface;
  expires_at: string;
  human_principal: UriRef;
  method: string;
  proposal_digest: Digest;
  proposal_ref: UriRef;
  proposer_actor_chain_digest: Digest;
  proposer_principal: UriRef;
  request_id: string;
  requested_at: string;
  risk_class: ManagementApprovalRequestRiskClass;
  schema_version: ManagementApprovalRequestSchemaVersion;
  session_ref?: UriRef;
  single_use: boolean;
}
