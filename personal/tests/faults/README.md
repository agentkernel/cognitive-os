# Fault-injection tests (placeholder)

Crash-point and fault-injection suites land here in M4: the three Effect
crash points (`conformance/vectors/eff-crash-001..003.json`), storage
fail-before-effect (`state-store-degradation.json`), and the eight-step
recovery order of whitepaper section 16.6. Evidence goes to
`artifacts/evidence/faults/`.
