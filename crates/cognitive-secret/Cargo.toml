[package]
name = "cognitive-secret"
description = "Personal SecretStore, Provider config binding, and fail-closed backends (P0-T05/P1-T02; not a registry REQ or Profile claim)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

# Isolated Personal product crate with zero external dependencies so the API
# and leak-negative tests avoid proc-macro/link pressure. Does not depend on
# kernel/store/runtime and is not an authority writer.
[dependencies]

[lints]
workspace = true
