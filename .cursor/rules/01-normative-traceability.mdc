---
description: 规范优先级、REQ 追溯与四类状态用语
alwaysApply: true
---

# 规范追溯纪律

## 必须做

- 实现任何行为前，先在 `specs/registry/requirements.yaml` 定位 REQ-ID，并确认对应 schema（`specs/schemas/`）与向量（`conformance/vectors/`）；在代码注释/提交信息中引用 REQ-ID。
- 规范优先级：digest 固定的机器 schema/registry/transition/vector 与 normative companion > 固定版本 RFC/Core/Profile 文本 > 白皮书（informative）> 实现建议。冲突时采用不扩大权限、数据范围、风险、预算或完成声明的解释。
- 发现规范资产之间漂移：先登记 `docs/traceability/findings-ledger.md`（漂移节），再最小修正闭合，提交说明注明。
- 全部文档区分四类状态用语：规范已登记 / 实现已提供 / 测试已执行 / Profile 已符合；`implemented` 仅指全部适用 MUST 有通过证据。

## 禁止做

- 禁止虚构 REQ-ID、错误码、schema、向量；禁止把白皮书 informative 文字、伪 schema、planned 向量当作已登记机器合同。
- 禁止为迎合实现改写向量或删除负例。
- 禁止读取、引用或参考 `History/` 目录下任何内容（冻结归档）。
- v0.1 前禁止新增对象族、Profile、REQ 域（规范表面冻结，IMP-01）；只允许实现反馈驱动的修正型规范变更。

## 完成前检查

- 本次变更触碰的每个 REQ-ID / 错误码在 registry 真实存在（可运行 `pnpm run check:consistency` 验证）。
- 若改了 `specs/**` 或 `conformance/**`：变更属于修正型且已在台账登记。
