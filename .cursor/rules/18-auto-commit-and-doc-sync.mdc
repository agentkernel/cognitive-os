---
description: 自动提交与自动 push 政策（所有者已授权）
alwaysApply: true
---

# 自动提交与自动 push

仓库所有者已明确授权（ADR-0008）：代理完成一个原子任务且全部验证通过后，自动提交并 push，无需逐次请示。授权不改变任何既有纪律——本规则收紧执行条件，不放宽任何红线。

## 必须做

- 只提交**完成且测试通过**的原子任务：提交前相关构建/测试/静态检查全绿；失败状态不得提交。
- 暂存一律逐路径 `git add`；一个提交只含一个原子任务的改动及其同批文档联动。
- **push 前必查** `git log --name-only origin/main..HEAD`（lane 分支对其远端比对）：逐文件确认无 `personal-blog/**`、无 `History/**`、无无关文件、无 secrets、无构建产物。
- 提交信息关联 REQ-ID、F/IMP 条目或文档条目；确无关联时写明原因。
- 直推 main 仅限 docs-only 低风险批；代码批走 lane 分支 + PR + CI 全绿后合并。分支保护拒绝直推时，自动改走 lane 分支 + PR，不试图绕过。

## 禁止做

- 禁止提交失败状态（构建红、测试红、lint 红、check:consistency 红）。
- 禁止 `git add -A` / `git add .`；禁止混入无关改动或他人工作区状态。
- 禁止 force-push 任何已推送分支。
- 禁止推送 `personal-blog/**`（独立子工程，不入本仓远端）。
- `clients/**` 仅允许核心 gate 变化引起的最小 Markdown 状态同步，禁止夹带其他客户端内容。

## 完成前检查

- push 后观察 CI（`gh run list` / `gh run watch`）到结论；红灯立即修复或回退，不得留红过夜。
