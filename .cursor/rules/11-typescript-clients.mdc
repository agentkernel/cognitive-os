---
description: TypeScript 客户端纪律（非 authority、双通道隔离、只消费生成合同）
globs: "**/*.ts,**/*.tsx"
alwaysApply: false
---

# TypeScript 客户端纪律

## 必须做

- TS 只消费机器合同：手写类型仅限 `packages/contracts-ts` 的编码层；对象类型自 schema 生成后（M1，ADR-0006）一律用生成物，生成代码禁止手改。
- Shell（`apps/agent-shell`）、Console（`planned`；产品文档在 `clients/pc`，`apps/cognitiveos-console` 为兼容 stub）与 `packages/sdk-ts` 是客户端，非 authority：展示状态一律来自 authority 投影（snapshot + watch），禁止客户端聚合状态冒充权威；项目根边界见 `.cursor/rules/17-client-project-boundaries.mdc`。
- 任务通道与管理通道的凭据、缓存、存储严格隔离（REQ-SHELL-*、REQ-MGMT-*；`shell-channel-isolation-003.json`）；一个客户端实例只绑定一个通道。
- canonical JSON 与 digest 只用 `@cognitiveos/contracts-ts`，禁止重新实现或用 `JSON.stringify` 直接哈希。
- tsconfig `strict` 全开；新包沿用 `packages/contracts-ts/tsconfig.json` 的编译器选项基线。

## 禁止做

- 禁止在客户端做授权判定、完成判定或状态迁移（只发请求、渲染投影）。
- 禁止跨通道复用 token、cookie、缓存键。
- Shell 退出/断连不得取消任务（`shell-detach-attach-004.json`）。

## 完成前检查

- `pnpm -r build && pnpm -r test` 全绿；无 `any` 逃逸新增。
- 新增网络调用都标明通道（task/management）与对应 REQ-ID。
