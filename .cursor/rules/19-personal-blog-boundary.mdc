---
description: personal-blog 嵌套独立仓边界（路径、远程、禁毁、禁散落）
alwaysApply: true
---

# personal-blog 边界

`personal-blog/` 是 **CognitiveOS Research** 独立子工程，不是 Cos 车道产物，也不入 `origin/main`。

## 必须做

- **唯一工作副本**：只在 Cos 工作树内的 `personal-blog/`（本机即 `D:\agent-kernel\personal-blog`）。
- **远程固定**：嵌套仓 `origin` 必须为 `https://github.com/agentkernel/blog.git`（可用 `git -C personal-blog remote -v` 核对）。
- **嵌套 git + 根忽略**：保留 `personal-blog/.git`；Cos 根 `.gitignore` 必须含 `/personal-blog/`。
- **提交隔离**：blog 改动在嵌套仓内 `git add` / commit / push；Cos 提交前用 `git log --name-only origin/main..HEAD` 确认无 `personal-blog/**`。
- **破坏性操作前保全**：对 Cos 做 `git reset --hard`、`git clean -fd`、删除/搬空工作树或重建目录前，先确认 `personal-blog/` 仍在且含 `.git`（或已备份到可恢复位置）。gitignore **不能**挡住对磁盘目录的删除。

## 禁止做

- 禁止在 `D:\blog-*`、`D:\agentkernel-blog`、`D:\backup-personal-blog-*` 等路径再建平行克隆当主副本。
- 禁止把 blog 源码/文档提交或 push 进 Cos `agentkernel/cognitive-os`。
- 禁止为「对齐 Cos 干净基线」而清空、覆盖或 `Remove-Item` 整个 `personal-blog/`（可清其内 `node_modules`/`.next`，不可毁嵌套仓）。
- 禁止用 Cos worktree / lane 分支承载 blog 实现；blog 不是 Lane-* 所有权表中的 crate。
- 禁止把 blog 本地笔记写进 Cos `docs/` 规范树；临时草稿仅 `docs/_local/`（已忽略）或 `personal-blog/docs/notes/`。

## 完成前检查

1. `Test-Path personal-blog/.git` 与 `git -C personal-blog remote get-url origin` 指向 `agentkernel/blog`。
2. `git check-ignore -v personal-blog` 命中根 `/personal-blog/`。
3. Cos `git status` 不出现待提交的 `personal-blog/**` 内容。
