---
description: JSON Schema 与协议资产变更纪律
globs: "specs/schemas/**"
alwaysApply: false
---

# Schema 与协议纪律

## 必须做

- 全部 schema 使用 JSON Schema draft 2020-12；跨文件引用用相对文件名 `$ref`（与既有 60 份一致）。
- 顶层 `$id` 必须存在且等于文件名（如 `"$id": "effect.schema.json"`，D-001/D-006 统一策略；`tools/src/check-consistency.mjs` 红灯强制）。
- canonical 编码与 digest 遵循 `docs/standards/canonical-encoding-and-digest.md` 与 ADR-0004/0005：UTF-8 无 BOM、I-JSON、RFC 8785、域分离前缀 `CognitiveOS-Digest-V1`。
- 未知 critical extension 必须 fail-closed（`CRITICAL_EXTENSION_UNKNOWN`），schema 中不得偷偷放宽。
- 任何 schema 变更必须同批：更新 `specs/registry/*.yaml` 中引用它的 REQ、检查受影响向量、跑 `pnpm run check:consistency`、登记 `docs/traceability/findings-ledger.md`（若属漂移修复）。
- 变更分类遵循 `docs/standards/docs-sync-contract.md`：语义型变更需在提交信息声明兼容性影响。

## 禁止做

- v0.1 前禁止新增对象族 schema（规范表面冻结）。
- 禁止为通过实现测试而放宽 schema 约束或删除 `additionalProperties: false`。
- 禁止手改生成绑定来"适配"schema 变更（重新生成，ADR-0006）。

## 完成前检查

- schema 通过 2020-12 元校验且相对 `$ref` 全部可解析（tools 检查）。
- registry↔schema↔vector 双向无孤儿；变更写入影响面扫描结果。
