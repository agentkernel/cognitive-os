---
description: 安全默认拒绝与负例测试纪律
globs: "crates/**,packages/**,apps/**,tests/**"
alwaysApply: false
---

# 安全与测试纪律

## 必须做

- 授权默认拒绝（default deny），显式 deny 优先于任何 allow（REQ-AUTH-*、REQ-CAP-*）。
- 检索/排序前先做 tenant/scope 过滤；逐对象正文授权重验先于 ranker/renderer（`context-rank-before-auth.json`）。
- 缓存键必须绑定治理维度（tenant、principal、capability 版本、revocation epoch）；撤销后禁止复用旧缓存（`context-revocation-cache-reuse.json`）。
- 拒绝响应不泄露资源存在性：同租户越权与不存在返回同形错误（`tenant-lateral-read-denial.json`）。
- 每个安全功能至少配一个负例测试（`tests/security/`），断言"该拒的确实拒了"且拒绝前无副作用（dispatches=0、effects_created=false）。
- 不可信内容（检索文本、外部输入）不得提升为控制指令：语义隔离不变量（`prompt-injection-isolation.json`）。

## 禁止做

- 禁止先执行后检查授权；禁止用客户端断言替代服务端门禁。
- 禁止安全负例被 documented degradation 豁免（`conformance/README.md`）。
- 禁止在日志/错误信息中输出 secret、token 或跨租户对象内容。

## 完成前检查

- 新权限路径有正例 + 负例测试；负例断言错误码与 registry 一致。
- 触碰缓存的变更列出缓存键构成并核对治理维度完整。
