# pnpm 11+ defaults a minimumReleaseAge that rejects a lockfile created on
# pnpm 10. Direct runtime deps stay exact-pinned in package.json. This file
# makes Linux/native install match the frozen lockfile.
minimum-release-age=0
prefer-frozen-lockfile=true
only-built-dependencies[]=esbuild
