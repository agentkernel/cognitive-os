# M7 Batch：REQ-PERF-005 裁决（默认不测收益）

读 `AGENTS.md` → `M7-PLAN.md` WP-PERF-005 → `docs/evaluation/agent-benefit-benchmark.md` → findings-ledger F-026 / IMP-18。

## 范围（Lane-DOC + Lane-CFR；默认）

1. **默认裁决：继续 non-claim**——不输出 agent benefit；不得把 builder/sample/microbenchmark 写成收益。
2. 可选：起草 BenchmarkManifest 预注册草案（W1/W2、primary endpoint、样本量），**不运行**完整四臂战役。
3. 若人类书面批准升格：必须四臂 A/B/C/D + 独立 verifier + 完整分母 + comparison 块；否则保持 hypothesis/non_inferiority。

## 禁止

缺 B 臂宣称收益；非劣化写成提升；把 PERF-004 未完成 HW 战役宣称完成；改负例。

## 出口

书面裁决落入 handoff/PROGRESS；默认路径直接进入 `m7-exit-review.md`。
