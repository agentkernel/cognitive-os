# M7 Batch：Discovery delta / 停滞 / read-separation

读 `AGENTS.md` → `PROGRESS.md` → `M7-PLAN.md` WP-M7-DISC → cognitive-discovery companion。

## 范围（Lane-RUN delta consumer + Lane-KRN admission/state）

1. `DISC-DISCOVER-READ-001`：discoverable ≠ body read；错误隐私。
2. `DISC-DELTA-SCOPE-003`：delta 不扩大 ActivityContext/purpose/scope/audience/egress/hard budget；累计预算单调；新增正文重授权。
3. 复核 `DISC-ADMISSION-002` / `DISC-STAGNATION-004`（M3 已有行为 pass）：不得扩成 Profile implemented；确认与 runtime delta/memory 路径一致。
4. 服务不可用不得解释为「无结果」。

## 禁止

ranker/LLM 决定最终 scope/authority；M8 catalog/SMS/CRB；Console 实现；改负例；把 static pass 写成 behavior。

## 出口

delta/read-separation 行为证据或诚实 not-run 理由；下一会话：`m7-cfr-vectors-and-self-check.md`。
