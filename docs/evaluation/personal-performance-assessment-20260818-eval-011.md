# PERSONAL-PERF-EVAL-011 running report

- Campaign: `PERSONAL-PERF-EVAL-011`
- Lease: `lease/personal/EVAL-011/c1-c2-b0-qualification`
- Frozen source: `979e52e4c6681d0fc8c6431c965e3267a7a0d917`
- Target: `B01-Desktop-Linux-002`
- Claim ceiling: `hypothesis` / non-claim
- Reviewer: `not_reviewed`
- Status: paused for B0 procedure amendment; measurement-only

Results are appended immediately after every completed campaign unit.

| Cell | Status | Evidence |
|---|---|---|
| Owner activation and EVAL lease | pass | Owner explicitly confirmed a new isolated B01 C1/C2 campaign. EVAL-004 through EVAL-010 remain closed and excluded. The frozen product source is pushed revision `979e52e4c6681d0fc8c6431c965e3267a7a0d917`; no product code changes are permitted. |
| B01 baseline / snapshot observation | partial / resolved | Read-only baseline completed through the registered host route: guest `hal9001-Standard-PC-Q35-ICH9-2009`, identity `hal9001`, Linux `7.0.0-28-generic x86_64`; domain `running`; registered snapshot `b01-platform-qualified-baseline` is `shutoff`; EVAL-011 ports `48300` and `48400` are not listening. The pre-existing `/home/hal9001/perfeval011-20260818` was not inspected until the owner explicitly confirmed it belongs to EVAL-011 and authorized use under the preregistered procedure. No snapshot or prior EVAL asset was changed. |
| Exact-source archive and binary freeze | pass | Host worktree fetched GitHub branch `personal/P2-T36-c1-public-production-path`, detached at exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917`, and was clean. `CARGO_NET_OFFLINE=true cargo build --release --locked -p admin-cli -p kernel-server -p pi-agent-adapter` passed. Git archive SHA-256: `01d7cd90f24df92085c10e57f88d0a535b4e7739886d414b68eac5d366164c14`; `cognitive`: `6917dca3a0f294c34d1f177dd5ebd3e1a36fff1c71de7661094049b30741a65f`; `kernel-server`: `3990e66484aa43a40bb4f0f2c723be096965419afc9bfd480ce0105095a4f298`; `pi-agent-adapter`: `54ce9eaa0e61febeff53d8e96b43f0d30570fcfb5fdd95e455715fe061991fce`. No guest source or runtime was changed by this host-only freeze. |
| Frozen guest artifact verification | pass | Campaign-owned `frozen/` received the archive and three exact-source binaries through ProxyJump. Guest SHA-256 values matched the host freeze: archive `01d7cd90f24df92085c10e57f88d0a535b4e7739886d414b68eac5d366164c14`; CLI `6917dca3a0f294c34d1f177dd5ebd3e1a36fff1c71de7661094049b30741a65f`; daemon `3990e66484aa43a40bb4f0f2c723be096965419afc9bfd480ce0105095a4f298`; adapter `54ce9eaa0e61febeff53d8e96b43f0d30570fcfb5fdd95e455715fe061991fce`. The archive was extracted only to campaign-owned `frozen/source`; existing root scripts, evidence, runtime, and Pi configuration were not overwritten. Existing local Pi reports pinned `0.81.1`. |
| Frozen Extension build and Pi configuration | pass after recovery | Guest build attempt was initially `not-run` because `pnpm` is absent from guest PATH and SCP did not preserve execute bits. Recovery changed only campaign-owned files: the exact frozen host worktree built `extension.js` with SHA-256 `d5ba4e47d2e05a260f9c5e3850572edf228628ab02c78e7acd75c98f2278d880`; the guest copy matched; `cognitive`, `kernel-server`, and `pi-agent-adapter` received mode `0700`; extension mode is `0600`. Public `cognitive pi configure` successfully wrote only non-secret `runtime-48300/config/cognitiveos/pi.json` referencing pinned Pi `0.81.1`, the frozen Extension, and frozen adapter. No Provider, SecretStore, daemon, Pi process, or B0 sample started. |
| New root, port, and SecretStore allocation | partial | The owner-authorized root has campaign-owned `frozen/` and `runtime-48300/` subtrees; daemon port `48300` was used and the broker reservation `48400` remains unstarted. The public CLI created/updated an opaque Provider SecretStore reference, but its item identity is intentionally not inspected. Never reuse EVAL-004 through EVAL-010 assets. The allocation is retained while the campaign is paused; its cleanup remains subject to the preregistered product attribute-triple procedure at campaign closure. |
| Public daemon readiness before Provider configuration | partial / expected | Public `cognitive daemon start` launched the frozen daemon at `127.0.0.1:48300`; public status reports a live lock/process. Redacted doctor reports system/database/secret/daemon/Pi `0.81.1` ready and `provider_config_missing`, so `first_conversation_ready: false`. This is a pre-credential readiness fact only, not a C1/C2 conclusion. No secret, Provider request, Pi candidate, scheduler lease, dispatch, verification, or acceptance occurred. |
| First hidden-input configuration check | not-run | After the owner reported input, the redacted EVAL-011 doctor still returned `provider_config_missing` and `first_conversation_ready: false`; the EVAL-011 runtime therefore received no Provider configuration. No secret material was observed, printed, searched, or retained. Reopen a visible terminal that preserves the public CLI result before retrying the approved hidden input. |
| Hidden-input configuration attempt 2 | fail / safely rejected | Public `cognitive init` on the visible linux-002 terminal rejected the submitted material before Provider discovery: `provider api key material contains control characters`. No raw material was logged, displayed, searched, copied, or retained in the running report. No Provider request, Pi candidate, scheduler lease, dispatch, verification, acceptance, or B0 sample started. Recovery: invoke public `cognitive init --rotate-key` on the same isolated runtime and enter only the raw key bytes, with no surrounding quote, whitespace, or newline characters. |
| Hidden-input configuration attempts 3-4 | fail / safely rejected | Two further public `cognitive init --rotate-key` hidden-input attempts returned the same redacted pre-discovery policy error: `provider api key material contains control characters`. No raw material was emitted, searched, retained, or used for a Provider request. The owner-designated local test-key file is now the approved recovery source: stream its bytes through a non-logging stdin path after discarding control bytes; do not place material in argv, environment, ordinary configuration, logs, evidence, Git, or chat. |
| Owner-designated file stdin recovery | security-stop, rotation confirmed | The attempted stdin recovery used a PTY-bearing SSH transport. The remote terminal echoed input and key material appeared in terminal capture. The material is not repeated, copied, searched, retained in this report, or used for further operations. Public EVAL-011 daemon cleanup returned `stopped`; no B0 sample began. The owner confirms the affected key was rotated and explicitly authorizes one new recovery attempt using the updated owner-designated local file through SSH **without PTY allocation**, with stdin never captured or echoed. No SecretStore search, lookup, or broad clear is permitted. |
| Non-PTY credential import | partial | Updated owner-designated file was processed in memory only to remove BOM/control formatting and sent to `cognitive init --api-key-file - --rotate-key` over SSH with PTY explicitly disabled. No material was echoed or captured. The public CLI reached Provider discovery, proving the previous control-character rejection was cleared, but rejected the requested `deepseek-chat` alias as absent from the discovered catalog. No B0 sample started. Recovery is non-secret: refresh the selected model to the previously qualified Flash catalog ID `deepseek-v4-flash` using the existing opaque SecretRef; do not send or inspect key material again. |
| Selected Flash model refresh | pass | Public non-PTY `cognitive init` reused the existing opaque SecretRef without secret input (`secret_material_written: false`) and selected `deepseek-v4-flash`; provider configuration is present with a redacted reference and snapshot digest `fnv1a64:c58ce6f2f7521544`. This is credential/readiness preparation only, not a Provider, C1/C2, B0, Gate, release, Profile, B01, or Agent-benefit result. |
| Public daemon readiness after credential recovery | pass | Public daemon started at EVAL-011 endpoint `127.0.0.1:48300`; status reports a live lock/process. Redacted doctor reports system/database/secret/provider/daemon/Pi ready, `secret_ref_resolves: true`, selected model digest match, Pi `0.81.1`, and `first_conversation_ready: true`. This is readiness only; it does not execute a Pi candidate, scheduler lease, Tool executor, verifier, acceptance, B0, Gate, release, Profile, B01, or Agent-benefit result. |
| Unfrozen B0 instrument interface check | fail / procedure deviation | SHA-256 `4cffeba1445b750f790fd9be805da41426ca337e8cf4187926cab9eae173adb9` was recorded for the campaign-root `eval011-b0-c1-search.py`; however, invoking its `--help` interface unexpectedly attempted a session mint and returned redacted `401` (`session mint failed`). This is retained as a started Provider-side failure with `retry=0`, but it is **not** a valid WorkspaceRead or WorkspaceSearch qualification sample: its complete manifest, bounded input, oracle, output schema, lifecycle observation, and cleanup procedure were not frozen before invocation. No C1/C2, scheduler-lease, Tool executor, verifier, acceptance, B0, Gate, release, Profile, B01, or Agent-benefit conclusion follows. Stop campaign-owned daemon and do not run another instrument without an owner-approved preregistration amendment. |
| B0 C1 WorkspaceRead O-arm | not-run | `retry=0`; at most one bounded sample after readiness. Record candidate validation, lease, dispatch, verification, and acceptance separately. |
| B0 C1 WorkspaceSearch O-arm | not-run | Starts only if WorkspaceRead reaches a fair scheduler-lease observation. |
| B0 P-arm / paired B1/B2 / C2a-C2d | not-run | Not authorized until B0 O-arm is complete, fair, and a future approved amendment freezes the missing assets. |
| Campaign pause cleanup | pass | Public campaign-owned daemon stop returned `stopped` for PID `305658`, with confirmed stale-lock removal. The associated `kill` observed the PID already absent; no retry or further process action occurred. Campaign root, runtime, frozen artifacts, and opaque SecretStore item are retained only for the paused amendment decision; no cleanup/discovery of SecretStore state was performed. |

## Unique next action

EVAL-011 daemon is stopped. Await an owner-approved amendment that freezes a B0
runner, manifest, input, oracle, output schema, lifecycle observation, and
cleanup procedure; do not retry the started failed instrument or run any B0
sample before that amendment.
