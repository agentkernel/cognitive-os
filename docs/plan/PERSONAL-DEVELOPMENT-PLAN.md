# CognitiveOS Personal 产品化开发计划与进度表

> **项目身份：** `cognitiveos-personal` 是本仓库当前唯一活动实现项目。原 CognitiveOS
> 设计、规范、符合性资产和通用内核是本项目的架构/合同基础，不是并行产品 backlog。
> 边界与来源优先级见 [PROJECT-IDENTITY.md](../governance/PROJECT-IDENTITY.md)。
> **状态：active（P0-T01..T08、P1-T01..T09、P2-T01..T08、P3-T01..T06、P4-T01..T06、P5-T01、P5-T02、P5-T05、P7-T01、P7-T02、P7-T03、P7-T04、P7-T05、P7-T06、P7-T08、P8-T01..T06、P9-T02、P9-T03 已完成；P0-T08 仓库子项目化重构 `done`；P10-T01 documentation-only semantic adoption `in-progress`；B02/B04/B05/B12 MVP `pass` under ADR-0046；B08 MVP `pass` under ADR-0048；B09 MVP `pass` under ADR-0047；GMVP-LINUX MVP `pass` under ADR-0049；B06/B07 仍为 non-claim observation；P6 对本 RC 为 disabled-NO-GO；Profile / Windows B01-W 未声明）**
> **最后更新：2026-08-27**

> **仓库子项目化与 1.0.0 定稿修订（2026-08-25，ADR-0054 / P0-T08）：** owner 指令将仓库
> 重组为 `core/`、`personal/`、`enterprise/`、`clients/` 四个子项目目录（clients 自独立
> 仓库以 subtree 并回），并定稿 `cognitiveos-core` 与 `cognitiveos-personal` 的 1.0.0 边界
> （[core/docs/VERSION-1.0.0.md](../../core/docs/VERSION-1.0.0.md)、
> [personal/docs/VERSION-1.0.0.md](../../personal/docs/VERSION-1.0.0.md)）；enterprise 仅
> 定义边界与激活门槛（[enterprise/docs/VERSION-1.0.0.md](../../enterprise/docs/VERSION-1.0.0.md)），
> 不激活实现。该 **structural** 修订只移动路径并同步工具链/文档索引，不改变任何任务
> 验收、Gate 结论、evidence 或 claim scope；1.0.0 定稿逐字保留各 Gate 的 MVP 声明上限。
> **计划追踪 ID：** `P0-T01` 至 `P10-T04`（含后续登记的 `P2-T09..P2-T38`、`P8-T12`、`P8-T13`、`P8-T14`、`P8-T15` 与 `P9-T05..P9-T12`）是本计划的管理 ID，不是 `core/specs/registry/` 中的 REQ-ID，也不构成实现、测试或 Profile 符合性声明。
> **详细研究与任务卡草案：** [`docs/plan/plan.md`](plan.md)；本文件是后续开发的**正式任务、typed dependency、验收与 Gate 定义源**。当前 task/Gate/claim 事实只由 [PROGRESS.md](PROGRESS.md) `Current snapshot` 拥有；`docs/plan/plan.md` 只补充经本文件对齐的研究依据、实施细节与验收方法。
> **可机读追踪：** [personal-trace.yaml](personal-trace.yaml) 将 `PERS-PR`、本计划任务与 Gate/benchmark 对齐；它不是 registry matrix，且不构成 REQ、测试执行或 Profile 符合性声明。

> **开发状态解耦（2026-07-30 修订）：** `not-started` 表示尚无任务专属实现或测试
> slice；纯研究、讨论和未提交计划草稿不改变状态，首个真实实现或测试 slice 开始后
> 必须改为 `in-progress`。`done` 才表示完整正式验收已满足。后续 P1/P2/P3 工作可在
> `experimental-local-only` 开发轨道推进，并将真实执行记录为 `tested-local`；这两个
> 是开发/证据标签，不是 Gate、Profile 或 release 结论。任务状态、实现证据、Gate 状态与声明范围按 [Development Operating Model](../governance/DEVELOPMENT-OPERATING-MODEL.md) 分列；真实 Provider/Pi 测试仍须使用当前 SecretStore/daemon 边界并脱敏记录。

> **计划修订（2026-07-26，一致性评审批）：** (a) 新增 P7-T07，为 ADR-0025 已决定但此前
> 无任务归宿的 Windows x86_64 安装面（credential 后端、installer/service、专门
> B01-W Gate）建立唯一落点；未执行前不得声称 Windows install parity，且不阻塞
> Linux RC。(b) P2-T08 验收增补 ADR-0018 本机开发例外的到期移除核查，使"P2 结束到期"
> 有明确验收归属。(c) `docs/plan/plan.md` 与本台账对齐：修正 P1-T04 过期状态行、任务卡状态行改为
> 指向本台账、ADR 候选表改用 DEC-P-* 编号以消除与 `docs/adr/0017-0025` 的编号冲突、
> 依赖图补 P7-T07 并修正 critical path。本批不改变任何既有任务状态、Gate、证据或
> Profile 结论。

> **MVP-first 路线修订（2026-07-29，ADR-0034）：** 首个生产安装路径收敛为
> 一个 canonical user service（`cognitiveos-personal.service`）和一个固定
> loopback 端口（48181），接受 Alpha 显式升级的有界停机。ADR-0032/0033
> 的 candidate unit、48182 和双服务 promotion 保留为后续数据触发的升级优化，
> 不再阻塞 P1-T08/P1-T09。既有任务 ID 不重编号；新增 `P7-T08 / GMVP-LINUX`
> 作为 P1/B01、P2 与 P7-T01..T03 的公开 Linux MVP 汇合 Gate。该日将 Context、Memory
> 放在后续能力列车的范围，已由 2026-08-02 ADR-0037/0038 六资源 1.0 重基线取代；
> MCP/dynamic Tool、Windows 安装面、Web UI 和 Multi-Agent 仍为后续独立能力列车；
> Multi-Agent NO-GO 且默认关闭是合法结果。本修订不改变任何既有任务状态、
> 已执行证据、规范机器资产或 Profile 结论。

> **Linux 1.0 与 Agent 管理修订（2026-08-02，ADR-0035/0036）：**
> `GMVP-LINUX` 是 Personal `1.0.0` 的既有发布 Gate，不新增平行 Gate。Pi-hosted
> Agent Shell 与 managed Pi Agent 是独立角色：P2-T02 负责 Shell 到 Task/management
> application service，P5-T01/T02 与 B09 负责官方 npm Pi acquisition、installation、
> registry/instance/supervision/lifecycle。Linux 1.0 只 product-qualify Pi，同时交付可供
> 后续 Agent 独立 qualification 的通用 adapter framework；该日排除 Memory 的 1.0
> scope 已由 ADR-0037 取代。OpenClaw、Hermes、Codex、WorkBuddy、MCP、Multi-Agent、
> Web UI 与 Windows installer 仍不进入 1.0 claim。
> 该 product-semantic 修订不修改 registry/schema/transition/vector，也不产生实现、Gate、
> release 或 Profile 证据。

> **统一认知资源基座与 Agent sidecar 修订（2026-08-02，ADR-0037/0038）：**
> Personal Linux 1.0 重新定位为统一认知资源基座，交付 Memory、Skill、Tool、Context、
> Task、Runtime 六类资源的最小真实 slice；三条 1.0 release track 为 Runtime Spine、
> Resource Value 与 Product Operability。Agent 集成采用 per-Agent sidecar-first 边界，
> Pi 仍是唯一 qualified Agent。复杂 Context 优化和收益 Gate 不阻塞 1.0，但 Context
> correctness 属于 1.0；embedding/vector/graph、MCP/dynamic Tool ecosystem、Multi-Agent、
> Web UI 与 Windows 安装面移入 post-1.0。该 owner-approved `product-semantic + structural`
> 修订不新增、删除或重编号任务，也不改变任何已有 task status、attempt、evidence 或 Gate
> current status：P1-T09 仍为 `in-progress`，B01 仍为 `running`（1/至少 20），P2-T01/
> P2-T03 仍为 `in-progress`，`GMVP-LINUX` 仍为 `not-run`，Profile `implemented: 0`。
> 本批不修改规范或实现，不产生 implementation、Gate、release 或 Profile evidence。

> **B01 successor campaign policy amendment（2026-08-09，ADR-0039）：** owner
> approved a product-semantic campaign policy for separately preregistered
> successor `B01-clean-linux-first-install-first-conversation-002`: exactly six
> immutable outcomes, at least five successes, zero critical safety failures,
> complete aggregate statistics, and affirmative independent-verifier closure.
> Retained failed campaign `001` remains its historical N=20 record. Attempts
> 1--6 of `002` remain immutable; owner-waived transition Attempt 7 is retained
> as audit evidence but excluded because no artifact, Pi, Provider, service, or
> route operation occurred. This amendment creates no B01/G1/GMVP-LINUX,
> release, or Profile pass claim.

> **2.0 设计基线与计划扩展修订（2026-08-10，P8-T01 批）：** owner 指令将
> cognitiveos-personal 的产品陈述扩展为"主流 agent 的认知资源统一管理底座"，
> 新增 Phase 8（通用 Agent 适配与设计基线，6 任务）与 Phase 9（性能与结构演进
> 候选池，3 任务），并授权公理体系重述（详见 ADR-0041+ 与
> `docs/governance/AXIOMS.md`）。本批同时完成 corrective 结构修复：移除打断
> Delivery Slice register 的过早"收口记录"标题（内容保留为注记）、对齐 Phase 3
> 任务表标题、澄清 B01 历史 addendum 的适用范围、按 slice 拆分澄清 P7-T04 依赖
> 表述。该 `product-semantic + corrective` 修订不改变任何既有任务状态、attempt、
> evidence、Gate 结论或 `GMVP-LINUX` 组合，不修改 registry/schema/transition/vector，
> 也不产生实现、Gate、release 或 Profile 证据。
>
> **Personal 2.0 desktop + MCP semantic adoption（2026-08-27，P10-T01 /
> ADR-0056/0057）：** owner 正式采用 desktop-first Control Plane，目标 IA 为
> Home / Agents / Work / Library / Activity / Settings（Providers 与 System 归入
> Settings）；global Agent Shell 仅建议/提案，installed Agent conversation 通过
> vendor-specific adapter 汇入 common internal projection/capability matrix，原生 Agent
> app 保持可用。Personal 2.0 同时增加真正第七 product family **MCP**，其 server/package/
> connection/capability/binding/health/quarantine 身份与生命周期独立治理；advertised tool
> 仍只是 Tool candidate，MCP resource/prompt 分别经 Context/Skill admission，禁止 generic
> `Resource` schema。Linux/Personal 1.0 仍是 finalized six-family。任何 credential import
> 都沿用 ADR-0055 的 per-source consent、daemon-only、SecretStore 与 raw-secret
> non-exposure，未实现入口必须标 `Requires-backend`。本批登记 Phase 10 四项任务且不新增
> Gate；P10-T01 为 documentation-only，未修改代码、合同、负例或任何 Gate/release/Profile
> 结论。
>
> **计划修订（2026-07-26，生产就绪与低摩擦授权批）：** 依 owner 指令与
> [ADR-0026](../adr/0026-personal-trust-profile-low-friction-authorization.md)
> 落地 Personal 低摩擦授权模型（DEC-P-20）：交互分层 Tier 0/1/2、任务准入预览为
> 唯一默认人工授权点、预算硬轨替代逐动作审批、不建审批链；同时补 P7-T02 面向用户
> 的 backup/restore（排除 secret）。仅修改 not-started 任务的验收摘要
> （P1-T09、P2-T01、P2-T02、P2-T08、P5-T01、P5-T02、P7-T02），治理层
> （Intent/Effect、audit、verifier、capability 类型）全保留。本批不改变任何既有
> 任务状态、Gate、证据或 Profile 结论。

## 1. 使用与更新规则

1. 纯研究、讨论和未提交的计划草稿不改变任务状态。首个任务专属实现或测试 slice
   （包括 failure-first 测试）开始时，在本表把对应任务标为 `in-progress`，并在同一
   atomic delivery 内补齐负责人/分支、开始日期和关联 PR（如有）；若未达到产品 Gate，
   额外填写 `development_track: experimental-local-only`，不得把 acceptance/promotion
   Gate 写成实现阻断。
2. 一个任务只有在其验收条件满足、相关测试真实执行并留有证据后，才可标为 `done`；未执行的测试必须明确标 `not-run`，不得推断为通过。
3. Operating Model **`TASK-ATOMIC-DELIVERY-01`** 要求默认 atomic delivery 是一个完整 `P*-T*` 正式任务。
   一个任务使用一个 task branch、一个持续更新的 Draft PR 和一个 task-scoped lease，并连续
   完成全部必要 Slice、真实集成、负例、supported validation、完整 acceptance assessment
   与文档/分支收口。Slice 完成、checkpoint、push、CI 发起或阶段结果均不是停点或汇报点。
   只有任务 `done`、不可自主消除的外部阻塞、未知并发改动、安全冲突或 owner 明确暂停/改
   范围时才可中断。
4. 发生范围、依赖、验收或安全边界变化时，先将任务标为 `blocked`，记录原因和决策，再更新详细任务卡与依赖图；不得静默改写完成标准。
5. 允许的任务状态为：`not-started`、`in-progress`、`blocked`、`done`、`cancelled`；实现证据为 `none` / `provided` / `tested-local` / `tested-supported-ci`；Gate 为 `not-run` / `running` / `pass` / `fail` / `blocked`。`done` 不等于 Gate pass 或 Profile `implemented`。
6. 如本表与 `docs/plan/plan.md` 的任务卡或依赖图不一致，应先按本表执行并在同一文档修正批中对齐 `docs/plan/plan.md`；不得仅凭详细卡片重新解释或复用既有 `P*-T*` ID。
7. 领取任务前必须一次性核对其完整验收、implementation dependencies、所需路径和验证
   环境，然后连续实现到任务收口。任务、依赖和安全路径已经明确时，不得用继续研究、
   新建平行计划、切换无关任务或阶段性汇报替代实现。本地可修复的代码、测试、格式、CI
   配置或集成问题必须直接修复；只有外部阻塞才能形成带 `blocked_paths` /
   `blocked_task_ids` / `blocked_gate_ids` / owner / next action 的中断记录。
8. 任务内执行步骤登记为稳定 Delivery Slice，ID 为 `<task-id>/DNN`。本文件拥有 slice 的
   目标、依赖、出口和 required validation；当前状态只由 `PROGRESS.md` Current snapshot
   拥有。Slice 是内部检查点，不是独立 branch、PR、lease、handoff 或默认用户汇报单位。
   Slice `done` 后立即进入下一未满足 acceptance 项；最终 Slice 必须汇总完整 acceptance，
   不得另开 `acceptance-assessment` 分支。Slice `done` 不等于 task `done`，task `done` 也不
   等于 Gate pass。
9. 同一正式任务最多一个 `in-progress` slice。一个 foundation/helper slice 后必须优先
   接入真实 caller、durable authority outcome 或端到端负例；不得连续新增 helper-only
   slice 回避集成。实现存在但 required supported validation 未运行时，slice 必须为
   `blocked`，不能凭 fmt、diff 或 consistency 关闭。
10. **MVP-first：** 首个真实路径优先使用 owner-local、single-principal、task-scoped、
    daemon-issued 的最小授权组合，对范围外请求 fail closed。完整 RBAC、审批链、通用
    capability administration、多租户策略语言和未来扩展框架，除非当前任务验收或已登记
    threat boundary 明确要求，否则不得成为 MVP implementation mutex。此规则不放松
    daemon-only authority、SecretStore、Intent/Effect、budget/fencing、audit 或 independent
    verifier 不变量。
11. 完整任务收口是实现的最后一步，不是后续管理任务：逐条映射 acceptance 与 exact
    evidence，完成 supported validation/required CI，同步正式计划、Current snapshot 与
    唯一最终 handoff；确认 task PR 只含已声明路径后 ready/merge；随后关闭 lease、删除安全
    可删的远端 task branch、本地切回并 fast-forward `main`，确认 worktree clean、HEAD/
    upstream 一致且无已完成任务的 active lease。每次正式任务完成后，提交、推送、合并、关
    闭 lease、删除可安全删除的本地与远端分支、切回主分支和本地收尾都应作为同一条收口链
    路一次完成，不得拆成后续“顺便处理”的独立步骤。任一步缺失时任务保持 `in-progress`
    或 `blocked`，不得留下“代码完成但验收、分支或状态待收口”的半完成状态。

### Typed dependency 规则

- `implementation_requires`：开始独立 implementation slice 前必须已有的代码/合同；
- `acceptance_requires`：任务标为 `done` 前必须满足的任务或证据；
- `promotion_requires`：进入产品 Gate、release 或扩大 claim 前必须通过的 Gate。

下方旧表“依赖”列是便于阅读的摘要；遇到顺序歧义，以 release-critical typed dependency
表为准。Acceptance/promotion dependency 不是 implementation mutex。

### 进度汇总

| 阶段 | 任务数 | done | in-progress | blocked | not-started | 阶段 Gate |
|---|---:|---:|---:|---:|---:|---|
| Phase 0 - 基线与决策 | 8 | 8 | 0 | 0 | 0 | G0 |
| Phase 1 - 安装到首次对话 | 9 | 9 | 0 | 0 | 0 | G1 / B01 `pass` |
| Phase 2 - 单 Agent 任务闭环 | 38 | 38 | 0 | 0 | 0 | G2 / B02、B04、B05、B12 |
| Phase 3 - Context Resource Value | 6 | 6 | 0 | 0 | 0 | G3 / B03 `pass`、B06、B07 |
| Phase 4 - Memory 与 Skill | 6 | 6 | 0 | 0 | 0 | G4 / B08 |
| Phase 5 - Agent sidecar 与 Tool 生态 | 5 | 5 | 0 | 0 | 0 | G5 / B09、B10 |
| Phase 6 - post-1.0 Multi-Agent | 4 | 0 | 0 | 0 | 4 | G6 / B11 |
| Phase 7 - 产品化与发布 | 8 | 7 | 0 | 1 | 0 | GMVP-LINUX / G7 / RC |
| Phase 8 - 通用 Agent 适配与设计基线 | 15 | 15 | 0 | 0 | 0 | post-1.0；沿用 B09 模式逐 agent 资格化 |
| Phase 9 - 性能与结构演进 | 12 | 12 | 0 | 0 | 0 | 无新 Gate；沿用 P7-T04 回归地板 |
| Phase 10 - Personal 2.0 desktop 与 MCP | 4 | 0 | 1 | 0 | 3 | 无新 Gate；先决策、再 Lane-CTR/实现 |
| **合计** | **115** | **106** | **1** | **1** | **7** | — |

## 2. 产品边界与不变量

- Rust daemon 是唯一 authority writer；Pi、CLI、Web UI 均为客户端，不可直接写 SQLite 或推进 Task、Effect、Verification 状态。
- Provider API Key 只保存在 approved `SecretStore` backend（desktop Secret Service 或
  headless encrypted vault）；不得进入 service unit/credential material、环境、配置、
  SQLite、命令行、日志或证据。唯一例外为 ADR-0018 已登记的 P0-T06 本机 Linux 开发
  路径：显式开关后从 native store 解析、仅传给初始 Pi 子进程、默认拒绝、不得用于
  CI/发布，并在 P2 结束到期。
- Linux-native development smoke 以环境资格清单为准；`wuz@192.168.1.2` 是优先候选而非唯一主机。只有预注册 formal campaign 可推进 B01，任何候选主机名称本身都不表示测试、Gate 或 release 证据。
- 所有外部 mutating operation 均须经 Intent/Effect、持久化后派发、幂等键、fencing 和结果 reconcile；外部工具成功不等于 Task 完成。
- Task 完成由独立 verifier/acceptance authority 推进；Pi Session 不等于 Task，Pi `agent_end` 不等于完成。
- **Pi 双角色（ADR-0035）：** Pi-hosted Agent Shell 是自然语言 UI/client；managed Pi
  是 package/installation/registry/instance/execution 资源。ShellSession、Pi session、
  AgentInstallation、Agent instance、AgentExecution、process 与 Task 身份不得合并；task
  与 management bearer/cache/projection 必须隔离。
- **统一认知资源基座（ADR-0037）：** Linux 1.0 必须提供 Memory、Skill、Tool、Context、
  Task、Runtime 六类资源的最小真实 slice。六类资源经 daemon application services 和
  私有、versioned Personal projection 统一呈现，但不合并各自 authority、生命周期或
  schema，也不新增巨型 `Resource` schema 或 `Process` domain。
- **Personal 2.0 desktop-first（ADR-0056）：** desktop Control Plane 是 2.0 primary
  entry/supervision center，目标 IA 为 Home / Agents / Work / Library / Activity /
  Settings，Providers 与 System 位于 Settings。global Agent Shell 与所有 installed-Agent
  conversation adapter 均为 candidate/observation client；common internal projection 必须
  通过 capability matrix 诚实暴露 vendor 差异，且 native Agent app 仍可使用。
- **Personal 2.0 MCP family（ADR-0057）：** MCP 是 2.0 第七 product family，拥有
  server/package/connection/capability/binding/health/quarantine 身份与生命周期；advertised
  tools 仍经 Tool admission，resources/prompts 经 Context/Skill admission。该结构不得生成
  generic `Resource` schema；Linux/Personal 1.0 六 family 定稿与 evidence 不变。
- **Credential import（ADR-0055）：** 每个 source 必须 user-initiated、per-source
  consented，由 daemon 读取并只写 approved SecretStore；raw secret 不得到达 UI/Agent。
  未有正式 backend implementation task 的 import surface 一律标 `Requires-backend`。
- **Sidecar-first（ADR-0038）：** Shell 经 per-Agent sidecar 调用 daemon application
  services；sidecar、Pi Extension、CLI 和其他 client 都不是 authority，不得持有 daemon
  bootstrap/management authority。Linux 1.0 只资格化 pinned Pi + sidecar；其他 Agent 和
  client 必须各自独立 qualification，不能继承 Pi 证据。
- **部署与授权边界（ADR-0038）：** Standard Workspace 保持低摩擦；Extended Home 只扩展
  显式 document/project roots 与可撤销的普通联网，不得触达 Secret/SSH/GPG/browser
  credential、authority/bootstrap、Docker/system socket、system directory 或 privilege
  management。desktop 使用 Secret Service；headless 使用 approved encrypted vault，locked
  start + SSH TTY unlock，optional unattended 仅使用 systemd encrypted credential 的 vault
  unlock material。三种 mode 共用同一 artifact/daemon/ports。
- Personal 计划不改变既有规范优先级，不得用 `PERS-*` ID 冒充 REQ-ID；合同变化必须走 Lane-CTR 流程。
- **低摩擦授权（ADR-0026）：** 治理记录（Intent/Effect、audit、verifier、capability）全保留；人机交互分层——Tier 0（只读与任务范围内可逆本地写）静默自动授权、Tier 1（幂等/可对账外部 mutating）首用一次授予并默认记住为 capability lease、Tier 2（不可逆/毁灭性/超预算）始终显式确认。任务准入预览是唯一默认人工授权点，默认路径人工确认 ≤1/task；预算与边界是硬轨，不建审批链；企业审批留在 Deferred Backlog。
- 产品目标平台仍为 **Linux x86_64 + Windows x86_64**（ADR-0025）；Personal
  `1.0.0` 是 Linux x86_64 single-service、Extended Home、headless/foreground release
  （ADR-0034/0036/0037/0038），由现有 `GMVP-LINUX` Gate 推广。1.0 只正式支持官方 npm
  获取并受管的 exact Pi + per-Agent sidecar；Memory 使用 SQLite FTS5 baseline，Skill
  使用 local package/revision/import/binding，Tool 使用 native catalog，Context/Task/Runtime
  使用真实 daemon ports。复杂 Context 收益优化不阻塞；embedding/vector/graph、MCP、
  dynamic marketplace、Multi-Agent、Web UI 与 Windows 安装面 post-1.0，Windows 专门
  Gate 的唯一任务归宿仍是 P7-T07。

## 3. 阶段路线图

下表的入场条件和“禁止提前作为产品主路径”只约束任务 `done`、产品集成、推广和声明范围；不禁止满足 `implementation_requires` 的隔离实现与 failure-first 测试。具体依赖必须区分 implementation、acceptance 与 promotion，禁止把后两者当作开发互斥锁。

| 能力组 | 目标 | Implementation start | Acceptance exit | 禁止扩大声明 |
|---|---|---|---|---|
| P0 | 平台、架构和安全决策 | 本计划批准 | 工具链、ADR、Secret/Pi PoC、benchmark 规格完成 | 产品功能、Memory、Multi-Agent、UI |
| P1 | 安装到首次对话 | 对应 P0/P1 implementation requirements | successor B01 固定 6 次正式 campaign 达标 | 用 dev smoke 或单次 attempt 宣称 B01 |
| P2 | Runtime Spine、统一 projection 与 native Tool | P1 contracts + 对应 P2 implementation requirements；B01 不是实现 mutex | B02/B04/B05/B12 | 未资格化 sidecar/adapter |
| P3 | 真实 Context、Artifact CAS 与 UCR-01 | P2-T01/P2-T02 稳定 application contracts；不等待 P2-T08 acceptance | B03 correctness；采集 B06/B07 | 未执行的 Context 收益 |
| P4 | Memory + Skill 资源价值 | P3-T01/P3-T02 stable ports | B08 correctness/actual consumption | embedding/vector/graph claims |
| P5A | managed Pi 与通用 adapter framework | P0-T06/P1-T08 和所需 P2 supervisor contracts | B09 | 非 Pi Agent support |
| P5B | post-1.0 MCP/dynamic Tool ecosystem | P2 native Tool/Effect 闭环 | B10 | 自动市场发现或未资格化 MCP |
| P6 | post-1.0 Multi-Agent 可选实验 | 单 Agent benchmark 与明确并行假设 | B11 GO 或合法 NO-GO/disabled | 默认开启 Multi-Agent |
| P7 | Linux 1.0 Product Operability 与后续 RC | 对应 P1/P2/P3/P4/P5/P7 implementation requirements | `GMVP-LINUX` 后按声明范围汇合 RC | 用未执行能力扩大 1.0/RC |
| P10 | Personal 2.0 desktop Control Plane 与 MCP family | P10-T01 semantic adoption；随后按 typed dependency 进入 Lane-CTR 与实现 | P10-T01..T04 各自 acceptance；**无新 Gate** | 把 docs/既有 P5/B10/P7 UI evidence 写成 seventh-family 或 desktop support |

### Linux 1.0 active release tracks（不替代现有 Phase/task ID）

| Active track | 1.0 任务范围 | 出口 | 非阻塞项 |
|---|---|---|---|
| Runtime Spine | P1-T08/T09；P2-T01..T08；P5-T01/T02/T05 的 B09；真实 Task/Runtime/native Tool 与 Pi sidecar | B01、B02、B04、B05、B09、B12 | B10、B11、non-Pi adapters |
| Resource Value | P3-T01..T06；P4-T01..T06；真实 Context/Artifact/Memory/Skill 与 UCR-01 | B03、B08；B06/B07 仅采集 | 复杂 Context 收益、embedding/vector/graph |
| Product Operability | P7-T01..T03、P7-T08 汇合 Runtime Spine 与 Resource Value | `GMVP-LINUX` / `1.0.0` | P5-T03/T04、P6、P7-T05、P7-T07 |

P3/P4 是 Linux 1.0 的 active Resource Value track，不是 1.0 之后的 Beta。已完成 P0 与
P1-T01..T07 仍是共同 foundation，但不作为第四条 active release track。

### Post-1.0 capability trains

| Capability train | 任务归宿 | 边界 |
|---|---|---|
| Embedding/semantic retrieval/vector/graph | P4 后续独立决策 | 不替代 SQLite FTS5 authority-first baseline，不阻塞 1.0 |
| MCP Tool adapter 与 dynamic Tool ecosystem（既有） | P5-T03、P5-T04、B10 | 既有 transport/Tool candidate evidence；不等于 Personal 2.0 MCP family |
| Personal 2.0 MCP seventh family | P10-T01、P10-T02、P10-T03 | 先完成 product semantics 与 Lane-CTR compatibility；不自动启用 advertised tools |
| Multi-Agent | P6-T01..T04、B11 | 默认关闭；NO-GO 是合法结果 |
| Web UI 与 Windows | P7-T05、P7-T07/B01-W | 独立 readiness/qualification，不继承 Linux 证据 |
| Personal 2.0 desktop Control Plane | P10-T01、P10-T02、P10-T04 | primary desktop target；现有 Web UI/Agent app evidence 不自动成为 2.0 support |
| non-Pi Agent 与其他 Linux/hardware | 后续 adapter/port qualification | 经既有 ports 演进；不得据此宣称底层 substrate 已支持 |

### Linux 1.0 release-critical typed dependencies

| Task/Gate | implementation_requires | acceptance_requires | promotion_requires |
|---|---|---|---|
| P1-T09 / B01 | P1-T08 与既有 Secret/Provider/daemon/Pi contracts | 固定 6 个 clean-Linux outcomes、至少 5 次成功、关键安全失败 0、完整统计和 affirmative independent verifier | B01 只在完整 campaign 后 pass |
| P2-T01 | 既有 authority/store/Intent/TaskContract contracts；P1-T09 route implementation 已可集成，B01 不是 mutex | proposal/clarify/preview/admit/control/query；raw-intent durability、preview-digest binding、epoch/stale-lease fencing focused evidence | G2: B02/B04/B05/B12；task `done` 不要求这些 Gate 已运行 |
| P2-T02 | P2-T01、P1-T07、task/management channel contracts | real authenticated Personal intent record/interpret→server-issued preview→admit Task API/watch; daemon-owned governance-context binding (including the ADR-0022 durable local-root bootstrap rule), Pi Shell and CLI use one application service; channel-isolation negatives | G2: B02/B04/B05/B12 |
| P2-T03 | P2-T01、P1-T01、现有 scheduler/contract slices | durable stop、worker/Effect closure、crash/duplicate/clock/budget evidence | G2: B05/B12 |
| P2-T21 | P2-T12、P2-T14、P2-T15、P2-T19、P2-T20 | public Task 到 Search/Write/Patch production sink；authority/CAS 重建的 task-channel terminal evidence；exact pushed revision 的 Ubuntu supporting CI 与 `B01-Desktop-Linux-002` 产品验证；Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL-004、Gate、release 或 Profile |
| P2-T36 | P2-T35、P2-T29、P2-T32 | public real-Pi WorkspaceRead/Search production route through daemon candidate admission, scheduler, Tool executor, independent verifier, and acceptance; exact pushed revision and focused public-path negatives | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T37 | P2-T21、P2-T29、P2-T36 | public real-Pi WorkspaceWrite/Patch production route through daemon candidate admission, scheduler, expected-preimage mutation executor, independent verifier, and acceptance; exact pushed revision and focused mutation negatives | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T38 | P2-T37 | public WorkspacePatch completes independent verification when the candidate names current file bytes as `digest:sha256:<raw SHA-256>` (P-arm / sha256sum form) as well as the domain-tagged workspace-image digest; a fail-closed Patch does not request verification against a non-RECONCILED Effect | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T22 | P2-T16、P2-T21、P2-T15、P2-T13、P2-T14 | TypeScript/Rust 固定 repair corpus、RegisteredCheckRun、diff Artifact、Effect closure、current verification/acceptance 与完整 repair negatives；exact-revision `linux-002` | 无；EVAL-004 是独立 measurement campaign |
| P2-T23 | P2-T19、P2-T21、P3-T01..T06、P4-T01..T06 | public Memory/Skill lifecycle、task consumption、session-2 durable resume，以及 scope/lifecycle/digest/restart negatives；exact-revision `linux-002` | 无 |
| P2-T24 | P2-T17、P2-T21、P2-T13、P2-T14、P2-T20 | default-off authorized fault profiles、bounded Effect history、original-key-only reconcile、0/1 mutation count、unknown/open Effect non-completion；exact-revision `linux-002` | 无 |
| P2-T25 | P2-T10、P2-T15、P2-T16、P2-T20、P2-T21、P5-T03、P5-T04 | lifecycle-to-Agent exposure、bounded selection receipt、RegisteredCheckRun 与 pinned read-only HTTPS positives/negatives、descriptor drift 和 restart reconcile；exact-revision `linux-002` | 无 |
| P2-T26 | P2-T23、P2-T24、P2-T25、P7-T04、P9-T06 | O2/O3/O4/O5/O13 authenticated bounded collectors、active negative controls、durable audit replay/gap detection 与 concurrency/restart negatives；exact-revision `linux-002` | 无 |
| P2-T27 | P7-T02、P5-T01、P5-T02、P5-T05、P2-T23 | public backup/restore、transactional migration/tamper/secret-exclusion/RTO/RPO evidence，以及 managed Pi full lifecycle/recovery；exact-revision `linux-002` | 无 |
| P2-T28 | P2-T21、P2-T22..P2-T27 | UJ1..UJ6 required rows each have public caller、mechanical oracle、cleanup 和 bounded evidence；Web UI/Multi-Agent 明确 scope-excluded；exact-revision `linux-002` | EVAL-004 重新冻结与 B0/B1 仍为独立 campaign，不能由 task completion 推导 |
| P2-T29 | P2-T21、P2-T23 | Pi Extension advertises daemon-governed WorkspaceSearch/Write/Patch without opening bash/edit/write; public Memory remember accepts unsealed owner fields and daemon-composes sealed headers from the persisted GovernanceSeed; exact-revision `linux-002` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T30 | P2-T12、P2-T21、P2-T29 | Public-admit C1/C2 Tasks leave DRAFT; scheduler acquires a lease; private-candidate Pi spawns on the P2-T29 Workspace* path (daemon-governed, not bash/edit/write). Failure-first: EVAL-005 skip `scheduler_row_skip_before_lease` (lease_acquired 0, no Intent/Effect). Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T31 | P2-T30 | Live `kernel-server` HTTP admit (not in-process `TaskApi::handle` + deterministic proposer) leaves DRAFT, scheduler acquires a lease, private-candidate Pi spawns. Failure-first: EVAL-006 skip `scheduler_row_skip_before_lease` on daemon `48290`. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T32 | P2-T31 | Production `cognitive daemon start` composition (not a test-spawned `kernel-server --personal`) accepts public HTTP admit, scheduler acquires a lease (`lease_acquired` ≥ 1), Task leaves `DRAFT`, and private-candidate Pi is invoked on the P2-T29 daemon-governed Workspace* path (stub adapter allowed in tests; not bash/edit/write). Failure-first: EVAL-007 public launcher skip (Task `DRAFT`, `lease_acquired` 0, no Pi child; stderr was `/dev/null`). Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T33 | P2-T32 | Public `cognitive daemon start` + real `pi-agent-adapter` on a long `--runtime-root`: private completion socket binds under Linux `UNIX_PATH_MAX`; adapter/Pi stderr is a public `daemon.log` fact; private-candidate Provider proxy accepts one text choice that includes `role=assistant` and strips `tools`/`tool_choice`. Failure-first: EVAL-008 `private_completion_socket_could_not_be_created`; EVAL-009 `private_pi_candidate_adapter_rejected_the_request` with stderr `/dev/null`. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T34 | P2-T33 | Real `pi-agent-adapter` JSON-fallback candidates: recompute `parameters_digest` from `parameters` when present; otherwise require `sha256:` plus 64 lowercase hex before the daemon `validate_untrusted_pi_candidate` check. Failure-first: EVAL-010 `candidate_has_missing_fields_or_an_invalid_parameters_digest`. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T35 | P2-T34 | Private-candidate JSON-fallback `parameters_digest` defaults empty and is recomputed from valid `parameters`; adapter usage/ runtime failures use distinct exit handling; daemon diagnostics retain tail errors with secret-shaped spans redacted and include adapter exit class. Failure-first: EVAL-011 `private_pi_candidate_adapter_rejected_the_request`, including missing `parameters_digest` and mixed adapter USAGE/real-error diagnostics. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | 无；task `done` 不推进 EVAL、Gate、release 或 Profile |
| P2-T36 | P2-T35、P2-T29、P2-T32 | A real Pi candidate on the public `cognitive daemon start` production composition reaches C1 WorkspaceRead and WorkspaceSearch through the daemon-owned candidate, scheduler, Tool executor, verifier, and acceptance path. The Pi candidate surface may expose only CognitiveOS WorkspaceRead/Search; Pi built-in filesystem/shell tools remain disabled. Candidate data remains untrusted, schema-bound, descriptor/digest/epoch validated, and cannot carry authority. Failure-first: real-adapter C1 `DRAFT`/zero-lease regression and descriptor/tool-policy drift. Exact pushed `DEV-LINUX-NATIVE-01` plus supporting Ubuntu CI; Windows `not-run by owner-directed Linux-only route` | 无；task `done` provides implementation evidence only and does not start an EVAL, Gate, release, Profile, B01, or Agent-benefit claim |
| P2-T37 | P2-T21、P2-T29、P2-T36 | Public `cognitive daemon start` real Pi composition exposes only daemon-governed WorkspaceWrite/Patch beside C1 tools; each mutation candidate is untrusted, schema-bound, descriptor/digest/epoch validated, and reaches the existing daemon candidate admission, scheduler, expected-preimage mutation executor, independent verifier, and acceptance path. Pi built-in filesystem/shell tools remain disabled; public fields cannot carry authority or bypass Intent/Effect. Failure-first: public allowlist and candidate-mapping omission, malformed base64/preimage/digest, built-in tool, descriptor drift, stale epoch, preimage mismatch, and duplicate/mixed candidates fail closed. Exact pushed `DEV-LINUX-NATIVE-01` plus supporting Ubuntu CI; Windows `not-run by owner-directed Linux-only route` | 无；task `done` provides implementation evidence only and does not start an EVAL, Gate, release, Profile, B01, or Agent-benefit claim |
| P2-T38 | P2-T37 | EVAL-012 B0 C2a O-arm Patch retained `ACTIVE`/`must_reconcile` with `fixed post-state is unavailable` while the seeded file stayed `c2a-patch-v1` plus LF. Public WorkspacePatch must (1) treat `digest:sha256:<64 hex of the current file bytes>` as an equivalent CAS preimage to the domain-tagged workspace-image digest so P-arm/sha256sum tokens can apply a valid unified diff, and (2) request independent verification only after the Effect is `RECONCILED`/`VERIFIED`/`VERIFY_FAILED`. Wrong preimage, invalid unified diff, and built-in Pi filesystem/shell tools still fail closed. Out of scope: replacement-bytes Patch payload (EVAL-012 Priority 3), system-prompt fairness, private-candidate epoch-1 skips. Exact pushed `DEV-LINUX-NATIVE-01` plus supporting Ubuntu CI; Windows `not-run by owner-directed Linux-only route` | 无；task `done` provides implementation evidence only and does not start an EVAL, Gate, release, Profile, B01, or Agent-benefit claim |
| P9-T08 | P2-T36, P2-T37, P2-T23, P2-T24, P2-T14, P2-T21 | Secret-safe pure-Pi P-arm broker and equivalent Workspace* fixture adapter; frozen C1/C2 runner/corpus/oracle/redactor/seeds/`retry=0`/digest ledger; checkable B0 fairness contract; B01 guest isolation procedure; secret bind runbook; rewritten C1/C2 confirmatory gates; EVAL preregistration scaffolding with no samples | 无；task `done` means ready to start a newly preregistered B0, not that B0/B1/B2 ran |
| P9-T09 | P9-T08 | P-arm and O-arm `system_task_prompt_bytes` observations must come from the same frozen UTF-8 prompt bytes (not the shared placeholder `frozen-c1-c2-prompt-v1`); dry-run fairness must record the measured byte length. Out of scope: a new EVAL, B0 samples, replacement-bytes Patch, private-candidate epoch-1 skips | 无；task `done` does not start an EVAL or promote Gate/release/Profile/B01 |
| P9-T10 | P9-T09 | Live P-arm and O-arm launches must send the same UTF-8 bytes from `frozen-system-task-prompt.txt` via Pi `--append-system-prompt <absolute-path>` (P: `pi --print --append-system-prompt <file>`; O: `cognitive pi launch --print --append-system-prompt <file>` forwarding to Pi). Relative, missing, and empty prompt files fail closed. Out of scope: a new EVAL, B0 samples, replacement-bytes Patch, private-candidate epoch-1 skips | 无；task `done` does not start an EVAL or promote Gate/release/Profile/B01 |
| P9-T11 | P9-T10 | P-arm WorkspacePatch must apply the same UTF-8 unified-diff `input_b64` the O-arm daemon accepts; replacement bytes, invalid diffs, and non-UTF-8 payloads fail closed. Freeze ledger pins `workspace_patch_payload: unified-diff` and includes `fixtures/c2a/workspace-patch.unified.diff`. Out of scope: a new EVAL, B0 samples, private-candidate epoch-1 skips, changing the daemon patch applier | 无；task `done` does not start an EVAL or promote Gate/release/Profile/B01 |
| P9-T12 | P9-T11 | Freeze a campaign-only live paired P/O executor in `paired-runner.mjs`: required injected `executeArm` (no accidental spawn), live `--append-system-prompt` argv, deterministic arm order from `ARM_ORDER_SEED`, `retry=0`/`timeout_ms=120000`, fairness-before-count, `counted_sample` only for b1/b2 when fairness passes and both arms exit 0 without timeout. Dry-run cannot be labeled counted. Out of scope: a new EVAL, B01 samples, live Provider cells, changing freeze default binds | 无；task `done` does not start an EVAL or promote Gate/release/Profile/B01 |
| P8-T09 | P8-T02, P8-T03, P2-T36, P2-T37 | DeepSeek Harness (`dsh`) candidate-only AKP adapter: exact dsh revision and AKP schema digest pins, session fencing, monotonic sequence, JSONL/HTTP transport bounds, `POST /task/akp/dsh` daemon front door mapping Workspace* candidates onto the existing public candidate admission path, and linux-002 real-machine read/search/mutation plus paired Path A/B observation. dsh is never an authority writer; a dsh response is never Task completion. Failure-first: version/protocol/digest/session/sequence/epoch/authority/secret/forbidden-field/malformed/oversized/timeout/crash/restart/unknown-outcome negatives. Keys only through approved SecretStore. | 无；task `done` is implementation/tested-local/linux-002 evidence only and does not promote Gate, release, Profile, B01, EVAL, or Agent-benefit |
| P8-T10 | P8-T09 | Install DeepSeek Harness onto cognitiveos-personal as a product agent path: `cognitive dsh configure` pins the exact dsh revision and records a candidate-only adapter digest; `cognitive dsh launch` is the installed Path B (`dsh → AKP → daemon → Flash`) after daemon-owned ready state (Pi may stay `not_configured`). linux-002 must run real WorkspaceRead/Search and a disposable Write through Intent/Effect/verification/acceptance, plus same-host paired Path A vs Path B timing (n≥5, cold/warm, TTFT/total, retained failures). Path A stays measurement-only and is not a product launch path. A dsh response is never Task completion. | 无；task `done` is implementation/tested-local/linux-002 evidence plus performance observation only and does not promote Gate, release, Profile, B01, EVAL, or Agent-benefit |
| P8-T11 | P8-T10 | Fix high Provider-path latency without weakening A1–A8: diagnose raw Provider vs bare dsh vs installed Personal dsh on linux-002; pass `stream:true` through the daemon Provider proxy (no SSE-to-unary wait); keep Pi unary and private-candidate non-streaming; expose daemon-owned dsh runtime inspect (`GET /personal/dsh/runtime` + `cognitive dsh status`) for session/process/lifecycle; run a real-task paired A/B corpus (read/search/write + one non-`pong` LLM task) with n≥5 retained. Path A remains measurement-only. A dsh response is never Task completion. | 无；task `done` is implementation/tested-local/linux-002 evidence plus performance observation only and does not promote Gate, release, Profile, B01, EVAL, or Agent-benefit |
| P8-T12 | P2-T02, P2-T05, P4-T05, P2-T25 | Common Resource Manager envelope (`list`/`inspect`/`bind`/`unbind`/`enable`/`disable`/`revoke`) over existing six-family stores; management-only HTTP; task-channel and generic create/install/execute/complete fail closed; `cognitive resource` caller; exact-revision `DEV-LINUX-NATIVE-01` 真机实测. Watch stays on existing `/resource/v1/watch`. No public generic Resource DTO. | 无；task `done` is implementation/tested-local evidence only and does not promote Gate, release, Profile, B01, EVAL, or Agent-benefit |
| P8-T13 | P1-T02, P1-T03, P1-T04, P1-T06, P1-T07, P8-T09, P8-T10 | Owner-local LLM Provider Control Plane: named OpenAI/Anthropic/OpenAI-compatible accounts, Secret Store opaque refs, explicit endpoint trust, model catalog (discovered/manual), fixed agent bindings, usage/cost/audit, soft budgets/alerts, management CLI. Maps design cards PCP-T01..PCP-T07. No fallback/routing/OAuth/UI. Pi and DeepSeek harness are independent adapter paths. | 无；task `done` is implementation/tested-local evidence only and does not promote Gate, release, Profile, B01, EVAL, provider-quality, or Agent-benefit |
| P8-T14 | P8-T13 | Bilingual operator handbook (en + zh-CN) matches shipped daemon API and `cognitive` CLI; docs-sync/consistency green. Documentation-only; does not reopen P8-T13. | 无；documentation-only，不构成 Gate、release、Profile、B01、EVAL、provider-quality 或 Agent-benefit |
| P8-T15 | P8-T10, P8-T11, P8-T14 | Native dsh Web UI control panel via `cognitive dsh web` (`dsh --profile web --no-open`, loopback `http://127.0.0.1:3080`); frontend dist fail-closed; Path B AKP+SecretStore preserved; not Personal `/ui/`. linux-002 usable GET `/` HTML. | 无；task `done` is implementation/tested-local/linux-002 evidence only and does not promote Gate, release, Profile, B01, EVAL, or Agent-benefit |
| P3-T01 | P2-T01/P2-T02 稳定 application contracts；**不要求 P2-T08 Gate** | real Context workspace/task/evidence source、scope-before-ranking、owner-local management-session MVP authorization | B03 |
| P3-T02 | P3-T01 stable Context source port | minimum Context Builder、required fail-closed、显式 loss 与预算 | B03 |
| P3-T06 | P3-T05 | MVP B03 fixed 33-test Context correctness matrix、native Linux/Clippy、required Ubuntu/Windows CI、redacted cleanup and owner review; B06/B07 only collect repeatable raw metrics | GMVP-LINUX Gate composition requires B03; B06/B07 do not block |
| P4-T01 | P3-T01/P3-T02 stable ports | Memory store/admission/policy 与 provenance/scope/freshness | B08 |
| P4-T05 | P4-T01/P4-T02/P4-T03/P4-T04；**不依赖 embedding** | Memory/Skill APIs、统一 projection、actual Context/Task consumption | B08 |
| P4-T06 | P4-T05 | B08 Memory + Skill lifecycle/correctness 与 UCR-01 actual consumption | GMVP-LINUX requires B08 |
| P5-T01 | P0-T04、P0-T06、P1-T08 | official npm exact Pi acquisition；SRI/digest/acquisition-lock；install/upgrade/rollback/uninstall negatives | B09 |
| P5-T02 | P5-T01 与所需 P2-T03/P2-T06 supervision contracts | sidecar contract/registration、instance/process identity、health、epoch fencing、pause/resume/stop、Pi foundation | B09 |
| P5-T05 | P5-T02 | B09 managed Pi + sidecar qualification；任务完成与 B10 解耦 | GMVP-LINUX requires B09 |
| P7-T01 | P0-T03、P1-T08、P2-T08 | production signing、immutable action/tool pins、SBOM、attestation、release manifest 与 acquisition-lock trust | GMVP-LINUX |
| P7-T08 | P1-T09、P2-T08、P3-T06、P4-T06、P5-T01/P5-T02/P5-T05 B09、P7-T01..T03 | Linux 1.0 六类资源 release manifest、native systemd、desktop/headless SecretStore、Pi sidecar、UCR-01 fixed-scenario correctness/utility、lifecycle/backup/doctor evidence | **B01+B02+B03+B04+B05+B08+B09+B12**；B06/B07/B10/B11 不阻塞 |

### Personal 2.0 Phase 10 typed dependencies

| Task | implementation_requires | acceptance_requires | promotion_requires |
|---|---|---|---|
| P10-T01 | owner-accepted 2026-08-27 scope；ADR-0037/0043/0055 与 finalized Personal 1.0 baseline | ADR-0056/0057 accepted；ADR-0037 partial-supersession backlink；formal plan/trace/support/version/current snapshot 与 stable product docs 同步；import 能力诚实标 `Requires-backend`；documentation-only validation | 无；不创建 Gate |
| P10-T02 | P10-T01 | Lane-CTR decision 覆盖 MCP family 与 common conversation projection 的 public/private boundary、identity/version compatibility、capability digest、binding、health/quarantine、P5-era migration、older-client fail-closed；若改变 machine contract，schema/generated binding/negative 同批 | 无；task `done` 不构成 support/release/Profile |
| P10-T03 | P10-T02、P5-T03、P5-T04、P8-T12 | daemon-owned MCP server/package/connection/capability/binding/health/quarantine lifecycle；advertised tools 走 Tool candidate/admission；resources/prompts 走 Context/Skill admission；SecretStore、Intent/Effect、fencing/reconcile negatives | 无；任何后续 support/promotion 需独立正式决策 |
| P10-T04 | P10-T02、P10-T03、P7-T05、P8-T13 | desktop primary entry；Home/Agents/Work/Library/Activity/Settings；Providers/System under Settings；global Agent Shell candidate-only；vendor conversation adapters + honest capability matrix；native Agent apps usable；缺 import backend 显示 `Requires-backend` | 无；不从现有 Web UI/Agent evidence 推导 2.0 support |

#### Context MVP authorization scope

The first runnable P3-T01/P2-T04 Context path uses one owner-local management
session as its admission boundary. It preserves daemon-only writes, immutable
source provenance, tenant/scope/conversation filtering, body-after-metadata
ordering, and Pi's candidate-only boundary. It does **not** require complete
multi-principal role evaluation, capability-chain attenuation, dynamic deny
rules, or revocation-policy administration before the MVP path can run. The
existing durable authorization/revocation ledger remains compatible hardening
infrastructure and its regressions remain valid, but advanced policy rollout
is a subsequent optimization rather than an MVP implementation mutex. This
scope change does not relax Intent/Effect, secret handling, scheduler fencing,
independent verification, Task acceptance, formal campaign, release, or
Profile requirements.

### Delivery Slice register（任务内交付出口）

Delivery Slice 是正式 `P*-T*` 任务内的可检查执行单元，不新增任务、Gate、REQ 或
产品声明，也不替代完整任务交付边界。Slice 的定义、依赖和出口由本节拥有；当前 `ready`、`in-progress`、`blocked`、
`done`、`cancelled` 状态及实际 evidence 只写入 [PROGRESS.md](PROGRESS.md) 的
Current snapshot。一个 slice 必须有一个真实 caller、durable authority outcome、可验证
端到端边界或闭合的负例出口；单独的 helper/parser/boundary 不能连续形成交付出口。每个
slice 至少需要 focused failure-first/negative test 和其定义的 supported validation，
除非明确是 non-executable documentation-only slice。实现存在但 required validation 未
执行时只能是 `blocked`，不能标为 `done`。一个任务只使用一个 task branch、Draft PR 和
task lease；一个 slice 完成后在同一工作流中立即继续下一 slice，直到最终 slice 同时完成
formal task acceptance assessment 和收口。

| Slice ID | Formal task | Delivery outcome and exit | Implementation dependency | Required validation / next dependency |
|---|---|---|---|---|
| `P0-T08/D01` | P0-T08 | ADR-0054 子项目目录结构一次性落地：core/personal/enterprise 目录 git mv、clients subtree 并入、三份 VERSION-1.0.0 边界/定稿文档、全部路径承载工具链与文档索引/规则重写、治理登记（plan/PROGRESS/lease/identity）与 ready/merge/tag/收口 | 两仓库 `main` clean（kernel PR #272 与 clients PR #7 已合并）；ADR-0054 | 本地 `check:consistency`、`check:handbook` + generator `--check`、tools suite、`docs-sync-gate`、`pnpm -r build/test`、`cargo metadata`、`git diff --check` 全绿；Rust build/test/Clippy/fmt/conformance/golden 按既有路由走 required CI（Ubuntu+Windows）于待合并 exact HEAD；合并后打 `core-v1.0.0`/`personal-v1.0.0` annotated tag 并完成 lease/branch/main 收口 |
| `P2-T01/D01` | P2-T01 | TaskApplicationService 的 proposal/clarify/preview/admit/control/query 六操作面；raw intent 先持久化、preview digest 绑定、epoch fencing、stale lease 负例全部闭合 | 既有 Intent/TaskContract kernel 与 authority store | Linux focused tests、store regressions、Clippy、fmt、required CI；已闭合后 task 可按正式验收标 `done` |
| `P2-T03/D01` | P2-T03 | scheduler persistence、CAS lease、owner/epoch fencing、next-eligible、cancel 与 monotonic clock eligibility | P1-T01、P2-T01 | scheduler/store focused tests、fmt、Clippy、required CI；完成后进入 authority ceiling slice |
| `P2-T03/D02` | P2-T03 | 从 durable TaskContract/progress/budget authority facts 计算 ceiling，STOP 在 lease acquisition 前注册并保持 fail-closed | `P2-T03/D01` 与既有 TaskContract/transition contracts | exact-Linux authority tests、STOP-ordering regressions、fmt/consistency；完成后进入 Effect closure slice |
| `P2-T03/D03` | P2-T03 | 通过 immutable TaskBinding→Intent reverse lookup 唯一解析 durable Effect，并对缺失、歧义、不一致、未知状态 fail-closed | `P2-T03/D02`、现有 Intent/Effect store ports | exact revision Linux storage/classifier tests；完成后接入 D04，验证阻塞时不得另开同任务 helper slice |
| `P2-T03/D04` | P2-T03 | 真实 leased dispatch 读取 D03 closure disposition，仅 Closed 执行 owner+epoch-fenced scheduler release；Pending/STOP 保留 reconciliation | `P2-T03/D03` 与 daemon worker boundary | exact revision Linux runtime/kernel tests、required CI；完成后才进入 D05 |
| `P2-T03/D05` | P2-T03 | candidate WIA 的 exact lease-bound handoff、Effect recovery/closure 与 private scheduler tick；只有 P2-T07/D01 发行的 continuation authority 才能进入 BoundedHarness | `P2-T03/D04`、`P2-T07/D01` | failure injection + worker/recovery integration tests；完成后为 P2-T03 task acceptance 汇总入口 |
| `P2-T07/D01` | P2-T07 | daemon-private fixed post-state、verification request/report、currentness revalidation、checkpoint 与 append-only continuation authority；只允许 `ACT -> VERIFY -> CONTINUE -> OBSERVE`，绝不触发 Task acceptance/completion | `P2-T03/D05` WIA/recovery boundary与既有 Loop/Effect contracts | durable positive/negative/restart tests、exact Linux validation、required CI；完成后由 D05 消费 continuation authority，P2-T07 的 Artifact、完整 criteria evidence 和 Task completion 仍不因此关闭 |
| `P2-T07/D02` | P2-T07 | daemon-private independent verifier seam that reloads the immutable verification request and fixed post-state, validates currentness and verifier identity, and persists only content-addressed artifact evidence references in an append-only report | `P2-T07/D01` fixed post-state/request/report persistence | verifier identity mismatch, stale post-state, malformed/duplicate artifact reference, fenced writer, and passed-without-evidence negatives; exact Linux validation and required CI; no Task acceptance/completion |

| `P2-T04/D01` | P2-T04 | private scheduler-to-deterministic-Context-to-pinned-Pi candidate worker composition；Pi output is an opaque candidate only, while scheduler lease, fencing, budget, WIA/continuation, Effect, progress, evidence, and Task state remain daemon-owned | `P2-T02`、`P2-T03/D05`、`P2-T07/D01` | real-store stale lease/fence, required Context failure, duplicate tick, exhausted budget, and self-report rejection coverage; exact Linux validation and required CI; no Tool execution or Task completion |
| `P2-T06/D01` | P2-T06 | validated daemon-private `WorkspaceRead` executor accepts only a descriptor-bound, Intent-keyed staged request; it fences stale writers before I/O, serializes duplicate key dispatch, retains only bounded/redacted output, and answers recovery queries using the original key | P2-T05 static native Tool catalog and validators | failure-first executor negatives plus exact-revision native Linux focused test and required CI; completion enables durable Effect dispatch wiring, without claiming progress, evidence, verification, Task completion, Gate, release, or Profile |
| `P2-T06/D02` | P2-T06 | wire one read-only `WorkspaceRead` through the existing durable Intent/Effect protocol: stage only after the persisted Intent reload, commit `EXECUTING` before filesystem dispatch, record an explicit outcome, and reconcile an unknown outcome by the original idempotency key | `P2-T06/D01` and the existing P2-T03 durable Effect/WIA boundary | real SQLite persist-before-dispatch, unknown-outcome/restart, stale-fence, duplicate-key, bounded-redacted output regressions; exact-revision native Linux and required CI; completion enables the remaining process supervision and mutation family work |
| `P2-T06/D03` | P2-T06 | daemon-private bounded process/check execution with supervisor-owned lifetime, timeout/orphan containment, redacted bounded output, and durable unknown-outcome reconciliation; process execution must consume the same pre-executor descriptor and Effect boundary rather than bypassing it | `P2-T06/D02` and P2-T05 process/check validator | failure-first before/mid/after fault, timeout, orphan, stale fence, output-limit, redaction, idempotency, and restart/reconcile coverage; exact-revision native Linux and required CI; no Task completion or release claim |
| `P2-T06/D04` | P2-T06 | daemon-owned private process supervisor seam with registered attempt identity, PID ownership, fencing/recovery lifecycle, bounded observation source, and fail-closed default; no arbitrary PID attach, public Process resource, or Task-completion implication | `P2-T06/D03` and daemon single-instance/lifecycle boundary | exact Linux supervisor lifecycle tests, supported CI, source-injection boundary review, and explicit evidence that absent production observation wiring fails closed; completion enables final P2-T06 acceptance assessment but does not itself claim product process management |
| `P2-T02/D01` | P2-T02 | real Task API/watch vertical path：server-issued preview→admit、watch cursor resume/dedup，并让 CLI/Shell/sidecar 共享 application service | P2-T01/D01、P1-T07 channel contracts | Rust/TS focused integration、channel isolation negatives、required CI；完成后继续 projection/parity slices |
| `P2-T02/D02` | P2-T02 | private versioned six-family Resource projection/list/watch, family-scoped cursor and task/management channel isolation; unavailable authority sources must be explicit rather than fabricated | P2-T02/D01 and existing daemon authentication boundary | focused daemon process negatives, exact-Linux projection test, fmt, required CI; completion enables deterministic CLI parity |
| `P2-T02/D03` | P2-T02 | deterministic CLI calls the same daemon Task/resource operations with distinct Task/management tokens, caches, cursors, and mutation retry policy | P2-T02/D01/D02 and admin CLI daemon client | daemon-plus-CLI process parity, channel/retry/cursor negatives, exact Linux, required CI; completion enables Shell sidecar parity |
| `P2-T02/D04` | P2-T02 | Pi Shell private sidecar calls the same daemon Task/resource application surfaces and remains a non-authority client | P2-T02/D03 and P1-T07 Pi client boundary | TS parity/isolation negatives, daemon-side integration, exact Linux, required CI; completion is P2-T02 task-closure evidence entry |
| `P3-T01/D01` | P3-T01 | daemon-issued TaskContract v0.4 strong ContextRequest binding plus append-only durable ContextRequest/ContextView persistence and daemon query/reload validation; request perspective must name the Task, views remain request-linked per-resolution artifacts, and no Pi output or Context record becomes Task/progress/evidence/acceptance authority | P2-T01/P2-T02 stable application contracts; existing Context schema and governed-object binding contract | focused schema/store/intent-binding positive and mismatch/replaced-reference/task-perspective negatives; exact-revision supported Linux validation and required CI. Until these run and the formal workspace/task/evidence source, scope-before-ranking, and revocation exits are satisfied, this slice remains in-progress and B03 remains not-run. |
| `P3-T02/D01` | P3-T02 | daemon-owned required System/Task fragments built from immutable ContextRequest/TaskContract inputs before ContextView persistence and private Pi transport; required hard-budget failure and semantic duplicate-content loss are explicit | P3-T01 stable source port and P2-T04 sealed ContextView-before-Pi path | focused kernel/scheduler negative tests, exact native Linux validation and Clippy; completion enables source-family/freshness trace work without making a B03/UCR-01/release/Profile claim |
| `P3-T02/D02` | P3-T02 | complete source-family policy, freshness, source digest/included-excluded trace, and loss/usage acceptance over the real scheduler builder path | `P3-T02/D01` and existing immutable ContextRequest/ContextView contracts | focused stale/source-role/trace negatives, exact native Linux validation, required Ubuntu/Windows CI, and final P3-T02 acceptance assessment; no benefit or Gate claim without separate formal campaign evidence |
| `P3-T03/D01` | P3-T03 | one daemon-owned bounded filesystem CAS with SHA-256 references, immutable metadata, atomic publication, caller-policy access admission, and interrupted-write cleanup; it must not create a parallel checkpoint/evidence store or infer authorization from a digest | P3-T02 stable Context Builder port and existing P2-T07 artifact-reference format | digest mismatch, partial-write cleanup, unauthorized fetch, and bounded-size negatives; exact native Linux validation and required Ubuntu/Windows CI; completion enables real Artifact consumers without a B03/Gate/release/Profile claim |
| `P3-T03/D02` | P3-T03 | connect P2-T07 verifier evidence to D01 so a syntactically valid artifact reference without present, digest-valid CAS bytes fails before report persistence | `P3-T03/D01` and the existing daemon-private verification executor | missing/tampered artifact and report-non-persistence negatives; exact native Linux validation and required Ubuntu/Windows CI; completion closes P3-T03 acceptance without Task completion or Gate/release/Profile claim |
| `P3-T04/D01` | P3-T04 | daemon-private Context cache boundary for stable-prefix and delta reuse, with a cache key bound to source versions, governance facts, Task epoch, and Tool identity; stale or revoked material must never be reused and telemetry must remain bounded/redacted | completed P3-T02 Context Builder and P3-T03 Artifact CAS | failure-first stale/revoked/missing-source cache negatives and stable-prefix/delta reuse tests; exact native Linux validation and required Ubuntu/Windows CI; completion enables governed builder integration without a B03/Gate/release/Profile claim |
| `P3-T04/D02` | P3-T04 | daemon-private loop-control observation that derives repeat/no-progress decisions from durable action, error-classification, and evidence-digest facts; switch/wait/block remains bounded and cannot create a Task completion claim | `P3-T04/D01` plus existing fenced append-only progress/retry/stagnation primitives | repeat-signature, changed-evidence, malformed-fact, retry/stagnation ceiling, and no-completion-authority negatives; exact native Linux validation and required Ubuntu/Windows CI; completion closes P3-T04 acceptance without a B03/Gate/release/Profile claim |
| `P3-T05/D01` | P3-T05 | daemon-private UCR-01 workload runner and stable-baseline artifact boundary; one task trace must exercise the six resource families without converting raw measurements into Gate or release claims | `P3-T04` Context/cache/loop boundary | fixed-scenario runner contract, raw-run capture, pinned baseline comparison, bounded/redacted measurement negatives, and non-claim report tests; exact native Linux validation and required Ubuntu/Windows CI; no B03/B06/B07/Gate/release/Profile pass claim |
| `P3-T06/D01` | P3-T06 | preregistered B03 Context correctness campaign manifest and non-claim evaluator; captures Context authorization/freshness/required-source/false-completion observations without allowing the runner to set Gate state | `P3-T05` UCR-01 raw-run boundary | manifest validity, complete required observation set, failure-first stale/unauthorized/required-source/false-completion negatives, and no-Gate-authority tests; exact native Linux validation and required Ubuntu/Windows CI; B03 stays `not-run` pending a supported campaign execution |
| `P3-T06/D02` | P3-T06 | execute the registered B03 Context correctness evidence matrix against real Context, Artifact, revocation, and cache authority paths; preserve complete redacted outcomes for independent review | `P3-T06/D01` complete evaluator and owner-approved campaign preregistration | exact native Linux focused Context/Artifact/cache tests, complete assertion/negative accounting, cleanup, independent-verifier review, and no B03 claim before review |
| `P4-T01/D01` | P4-T01 | daemon-private append-only Memory proposal-to-deterministic-admission vertical slice; persist a source-bound proposal and reason-coded decision, creating an immutable Memory object only after daemon policy admission | completed P3-T01/P3-T02 Context source and builder ports plus existing authority-store conventions | failure-first direct-admit, source/version/provenance mismatch, invalid freshness/retention, cross-scope promotion, conflict/replay, and no-partial-object tests; exact native Linux validation and required Ubuntu/Windows CI; no FTS/retrieval, lifecycle/forget, public API, Gate, release, or Profile claim |
| `P4-T03/D01` | P4-T03 | daemon-private forget/tombstone transition appends an auditable lifecycle fact, atomically invalidates the current FTS row, and prevents a stale or rebuilt derived index from resurrecting forgotten Memory | P4-T01 admission/object store and P4-T02 rebuildable FTS5 index | failure-first unknown/duplicate forget and stale-index non-resurrection tests; exact native Linux focused store validation, Clippy, and required Ubuntu/Windows CI; implementation-only with version/update/conflict/expiry and public Memory API still remaining |
| `P4-T03/D02` | P4-T03 | daemon-owned expiry sweep appends a reason-coded lifecycle fact for each retention-expired admitted Memory object and atomically invalidates its derived FTS row without deleting immutable admission history | `P4-T03/D01` lifecycle audit and existing retention metadata | failure-first expiry-boundary, duplicate-sweep, and stale-index non-resurrection tests; exact native Linux focused store validation, Clippy, and required Ubuntu/Windows CI; versioned update/conflict and public Memory API still remain |
| `P4-T03/D03` | P4-T03 | daemon-owned versioned Memory replacement uses an expected-version CAS, preserves immutable version lineage, appends a supersede audit fact, and atomically moves the derived FTS row | `P4-T03/D01` lifecycle audit and existing immutable Memory admission rows | failure-first stale-version/conflict and superseded-index non-resurrection tests; exact native Linux focused store validation, Clippy, and required Ubuntu/Windows CI; public Memory API and B08 remain separate |
| `P8-T01/D01` | P8-T01 | 公理体系与治理规则收敛：单一公理文档（AXIOMS）落地并由 Operating Model §8 指向；AGENTS/.cursor 规则薄入口化并消除清单漂移；PARALLEL-LANES closed 历史外置；ADR-0008 标注 superseded；milestone prompts 标注 non-executable；docs/standards 状态标签校正；PROGRESS Layer 3 过期 Gate 行、GMVP 行、PERSONAL-TEST-ENVIRONMENTS 截断/过期文案与本计划结构错乱修复。documentation-only 出口 | 已合并 `main`（P7-T04 收口后）与既有治理文档 | 本地 `pnpm run check:consistency`、`git diff --check`、守卫片段与相对链接核验；task Draft PR 上的 required Ubuntu/Windows CI；无 Gate/release/Profile claim |
| `P8-T01/D02` | P8-T01 | 现行白皮书（OS 定位正名/对标差距/三支柱工程原则/生态定位）、产品与架构设计扩展（通用 Agent Adapter Contract、多 agent 编排、性能架构、IoT 与企业多租户延伸余量章）、`personal-2.0-scope` 与 ADR-0041+ 系列决策文档 | `P8-T01/D01` 公理与规则基线 | 本地 consistency/链接核验与 required Ubuntu/Windows CI；documentation-only，设计文档不改 specs 机器合同，不产生实现/Gate/release/Profile claim |
| `P8-T01/D03` | P8-T01 | 任务文档扩展收口：P8/P9 任务卡细化与 typed dependency 对齐、根 `docs/plan/plan.md` 研究层同步、traceability 联动、全栈交叉校验、完整 acceptance mapping 与任务收口 checkpoint | `P8-T01/D02` 设计文档基线 | 本地 consistency/diff/链接核验、required Ubuntu/Windows CI、closure checkpoint 与 lease/branch/main 收口；documentation-only |
| `P10-T01/D01` | P10-T01 | ADR-0056 desktop-first Control Plane 与 ADR-0057 MCP seventh-family accepted；ADR-0037 仅对 Personal 2.0 family count 增 partial-supersession backlink；formal plan/trace/support/version/current snapshot 与 stable product semantics 对齐，明确 ADR-0055 import `Requires-backend`、1.0 six-family 不变、无 generic `Resource` schema | owner-accepted 2026-08-27 scope；ADR-0037/0043/0055；finalized Personal 1.0 baseline | documentation/static consistency、相对链接与 `git diff --check`；只含文档语义，不改 code/contracts/tests/generated references，不创建 Gate/release/Profile claim；完成后进入 D02 |
| `P10-T01/D02` | P10-T01 | 将 desktop IA、global candidate-only Agent Shell、vendor conversation projection/capability matrix、native Agent app coexistence 与 MCP family lifecycle 同步到详细 product/architecture/bilingual design surfaces；所有未实现 import/backend action 显示 `Requires-backend` | `P10-T01/D01` | documentation/static consistency、链接/terminology/secret-boundary review；不得发明 backend capability；完成后进入 D03 |
| `P10-T01/D03` | P10-T01 | 全量 acceptance mapping、source-of-truth/版本边界复核、docs-sync/handbook closure、唯一 closure record 与 task/lease/branch/main deterministic closure | `P10-T01/D01-D02` | `check:consistency`、affected handbook/generator/docs-sync gates、`git diff --check` 与 required CI；documentation-only；无 Gate/release/Profile/B01/Agent-benefit claim |

> **历史收口注记（原位于本节中部的"收口记录"，移此保留）：** `P2-T07` 已完成并在
> PR #164 中合并到 `main@7e75e6642d289e1127928c79fed116e00b61c987`；
> `lease/personal/P2-T07/checkpoint-artifact-verifier` 已关闭；后续只从本计划中选择
> 下一个正式任务，不再将该 lease 视为 active。

**执行顺序约束：** `P2-T03/D03 -> D04 -> D05` 是同任务闭合顺序；required validation
未满足时不得越过该 slice 新增横向 helper。`P2-T02/D01` 的 implementation dependencies
不包含 B01 或 P2-T03 acceptance，可作为不相关的并行 forward-progress 出口。所有 slice
当前状态只查 `PROGRESS.md`。B01 attempt 统计、clean-reset checkpoint 和 Gate threshold
不由本 register 改写。

### Lane-CTR public-contract prerequisites

后续实际需要 public machine contract 时，Lane-CTR 分别登记并评审
`skill-manifest`、`operation-descriptor`、`agent-adapter-manifest`、TaskContract resource
bindings、server-issued preview/admit 与 Memory codegen。P2-T02/D01 当前仅冻结
`task.preview` / `task.admit` 的窄 request/result schema 与 generated bindings：draft 不可
携带 governed header、epoch、acceptance 或 preview digest；daemon 签发 preview 并在任何
authority mutation 前重算 draft digest、核验显式 acceptance 和 epoch CAS。既有 watch
cursor-resume/dedup contract 不在此批改变。统一 Personal projection 先保持 private +
versioned，出现第二个真实 adapter/client 后再评估最小 public `ResourceSummary`。禁止为此
新增 `Process` domain 或 giant `Resource` schema。

### Informative open-source references

| Reference | 可吸收边界 | 明确拒绝/不得推断 |
|---|---|---|
| Letta Code、Mem0 | Memory/agent-memory UX、provenance 和 lifecycle 研究输入 | 不采纳其存储为 authority，不据宣传推断 B08 |
| LangGraph | checkpoint/thread/store 概念对照 | 不替代 CognitiveOS recovery、fencing、Effect 顺序 |
| OpenHands ACP、Goose | Agent/sidecar protocol、headless lifecycle 研究输入 | 不继承 adapter qualification，不授予 daemon authority |
| ToolHive | MCP/tool packaging、隔离与 operation catalog 研究输入 | MCP/dynamic marketplace 保持 post-1.0，不绕过 native Tool Registry |
| Anthropic Agent Skills | local Skill package、revision、binding 的 informative 输入 | 不允许 skill 自授权、直接执行或成为 public contract 真相源 |
| ElizaOS | 多 Agent/plugin 生态对照 | Multi-Agent 保持 post-1.0 默认关闭 |

这些参考的 star 数、下载量、宣传文案或自报 benchmark 均不是 CognitiveOS
implementation、Gate、release、Profile 或选型证据。

### 本机实验轨道（不改变产品 Gate）

`experimental-local-only` 允许在正式阶段 Gate 尚未完成时并行实现和测试
P1/P2/P3 及后续 Personal 功能。它只表示代码和测试被限制在本机、临时目录或
明确隔离的实验环境；`tested-local` 只表示该次命令真实执行过。两者都不能填写
为 `implemented`，不能生成 Profile/release 证据，也不能把 sample、fixture、
smoke 或局部测试升级为产品验收。

允许并行推进的工作包包括：

- Pi proxy、Task runtime、scheduler、Effect、recovery、Context、Memory；
- 真实 Pi Extension/RPC load（只保存脱敏的事件、计时和状态结果）；
- `kernel/store/runtime` benchmark harness；
- Personal 端到端性能 runner，并明确拆分 CognitiveOS deterministic overhead、
  Pi/Node process overhead、Provider/network/model latency、filesystem/SQLite overhead。

实验轨道的硬约束仍与正式路径相同：secret 不得进入日志、argv、普通配置、
SQLite、证据或 CI；Pi、CLI、SDK、UI 不得成为 authority；状态迁移、授权、CAS、
预算、幂等、fencing、Effect 提交和完成验收只能由确定性服务端代码执行；规范向量
只能被真实规范修正流程修改，不能为实现迎合。

后续 Linux-native development smoke 使用环境资格清单，而不是绑定唯一主机：Linux x86_64、non-WSL、native user-systemd、支持的 Secret Service、可清理/重置、exact artifact/Pi pins 和脱敏 evidence collector。`wuz@192.168.1.2` 是优先候选而非唯一允许环境。只有预注册的 formal campaign 环境能推进 B01；其他合格主机仍按 `experimental-local-only` / `tested-local` 记账，并与 CI Ubuntu、Windows/MSVC 证据分开陈述。

正式平台 campaign 与 A/B/C/D agent-benefit 测评后置到固定平台、固定拓扑、
预注册 workload 和独立 verifier 准备完成之后；在此之前所有 Personal 性能结果
均属于 local experimental evidence，收益字段必须保持 non-claim。

## 4. 任务进度表

> **填写约定：** “证据/备注”须写测试命令、evidence digest、PR、handoff 或阻塞原因。首次领取时只改当前任务，避免并行会话争用本文件。

### Phase 0 - 基线与决策

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P0-T01 | 固定可复现基线与支持工具链 | — | Linux runner 与支持的 Windows 工具链结论可复现 | done | 2026-07-25；Lane-DOC `lane/personal-p0-t01-baseline-2`。基线 `01ceb93`：CI run [30140381194](https://github.com/agentkernel/cognitive-os/actions/runs/30140381194) 的 Ubuntu/Windows jobs 均成功；本机 `pnpm install --frozen-lockfile`、3 次 `pnpm -r build` + `pnpm -r test`（p50 29.669s）和 `cargo fmt --all -- --check` 通过。本机 Windows GNU `cargo build --workspace --locked` 在 linker exit 121 下失败，LLVM-MinGW/shim 重试同样失败；因此支持组合明确为 CI Linux 与 Windows/MSVC，GNU host 非支持基线。详见 [personal/tests/baseline/README.md](../../personal/tests/baseline/README.md)。 |
| P0-T02 | 冻结 Personal 需求、追踪与架构边界 | P0-T01 | PERS-PR、任务、benchmark 无孤儿映射 | done | 2026-07-25；Lane-DOC `lane/personal-p0-t02-trace`。新增 [personal-trace.yaml](personal-trace.yaml)：20 个 PERS-PR、51 个正式任务和 21 个 Gate/benchmark 均可交叉核对；仅引用真实登记 REQ，product-only 项明确为空映射，所有证据状态为 `not-run`。验证：专用 Node 映射核对、`pnpm run check:consistency`、`git diff --check` 均通过。 |
| P0-T03 | License、首发平台与分发决策 | P0-T02 | owner GO/NO-GO 与 notices 完整 | done | 2026-07-26；PR [#99](https://github.com/agentkernel/cognitive-os/pull/99) 合入 `main@fd6ff6b`。Owner GO：Apache-2.0；首发产品平台 Linux x86_64 + Windows x86_64；GitHub Release 可检查 bundle；**不** vendor Pi/Node；crates.io/npm 仍不发布。交付：根 `LICENSE`/`NOTICE`、[ADR-0025](../adr/0025-personal-license-platform-distribution.md)、[THIRD-PARTY-NOTICES](../legal/THIRD-PARTY-NOTICES.md)、[PERSONAL-SUPPORT-MATRIX](PERSONAL-SUPPORT-MATRIX.md)；workspace `license=Apache-2.0` 且 `publish=false`/`private=true`。验证：`pnpm run check:consistency`、`git diff --check`；CI [30180002937](https://github.com/agentkernel/cognitive-os/actions/runs/30180002937) / [30179991223](https://github.com/agentkernel/cognitive-os/actions/runs/30179991223) Ubuntu/Windows-MSVC SUCCESS。非 G0/B01-B12/Profile；SBOM/attestation 归 P7-T01。handoff：[20260726-personal-p0-t03-license-platform-distribution-handoff.md](../checkpoints/20260726-personal-p0-t03-license-platform-distribution-handoff.md)。 |
| P0-T04 | 数据布局、迁移、备份与回滚设计验证 | P0-T02 | migration dry-run、重放与失败恢复评审 | done | 2026-07-25；Lane-KRN `lane/krn-personal-p0-t04-migrations`，PR #89。ADR-0017 + adapter-local `schema_migrations` dry-run/apply/replay/digest-drift/failure-recovery tests；CI run [30150183941](https://github.com/agentkernel/cognitive-os/actions/runs/30150183941) 在 Ubuntu 与 Windows/MSVC 均通过 `cargo test --workspace --locked`。本机 Windows GNU linker exit 121 是已知非支持环境，不再阻断。未修改 registry/schema/vector 或 authority transition 语义。 |
| P0-T05 | Linux Secret Service PoC | P0-T01 | set/get/rotate/delete 与泄漏负例通过 | done | 2026-07-25；分支 `lane/personal-p0-t05-secret-store-api`，PR [#90](https://github.com/agentkernel/cognitive-os/pull/90)。隔离 crate `cognitive-secret` + ADR-0018：冻结 `SecretStore::{probe,put,get,delete}` 与 opaque `SecretRef`；模拟 put/get/rotate/delete、absent/locked/prompt fail-closed、Debug/Display/env 泄漏负例；Linux native probe-only（mutating D-Bus 归 P1-T02）；无明文 fallback。CI run [30153311857](https://github.com/agentkernel/cognitive-os/actions/runs/30153311857) Ubuntu/Windows-MSVC `cargo test --workspace --locked` 通过（含 `p0_t05_secret_store`）。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile 声明。handoff：[20260725-personal-p0-t05-secret-store-api-handoff.md](../checkpoints/20260725-personal-p0-t05-secret-store-api-handoff.md)。 |
| P0-T06 | Pi 版本、Extension 与 RPC 兼容性 PoC | P0-T03 | 固定版本、integrity、Extension/RPC fixture 通过 | done | 2026-07-27；Lane-RUN `lane/run-personal-p0-t06-extension-poc`。第一个原子部分已固定 `@earendil-works/pi-coding-agent@0.81.1`、npm SRI、source commit、repository path 与 Node engine；candidate adapter 在读取 scoped Provider key 前以 `pi --version` fail-closed 拒绝版本漂移；strict-LF JSONL parser fixtures覆盖 CRLF normalization、U+2028 preservation、malformed/non-object negative。第二个独立原子部分新增 pinned-API Extension fixture：`project_trust` 固定拒绝、`write`/`edit`/`bash` 在 `tool_call` 前阻断、`session_start` 仅展示 session-local status，且 Rust safety guard 断言无 provider credential/durable-state access。Owner 于 2026-07-26 批准默认关闭的本机 Linux 开发例外：只有显式 `--allow-local-native-provider-secret-development` 与独立 `--provider-config-dir` 同时提供时，adapter 才从 `ProviderKeyService`/native Secret Service 解析已配置的 DeepSeek key，并只注入初始 Pi 子进程；不读取父进程 Key 环境变量，Windows/CI/unavailable backend fail-closed，例外在 P2 结束到期。后续 Linux-native 本地执行优先使用相和歌设备 `wuz@192.168.1.2`，并继续与 WSL/CI 证据分列。WSL test：`CARGO_TARGET_DIR=/tmp/cognitiveos-p0-t06-exception-target-two cargo test -p pi-agent-adapter --offline`（16 passed）。无 Provider key 的 RPC clean-run 无法自然退出，未将 CLI version check 表述为 Extension runtime load evidence。第三个原子部分新增 `extension-load` 证据动词：只接受 pinned fixture 路径与已注册的 `/cognitiveos-p0-t06-status` 命令，以 `--mode rpc` 驱动一次真实 Pi 子进程会话，读取 `get_commands`/`get_state`/`prompt` 三条 RPC 响应，30s 超时后强杀，并只输出**脱敏且不含原始输出**的证据记录（固定 `authority_committed=false`/`effects_created=false`/`task_transitions=0`/`capabilities_granted=0`/`classification=uncontained_candidate_only`）；同时把 host 分类从 `cfg!(target_os)` 升级为读取 `/proc/version`、`/proc/sys/kernel/osrelease` 与 WSL 环境变量，使 WSL2 guest 在解析任何 credential **之前**即被拒绝。2026-07-27 在 `wuz@192.168.1.2` 上实际执行 `extension-load` probe，证据记录已脱敏并核对：`extension_command_registered=true`、`session_start_hook_observed=true`、`status_command_observed=true`、`status=executed`、`raw_output_included=false`、`output_redacted=true`、`authority_committed=false`、`effects_created=false`、`task_transitions=0`、`capabilities_granted=0`；这仍是 PoC / non-claim evidence，不构成 containment、Profile 或 release claim。2026-07-26 本机 Linux 工具链恢复后已在 WSL2 guest 真实执行受支持的测试面：`cargo test --workspace --locked` **358 passed / 0 failed（67 suites）**、clippy `-D warnings` 通过、`cargo fmt --all -- --check` 通过、`pnpm -r build`/`pnpm -r test`/`check:consistency` 通过（`tested-local`，平台标签 `windows_wsl2_linux_guest`）。archive integrity/source provenance verifier 仍归 Pi P2。非 G0/B01-B12/C0/C1/Profile 声明。 |
| P0-T07 | daemon transport、认证和威胁模型 | P0-T02 | transport 限制与威胁模型评审完成 | done | 2026-07-25；分支 lane/personal-p0-t07-daemon-transport-threat-model，PR #91 合入 main@ff341ef；CI run 30154100260。ADR-0019 冻结：默认 HTTP/1.1 over UDS（XDG runtime）、可选 loopback TCP、disabled-by-default listener、channel-scoped bearer bootstrap、请求/并发/会话上限；threat model 覆盖 CSRF、DNS rebinding、token theft、channel confusion、replay。不实现业务路由；非 G0/B01-B12/Profile 声明。handoff：[20260725-personal-p0-t07-daemon-transport-threat-model-handoff.md](../checkpoints/20260725-personal-p0-t07-daemon-transport-threat-model-handoff.md)。 |
| P0-T08 | 仓库子项目化重构与 1.0.0 边界定稿 | P0-T01 | [ADR-0054](../adr/0054-repository-subproject-structure-and-1.0.0-finalization.md) 目录结构（core/personal/enterprise/clients）落地且 clients 以 subtree 并入；core 与 personal 的 1.0.0 定稿文档 + annotated tag（`core-v1.0.0`/`personal-v1.0.0`）；enterprise 1.0.0 边界与激活门槛定义；全部路径承载工具链（Cargo/pnpm/CI/tools/handbook meta/规则/索引）同步重写；本地 Node 门与 required CI 全绿后合并收口 | done | 2026-08-25 owner-directed **structural** 任务（git mv + 路径重写，无产品/合同语义变更）。Draft PR [#273](https://github.com/agentkernel/cognitive-os/pull/273)；required CI run [32834054468](https://github.com/agentkernel/cognitive-os/actions/runs/32834054468) **SUCCESS** at `366c0bee`（ubuntu + windows + required-ci）。1.0.0 定稿只登记既有 MVP Gate 结论（ADR-0046..0049），不升级任何 Gate/release/Profile 声明。Annotated tags `core-v1.0.0`/`personal-v1.0.0` 打在本 PR 的 merge revision。lease 已关闭。handoff：[20260825-personal-p0-t08-subproject-restructure-closure.md](../checkpoints/20260825-personal-p0-t08-subproject-restructure-closure.md)。 |

### Phase 1 - 安装到首次对话

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P1-T01 | 版本化数据库迁移与 XDG 布局 | P0-T04 | upgrade/reapply/corruption/disk-failure 测试 | done | 2026-07-25；Lane-KRN `lane/krn-personal-p1-t01-xdg-migrations`，PR [#92](https://github.com/agentkernel/cognitive-os/pull/92)。`PersonalDataLayout` + `prepare_personal_databases`：XDG 五根、Unix 0700/0600、`migration.lock`、双库 v1 plan（与 open 共享 schema 常量）、state/backups 非覆盖备份。CI run [30155053950](https://github.com/agentkernel/cognitive-os/actions/runs/30155053950) Ubuntu/Windows-MSVC 全绿（含 `p1_t01_layout_migrations` 7 pass、clippy、fmt）。本机 Windows GNU linker exit 121 为非支持基线。未改 registry/schema/vector/transition；非 G0/B01-B12/Profile。handoff：[20260725-lane-krn-personal-p1-t01-handoff.md](../checkpoints/20260725-lane-krn-personal-p1-t01-handoff.md)。 |
| P1-T02 | SecretStore 正式后端与 Provider 配置 | P0-T05, P1-T01 | rotation/restart/redaction negatives 通过 | done | 2026-07-25；分支 `lane/personal-p1-t02-secret-provider-config`，PR [#93](https://github.com/agentkernel/cognitive-os/pull/93)。`cognitive-secret`：ProviderConfig/ProviderKeyService、LinuxSecretToolStore、production selection、hidden-input helper；ADR-0020。CI run [30156079691](https://github.com/agentkernel/cognitive-os/actions/runs/30156079691) Ubuntu/Windows-MSVC 全绿（含 `p1_t02_provider_secret`）。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile。handoff：[20260725-personal-p1-t02-secret-provider-config-handoff.md](../checkpoints/20260725-personal-p1-t02-secret-provider-config-handoff.md)。 |
| P1-T03 | Provider、模型发现与能力快照 | P1-T02 | probe 正负例与 model snapshot 通过 | done | 2026-07-25；分支 `lane/personal-p1-t03-provider-discovery-probe`，PR [#94](https://github.com/agentkernel/cognitive-os/pull/94) 合入 `main@118d20a`。`cognitive-secret`：`ProviderTransport`、`ProviderDiscoveryService`、capability snapshot + `fnv1a64` identity digest、`persist_selected_snapshot_digest`；ADR-0021。CI runs [30157577277](https://github.com/agentkernel/cognitive-os/actions/runs/30157577277) / [30157576277](https://github.com/agentkernel/cognitive-os/actions/runs/30157576277) Ubuntu/Windows-MSVC `cargo test --workspace --locked` SUCCESS（含 `p1_t03_provider_discovery`）。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile；无真实 Provider Key；无 registry/schema/vector 变更。handoff：[20260725-personal-p1-t03-provider-discovery-handoff.md](../checkpoints/20260725-personal-p1-t03-provider-discovery-handoff.md)。 |
| P1-T04 | 有界 Personal daemon 与本地认证 | P0-T07, P1-T01 | auth/size/timeout/concurrency/restart 测试 | done | 2026-07-25；PR #95（auth/size/host/cookie/restart）+ PR [#96](https://github.com/agentkernel/cognitive-os/pull/96)（timeout/concurrency）。`kernel-server --personal`：loopback、daemon lock、bootstrap secret、channel bearer、header/body 读超时 408、connection/in-flight 429、Host/Cookie fail-closed；ADR-0022。CI runs [30162481713](https://github.com/agentkernel/cognitive-os/actions/runs/30162481713) / [30162477963](https://github.com/agentkernel/cognitive-os/actions/runs/30162477963) Ubuntu/Windows-MSVC SUCCESS（含 timeout/concurrency 单元测试与既有 `p1_t04_personal_daemon`）。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile；无 Task/Memory/MCP；无 registry/schema/vector 变更。handoff：[20260725-personal-p1-t04-timeout-concurrency-handoff.md](../checkpoints/20260725-personal-p1-t04-timeout-concurrency-handoff.md)。 |
| P1-T05 | Readiness、status 与 doctor 应用服务 | P1-T03, P1-T04 | blocked/degraded/ready 事实区分 | done | 2026-07-25；分支 `lane/personal-p1-t05-readiness-doctor`，PR [#97](https://github.com/agentkernel/cognitive-os/pull/97)。`kernel-server` Personal composition root：`evaluate_personal_readiness` + management-auth `GET /personal/status|readiness|doctor`；ADR-0023；blocked/degraded/ready 分离；`static_check_is_not_runtime_ready`；secret_ref/bootstrap 不入投影。CI runs [30164114878](https://github.com/agentkernel/cognitive-os/actions/runs/30164114878) / [30164113787](https://github.com/agentkernel/cognitive-os/actions/runs/30164113787) Ubuntu/Windows-MSVC SUCCESS。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile。handoff：[20260725-personal-p1-t05-readiness-doctor-handoff.md](../checkpoints/20260725-personal-p1-t05-readiness-doctor-handoff.md)。 |
| P1-T06 | `cognitive init/doctor/status/daemon` | P1-T02, P1-T05 | 重复 init、hidden input、可操作错误 | done | 2026-07-25；分支 `lane/personal-p1-t06-cognitive-cli`，PR [#98](https://github.com/agentkernel/cognitive-os/pull/98) 合入 `main@adbb0e5`。`cognitive` bin + `personal_cli`（init/status/doctor/daemon）、ADR-0024、`tests/p1_t06_cognitive_cli.rs`（live daemon 路径以 Ubuntu 为权威；Windows 跑 init/usage）。CI run [30167503487](https://github.com/agentkernel/cognitive-os/actions/runs/30167503487) Ubuntu/Windows-MSVC SUCCESS。本机 Windows GNU linker exit 121 为非支持基线。非 G0/B01-B12/Profile。handoff：[20260725-personal-p1-t06-cognitive-cli-handoff.md](../checkpoints/20260725-personal-p1-t06-cognitive-cli-handoff.md)。 |
| P1-T07 | CognitiveOS Pi Package/Extension 与 proxy | P0-T06, P1-T03, P1-T04, P1-T05 | 禁用直接 mutating tool；无 key 泄漏 | done | 2026-07-27；PR [#105](https://github.com/agentkernel/cognitive-os/pull/105) merged as `main@9d4c3d9` after the Ubuntu and Windows/MSVC CI checks succeeded. `personal/packages/pi-cognitiveos/` defaults to denying `project_trust` and every Pi tool, displays only daemon facts, and source-scan tests forbid Provider key/config, `SecretRef`, SQLite, subprocess, and filesystem-write access. The daemon exposes a management-authenticated, non-secret selected-model projection and a bounded Pi complete-provider bridge: exactly one daemon-projected model forwards a one-shot `stream:false` completion through the authenticated daemon proxy. Provider material remains daemon-only; the proxy creates no Intent/Effect, capability, or state transition. `RustlsProviderTransport` remains HTTPS-only, redirect-free, time- and 1 MiB-response-bounded, and rejects URL user-info/header injection; `stream:true` remains fail-closed. Local WSL tests and the supported CI matrix passed. This is implementation and test evidence only, not a G0/B01-B12, Profile, containment, or release claim. Handoff: [20260727-personal-p1-t07-closeout-handoff.md](../checkpoints/20260727-personal-p1-t07-closeout-handoff.md). |
| P1-T08 | 可检查 Linux bundle installer 与 user service | P0-T03, P1-T01, P1-T04, P1-T06, P1-T07 | verifier、interruption、rollback 测试 | done | 2026-07-29；`lane/personal-p1-t08-mvp-single-service`。已交付固定单服务安装事务、release-shaped campaign builder 与 Linux-native user-systemd 验证：clean install `.3`、healthy upgrade `.4`、pre-pointer `.5` failure 与 post-pointer `.6` failure；两种 failure 后均恢复 canonical unit/service、48181 liveness 及 non-secret `active-version=.4`，并保留 immutable campaign versions。聚焦 WSL tests：campaign builder、service lifecycle、single-service 及 adapter **20/20 passed**；strict runtime Clippy、formatting、consistency 和 whitespace 均通过。该结论仅完成 P1-T08 installer 验收；campaign 仍为 `experimental-local-only` / `tested-local` evidence，不构成 production release/signing、B01、Gate、Profile、containment、uninstall 或 first-conversation claim。 |
> **P1-T09 B01 preregistration (2026-07-31; implementation-only):** campaign
> `B01-clean-linux-first-install-first-conversation-001` is registered as a
> future, separate clean-Linux Gate attempt. Its environment, immutable
> artifact, formal runner, independent verifier, operator-owned SecretStore
> opt-in, reset, attempt accounting, redaction, and cleanup requirements are
> fixed in [the preregistration record](../checkpoints/20260731-personal-p1-t09-b01-preregistration.md), but all execution fields are `not-run`.
> The previously qualified experimental host and campaign `.4` are excluded.
> B01, GMVP-LINUX, release, and Profile remain non-claims. A separately leased
> KVM Ubuntu 24.04/x86_64 guest now exists with a clean pre-install reset
> snapshot, and Operator A / Verifier B are assigned. Its non-secret OS,
> architecture, user-systemd, and clean-state checks passed. `gnome-keyring`
> and `secret-tool` support a non-sensitive transient-session probe, but the
> Product-compatible persistent default/login collection is absent. No B01
> attempt has started. The next action is owner initialization of that
> persistent native Secret Service collection without entering a Provider
> credential; see [the clean-VM handoff](../checkpoints/20260731-personal-p1-t09-b01-clean-vm-handoff.md).

> **P1-T09 product-route bounded blocker (2026-07-30; corrective evidence,
> normative surface unchanged):** The experimental host retains an active
> restored user service and exact Pi `0.81.1`, but has neither a product
> `cognitive` CLI nor a deployed built CognitiveOS Extension entry. The
> non-secret configure, doctor, launch, and direct first-response commands are
> therefore `not-run`; no secret or Provider/internal state was read. This is
> a blocker, not a failed first-response result: `blocked_paths` are the
> product bundle deployment paths; `blocked_task_ids`: `P1-T09`;
> `blocked_gate_ids`: `B01`, `GMVP-LINUX`, Profile; owner: P1-T08
> bundle/release artifact owner; next action: deploy a coherent CLI-plus-
> Extension bundle and rerun the bounded redacted route diagnostic. P1-T09
> remains `in-progress`, and B01 remains `not-run`. Handoff:
> [20260730-personal-p1-t09-product-pi-configuration-timeout-diagnosis-handoff.md](../checkpoints/20260730-personal-p1-t09-product-pi-configuration-timeout-diagnosis-handoff.md).

#### Historical P1-T08 slice journal

以下 blockquote 保留各原子批当时的状态与 non-claim，属于历史记录；不得覆盖上表当前状态。

> **2026-07-28 P1-T08 bootstrap/download slice:** `lane/personal-p1-t08-linux-bootstrap` adds an inspectable POSIX `personal/deploy/linux/install.sh` template and a narrow `linux-bundle-verifier` adapter. The unrendered source template rejects before network access; release rendering must bind the version, HTTPS object directory, allowed redirect host, bootstrap verifier digest, public keyring, and Pi pin. Downloads use a private temporary directory, partial paths, bounded HTTPS curl calls, and invocation-owned cleanup. The adapter verifies its script-bound SHA-256 before execution and delegates only to the existing offline `verify_linux_bundle`; it performs no activation because bounded service health is not yet defined. Focused WSL behavior tests are local `windows_wsl2_linux_guest` evidence only. P1-T08 remains `in-progress` with `development_track: experimental-local-only`; P1-T09 remains `not-started`. No production key/release, service/health/rollback, uninstall, Linux-native campaign, B01, Gate, Profile, containment, or release claim is added. |

> **2026-07-28 P1-T08 service-health slice:** `lane/personal-p1-t08-service-health` adds a separate lease-held service-lifecycle transaction, a fail-closed source user-unit template, a fixed-argument `systemctl --user` controller boundary, strict bounded loopback `/personal/health` parsing, and deterministic compensation tests. The existing offline installer callback remains unchanged. The checked-in unit and controller reject unresolved templates and the still-absent safe extracted `bin/kernel-server` layout before any systemd action; no runnable archive or real systemd service is claimed. Focused service tests passed **6/6** in `windows_wsl2_linux_guest`; this is local fixture evidence only. P1-T08 remains `in-progress` with `development_track: experimental-local-only`; P1-T09 remains `not-started`. |

> **2026-07-28 P1-T08 rendered-user-service foundation:** ADR-0032 fixes
> product-owned candidate and canonical unit identities, disjoint loopback
> ports, staged-versus-active executable paths, and candidate-stop before
> pointer activation/canonical restart. The runtime renders fixed unit content
> only from a constrained version and product deployment root, and fixture unit
> publication uses a private temporary file followed by atomic rename. Focused
> service lifecycle tests **9/9 passed** in `windows_wsl2_linux_guest`.
> PR [#114](https://github.com/agentkernel/cognitive-os/pull/114) merged as
> `main@b151b54`; after a Windows path-separator correction (`0a90033`), both
> supported Ubuntu/Windows-MSVC push and pull-request matrices passed in runs
> [30379506413](https://github.com/agentkernel/cognitive-os/actions/runs/30379506413)
> and
> [30379508772](https://github.com/agentkernel/cognitive-os/actions/runs/30379508772).
> This remains implementation-fixture and supported-matrix evidence only; a
> production user-systemd installation path, daemon-reload fixture,
> Linux-native campaign, B01, Gate, Profile, containment, and release evidence
> remain absent. P1-T08 remains `in-progress`; P1-T09 remains `not-started`. |

> **2026-07-28 P1-T08 fake-systemctl controller fixture:** ADR-0033 fixes the
> private/injected unit-root controller boundary and the fixed daemon-reload,
> candidate start/stop, and canonical active restart actions. The focused Unix
> fake harness verifies candidate unit publication then daemon-reload then
> fixed candidate start, with candidate work never publishing the canonical
> active unit. Focused lifecycle tests **10/10 passed** in
> `windows_wsl2_linux_guest`. PR
> [#115](https://github.com/agentkernel/cognitive-os/pull/115) merged as
> `main@aa09f6c` after supported Ubuntu/Windows-MSVC push and pull-request CI
> passed in runs
> [30382894322](https://github.com/agentkernel/cognitive-os/actions/runs/30382894322)
> and
> [30382932475](https://github.com/agentkernel/cognitive-os/actions/runs/30382932475).
> This is implementation-fixture evidence only;
> pointer/unit/service compensation fault injection, Linux-native systemd,
> B01, Gate, Profile, containment, and release evidence remain absent.

> **2026-07-28 P1-T08 safe-extraction slice:** ADR-0031 specifies the
> implementation-local `tar.gz` extraction policy: a verified artifact is
> re-hashed and extracted only in the lease-held private staging area; the
> direct fixed layout is `bin/kernel-server`; traversal, links, special files,
> unsafe modes, duplicate/conflicting paths, unsupported layouts, and bounded
> resource violations are fail-closed. `cognitive-runtime` now provides the
> bounded in-process extraction and only publishes `staged/<version>` after the
> fixed layout validates; an extraction failure leaves the active pointer and
> canonical active service untouched and returns no receipt. Successful fixture
> candidates satisfy the controller's static layout preflight, but the
> unresolved user-unit still prevents every systemd action. Focused installation
> tests executed **12/12**, lifecycle tests **12/12** plus one ignored child
> entrypoint, and service-lifecycle tests **6/6** in
> `windows_wsl2_linux_guest`; strict feature Clippy, formatting, and consistency
> checks also executed successfully. This is implementation and local-test
> evidence only. P1-T08 remains `in-progress` with
> `development_track: experimental-local-only`; it is not Linux-native systemd,
> B01, Gate, Profile, containment, or release evidence. |

> **2026-07-28 P1-T08 verifier merge update:** PR
> [#108](https://github.com/agentkernel/cognitive-os/pull/108) merged as
> `main@afa1d5d`; both push and pull-request Ubuntu/Windows-MSVC CI matrices
> passed. This closes only the offline verifier slice. P1-T08 remains
> `in-progress`, and every installer, service, B01, Gate, Profile, containment,
> Linux-native, and release non-claim above remains unchanged.

> **2026-07-27 verification correction for P1-T07:** Cargo is available in the
> WSL guest at `/root/.cargo/bin/cargo`. The focused provider-proxy process test
> and `kernel-server` strict Clippy check passed there. This is focused local
> Linux evidence only. The supported CI matrix subsequently passed for PR #105,
> including Windows/MSVC. Windows GNU linker exit 121 remains a non-supported
> local limitation.

> **2026-07-29 P1-T08 production-path amendment:** ADR-0034 supersedes the
> dual-service controller only as the first production path. P1-T08 now closes
> against one canonical `cognitiveos-personal.service`, port 48181, a shared
> verify/lease/stage transaction, deterministic single-service rollback and a
> Linux-native user-systemd campaign. Existing candidate/active fixture results
> remain valid implementation-fixture evidence, but candidate port 48182 and
> zero-downtime promotion no longer block P1-T08 or B01. P1-T08 remains
> `in-progress`; no implementation or Gate status changes in this planning batch.

> **2026-07-29 P1-T08 single-service implementation slice:** the shell-to-Rust
> handoff now invokes a digest-bound `linux-bundle-installer`; the runtime
> shares the existing offline verification, OS lifecycle lease and private
> staging prefix before separately publishing immutable version bytes and the
> active pointer. A narrow canonical-service controller owns only
> `cognitiveos-personal.service`, fixed 48181 health, fixed user-systemd
> actions and Rust-rendered unit content. Upgrade and first-install failures
> compensate pointer/unit/service state deterministically and issue no receipt;
> incomplete compensation returns `RollbackIncomplete`. Focused WSL2 Linux
> guest tests passed **46/46** with one ignored child-process entrypoint;
> runtime strict Clippy, formatting, consistency and diff checks passed. This
> is implementation/fixture evidence only. P1-T08 remains `in-progress`;
> Linux-native user-systemd, production release/signing, uninstall, B01,
> product Gate, Profile and first-conversation evidence remain outstanding.

| P1-T09 | 安装到首次对话 route 与 B01 campaign | P1-T08 | route implementation、deterministic fixture、dev smoke、usability 与 formal B01 分阶段；successor B01 固定 6 次独立 clean Linux VM outcome，全部计入，至少 5 次成功，关键安全失败为 0，并须完整统计与 affirmative independent-verifier closure（ADR-0026/0034/0039） | done | Retained `B01-clean-linux-first-install-first-conversation-001` remains failed at 2 successes / 8 failures after 10 started attempts under its historical N=20 rule. Separately preregistered successor `002`, fixed N=6 on sole `B01-Desktop-Linux-002`, independently verified artifact `0.0.0-campaign.20260809.1` at exact revision `4ea42c0c8f856aa22e2a360bd42005c8dbec400f`. Attempts 1, 3, 4, 5, and 6 passed the clean signed-installation, exact Pi, graphical hidden-input Provider, doctor-ready, bounded-marker, SecretStore-delete, and baseline-revert route; Attempt 2 failed at bounded graphical Desktop readiness before product activation. The completed denominator is 5 successes / 1 failure / 6, with zero recorded critical safety failures. Aggregate, affirmative independent-verifier, required CI, branch and lease closure are complete. Owner-waived transition Attempt 7 is retained outside the denominator because no product operation occurred. B01 is `pass`; G1, GMVP-LINUX, release, and Profile remain unclaimed. Evidence: [ADR-0039](../adr/0039-personal-b01-six-attempt-campaign-policy.md), [successor preregistration](../checkpoints/20260809-personal-p1-t09-b01-successor-preregistration.md), [native verification](../checkpoints/20260809-personal-p1-t09-b01-successor-native-verification.md), and [successor attempt ledger](../checkpoints/20260809-personal-p1-t09-b01-successor-attempt-ledger.md). |

### Phase 2 - 单 Agent 任务闭环

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P2-T01 | TaskApplicationService | P1-T09 | raw intent、preview digest、epoch fencing；admission preview 为唯一默认人工授权点（ADR-0026） | done | 2026-08-03 closure reconciliation：`P2-T01/D01` 已满足本任务全部验收摘要。`personal/crates/cognitive-management/src/task_application.rs` 提供 proposal/clarify/preview/admit/control/query 六操作面且只组合 kernel intent-chain primitives；raw-intent durable restart、digest mismatch before mutation、epoch supersession/fencing、stale writer lease 均有 focused regression。PR #127 merged as `main@7f763c8`；Linux focused 4/4、management lib 3/3、store 6/6、Clippy/fmt 与 required Ubuntu/Windows CI 通过。P2 Gates B02/B04/B05/B12 仍为 `not-run`，task `done` 不产生 Gate/release/Profile claim。证据：[handoff](../checkpoints/20260801-personal-p2-t01-task-application-service-handoff.md)。 |
| P2-T02 | 真实 Resource + Task API/watch、统一 projection 与 CLI/Shell parity | P2-T01, P1-T07 | 六类资源的 private versioned projection；真实 Task API/watch；deterministic CLI 与 Shell 经 sidecar 调同一 daemon application services；task/management bearer、cache、retry、cursor、projection 隔离（ADR-0026/0035/0037/0038） | done | 2026-08-03 closure assessment: D01-D04 satisfy the unchanged acceptance. D01 supplies generated authenticated intent record/interpret, server-issued preview/admit, daemon-owned governance root, server WriterLease, admission negatives and bounded snapshot-first Task watch (`734cbce`, PR #141). D02 supplies private versioned six-family projection/watch and resource family/cursor/channel negatives (`70f40a5`, PR #142). D03 supplies deterministic CLI parity (`af2f6c9`, PR #143), and D04 supplies Pi sidecar parity with isolated management/Task bearer caches and snapshot-first streams (`ed01c27`, PR #144). Every slice has exact Linux and required Ubuntu/Windows CI evidence. B02/B04/B05/B12, release and Profile remain not-run/incomplete. Evidence: [acceptance handoff](../checkpoints/20260803-personal-p2-t02-acceptance-assessment-handoff.md). |
| P2-T03 | durable scheduler、lease 与 timer | P2-T01, P1-T01 | crash/duplicate lease/clock/budget、durable stop、Effect closure 与 worker fencing 测试 | done | 2026-08-07 closure: D01-D05 satisfy the unchanged acceptance. Scheduler persistence/cancel/monotonic eligibility and owner/epoch CAS fencing establish the durable lease boundary; durable TaskContract/progress/budget ceilings register STOP before lease acquisition; immutable TaskBinding reverse lookup and unknown-state handling fail closed; only a closed durable Effect releases the exact leased owner+epoch while pending/STOP work remains for reconciliation; and the daemon alone persists/adopts a candidate WIA, exact lease-bound one-time handoff, recovery, and independently verified continuation authority. Exact native Linux `08932f7868d46f494aaa76835f4818fd7a1f2962` passed the focused worker/recovery matrix, workspace fmt/build/test/Clippy, and required Ubuntu/Windows CI. B05/B12, release, and Profile remain `not-run`/incomplete. Evidence: [task closure handoff](../checkpoints/20260807-personal-p2-t03-scheduler-runtime-closure.md). |
| P2-T04 | scheduler→Context→Pi sidecar→BoundedHarness worker | P2-T02, P2-T03 | TaskContract/lease 每轮重载；Context 经真实 port；Pi sidecar candidate-only；no-progress/budget/stale-lease fail-closed | done | 2026-08-07 closure: `P2-T04/D01` is complete at `a8ef5c00654e1c05a4c30beb193b9c026654c2f1`. The daemon resolves a durable ContextRequest, revalidates authorization/revocation immediately before every body load, seals a request-bound ContextView before Pi, and admits only a daemon-sealed opaque candidate. Real SQLite negatives cover revocation after metadata discovery, missing required Context, duplicate Pi retry suppression, atomic candidate/WIA handoff, and stale/replaced lease rejection; the Pi protocol rejects authority-shaped output. Exact native Linux and required Ubuntu/Windows CI passed. No Tool execution, Task completion, Gate, release, or Profile claim is created. Evidence: [P2-T04 closure](../checkpoints/20260807-personal-p2-t04-d01-context-view-persistence-checkpoint.md). |
| P2-T05 | Native Tool Registry 与 useful operation family | P2-T04 | workspace read/search/write/patch、bounded process/check、read-only HTTP fetch；descriptor/version/digest/risk 绑定；未注册、drift、disabled 均 dispatch=0 | done | 2026-08-07 closure: the daemon-owned static six-family catalog, immutable descriptor digest binding, pre-admission persisted-descriptor verification, private Tool projection, and bounded workspace/process/HTTP pre-executor validators satisfy the unchanged acceptance. Exact native Linux `72a7e55e5a780827438bfb0fb42172cfd1e5bec1` passed focused Tool registry tests 7/7 and `cargo fmt --all -- --check`; required Ubuntu and Windows CI passed in PR #159. P2-T06 execution/supervision, external I/O, actual workspace mutation, and unknown-outcome reconciliation remain explicitly out of scope. Evidence: [closure handoff](../checkpoints/20260807-personal-p2-t05-native-tool-registry-closure.md). |
| P2-T06 | Tool/process executor、supervisor、cursor 与 reconcile | P2-T05 | persist-before-dispatch；bounded output cursor；before/mid/after fault、orphan、redaction、idempotency、unknown-outcome reconcile | done | 2026-08-08 closure: D01-D04 satisfy the unchanged acceptance at `bfcc684db6685e1077050a4b3c82fcf84c524711`. Exact native Linux passed 26 focused tests, Clippy, and fmt; required Ubuntu/Windows CI passed in Draft PR #162. The daemon-private supervisor uses registered attempt identity, PID ownership, epoch fencing, orphan/recovery/shutdown lifecycle, bounded injected observation, and a fail-closed default source. The Effect path proves persist-before-dispatch and original-key unknown-outcome reconciliation without Task advancement. No public Process resource, arbitrary PID attach, Task completion, release, Gate, or Profile claim is made; platform observation is deferred to managed-Pi/process-supervision work. Evidence: task-scoped Draft PR #162 and `docs/plan/PROGRESS.md` D01-D04 records. |
| P2-T07 | Checkpoint、Artifact、Evidence 与独立 Verifier | P2-T03, P2-T04, P2-T06 | checkpoint/restart、artifact digest、criteria evidence、Effect closure；partial/receipt/exit/`agent_end` 不得 complete | done | 2026-08-08 closure: D01-D02 satisfy the unchanged acceptance. The daemon-private fixed-post-state and verification-request/report boundary stays append-only; verifier identity, currentness, fenced-writer, malformed/duplicate artifact reference, and passed-without-evidence regressions fail closed before report persistence. Exact remote Linux `df7d483282f3ef0a6bbb17bae3d29bb24f13e0f7` passed the focused verifier test module 7/7 and targeted Clippy; local `cargo fmt --all`, `git diff --check`, and lints passed. No Provider/Tool execution, Artifact closure, Task completion, Gate, release, or Profile claim is made. Evidence: [P2-T07 closure](../checkpoints/20260808-personal-p2-t07-d02-verifier-persistence-closure.md). |
| P2-T09 | 已登记 Tool family 的 execution readiness 投影诚实性 | P2-T05, P2-T06 | private Tool projection 必须区分 `registered/enabled` 与 `execution_ready`，composition root 只为已装配 executor 的 family 报 execution ready；readiness 不得进入 immutable descriptor 或其 digest，也不得由 registry availability 伪造 | done | 2026-08-12 owner-authorized under optimization-proposal INV-TASK-1（由 P9-T04 `L4` 能力缺口直接触发）。D01 完成：`tool_execution_readiness` 以已装配 executor 集合（`WorkspaceRead`、`ProcessCheck`）为准，四个 failure-first 负例含"readiness 不改变任何 descriptor digest"。exact native Linux `cbb830a2dc64855f51c4b5ffdf04ae4f6fba4e4c` tool_registry 11/11 + Clippy，tool_executor 27/27 不变；required Ubuntu/Windows CI 于 `0227b97` 通过；PR #201。无 Gate/release/Profile claim；不继承 P9-T04 证据。 |
| P2-T10 | 已登记 Tool family 的 executor parity | P2-T09 | 按 WorkspaceSearch → WorkspaceWrite/Patch → HttpFetchReadOnly 顺序补齐已登记 family 的真实 executor，每个都沿用 daemon authority、descriptor digest、persist-before-dispatch、幂等、fencing 与 reconcile；mutation 另加 expected preimage、atomic publish、partial write、symlink/path race、duplicate dispatch 与 OUTCOME_UNKNOWN 负例；不得新增未登记 family，不得因"本地工具"绕过 Effect 语义 | done | 2026-08-12 closure：D01–D04 满足未改动的验收，PR #207 合并于 `main@3f766020c4d822556887ff8af59d41ed0cb92d75`。四个此前 `registered_only` 的 family 现在都有真实 sink：`WorkspaceSearch`（双重 containment：canonical root 检查加逐条目 `symlink_metadata` 跳过；固定 visit/match/file-size/line 上限）、`WorkspaceWrite`/`WorkspacePatch`（expected-preimage 比较交换，validation 拒绝未声明 preimage 的 mutation；staging 文件加 `sync_all` 加 rename 原子发布；`query_outcome` 重读目标，因此对账来自持久文件状态而非进程内存；patch applier 对上下文漂移、未知前缀、重叠/乱序 hunk 与自相矛盾的 header 一律 fail closed）、`HttpFetchReadOnly`（走仓库唯一受审计的 Rustls 边界，与 Provider egress 分离以免继承其授权；无调用方 header、无 body、不跟随重定向、不继承环境代理、有界超时、超限响应拒绝而非截断）。`ASSEMBLED_EXECUTOR_FAMILIES` 现列全部六族，并由 parity 负例守护：该列表若命名任何 sink 不接受的 family 即失败。exact native Linux `8979749e9efa2fcb9bef8f6be47f0ea76e5d1751`：`tool_executor` 54/54（原 27）、`kernel-server` 全包集成测试通过、`cognitive-provider-transport` 10/10（含四条真实回环 TLS 负例）、`tool_registry` 11/11、fmt 与三个包的 Clippy 全清。required Ubuntu/Windows CI 在 closure head `effae9a` 通过（run `31617885666`；首轮 windows 失败为已确认 flake——同一 revision 重跑通过，失败点是 `p1_t05_personal_readiness` 只给 daemon 启动 2 秒预算）。**`execution_ready` 只表示本二进制含有该族 sink，不表示 Agent 能到达它**：gap 1（admission 不插入 scheduler row）、gap 2（无周期 tick）、gap 4（verifier 无生产调用者）不变，分别登记为 P2-T12 与 P2-T13。Kill criterion（提案 §4.3）未触发。`ProcessRun` 与 Git 专用面属 INV-TASK-3，需 owner 先做 product-semantic 决策。closure：[20260812-personal-p2-t10-tool-executor-parity-closure.md](../checkpoints/20260812-personal-p2-t10-tool-executor-parity-closure.md)。无 Gate/release/Profile claim。 |
| P2-T12 | admitted Task 进入 worker loop（scheduler 引导与受治理 Tool 派发） | P2-T10 | (1) `task.admit` 在同一 authority 事务内为该 Task 建立**可调度前置状态的全部三项**：scheduler work row、契约命名的 Loop governed object（`START`）、契约命名的 Budget row。今天 admission 三者都不写，而 `load_scheduler_authority_snapshot`（`dispatch.rs:90`）在缺 Loop 或 Budget 时以 `LoopUnavailable`/`BudgetUnavailable` 失败，因此只补 scheduler row 不足以让 tick 走通。未准入的 Task 不得有 row，重复 admit 不得产生第二行，启动恢复必须修复"已准入但缺前置"而不是让 Task 静默停摆。(2) 打破 tick 内部的循环前置：`resolve_scheduler_work_for_task`（`dispatch.rs:527`）要求恰好一条既有 Intent，而该 Intent 正是其后 pre-admission Pi 分支（`dispatch.rs:554`）要创建的行，故新 Task 的首次执行分支不可达且 `?` 会中止整趟 tick。缺 Intent 必须路由进 pre-admission 分支而不是传播错误。(3) daemon 以有界周期 tick 取代单次启动 tick。现状不是"没有 tick"：`server.rs:128` 确实在生产调用它，但位于 `TcpListener::bind`（`server.rs:160`）之前，故它必然早于本进程内任何 admission，结构上不可能观察到新 Task；且 tick 错误映射为 `PersonalDaemonError::Io` 向上传播，**一行坏 scheduler row 会阻止 daemon 启动**。新 tick 必须单写者、不重入、可取消、关闭时干净退出，并按行隔离错误：单行失败只记录并跳过。(4) worker attempt 依据已持久化 descriptor（`resolve_persisted_native_descriptor`）选择已装配 native executor 并经 Effect 协议派发，沿用 persist-before-dispatch、原键幂等、fencing 与 reconcile；未装配 family 必须 fail closed，不得静默把 Effect 记为已闭合。(5) 负例：未准入无 row/Loop/Budget；重复 admit 不重复建行；缺 Intent 走 pre-admission 而非中止 tick；单行失败不影响同趟其他行也不阻止启动；tick 自重入不得双重 lease；stale/replaced scheduler lease 不得派发；unsupported family fail closed；派发后崩溃经原键 reconcile 且不重复外部操作；STOP/budget ceiling 仍先于派发生效。(6) 本任务不产生 Task 完成，也不放松 A1/A2/A3。 | done | 2026-08-13 closure: D01–D05 satisfy the unchanged acceptance at post-review exact `06c6a6f2cff96a33b8ac9e9f1be5c8a7a58f0758`. Admission atomically publishes the TaskContract, runnable scheduler row, contract-named `START` Loop and hard Budget; startup repairs only missing current-epoch members. Zero-Intent rows reach candidate admission, and one post-bind 250 ms worker is serial, non-reentrant, cancellable and row-isolated. The daemon publishes stable immutable native descriptor IDs, re-authorizes current target/action/purpose, stages before WIA consumption, records `EXECUTING` before WorkspaceRead I/O, and reconciles/restarts only under the original key. Negatives cover unadmitted/duplicate/rollback, malformed row, reentry, STOP/budget ordering, unsupported family, stale/replaced lease, same-process crash and process restart with zero redispatch. Exact native passed scheduler authority 50/50, server 9/9 and kernel-server Clippy; required Ubuntu/Windows CI `31687579721` passed. No Task completion, verification, Gate, release, Profile or B01 claim. Evidence: `docs/checkpoints/20260813-personal-p2-t12-worker-loop-report.md`; Draft PR #209. |
| P2-T13 | 独立 verification 在生产中真正运行（`ACT -> VERIFY -> CONTINUE -> OBSERVE` 闭环） | P2-T12 | (1) daemon 在启动组合期构建 `ArtifactStore`（今天 `ArtifactStore::open` 只有测试与 benchmark 调用者，daemon 从不构建，故 verifier 即使被调用也无法解析证据）。(2) 在受治理 Tool Effect 到达终态且对账后，生产路径持久化 `FixedPostStateRow` 与 `VerificationRequestRow` 并提交 Loop `ACT -> VERIFY`；三者今天都无生产写入者。(3) `VerificationRequestRow.criteria_canonical_json` 必须由真实来源生成：筛选 `TaskContract.conditions` 中 `kind == Acceptance` 的项并序列化，`verifier_ref`/`verifier_version` 取自 `ContractCondition::verifier_ref`（该字段已在 admission 落库但从未被读回）。(4) 提供至少一个生产 `IndependentVerifier` 实现与 `verifier_ref` → 实例的解析表；今天仅存在两个测试替身。(5) 生产路径调用 `record_independent_verification`，并在报告 `passed` 后发出 checkpoint 与 `issue_continuation_authorization`；`ProtocolStore::append_checkpoint` 同样无生产调用者，而 `issue_continuation_authorization` 的 SQL 前置要求同 fencing epoch 的 checkpoint，故两者必须一起补。消费侧（`dispatch.rs:436` → `begin_verified_continuation_atomically`）**已接线**，无需新建。(6) 负例：Effect 未闭合或未对账即请求验证；stale epoch；缺失或 digest 不符的 CAS 证据引用；报告由 actor 自身身份产出；报告绑定到不同 fixed post-state；缺 checkpoint 时拒发 continuation authority。(7) 本任务**不**完成 Task：`CANDIDATE_COMPLETE -> COMPLETED` 属 P2-T14。 | done | 2026-08-13 closure: D01–D04 satisfy the unchanged acceptance at exact `dab38cdf86c8e3486a913d85660593b6671bf114`. The daemon composes one bounded ArtifactStore; after reconciled WorkspaceRead it atomically pins the current Effect, persists contract-derived verification criteria, and enters `ACT -> VERIFY`. The registered independent fixed-Effect verifier writes CAS evidence, persists a current passed report, records progress, enters `VERIFY -> CONTINUE`, appends a fenced checkpoint, issues one-time continuation authority, recovers the issue/requeue crash window, and consumes the authority through `CONTINUE -> OBSERVE`. Negatives cover unclosed/stale/mismatched fixed state, unknown verifier, missing/tampered Artifact, stale Loop rollback, missing checkpoint, and false Task completion. Exact native passed scheduler authority 50/50, verification executor 12/12, `m5_harness` 7/7 and kernel/store/server Clippy; required Ubuntu/Windows CI `31707250244` passed. Zero Tasks complete; P2-T14 remains separate. Evidence: `docs/checkpoints/20260813-personal-p2-t13-verification-loop-report.md`; Draft PR #210. |
| P2-T14 | verified Task 完成与 acceptance authority | P2-T13 | (1) 实现 Task `ACTIVE -> CANDIDATE_COMPLETE`（`COMPLETION_CLAIMED`，guards `required_effects_closed`、`verification_requested_for_fixed_post_state`）与 `CANDIDATE_COMPLETE -> COMPLETED`（`ACCEPTANCE_GRANTED`，guards `acceptance_authority_matches`、`verification_passed_and_current`、`fixed_post_state_unchanged`）；两条边今天在 `crates/`、`apps/` 生产代码中**完全没有实现**，只有测试与 conformance runner 构造过。(2) guard 在引擎中是 caller 提供的字符串集合（`engine.rs:189`/`:586`），因此必须有生产代码真实推导这三个 guard，而不是断言它们；`verification_passed_and_current` 在既有测试中被显式标注为 `// underivable`，其推导必须重做 currentness 检查并确认报告未被取代。(3) `required_evidence` 含 `acceptance_decision`，但全仓**没有**对应的 Rust 类型、schema binding、表或行。新增它是 public contract 变更，须经 Lane-CTR 并由 owner 先做 normative 决策；在该决策落地前本任务保持 `blocked`，不得以私有结构冒充已登记 evidence。(4) 需要一个 acceptance authority principal 概念以满足 `acceptance_authority_matches`；代码中目前不存在。(5) 负例：Effect 未闭合时不得进入 `CANDIDATE_COMPLETE`；Tool receipt、Provider 响应、process exit、Pi `agent_end` 单独都不得完成 Task；stale fixed post-state 不得验收；缺 `acceptance_decision` 证据不得验收。(6) 完成后 `verified completion` 在测试中成立不构成 Gate/release/Profile claim。 | done | 2026-08-12 registered；owner 2026-08-13 决策已落地（`acceptance_decision` 为 daemon-authored canonical bytes 存 Artifact CAS via 既有 `StrongReference`、distinct `principal://personal/acceptance-authority`），D01/D02 已实现并通过 native 57/0 + Clippy + CI Ubuntu；Windows 仅 flaky 于既有 `p1_t07_pi_readiness` bootstrap-secret 时序。 |
| P2-T11 | 公共面正确性缺陷修复（PERSONAL-PERF-EVAL-002 实测发现） | P2-T09 | (1) Provider readiness 必须反映 `secret_ref` 的**可解析性**而非仅字段存在：配置存在但引用无法解析时 `provider` 组件必须 `Blocked`（`provider_secret_unresolvable`），backend 不可答时为 `provider_secret_store_unavailable`，且 `overall`/`first_conversation_ready` 不得为 ready；readiness 不得输出任何 secret material。(2) `POST /management/resource/v1/skill/binding/revoke` 必须到达 revoke handler：路由分派不得被 `skill/bind` 前缀吞掉，并以只有 revoke handler 能产生的 registered code 作为判别性负例 | done | 2026-08-12 由 owner 授权、依据 `PERSONAL-PERF-EVAL-002` 最终评估的证据排序 Priority 1 与 Priority 2 登记。实测证据：80/80 Provider 请求以 `PERSONAL_PROVIDER_SECRET_UNAVAILABLE` fail closed，同时 `status`/`doctor` 报 `provider: ready`、`overall: ready`、`first_conversation_ready: true`；差分探针证明 revoke 路径返回 bind handler 的报文。不含 Priority 3（readiness 热路径成本）与 Priority 1b（Extension per-request 开销），二者需独立任务与 nested timing。 |
| P2-T15 | merged P2-T10/P2-T11 independent-review hardening | P2-T10, P2-T11 | Close every owner-confirmed post-merge defect without weakening contracts or negatives: handle-relative no-follow workspace search/mutation with Windows reparse rejection and active swap coverage; target-locked final-window CAS; durable original-key mutation/HTTP attempts and receipts across restart/state loss; exact immutable catalog equality at every sink; enumeration-time visit bounds; streamed write and bounded patch preimages; surfaced/repairable orphan cleanup; complete unified-diff final-newline semantics; and one-snapshot Provider readiness secret resolution. Required evidence is focused failure-first coverage, required Ubuntu/Windows CI, exact-revision native Linux focused/full validation, bilingual docs-sync, and a no-findings final defect review. | done | Closed 2026-08-13 through PR [#208](https://github.com/agentkernel/cognitive-os/pull/208). Accepted implementation/native revision `580c0a06d39ee3d6fb460e23be9c7ac0939a4b63`: required Ubuntu/Windows CI run `31666003044` passed; exact native Tool executor 76/76, readiness 11/11, full kernel-server 179/179 plus integrations, full workspace Rust tests, targeted Clippy and fmt all pass; final defect-first review reports no findings. Closure: [20260813-personal-p2-t15-executor-hardening-closure.md](../checkpoints/20260813-personal-p2-t15-executor-hardening-closure.md). No Gate/release/Profile/B01/benchmark claim. |
| P2-T19 | production governed Memory/Skill Agent consumer | P2-T13, P4-T05, P4-T06, P8-T06; acceptance requires P2-T14 on `main` | After Task admission, daemon derives authorized Memory objects and exact-pinned Skill bindings from the current TaskContract/ContextRequest workspace, scope, purpose and epoch; authorization, lifecycle and revocation filtering precede body access/ranking; selected Memory/Skill provenance and exact digests are sealed into ContextView before candidate-only Pi input; one durable request/context-bound consumption record supports restart/session-2 resume without chat replay or user restatement. Forget/revoke/supersede blocks future loads without drifting existing exact pins. Public management lifecycle preconditions support valid remember→review→forget and import→inspect→bind→supersede→revoke cycles without SQLite/test helpers. Required negatives cover cross-scope leakage, pre-rank body access, stale/revoked/forgotten reuse, digest or epoch drift, forged/manual-prompt consumption, missing durable Skill-use evidence, session-2 restatement, partial residue and task-bearer mutation. | done | Owner-directed 2026-08-13 isolated branch `personal/P2-T19-memory-skill-consumer`. D01 failure-first was observed on CI `31718358206`; D02 is the current implementation slice (governed consumer + v24 records + fail-closed negatives). Lease `lease/personal/P2-T19/memory-skill-agent-consumer`; Draft PR #214. No Gate/release/Profile/B08/Agent-benefit claim. |
| P2-T16 | `RegisteredCheckRun` 固定登记检查执行 | P2-T13 (`implementation_requires`), P2-T14 (`acceptance_requires`; merge mutex only) | 调用方只提交 `check_id`；daemon-owned immutable registry 固定绑定 executable/tool identity、精确 argv template、cwd、最小环境、timeout、输出/process-tree/write-root/network 边界与 descriptor digest。执行复用 P2-T15 hardened Tool/Effect authority path：Intent/Effect persist-before-dispatch、durable `EXECUTING`、budget/fencing/idempotency、original-key query/reconcile、bounded supervision、Artifact/CAS evidence 与独立 verifier。未知 check、descriptor drift、任何 argv/env/cwd/shell/credential/network 扩权、timeout/oversize/orphan/越界写、duplicate/crash/restart unknown/stale epoch 均 fail closed；exit 0 仅产生 Evidence，未通过独立 verifier 不得完成 Task。至少一个固定 check 支持 EVAL-003 `C2a` repair fixture，但不得形成通用 `ProcessRun`。 | done | Merged via PR #211 at `main@55cadbc5`，required CI 双平台绿；exact native `registered_check` 16/16；与 C2/A7 合并。无 Gate/release/Profile/B01 语义变化。 |
| P2-T17 | A7 external-mutation unknown-outcome production observation | P2-T13, P2-T15; acceptance additionally requires P2-T14 and any separately registered mutation carrier used by the production caller to be merged on `main` | Provide a campaign-owned loopback external-state fixture with bounded mutate/query/reset/cleanup, durable original-key idempotency, post-state digest and mutation count; drive it through a daemon-owned persist-before-dispatch Effect path with campaign-authorized/default-off fault points at dispatch-before, mutation-after/receipt-before, receipt-after/Effect-close-before and verification-before; after restart query only the original key, preserve timeout/ambiguous outcomes as Indeterminate, reconcile the actual outcome exactly once, independently verify the fixed post-state through P2-T13, and never grant Task completion in this task. Publish only bounded campaign observation fields (outcome class, key digest/reference, mutation count, stage timings, Effect/report/acceptance references and cleanup), never raw SQLite or a sensitive raw key. Required negatives cover duplicate mutation, generated replacement key, blind retry, stale epoch/lease, ambiguous query, receipt/post-state mismatch, completion with open/unknown Effect, missing/tampered verifier evidence, duplicate restart worker, every fault point, cleanup residue and unauthorized fault enablement. | done | Merged via PR #212 at `main@300552e0`，required CI 双平台绿；C2（P2-T20）与 P2-T14 已在 `main`，A7 依赖解除。无 Gate/release/Profile/B01/EVAL-003 claim。 |
| P2-T18 | 本地 bootstrap/session token OS CSPRNG 安全修正 | P1-T04、ADR-0019/0022；与 P2-T14 无依赖 | 生产 bootstrap 与 channel-scoped session bearer 只从 OS CSPRNG 取得至少 256 bit；熵失败、零/短输出或重复 block 在创建文件、session、token 前 fail closed，绝无 PID/时间/`DefaultHasher` fallback。保留 private runtime `0600`、opaque `prefix-group-group` 形状、constant-time bootstrap 比较、channel binding、expiry/revocation 与 Debug/log/evidence redaction。RNG 注入只存在于 private/test seam；负例覆盖静态 fallback 防回归、无文件/无 session、bounded uniqueness（无统计声明）及既有 auth 行为。 | done | Closed 2026-08-14：bootstrap/session 两个生产生成点统一使用 `getrandom::fill` 的 64-byte sample（32-byte token + independent probe）；failure/short/zero/repeated block 在文件/session/token 前 fail closed。persisted empty、旧 16+16 hex 可预测或畸形 bootstrap 不再 grandfather；request/view/authority `Debug` 与 HTTP/stderr/error 路径不泄露 token。Exact `6ad5122ae500c2237bb79dc34c131f5c00df903d` native auth 15/15、source guard 1/1、kernel-server 238/238 与 all-target Clippy 通过；final required CI [31737253872](https://github.com/agentkernel/cognitive-os/actions/runs/31737253872) Ubuntu/Windows 2/2 通过 at head `17b848ea`; fmt/consistency/handbook/generated/docs-sync、双语 upgrade recovery 与 final defect-first review 均通过。PR [#215](https://github.com/agentkernel/cognitive-os/pull/215) merged as `main@203b3fca69b9a879fcbb5e933a5305141fcfb442`; lease closed. 无 public contract、P2-T14、Gate/release/Profile/B01/campaign 语义变化。 |
| P2-T20 | 参数化 mutation 请求载体（C2） | P2-T10, P2-T12, P2-T15 | 生产周期 worker 目前只对无参 WorkspaceRead 做 production dispatch；WorkspaceSearch/WorkspaceWrite/WorkspacePatch/HttpFetchReadOnly 仍 test-only，缺独立的 governed payload/preimage/origin 载体。本任务为每族补齐生产请求载体：受治理的搜索 query/cursor、写/patch 的 expected-preimage 与参数、HttpFetch 的 registered origin，且全部复用 P2-T15 hardened Effect/authority path（persist-before-dispatch、budget/fencing/idempotency、original-key reconcile、Artifact/Evidence）。不得新增未登记 family、不得绕过 Effect 语义、不得形成通用 ProcessRun 或任意 argv/env。完成后 C1+C2 覆盖六族生产派发，并解锁 A7（P2-T17）的依赖 | done | Merged via PR #219 at `main@1989b6ff`，required CI 双平台绿；六族生产请求载体全部接线并解锁 A7（P2-T17）。无 Gate/release/Profile claim |
| P2-T21 | 受治理候选参数与公开终态证据（C1/C2） | P2-T12, P2-T14, P2-T15, P2-T19, P2-T20 | 将私有 Pi 候选协议扩展为仅限 WorkspaceSearch、WorkspaceWrite 和 WorkspacePatch 的严格有界参数变体；daemon 自行 canonicalize、重算并精确校验 digest，同时验证 descriptor/family/epoch/fencing，并将已验证字节与 governed Intent 原子绑定。候选仍是不可信数据，绝不可控制 authority、Task lifecycle、budget、fencing、Effect binding、idempotency、verifier、acceptance 或原始 process/network capability。另提供经认证、task-channel、只读且从 authority/CAS 状态重建的耐久终态查询；仅返回 bounded/redacted lifecycle、opaque Intent/Effect refs、reconcile class、verification/acceptance refs/digests/currentness 和 bounded terminal transition/cursor，不泄露候选参数、workspace/path bytes、Pi/Provider 内容、receipt、SecretStore、bearer 或 SQLite。真实 production caller 必须贯通 Task admission -> candidate -> verified governed Intent -> scheduler -> router -> Search/Write/Patch sink，保留 preimage CAS、atomic publish、containment、persist-before-dispatch、original-key reconcile、budget/fencing/idempotency 和独立 verification/acceptance | done | Merged via PR [#220](https://github.com/agentkernel/cognitive-os/pull/220) at `main@5d1f5c2643d82807eb96f2c615d460dbdca749c3`. D01 bounded Search/Write/Patch candidate contract + digest binding, D02 end-to-end production Search/Write/Patch reachability with failure-first negatives, D03 authority/CAS-rehydrated redacted terminal evidence via `GET /task/evidence` + `admin-cli evidence`. Ubuntu required CI green at `a942abd5` (run `31902392486`); `DEV-LINUX-NATIVE-01` at `0404f0be`: kernel-server 283/283, workspace tests, Clippy `-D warnings`, fmt. Windows `not-run by owner-directed Linux-only route`. Closure: [P2-T21 running validation report](../checkpoints/20260816-personal-p2-t21-governed-candidate-terminal-evidence-report.md). Claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL claim. |
| P2-T22 | Governed software-repair journey | P2-T16, P2-T21, P2-T15, P2-T13, P2-T14 | 固定无 secret 的 TypeScript/Rust corpus 上完成 read/search -> write/patch -> RegisteredCheckRun -> diff Artifact -> independent verification -> acceptance；hidden test、越界写、test weakening、descriptor drift、timeout/orphan/oversize、exit-0-without-verification 与 restart unknown-outcome 全部 fail closed | done | BR-02；merged PR [#221](https://github.com/agentkernel/cognitive-os/pull/221) at `main@03bc530d`. D01–D03 complete at `4a803070`. Ubuntu required CI run `31919267639` green; exact-revision `DEV-LINUX-NATIVE-01` registered_check 21/21, A7 15/15, production_ 10/10, workspace tests 0-failed, Clippy `-D warnings`, fmt. Windows `not-run by owner-directed Linux-only route`；无 EVAL/Gate/release/Profile claim |
| P2-T23 | Public Memory/Skill consumption and resume | P2-T19, P2-T21, P3-T01..T06, P4-T01..T06 | public remember/review/forget 与 import/inspect/bind/supersede/revoke；Context 在 rank/body load 前完成 scope/purpose/epoch/lifecycle/exact-pin 授权；durable redacted consumption 与 session-2 resume 不依赖 chat replay/user restatement | done | BR-03；merged PR [#222](https://github.com/agentkernel/cognitive-os/pull/222) at `main@795bfac8`. Ubuntu `31922660543`; linux-002 bin 299/299；无 EVAL/Gate/release/Profile claim |
| P2-T24 | Effect fault and reconciliation observation | P2-T17, P2-T21, P2-T13, P2-T14, P2-T20 | default-off task/campaign-authorized fault profile；五个固定 fault point；task-channel bounded Effect history；restart 只按 original key query/reconcile；mutation count 仅 0/1，Indeterminate/open Effect 永不完成 Task | done | BR-04；merged PR [#223](https://github.com/agentkernel/cognitive-os/pull/223) at `main@2b803e0f`. Ubuntu `31925698730`; linux-002 bin 309/309；无 EVAL/Gate/release/Profile claim |
| P2-T25 | Tool lifecycle and real invocation | P2-T10, P2-T15, P2-T16, P2-T20, P2-T21, P5-T03, P5-T04 | lifecycle 到 Agent exposure 原子传播；bounded selection receipt；immutable RegisteredCheckRun success；task/campaign-scoped pinned HTTPS GET/HEAD；descriptor drift、redirect、credential URL、oversize、duplicate/restart negatives | done | BR-05；merged PR #224 at `main@4b10db9a`. 不新增通用 ProcessRun、任意 argv/env/cwd/shell 或 credential capability |
| P2-T26 | Bounded OS observation plane | P2-T23, P2-T24, P2-T25, P7-T04, P9-T06 | O2/O3/O4/O5/O13 authenticated bounded read plane：authorization、cache/compaction、queue/fairness/fence、Effect、audit chain/replay；每个 zero 有 denominator 与 active negative control | done | BR-06；merged PR [#225](https://github.com/agentkernel/cognitive-os/pull/225) at `main@9e1404a1`. Ubuntu `31934786837`; linux-002 observation 27/27, bins 329/329；无 EVAL/Gate/release/Profile claim |
| P2-T27 | User lifecycle and backup/restore | P7-T02, P5-T01, P5-T02, P5-T05, P2-T23 | 真实 CLI/API backup/restore，排除 secret/bearer/raw Provider/Pi/authority DB copy；preflight + transactional restore；managed Pi install 到 recover 全生命周期 | done | BR-07；merged PR [#226](https://github.com/agentkernel/cognitive-os/pull/226) at `main@70980131`. Ubuntu `31940138451`; linux-002 focused matrix at `5a561fbf`. Windows `not-run by owner-directed Linux-only route`；无 EVAL/Gate/release/Profile claim |
| P2-T28 | End-to-end journey and capability truth | P2-T21, P2-T22..P2-T27 | UJ1..UJ6 required rows逐项具备 public caller、mechanical oracle、cleanup、bounded evidence；覆盖 cold/warm、nested timing、Task terminal query、fault/restart、Memory/Skill、Tool、Pi lifecycle、backup/restore | done | BR-08；merged PR [#227](https://github.com/agentkernel/cognitive-os/pull/227) at `main@1e71344a`. Web UI/Multi-Agent 明确 scope-excluded；完成不等于 EVAL-004 B0/B1 或任何 Gate/claim |
| P2-T29 | C1/C2 enablement: daemon Workspace* advertisement and Memory sealed-header composer | P2-T21, P2-T23 | (1) Pi Extension registers daemon-governed WorkspaceSearch/WorkspaceWrite/WorkspacePatch tools; `READ_ONLY_TOOL_ALLOWLIST` stays empty; bash/edit/write and other Pi natives remain blocked; adapter `--no-builtin-tools` plus event extraction maps exactly one Workspace* call onto the P2-T21 candidate/Intent/Effect path; Pi native filesystem tools must not become the C1/C2 arm. (2) Public `POST /management/resource/v1/memory/remember` accepts unsealed owner fields and daemon-composes GovernanceSeed sealed headers; campaign/test code must not mint sealed headers on the public positive path; existing sealed envelopes remain valid. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#230](https://github.com/agentkernel/cognitive-os/pull/230) at `main@98cb23d1`. Product SHA `a317d817`; Ubuntu/Windows CI `32000735588` green at docs-head `dbbd176f`. linux-002: p4_t05 7/7, adapter 14/14, bin 336/336, workspace 0-failed, Clippy `-D warnings`, fmt. Report: [20260817-personal-p2-t29-c1-c2-enablement-report.md](../checkpoints/20260817-personal-p2-t29-c1-c2-enablement-report.md). Task completion is not EVAL/Gate/release/Profile promotion. |
| P2-T30 | Public-admit scheduler lease acquisition | P2-T12, P2-T21, P2-T29 | Admitted C1/C2 Tasks must leave `DRAFT`; the scheduler must acquire a lease; private-candidate Pi must spawn on the P2-T29 daemon-governed Workspace* path (not bash/edit/write). Failure-first: current skip (`scheduler_row_skip_before_lease`; `lease_acquired` 0; no Intent/Effect). Public `POST /task/admit` persists owner-local Context authorization facts and the tenant `personal` revocation epoch; the first scheduler tick walks Loop `START -> DECIDE` from the sealed ContextView before Pi. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#233](https://github.com/agentkernel/cognitive-os/pull/233) at `main@b13655b9`. Product SHA `85d6a2bb`; CI `32020479890` Ubuntu/Windows/`required-ci` green. linux-002: focused public-admit pass, bin 337/337, workspace 0-failed, Clippy `-D warnings`, fmt. Report: [20260817-personal-p2-t30-scheduler-lease-acquisition-report.md](../checkpoints/20260817-personal-p2-t30-scheduler-lease-acquisition-report.md). Task completion is not EVAL/Gate/release/Profile promotion. |
| P2-T31 | Live-daemon HTTP scheduler lease acquisition | P2-T30 | EVAL-006 proved P2-T30's in-process `TaskApi::handle` + `DeterministicProductionChainProposer` fixture is not the live daemon tick. A spawned `kernel-server --personal` must accept public HTTP record/interpret/preview/admit, leave `DRAFT` after scheduler ticks, acquire a lease (`lease_acquired` ≥ 1), and invoke the Unix private-candidate Pi transport (stub adapter allowed in tests; not bash/edit/write). Failure-first: EVAL-006 Task `DRAFT`, `lease_acquired` 0/0, no Pi child. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#236](https://github.com/agentkernel/cognitive-os/pull/236) at `main@da6fca2e`. Product SHA `49cdfc0c`; CI `32038273275` Ubuntu/Windows/`required-ci` green. linux-002: focused live HTTP 1/1, bin 337/337, workspace 0-failed, Clippy `-D warnings`, fmt. Report: [20260817-personal-p2-t31-live-daemon-scheduler-report.md](../checkpoints/20260817-personal-p2-t31-live-daemon-scheduler-report.md). Task completion is not EVAL/Gate/release/Profile promotion. |
| P2-T32 | Public `cognitive daemon start` scheduler lease | P2-T31 | EVAL-007 proved P2-T31's test spawn of `kernel-server --personal` with piped stderr is not the public `cognitive daemon start` launcher. A daemon started by production `cognitive daemon start` must accept public HTTP record/interpret/preview/admit, leave `DRAFT` after scheduler ticks, acquire a lease (`lease_acquired` ≥ 1), and invoke the Unix private-candidate Pi transport on daemon-governed Workspace* (stub adapter allowed in tests; not bash/edit/write). Failure-first: EVAL-007 Task `DRAFT`, `lease_acquired` 0, no Pi child; public stderr was `/dev/null`. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#239](https://github.com/agentkernel/cognitive-os/pull/239) at `main@c7ce4e5f`. Product `fb85cfff`; later head `a653dc7b` CI `32049064078` Ubuntu+Windows SUCCESS; linux-002 focused `p2_t32` 1/1 + `daemon_log` 2/2. Stub pass ≠ C1/C2 真机. Claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL promotion. |
| P2-T33 | Private-candidate host path and public adapter diagnosis | P2-T32 | EVAL-008 nested `completion.sock` under a long `--runtime-root` exceeded Linux `UNIX_PATH_MAX`. EVAL-009 spawned the real adapter then skipped `private_pi_candidate_adapter_rejected_the_request` because adapter stderr was `/dev/null` and the private-candidate proxy required a one-key `content`-only message (real Provider responses include `role`). A public `cognitive daemon start` must bind the completion socket under the host path limit, retain a redacted adapter diagnostic on `daemon.log`, accept one text choice that includes `role=assistant`, and strip `tools`/`tool_choice` before Provider forward. Stub Workspace* on a long root must still leave DRAFT (`lease_acquired` ≥ 1). Failure-first: EVAL-008/009 skip classes. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#240](https://github.com/agentkernel/cognitive-os/pull/240) at `main@09a44455`. Product `289eebad`; CI `32064313430` Ubuntu+Windows SUCCESS at `9dc40417`; linux-002 focused `p2_t33` 2/2. Stub pass ≠ C1/C2 真机. Claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL promotion. |
| P2-T34 | Private-candidate parameters digest canonicalization | P2-T33 | EVAL-010 spawned the real adapter on a long root (UNIX_PATH_MAX bind confirmed) then skipped `candidate_has_missing_fields_or_an_invalid_parameters_digest` at `validate_untrusted_pi_candidate`. JSON-fallback candidates must not trust a model-supplied digest: recompute from `parameters` when present, otherwise require `sha256:` plus 64 lowercase hex. Workspace* tool-event path already recomputes. Failure-first: EVAL-010 skip class. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Merged PR [#241](https://github.com/agentkernel/cognitive-os/pull/241) at `main@106cfcc0`; required Ubuntu/Windows CI passed. linux-002 at `a60ceed5`: adapter protocol 17/17 including empty-digest recompute, Clippy `-D warnings`, and fmt. Claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL promotion. |
| P2-T35 | Private-candidate JSON-fallback digest default and adapter diagnostic separation | P2-T34 | A JSON fallback with valid `parameters` but omitted `parameters_digest` must reach the existing canonical recomputation and eventually daemon `validate_untrusted_pi_candidate`; missing both fields and unknown fields stay fail closed. Adapter usage errors retain USAGE with exit 2, runtime failures use exit 3 without USAGE. Daemon diagnostics retain redacted tail errors and record adapter exit class so usage, Pi runtime, and invalid-candidate skips are distinguishable. Failure-first: EVAL-011 `private_pi_candidate_adapter_rejected_the_request` (`missing field parameters_digest at line 1 column 118` and mixed USAGE/runtime stderr). The daemon's independent family/epoch/descriptor/digest validation remains unchanged. Out of scope: C2d `acceptance_decision` contract (P2-T14/Lane-CTR) and pure-Pi broker/RAT-01 paired-runner qualification. Ubuntu supporting CI + exact-revision `DEV-LINUX-NATIVE-01`; Windows `not-run by owner-directed Linux-only route` | done | Owner authorized 2026-08-18. PR [#243](https://github.com/agentkernel/cognitive-os/pull/243) pending merge. D01-D03 pass: exact Linux adapter protocol 20/20, kernel-server 341/341, Clippy, fmt; required CI `32107175113` Ubuntu/Windows/required-ci pass at `ecc89fd1`. EVAL-011 stays closed and EVAL-012 live Pi evidence is not-run pending separate preregistration. Claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL promotion. |
| P2-T36 | C1 WorkspaceRead/Search public production path | P2-T35, P2-T29, P2-T32 | C1 requires the actual public `cognitive daemon start` composition, not a stub, private transport injection, SQLite fixture, or EVAL campaign. A real Pi candidate must be able to select daemon-governed WorkspaceRead and WorkspaceSearch only; both route through candidate validation, scheduler lease, production executor, independent verifier, and daemon acceptance. The candidate remains candidate-only; built-in Pi filesystem/shell tools remain disabled; descriptor/digest/epoch drift and tool-policy violations fail closed. The task uses a cleanable non-B01 supported Linux environment. C2, pure-Pi broker, paired runner/corpus/oracle, and any benchmark sample remain out of scope. | done | PR [#244](https://github.com/agentkernel/cognitive-os/pull/244) merged at `main@3efd7011b605a32ac0c9ec114321831995f32d90`; final documentation-head CI `32245868452` passed Ubuntu/Windows/required-ci. Fresh non-B01 Linux WorkspaceRead and WorkspaceSearch Tasks independently completed candidate admission, scheduler lease, executor, independent verification, and acceptance. Lease archived; branch/main reconciled. Claim ceiling `hypothesis`; no C2/EVAL/Gate/release/Profile/B01/Agent-benefit claim. |
| P2-T37 | C2a public WorkspaceWrite/Patch production path | P2-T21, P2-T29, P2-T36 | Public `cognitive daemon start` must expose only daemon-governed WorkspaceWrite/Patch beside C1 tools; mutation candidates remain untrusted, schema-bound, descriptor/digest/epoch validated, and flow through existing candidate admission, scheduler, expected-preimage executor, independent verifier, and acceptance. Pi built-in filesystem/shell tools remain disabled, and public fields cannot carry authority or bypass Intent/Effect. Failure-first coverage includes public allowlist/mapping omission, malformed mutation fields, built-in tools, descriptor drift, stale epoch, preimage mismatch, and duplicate/mixed candidates. | done | Merged PR [#246](https://github.com/agentkernel/cognitive-os/pull/246) at `main@286f7538148ba0d22f496f1f44d1af46f0f44aa0`. D01–D03: public Write `task://personal/p2-t37-public-write` and Patch `task://personal/p2-t37-public-patch-reseed` each reached `COMPLETED` / `lease_acquired: 1` / verification passed / acceptance current. Required CI `32290876044` and `32292548920` passed Ubuntu/Windows/required-ci. Running report: [P2-T37 validation](../checkpoints/20260819-personal-p2-t37-c2a-public-mutation-path-report.md). Claim ceiling `hypothesis`; no C2/EVAL/Gate/release/Profile/B01/Agent-benefit claim. EVAL-012 later retained a public Patch fail when the candidate named raw file SHA-256; that gap is P2-T38. |
| P2-T38 | Public WorkspacePatch post-state and raw-file SHA-256 preimage (EVAL-012 Priority 1) | P2-T37 | A public WorkspacePatch whose unified-diff payload is valid and whose `preimage` is `digest:sha256:<raw SHA-256 of current file bytes>` must persist a RECONCILED Effect, publish fixed post-state, pass independent verification, and reach acceptance the same way the domain-tagged workspace-image digest already does. A fail-closed Patch (preimage mismatch or patch that does not apply) must not request verification against a non-RECONCILED Effect (`fixed post-state is unavailable`). Wrong preimage still refuses the write. Out of scope: replacement-bytes Patch payload, system-prompt fairness, private-candidate epoch-1 skips. | done | Merged PR [#248](https://github.com/agentkernel/cognitive-os/pull/248) at `main@2b4c8f17`. Linux exact `5f4a538f` focused kernel-server **4/4**, Clippy `-D warnings`, fmt. Required CI `32333296569` at `31f8f165` passed Ubuntu, Windows, and required-ci. Report: [P2-T38 validation](../checkpoints/20260820-personal-p2-t38-workspace-patch-post-state-report.md). Claim ceiling `hypothesis`; no Gate/EVAL/release/Profile/B01/Agent-benefit claim. |
| P2-T08 | Runtime Spine E2E Gate | P2-T07 | 真实 projection→scheduler→Context→sidecar→Tool/process→checkpoint/recovery/verifier；B02/B04/B05/B12 与 false-completion negative；ADR-0018 到期核查；Tier-2 负例（ADR-0026） | done | 2026-08-11；ADR-0046 MVP: D01–D04 closed. Fixed matrix + non-claim report at `be7febb`; CI `31407542786`; owner `affirm all` → B02/B04/B05/B12 MVP `pass`. Closure: `docs/checkpoints/20260811-personal-p2-t08-runtime-spine-closure.md`; Draft PR #182. No GMVP-LINUX/B08/B09/release/Profile claim. |

### Phase 3 - Context Resource Value（Context、Token 与 Loop 效率）

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P3-T01 | 真实 Context source/retrieval port | P2-T01, P2-T02 | P2 application contracts 稳定即可开始，不等待 P2-T08 acceptance；workspace/task/evidence source；scope-filter 先于 ranking；revocation 测试 | done | 2026-08-07; `P3-T01/D01` closes at `0ad1ddb95f4e347d0c205597e69ad8818819948e`: immutable TaskContract v0.4 ContextRequest binding and workspace Context sources provide task-bound source provenance; source roles preserve working, authoritative-state and evidence inputs without weakening per-source authority. The daemon performs tenant/scope metadata filtering before body access/ranking, reloads durable authorization/revocation facts before every body load, persists the sealed request-bound ContextView before candidate-only Pi transport, and fails closed for revoked or required-missing Context. Exact native Linux passed `cargo test -p kernel-server` and `cargo test -p cognitive-store --test m5_context_store` (9/9); required Ubuntu/Windows CI passed in PR #161. B03 remains `not-run`; task completion creates no Gate, release or Profile claim. |
| P3-T02 | 真实最小 Context Builder 与预算 | P3-T01 | System/Shell/Task/Working/Evidence fragments；required fail-closed；loss 显式；同一 Task trace | done | 2026-08-09 closure: D01-D02 satisfy the unchanged acceptance. The daemon derives required System/Task fragments from immutable request/contract inputs, rejects required budget overflow and semantic duplicates explicitly, and applies source-family plus role-specific freshness policy before any workspace body load. Loaded sources retain strong digest-bound ContextView references; excluded source losses retain source-digest verification. Exact native Linux focused test and Clippy passed at `0d8f5628a897aea32ee4cb7929bac1320ccb2a96`; required Ubuntu/Windows CI passed in PR #166. B03, UCR-01, release, and Profile remain `not-run`/non-claim. Evidence: [task closure handoff](../checkpoints/20260809-personal-p3-t02-context-builder-closure.md). |
| P3-T03 | 唯一 Artifact CAS | P3-T02 | filesystem CAS + authority metadata；digest/partial-write/GC/access 测试；不得建立第二 artifact store | done | 2026-08-09 closure: D01-D02 deliver the single daemon-owned bounded filesystem CAS with SHA-256 content references, immutable metadata, atomic publication, caller-policy admission, staging-file cleanup, and real verifier consumption. Missing or tampered evidence bytes fail closed before append-only verification-report persistence. Exact native Linux at `87e436e22dae3722fb0ced6c8ceeb8f0f4deddc8` passed focused verifier/CAS tests, Clippy, and fmt; required Ubuntu/Windows CI passed in PR #168. No Task completion, B03, Gate, release, or Profile claim is created. |
| P3-T04 | Context delta、stable prefix、cache 与 telemetry | P3-T02, P3-T03 | delta/stable-prefix 构建、治理绑定 cache key、loss/usage/loop telemetry；stale/revoked cache fail-closed | done | 2026-08-09: D01 added a daemon-private digest-only Context cache whose strict key binds governance, request, contract, source, renderer, and daemon-known Tool facts; fresh discovery, authorization, and body/digest validation always precede reuse. D02 derives repeat/no-progress decisions from daemon-issued action identities plus append-only evidence digests, and fails closed when wait/switch/block lacks a daemon-owned continuation path. Exact native Linux validation passed at `128915e15d4f4b4b98f195f0b6a49a6de76f34f2`; required Ubuntu/Windows CI passed in PR #169. No B03, Gate, release, or Profile claim is made. |
| P3-T05 | UCR-01 benefit runner 与稳定基线 | P3-T04 | 跨 Gate workload runner、raw run/CI/non-claim；B06/B07 收益只采集，不作为 1.0 pass 前提 | done | 2026-08-09: D01 adds a fixed UCR-01 raw-run report boundary that requires all six families, fixture/trace/baseline digests, finite bounded measurements, and a matching pinned baseline. It rejects Gate/release/Profile/completion/pass claims and emits deterministic non-claim report digest metadata. Exact native Linux passed at `72690e028c7f3fb3896782c1874f575f35ebe165`; required Ubuntu/Windows CI passed in PR #170. B03/B06/B07, all Gates, release, and Profile remain unclaimed. |
| P3-T06 | B03 Context correctness 与收益采集 | P3-T05 | MVP B03 uses the fixed 33-test authority-path matrix, native Linux/Clippy, Ubuntu/Windows CI, cleanup/redaction, and owner review under ADR-0040. B06/B07 only collect repeatable raw performance observations and never block correctness completion. | done | ADR-0040 fixed the MVP denominator. Exact branch revision `7ea39472899e8ac77f30e589da89b7b4e0b316a2` passed required Ubuntu/Windows CI run `31347323835`; native evidence records 22/22 Rust authority-path tests, 11/11 evaluator/tooling tests, focused Clippy, cleanup/redaction, and owner review. B03 MVP is `pass`; B06/B07, UCR-01 utility, GMVP-LINUX, release, and Profile remain separate. |

### Phase 4 - Memory 与 Skill

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P4-T01 | Memory store、admission 与 policy | P3-T01, P3-T02 | 基于 stable Context ports 的 source/version/provenance/scope/freshness/retention；写入只接受 proposal 并由 deterministic policy admission | done | 2026-08-09 closure: D01 at `e4eb38ad9aaba13f04fb51657dfdc884af66cdc5` adds deterministic proposal policy, append-only SQLite v16 candidate/decision/object records, atomic source revalidation, daemon-private admission service, and failure-first direct-admit/source-mismatch/retention/scope/no-partial-object coverage. Exact native Linux focused tests and Clippy passed; required Ubuntu/Windows CI passed. FTS/retrieval, lifecycle/forget, public API/projection, B08, Gate, release, and Profile remain separate/unclaimed. |
| P4-T02 | SQLite FTS5 + metadata filter baseline | P4-T01 | authority metadata pre-filter 后检索；unauthorized/stale/conflict/delete/rebuild 负例 | done | 2026-08-09 closure：v17 daemon-private SQLite FTS5 derived index filters admitted decision/scope/purpose/retention/current source binding before ranking and returns metadata-only candidates. Exact Linux focused migration + FTS regressions and required Ubuntu/Windows CI passed; Memory lifecycle/forget, public API, B08, Gate, release, and Profile remain unclaimed. Evidence: [closure](../checkpoints/20260809-personal-p4-t02-memory-fts-closure.md). |
| P4-T03 | Memory lifecycle、retention 与 forget | P4-T01 | version/update/conflict/expire/forget/tombstone/audit 可验证；派生索引可完整失效 | done | `P4-T03/D01-D03` complete under `lease/personal/P4-T03/memory-lifecycle`; immutable forget/expiry/supersede facts, version CAS lineage, and atomic FTS invalidation passed exact native Linux focused tests and required Ubuntu/Windows CI at `8f9250dcd4cbcd8f15867e7a0f45165032e26c9d`. No public API, B08, Gate, release, or Profile claim. |
| P4-T04 | Skill package、revision、local import 与 binding | P3-T02 | local package/revision/digest/import；Agent/Task/workspace binding；Skill 不自授权或直接成为 authority | done | `P4-T04/D01-D03` complete daemon-private immutable package/revision/digest/import, scope-bound binding, append-only revoke, same-package supersede, exact-pin explanation, digest-drift rejection, and management-session import authorization. Exact native Linux focused tests/Clippy and required Ubuntu/Windows CI passed at `883cd5fca9b14182cc5b5632948476b31b8744a3`. Public API/projection, Context/Task consumption, B08, Gate, release, and Profile remain separate. Evidence: [closure](../checkpoints/20260810-personal-p4-t04-skill-package-closure.md). |
| P4-T05 | Memory/Skill API 与统一 projection | P4-T01, P4-T02, P4-T03, P4-T04 | CLI/Shell/sidecar 同 application services；Memory/Skill 可解释、可撤销、可实际进入 Context/Task；不依赖 embedding | done | Closure checkpoint `20260810-personal-p4-t05-memory-skill-api-closure.md`; D01-D05 deliver daemon-private task-bound projection, authority-backed explain reads, Memory forget/remember, Skill revoke/import/bind callers, and failure-first channel/payload/authority negatives. Required Ubuntu/Windows CI run `31335218082` passed. Public contract generation, B08, Gate, release, Profile, and actual Context/Task consumption remain separate. |
| P4-T06 | B08 Memory+Skill correctness 与 UCR-01 consumption | P4-T05 | lifecycle/privacy/forget/binding/actual consumption 证据；UCR-01 同一 Task trace 消费 Memory 与 Skill | done | Closure checkpoint `20260810-personal-p4-t06-memory-skill-correctness-closure.md`; D01-D03 deliver daemon-private Task-contract/ContextRequest-bound Memory/Skill consumption with authority-derived scope, lifecycle/eligibility checks, and failure-first channel/payload/task negatives. Exact native Linux focused test and Clippy passed at `f4b4d38`; required Ubuntu/Windows CI passed for code run `31338813801` and closure run `31339698492`. PR #177 merged at `main@f35c2e1dfd0f8a841e009bac4c1458b0dfcfde28`. B08 remains an independent Gate campaign and is unclaimed. |

| Slice | Parent | Delivery | Prerequisites | Verification boundary |
|---|---|---|---|---|
| `P4-T04/D01` | P4-T04 | daemon-private immutable local Skill package/revision import, compatibility result, and Agent/Task/workspace eligibility binding records | P3-T02 stable Context boundary; existing SQLite authority migration model | failure-first unsafe-path, revision compatibility, duplicate immutable identity, revoked binding, and cross-workspace binding negatives; exact native Linux focused store tests and Clippy; required Ubuntu/Windows CI; no public API/projection, Context/Task consumption, B08, Gate, release, or Profile claim |
| `P4-T04/D02` | P4-T04 | append-only binding revocation facts preserve explain/audit history while preventing revoked bindings from reappearing on daemon eligibility reads | `P4-T04/D01` immutable package/revision/binding facts | failure-first duplicate/unknown revocation and revoked-binding non-resurrection tests; exact native Linux focused store tests and Clippy; required Ubuntu/Windows CI; no public API/projection, Context/Task consumption, B08, Gate, release, or Profile claim |
| `P4-T04/D03` | P4-T04 | immutable same-package revision supersession preserves exact binding pins and prevents competing replacement lineage; import payloads must carry their recorded manifest/content digest | `P4-T04/D01` revision facts | failure-first digest-drift, unknown/cross-package/duplicate supersede, and pinned-binding stability tests; exact native Linux focused store tests and Clippy; required Ubuntu/Windows CI; no public API/projection, Context/Task consumption, B08, Gate, release, or Profile claim |
| `P4-T05/D01` | P4-T05 | daemon-private task-bound Memory/Skill resource projection and application boundary over existing authority facts | P4-T01, P4-T02, P4-T03, P4-T04 | failure-first unauthorized/task-channel, revoked/forgotten, stale-projection, and unsupported-family negatives; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no B08, Gate, release, or Profile claim |
| `P4-T05/D02` | P4-T05 | daemon-private Memory/Skill lifecycle application operations remain authority-backed and expose explain/list results without granting client authority | `P4-T05/D01` task-bound application boundary | failure-first missing/stale/revoked/forgotten operation negatives, immutable pin preservation, and no-partial-write checks; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no B08, Gate, release, or Profile claim |
| `P4-T05/D03` | P4-T05 | daemon-private lifecycle command seam for remember/forget and Skill import/bind/revoke, preserving management/task channel separation | `P4-T05/D02` authority-backed explain/list boundary | failure-first malformed payload, unauthorized channel, duplicate immutable operation, revoked/forgotten non-resurrection, and no-partial-write negatives; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no B08, Gate, release, or Profile claim |
| `P4-T05/D04` | P4-T05 | daemon-private remember admission and local Skill import/bind command callers use existing policy/store authority seams without direct client writes | `P4-T05/D03` lifecycle command channel boundary | failure-first source mismatch, digest drift, unsafe import, incompatible binding, duplicate identity, and no-partial-write negatives; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no B08, Gate, release, or Profile claim |
| `P4-T05/D05` | P4-T05 | final daemon-private Memory/Skill application and projection acceptance mapping over the delivered authority callers | `P4-T05/D04` remember/import/bind authority callers | exact acceptance mapping, focused native Linux tests and Clippy, required Ubuntu/Windows CI, consistency/evidence synchronization, and explicit no-public-API/B08/Gate/release/Profile claims |
| `P4-T06/D01` | P4-T06 | daemon-private Memory/Skill consumption boundary records one Task trace with authority-backed eligibility and provenance | `P4-T05` complete private application boundary | failure-first forgotten/expired Memory, revoked/incompatible Skill, workspace/task mismatch, and missing authority facts; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no B08 pass claim |
| `P4-T06/D02` | P4-T06 | same-task Context/Task trace consumes eligible Memory and Skill facts without granting client write authority | `P4-T06/D01` eligibility/provenance boundary | failure-first privacy leakage, stale source, duplicate consumption, and cross-task trace negatives; exact native Linux focused tests and Clippy; required Ubuntu/Windows CI; no public API or Gate claim |
| `P7-T04/D01` | P7-T04 | deterministic module benchmark harness measures Context filtering/builder/cache, Memory FTS5, Artifact CAS, scheduler CAS, Intent/Effect persistence, and canonical report serialization without Provider or Gate claims | P3-T04/P3-T05/P3-T06 and P4-T05 stable boundaries | failure-first invalid fixture/revision/measurement negatives; exact native Linux benchmark run, supported CI, and consistency; output must be digest-bound and non-claim |
| `P7-T04/D02` | P7-T04 | real governed-path stage collector records authorization, Context resolution, cache reuse, Effect persistence/reconcile, and bounded counters from one daemon-owned path | `P7-T04/D01` benchmark evidence boundary | focused instrumentation tests, warm/cold and omitted-stage negatives, exact native Linux validation, supported CI; no fabricated p95/p99 or Agent-benefit claim |
| `P7-T04/D03` | P7-T04 | B06/B07 stable and changed Context raw observations compare real bounded execution against full replay with complete denominator and safety accounting | `P7-T04/D02` stage measurement boundary and UCR-01 runner | fixed scenario run, raw evidence digest, independent review, and required CI; observations remain non-claim and do not block GMVP-LINUX |
| `P7-T04/D04` | P7-T04 | controlled benchmark workflow evaluates module regression floors and report policy without making floating CI a product performance environment | `P7-T04/D01` and shared report validator | positive/negative policy fixtures, workflow consistency, supported CI; release thresholds require preregistration and explicit breach action |
| `P7-T04/D05` | P7-T04 | fixed-environment release campaign produces A/B governance non-inferiority evidence, exact revision/environment digests, and complete task acceptance closure | `P7-T04/D01-D04` and owner-approved preregistration | fixed native environment, complete denominator, p50/p95/p99, safety/no-false-completion accounting, independent verifier, required CI; no significant-benefit claim without A/B/C/D protocol |
| `P7-T01/D01` | P7-T01 | signed Personal six-resource release manifest verifies Memory/Skill/Tool/Context/Task/Runtime schema/version/digest pins plus sidecar/adapter/Skill/Tool pins against caller-fixed authority pins and production-trust Ed25519 signature | completed P0-T03 license/distribution fields; P1-T08 linux-bundle attestation patterns; P2-T08 done | failure-first missing-family, pin-drift, Gate/Profile claim, Pi-drift, non-HTTPS attestation, non-canonical, and bad-signature negatives; exact native Linux focused module tests, Clippy, and required Ubuntu/Windows CI; no SBOM generation, GitHub Release, production key, Gate, release, or Profile claim |
| `P7-T01/D02` | P7-T01 | SBOM digest binding and Linux artifact digest verification consume only D01-verified release manifests and reject secret/dev-path contaminated inventories | `P7-T01/D01` signed six-family manifest | failure-first SBOM digest mismatch, missing inventory, and contaminated-path negatives; exact native Linux and required CI; no production signing ceremony or release publication claim |
| `P7-T01/D03` | P7-T01 | immutable toolchain/environment/action pins and acquisition-lock production-trust reference bind Rust/Node/pnpm/Pi/sidecar/CI identities without floating refs | `P7-T01/D01` and D02 digest boundary | failure-first floating-ref and pin-drift negatives; exact native Linux and required CI; no GMVP-LINUX/release/Profile claim |
| `P7-T01/D04` | P7-T01 | final acceptance mapping for release pipeline, six-resource manifest, SBOM, and attestation over D01-D03 evidence | `P7-T01/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P7-T02/D01` | P7-T02 | daemon-private Personal backup inventory plans eligible layout roots and explicitly excludes secret/bootstrap/provider secret carriers | completed P1-T01 layout; P7-T01 done | failure-first secret-path reintroduction and missing-category negatives; exact native Linux focused tests, Clippy, required CI; no archive write, restore, Gate, release, or Profile claim |
| `P7-T02/D02` | P7-T02 | Memory/Skill/bindings export consumes only D01-approved inventory categories and binds digests without secret material | `P7-T02/D01` | failure-first secret contamination and missing-binding negatives; exact native Linux and required CI |
| `P7-T02/D03` | P7-T02 | restore preflight and migration checks reject incompatible/incomplete backups before mutation | `P7-T02/D02` | failure-first schema/migration/preflight negatives; exact native Linux and required CI |
| `P7-T02/D04` | P7-T02 | transactional update/rollback/uninstall and final acceptance over D01-D03 backup/restore evidence | `P7-T02/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P7-T03/D01` | P7-T03 | redacted six-resource doctor health projection with stable error codes and no secret/authority claims | completed P1-T05 doctor surface; P7-T02 done | failure-first missing-family, secret-contamination, and authority-claim negatives; exact native Linux focused tests, Clippy, required CI; no vault unlock, Gate, release, or Profile claim |
| `P7-T03/D02` | P7-T03 | headless vault locked/TTY/unattended diagnostic paths remain redacted and secret-free | `P7-T03/D01` | failure-first secret-in-unit/env negatives; exact native Linux and required CI |
| `P7-T03/D03` | P7-T03 | sidecar drift, process/effect reconcile, and migration doctor facts with operable recovery hints | `P7-T03/D02` | failure-first drift/reconcile negatives; exact native Linux and required CI |
| `P7-T03/D04` | P7-T03 | final acceptance over D01-D03 doctor/support evidence | `P7-T03/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P8-T02/D01` | P8-T02 | daemon-private AKP-only adapter capability declaration/registration with candidate-only and no-public-listener negatives | completed P8-T01; P5-T02 done | failure-first public-listener, authority-writer, and digest-tamper negatives; exact native Linux focused tests, Clippy, required CI; Lane-CTR public schema remains later |
| `P8-T02/D02` | P8-T02 | adapter lifecycle activate/pause/stop over registered declaration digests | `P8-T02/D01` | failure-first stale-digest and channel-isolation negatives; exact native Linux and required CI |
| `P8-T02/D03` | P8-T02 | Lane-CTR `agent-adapter-manifest` contract registration + generated bindings | `P8-T02/D02` | failure-first schema/consistency negatives; required CI |
| `P8-T02/D04` | P8-T02 | final acceptance over D01-D03 adapter evidence | `P8-T02/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P8-T04/D01` | P8-T04 | daemon-owned lifecycle hook registry for admission/pre-dispatch/post-effect/verification with axiom-relaxation and authority-writer negatives | completed P8-T01; P2-T08 done | failure-first axiom-relaxation, authority-writer, stale-digest, and event-mismatch negatives; exact native Linux focused tests, Clippy, required CI |
| `P8-T04/D02` | P8-T04 | owner-programmable deterministic hook invocation bound to declaration digests with management-channel isolation | `P8-T04/D01` | failure-first Task-channel and digest negatives; exact native Linux and required CI |
| `P8-T04/D03` | P8-T04 | graded Skill/rule loading by context cost with fail-closed budget overflow | `P8-T04/D02` | failure-first cost overflow and undeclared-load negatives; exact native Linux and required CI |
| `P8-T04/D04` | P8-T04 | final acceptance over D01-D03 harness-hook evidence | `P8-T04/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P8-T05/D01` | P8-T05 | daemon-owned digest-bound compact Context artifact with explicit loss records and self-authorization negatives | completed P8-T01; P3-T04 done | failure-first self-authorization, missing-identity, and empty-retention negatives; exact native Linux focused tests, Clippy, required CI |
| `P8-T05/D02` | P8-T05 | adaptive fragment budgets from durable telemetry without skipping body reauthorization | `P8-T05/D01` | failure-first skip-reauth and unbounded-budget negatives; exact native Linux and required CI |
| `P8-T05/D03` | P8-T05 | UCR-01-compatible non-claim benefit observation over compaction digests | `P8-T05/D02` | failure-first Gate/authority-shaped claim rejection; exact native Linux and required CI |
| `P8-T05/D04` | P8-T05 | final acceptance over D01-D03 compaction evidence | `P8-T05/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P8-T06/D01` | P8-T06 | private Reflexion-family failure-lesson planner emits digest-bound Memory candidate proposals without self-authorization or durable writes | completed P8-T01; P4-T05 done | failure-first self-authorization, direct Policy/Verifier promotion, and missing-identity negatives; exact native Linux focused tests, Clippy, required CI |
| `P8-T06/D02` | P8-T06 | admitted Memory candidates persist only through existing daemon Memory admission; forget/tombstone remains explainable | `P8-T06/D01` | failure-first producer-forged admit and source-mismatch negatives; exact native Linux and required CI |
| `P8-T06/D03` | P8-T06 | Skill candidate import/bind/revoke through existing Skill package lifecycle without capability grant | `P8-T06/D02` | failure-first channel/self-auth/capability-grant negatives; exact native Linux and required CI |
| `P8-T06/D04` | P8-T06 | final acceptance over D01-D03 learning-loop evidence | `P8-T06/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P8-T07/D01` | P8-T07 | handbook machine core: `personal/handbook/_meta` manifest/frontmatter schema/source-map/coverage/annotations plus standalone `tools/src/check-handbook.mjs`, `generate-handbook.mjs`, fingerprint filler, and focused negative fixtures proving missing-source, stale-fingerprint, broken-link, unmapped-new-file, illegal-status, generated-drift, missing-locale, secret-shaped, dynamic-status-copy, and History-link failures | merged `main` after P9-T04 closure; existing tools Node/Ajv/YAML stack | local tools Node tests (handbook negatives green), `node tools/src/check-handbook.mjs`, generator smoke, `pnpm run check:consistency`, `git diff --check`; required Ubuntu/Windows CI; no Gate/release/Profile claim |
| `P8-T07/D02` | P8-T07 | complete bilingual user tree plus generated reference tree (cognitive/admin CLI, HTTP surface, errors, config files, env vars, transitions, schemas, tool catalog) produced from implementation and machine contracts with bidirectional annotation anti-rot checks | `P8-T07/D01` | `node tools/src/generate-handbook.mjs --check` byte equality, handbook checker, link/coverage checks; required Ubuntu/Windows CI |
| `P8-T07/D03` | P8-T07 | complete bilingual developer and AI trees plus future-sync surfaces: `.cursor/rules/20-cognitiveos-personal-handbook-sync.mdc`, root `llms.txt`, one AGENTS.md pointer, root `check:handbook` script and a named CI step | `P8-T07/D02` | handbook checker, `pnpm run check:consistency`, diff checks; required Ubuntu/Windows CI; legacy documentation byte-identical except allowlisted governance paths |
| `P8-T07/D04` | P8-T07 | final acceptance mapping: fingerprints and source-set record recomputed at the merge-candidate revision, coverage/locale-pairing zero-gap, `--diff-base` legacy-integrity proof, closure checkpoint, and ready/merge/lease/branch/main closure | `P8-T07/D01-D03` | full local check set (`check:consistency`, `check:handbook`, generator `--check`, `git diff --check`) plus required Ubuntu/Windows CI at the exact merge head; documentation-only; no Gate/release/Profile claim |
| `P8-T08/D01` | P8-T08 | conditional docs-sync gate (`tools/src/docs-sync-gate.mjs`: source-map routing for staged/push/range modes, handbook check set on hit, fail-closed on unsynced mapped changes with an explicit recorded `DOCS_IMPACT_NONE` escape), repo `.githooks/` pre-commit/pre-push, `hooks:install` + `check:docs-sync` scripts, and focused routing/negative tests | merged `main` after P8-T07 (handbook machine model present) | tools Node tests green including new gate negatives; gate smoke on real repo; `check:consistency`; `git diff --check`; required Ubuntu/Windows CI |
| `P8-T08/D02` | P8-T08 | canonical obligation registration: docs-sync-contract §2 handbook obligations (all change classes) + §5 machine gates + §6 checklist item; rule 10/20 and AGENTS.md checkpoint/closure wording; affected handbook pages (sync-policy, ai docs-impact/validation-commands, contributing workflow, coverage/source-map/legacy-allowlist) updated bilingually with fingerprints refreshed | `P8-T08/D01` | `check-handbook`, `generate --check`, `check:consistency`, gate self-run green; required Ubuntu/Windows CI |
| `P8-T08/D03` | P8-T08 | final acceptance mapping, closure checkpoint, and ready/merge/lease/branch/main closure | `P8-T08/D01-D02` | full local check set plus required Ubuntu/Windows CI at the exact merge head; documentation/tooling-only; no Gate/release/Profile claim |
| `P8-T09/D01` | P8-T09 | candidate-only dsh AKP protocol: exact dsh revision and AKP schema digest pins, session fencing, monotonic sequence, snake_case JSONL/HTTP wire, frame/timeout bounds, and authority/secret/forbidden-field/malformed/oversized/sequence negatives | completed P8-T02; P8-T03 done | TypeScript source build/test; Rust focused `deepseek_harness` tests, fmt, and Clippy on supported Linux/CI; no local Windows GNU linking |
| `P8-T09/D02` | P8-T09 | daemon `POST /task/akp/dsh` session table, Workspace* mapping onto existing public candidate admission, independent dsh adapter registration (no public listener, no authority writer, no Pi evidence transfer), and HTTP-route handbook annotation | `P8-T09/D01` | focused kernel-server protocol negatives; exact-revision Linux/CI; generated HTTP reference regenerated |
| `P8-T09/D03` | P8-T09 | linux-002 exact-revision E2E: disposable worktree/runtime; SecretStore-bound DeepSeek Flash; read-only WorkspaceRead and WorkspaceSearch; one disposable WorkspaceWrite/Patch through Intent/Effect/verification/acceptance; fail-closed restart/duplicate/unknown-outcome matrix; paired Path A/B timing observation | `P8-T09/D02` | identity-confirmed `B01-Desktop-Linux-002` only; pushed exact Git revision; secret residue cleanup; no Gate/B01/release/Profile/Agent-benefit claim |
| `P8-T09/D04` | P8-T09 | docs-sync, handbook checker/generator/fingerprints, consistency, required CI, acceptance mapping, and ready/merge/lease/branch/main closure | `P8-T09/D01-D03` | claim ceiling `hypothesis`; distinguish implementation / tested-local / linux-002 / performance observation |
| `P8-T10/D01` | P8-T10 | `cognitive dsh configure` and `cognitive dsh launch`: exact dsh revision pin, candidate-only adapter digest in `dsh.json`, launch prep without requiring Pi `first_conversation_ready`, fail-closed relative/wrong-revision/missing-config/unready-secret/direct-Flash product path | completed P8-T09 | focused admin-cli tests on supported Linux/CI; no local Windows GNU linking |
| `P8-T10/D02` | P8-T10 | linux-002 installed Path B: identity-confirmed guest; `cognitive dsh configure` then `cognitive dsh launch --print`; real WorkspaceRead/Search and disposable Write through Intent/Effect/verification/acceptance; SecretStore-bound Flash; dsh response is not Task completion | `P8-T10/D01` | identity-confirmed `B01-Desktop-Linux-002` only; pushed exact Git revision; secret residue cleanup |
| `P8-T10/D03` | P8-T10 | same-host Path A (`dsh → Flash` direct) vs Path B (`dsh → AKP → daemon → Flash`) paired observation: n≥5, cold/warm, TTFT/total, p50 when n allows, timeout/error counts, retained-failure denominator; do not preset lossless; locate overhead if present | `P8-T10/D02` | measurement-only; Path A key via `--api-key-file` stdin/0600 file never argv/env/logs; no Gate/B01/release/Profile/Agent-benefit claim |
| `P8-T10/D04` | P8-T10 | docs-sync, handbook checker/generator/fingerprints, consistency, required CI, acceptance mapping, and ready/merge/lease/branch/main closure | `P8-T10/D01-D03` | claim ceiling `hypothesis`; distinguish implementation / tested-local / linux-002 / performance observation |
| `P8-T11/D01` | P8-T11 | daemon Provider proxy accepts `stream:true` on the public management route, forwards SSE from the selected model, and flushes first-byte events without waiting for unary JSON; private-candidate and Pi `stream:false` stay unary; fail-closed selected-model/secret/auth pins unchanged | completed P8-T10 | focused transport + proxy negatives (auth, model mismatch, private-candidate stream refuse, delayed-chunk first-byte-before-last); exact-revision Linux/CI; no local Windows GNU linking |
| `P8-T11/D02` | P8-T11 | daemon-owned dsh runtime inspect: `GET /personal/dsh/runtime` and `cognitive dsh status` report ACTIVE sessions during a live task and INACTIVE/CRASHED after stop or dead pid; process observation never opens cmdline/environ; launch/helper bind is observation-only | `P8-T11/D01` | focused kernel-server + admin-cli negatives; exact-revision Linux/CI |
| `P8-T11/D03` | P8-T11 | linux-002 exact-revision: identity-confirmed guest; raw Provider probe vs Path A vs installed Path B; real-task corpus (WorkspaceRead/Search, disposable Write through Intent/Effect/verification/acceptance, one non-`pong` LLM task); n≥5 retained per path; SecretStore-bound Flash; residue cleanup | `P8-T11/D02` | identity-confirmed `B01-Desktop-Linux-002` only; pushed exact Git revision; Path A key via `--api-key-file` stdin/0600 file never argv/env/logs |
| `P8-T11/D04` | P8-T11 | docs-sync, handbook checker/generator/fingerprints, consistency, required CI, acceptance mapping, and ready/merge/lease/branch/main closure | `P8-T11/D01-D03` | claim ceiling `hypothesis`; distinguish implementation / tested-local / linux-002 / performance observation |
| `P8-T12/D01` | P8-T12 | Failure-first Resource Manager negatives: unknown family, generic create/install/execute/complete, task-channel 403, missing id/version/idempotency, stale expected_version, unsupported family+op | completed P2-T02, P2-T05, P4-T05, P2-T25 | focused kernel-server tests; exact-revision Linux/CI; no local Windows GNU linking |
| `P8-T12/D02` | P8-T12 | List/inspect from authority stores (tool catalog+overlay, non-tombstoned memory, skill bindings, current task contracts; honest empty context/runtime) plus mutating dispatcher onto existing Skill bind/revoke and Tool enable/disable/revoke, and `cognitive resource` CLI caller | `P8-T12/D01` | focused kernel-server + admin-cli parse tests; exact-revision Linux/CI |
| `P8-T12/D03` | P8-T12 | Exact-revision native Linux 真机实测 on `personal-linux-native-01` (`wuz@192.168.1.2`): focused kernel-server tests, Clippy `-D warnings`, fmt | `P8-T12/D02` | pushed exact Git revision in a cleanable worktree; not B01 guests |
| `P8-T12/D04` | P8-T12 | docs-sync, handbook checker/generator/fingerprints, consistency, required CI, acceptance mapping, and ready/merge/lease/branch/main closure | `P8-T12/D01-D03` | claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL/Agent-benefit promotion |
| `P8-T13/D01` | P8-T13 | Failure-first endpoint trust / SSRF / secret-leak / wrong-channel negatives: embedded URL credentials, HTTP/private without grants, Anthropic-compatible custom endpoints, header injection, unauthenticated 401, task-channel 403 | completed P1-T02, P1-T03, P1-T04, P1-T06, P1-T07 | focused kernel-server + cognitive-secret tests; exact-revision Linux/CI; no local Windows GNU linking |
| `P8-T13/D02` | P8-T13 | Named accounts, Secret Store opaque refs, key set/rotate/remove, delete blocked by active binding | `P8-T13/D01` | focused store + kernel-server tests; keys never in SQLite/config/argv/env/logs/CLI/audit |
| `P8-T13/D03` | P8-T13 | Official OpenAI, official Anthropic, OpenAI-compatible transport/discovery; model catalog; manual models; versioned prices; discovery failure preserves catalog/binding | `P8-T13/D02` | focused store + kernel-server tests; no background refresh |
| `P8-T13/D04` | P8-T13 | Usage normalization (four token classes; unknown never 0), audit events, 30-day events / 90-day aggregates, `cost_unavailable` | `P8-T13/D03` | focused store tests; missing price is not zero cost |
| `P8-T13/D05` | P8-T13 | Pi fixed binding + daemon proxy; independent DeepSeek harness adapter on `/provider/v1/dsh/*`; no fallback; Path B uses the dsh route | `P8-T13/D03` | focused kernel-server isolation tests; Pi evidence does not qualify dsh |
| `P8-T13/D06` | P8-T13 | Observe-only budgets, 80%/100% deduped alerts, usage/cost/audit CLI callers | `P8-T13/D04` | focused store + admin-cli parse tests; alerts do not block or reroute |
| `P8-T13/D07` | P8-T13 | docs-sync, handbook checker/generator/fingerprints, consistency, exact-revision Linux, required CI, acceptance mapping, and ready/merge/lease/branch/main closure | `P8-T13/D01-D06` | claim ceiling `hypothesis`; no Gate/release/Profile/B01/EVAL/provider-quality/Agent-benefit promotion |
| `P8-T14/D01` | P8-T14 | Bilingual handbook operator usage for the shipped Provider Control Plane CLI/daemon API (accounts, keys, trust flags, models, bindings, usage/audit projection, observe-only budgets/alerts, common failures), source-map/manifest routing, related user/reference pointers, docs-sync/consistency, Draft PR | completed P8-T13 | `check-handbook`, generator `--check`, `check:consistency`, `docs-sync-gate --staged`; documentation-only; no Gate/release/Profile/B01/EVAL/provider-quality/Agent-benefit promotion |
| `P8-T15/D01` | P8-T15 | `cognitive dsh web`: loopback-only `--profile web --no-open`, fail-closed missing `apps/web/dist`, Path B AKP patch without Workspace* admits, `cognitive dsh status` observes web pid; focused negatives (`0.0.0.0`, missing dist, Path A still measurement-only); headless `--print` unchanged | completed P8-T10, P8-T11 | focused admin-cli + adapter node tests; exact-revision Linux/CI; no local Windows GNU linking |
| `P8-T15/D02` | P8-T15 | linux-002: disposable runtime; `pnpm run build` produces dist; `ss` shows `127.0.0.1:3080`; `GET /` returns native SPA HTML (not empty 404); status ACTIVE; headless Path B `--print` still works; cleanup disposable DSH_HOME/port | `P8-T15/D01` | identity-confirmed `B01-Desktop-Linux-002` via standing Personal guest; pushed exact Git revision; do not stop P7-T05 daemon 465376/48681 |
| `P8-T15/D03` | P8-T15 | bilingual handbook (native panel vs Personal `/ui/`), docs-sync/consistency, Draft PR, claim ceiling `hypothesis` | `P8-T15/D01` | `check-handbook`, generator `--check`, `docs-sync-gate --staged`; no Gate/release/Profile/B01/EVAL/Agent-benefit promotion |
| `P8-T15/D04` | P8-T15 | Apply Cos dsh binding to running native web: Path B remaps request model to `agent://personal/dsh`; `POST /personal/dsh/runtime` `op=apply`; `cognitive dsh apply` restarts Cos web; SPA Apply button; Pi mismatch unchanged | `P8-T15/D02` | focused kernel-server + admin-cli + adapter node + clients policy tests; linux-002 apply+Path B; no local Windows GNU linking |
| `P8-T03/D01` | P8-T03 | select OpenAI Codex CLI as first non-Pi agent; bind fixture package identity and AKP registration independent of Pi/B09 evidence | completed P8-T02; P5-T05 done | failure-first Pi-evidence-transfer, public-listener, and authority-writer negatives; exact native Linux focused tests, Clippy, required CI |
| `P8-T03/D02` | P8-T03 | Codex fixture lifecycle activate/pause/stop/recover over registered declaration digests with management-channel isolation | `P8-T03/D01` | failure-first channel/digest/epoch negatives; exact native Linux and required CI |
| `P8-T03/D03` | P8-T03 | fixed-denominator independent qualification matrix and non-claim report (B09-mode generalization; no Pi evidence transfer) | `P8-T03/D02` | failure-first incomplete matrix and authority-shaped claim rejection; exact native Linux and required CI |
| `P8-T03/D04` | P8-T03 | final acceptance over D01-D03 non-Pi qualification evidence | `P8-T03/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile/Pi-transfer claim |
| `P9-T02/D01` | P9-T02 | extract embedded tests from `scheduler_authority.rs` into `scheduler_authority/tests.rs` without behavior change | completed Phase 8 structure baseline on main | exact native Linux focused scheduler_authority tests, Clippy, required CI |
| `P9-T02/D02` | P9-T02 | split `scheduler_authority` production helpers into cohesive submodules without behavior change | `P9-T02/D01` | exact native Linux focused tests, Clippy, required CI |
| `P9-T02/D03` | P9-T02 | extract/split oversized regions of `sqlite.rs` and `tool_executor.rs` with focused-test parity | `P9-T02/D02` | exact native Linux focused tests, Clippy, required CI |
| `P9-T02/D04` | P9-T02 | final acceptance over D01-D03 structure-debt evidence | `P9-T02/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P9-T03/D01` | P9-T03 | daemon startup opens one long-lived `SqliteAuthorityStore` and reuses it for recovery + private scheduler tick | completed P9-T02 | failure-first shared-store recovery/tick path; exact native Linux focused tests, Clippy, required CI |
| `P9-T03/D02` | P9-T03 | wire remaining personal request-path opens through the daemon-owned store (or prove fail-closed single-writer handoff) | `P9-T03/D01` | exact native Linux focused tests, Clippy, required CI |
| `P9-T03/D03` | P9-T03 | sink one Personal vertical composition seam out of `kernel-server` and record stage-timing non-claim comparison | `P9-T03/D02` | exact native Linux focused tests / stage timing observation, Clippy, required CI |
| `P9-T03/D04` | P9-T03 | final acceptance over D01-D03 store/composition evidence | `P9-T03/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no Gate/release/Profile claim |
| `P4-T06/D03` | P4-T06 | final acceptance mapping and non-claim evidence for the daemon-private correctness slice | `P4-T06/D02` same-task consumption trace | exact native Linux, Clippy, required Ubuntu/Windows CI, consistency, checkpoint, PR, lease, and branch closure; B08/Gate/release/Profile remain separate |
| `P7-T08/D01` | P7-T08 | ADR-0048 fixed-denominator B08 Memory/Skill non-claim Gate suite harness binds required observations and rejects incomplete/authority-shaped claims | completed P4-T01..T06 Memory/Skill authority paths; ADR-0040/0046/0047 Gate suite patterns | failure-first incomplete observation, wrong-gate, and authority-shaped claim negatives; tools Node tests, consistency, and required Ubuntu/Windows CI; B08 stays `not-run` until D02 disposition |
| `P7-T08/D02` | P7-T08 | execute ADR-0048 B08 matrix on exact native Linux + Clippy, produce non-claim report, and record B08 MVP disposition under §2.3 | `P7-T08/D01` harness and ADR-0048 | exact native Linux focused store/server tests + Clippy; non-claim report; required CI; B08 MVP disposition; no GMVP-LINUX/release/Profile claim |
| `P7-T08/D03` | P7-T08 | GMVP-LINUX composition binder: UCR-01 fixed-scenario assertion checklist + six-resource/release/doctor/backup/SecretStore/Pi pin evidence rollup as non-claim composition report | `P7-T08/D02` B08 MVP; completed B01/B02/B03/B04/B05/B09/B12 | failure-first incomplete composition and authority-shaped claim negatives; tools tests; required CI; GMVP remains `not-run` until D04 |
| `P7-T08/D04` | P7-T08 | formal GMVP-LINUX disposition over D01–D03 composition evidence plus final task acceptance, checkpoint, PR, lease, and branch closure | `P7-T08/D01-D03` | preregistered composition evidence; §2.3 ADR-0040-class disposition when complete; required CI; no Profile claim |
| `P7-T06/D01` | P7-T06 | freeze the Personal Linux RC claim set: digest-bound composition binder with failure-first incomplete/Profile/P6-enabled negatives; P6 explicit disabled-NO-GO; support-matrix honesty; RC-scope critical-risk inventory = 0 | completed P7-T08, P7-T04, P5-T05; existing B01–B12 MVP dispositions | tools Node tests for `personal-rc-gate`; consistency; required Ubuntu/Windows CI; evaluator does not set Gate/Profile state |
| `P7-T06/D02` | P7-T06 | publish bilingual install/init/provider/Pi/task/recovery/update/uninstall runbooks that point each step at existing handbook pages and refuse invented public verbs | `P7-T06/D01` claim freeze | `check-handbook` + generator `--check` + fingerprints; no production GitHub Release or Profile claim |
| `P7-T06/D03` | P7-T06 | bind declared-scope B01–B12, CI/SBOM/attestation/lifecycle, clean-VM suite (B01 successor `002` plus P7-T02/P7-T01 authority-path evidence), and D02 runbooks into one digest-bound RC declaration report | `P7-T06/D01-D02` | binder report with every evidence observation digested; required CI; no new B01 campaign guest mutation |
| `P7-T06/D04` | P7-T06 | final task acceptance over D01–D03, VERSION/plan/PROGRESS/trace closure, required CI, ready/merge/lease/branch/main | `P7-T06/D01-D03` | exact acceptance mapping; required CI; no Profile, Windows B01-W, production publication, or P6 enablement claim |

### Phase 5 - Agent sidecar 与 post-1.0 Tool 生态

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P5-T01 | Agent + sidecar package acquisition/install lifecycle | P0-T04, P0-T06, P1-T08 | adapter-neutral Agent/sidecar package pins；fixed official npm exact Pi；identity/version/SRI/digest/dependency/Node checks；signed acquisition lock；stage/commit/upgrade/rollback/uninstall；安装不授权 | done | 2026-08-10；`lease/personal/P5-T01/pi-acquisition` on `personal/P5-T01-pi-acquisition`; closure checkpoint `20260810-personal-p5-t01-pi-acquisition-closure.md`; D01-D03 complete with exact native Linux focused validation and required Ubuntu/Windows CI. No AgentInstance/sidecar/process supervision, Effect, Task completion, B09, release, or Profile claim. |
| P5-T02 | Sidecar contract、registration、instance/process identity 与 Pi foundation | P5-T01, P2-T03, P2-T06 | versioned sidecar protocol/adapter/instance pin；AgentInstallation/Instance/Execution、SidecarSession、PiSession、process、Task 分离；health/activate/pause/resume/stop/recover 与 epoch fencing | done | 2026-08-10；`lease/personal/P5-T02/sidecar-foundation` on `personal/P5-T02-sidecar-foundation`; closure checkpoint `20260810-personal-p5-t02-sidecar-foundation-closure.md`; D01-D03 complete with exact native Linux focused validation and required Ubuntu/Windows CI at `58ff0a723a8eae0f7fc89d9a99e9fdd55406aa92`. No OS process spawn, AgentExecution/PiSession resource, B09, release, or Profile claim. |
| P5-T03 | Post-1.0 MCP Tool adapter qualification | P2-T05, P2-T08 | MCP 不成为 authority；protocol/manifest drift、timeout、direct-bypass 测试；不阻塞 1.0 | done | 2026-08-11；`lease/personal/P5-T03/mcp-tool-adapter` / PR #195. D01–D04 closed. Linux `mcp_tool_adapter` 4/4 + Clippy at `a83bdb8`; required CI `31482773002` on `4c06161`. Closure: `docs/checkpoints/20260811-personal-p5-t03-mcp-tool-adapter-closure.md`. No B10/Gate/release/Profile claim. |
| P5-T04 | Post-1.0 dynamic Tool ecosystem 与 B10 | P5-T03 | dynamic discovery/package/exposure/enable/disable/quarantine/reconcile；B10 独立 campaign；不阻塞 1.0 | done | 2026-08-11；`lease/personal/P5-T04/dynamic-tool-ecosystem` / PR #196. D01–D04 closed under ADR-0050. Linux `dynamic_tool_ecosystem` 4/4 + Clippy at `b49d274`; required CI `31486478177` on `992dfe3`; B10 MVP `pass`. Closure: `docs/checkpoints/20260811-personal-p5-t04-dynamic-tool-closure.md`. No GMVP-LINUX/release/Profile claim. |
| P5-T05 | B09 managed Pi + sidecar qualification | P5-T02 | 只负责 Pi + sidecar acquisition/install/registration/instance/process/lifecycle/recovery qualification；Pi 证据不资格化其他 adapter；任务完成与 B10 解耦 | done | 2026-08-11；`lease/personal/P5-T05/b09-managed-pi` on `personal/P5-T05-b09-managed-pi` / PR #183. D01–D04 closed under ADR-0047. Fixed matrix + non-claim report at `548f138`; CI `31423464703`; owner `affirm B09` → B09 MVP `pass`. Closure: `docs/checkpoints/20260811-personal-p5-t05-b09-managed-pi-closure.md`. No GMVP-LINUX/B08/release/Profile/non-Pi claim. |

| Slice | Parent | Delivery | Prerequisites | Verification boundary |
|---|---|---|---|---|
| `P5-T01/D01` | P5-T01 | authenticated official-Pi acquisition transaction commits one daemon-private immutable acquisition lock only after fixed official package identity, origin, npm SRI, independently computed tarball digest, dependency lock, Node compatibility, adapter pin, and signed-lock reference validation | P0-T04 persistence; P0-T06 Pi pin; P1-T08 staging/rollback patterns | failure-first wrong identity/origin/SRI/digest/dependency/Node/signature and no-commit negatives; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no activation, capability, Effect, Task completion, B09, release, or Profile claim |
| `P5-T01/D02` | P5-T01 | immutable versioned Pi installation root and activation-pointer transaction consume only D01 committed acquisition locks; upgrade preserves the prior complete binding and rollback never emits an early success receipt | `P5-T01/D01` immutable official acquisition lock and P1-T08 lifecycle lease/staging patterns | failure-first uncommitted-lock, incompatible/failed health, pointer/publish fault, competing-upgrade, and rollback-incomplete negatives; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no AgentInstance, sidecar session, process supervision, Effect, Task completion, B09, release, or Profile claim |
| `P5-T01/D03` | P5-T01 | daemon-private uninstall/quarantine removes only the active Pi installation pointer and versioned package binding after a stopped/absent lifecycle precondition, while preserving acquisition evidence, user data, secrets, and unrelated installations | `P5-T01/D02` versioned activation pointer; existing daemon lifecycle and secret-isolation boundaries | failure-first active-instance, missing-pointer, wrong-root, data/secret-deletion, and partial-uninstall negatives; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no AgentInstance, sidecar session, process supervision, Effect, Task completion, B09, release, or Profile claim |
| `P5-T02/D01` | P5-T02 | daemon-private Agent registration from an active official Pi installation root persists an inactive `registered` AgentInstance identity bound to exact package/adapter/protocol/policy digests without creating a SidecarSession, process, capability, Effect, or Task completion | P5-T01 active installation root; P2-T03/P2-T06 fencing concepts | failure-first inactive-root, adapter-digest mismatch, duplicate current-root registration, and zero-capability negatives; management-session caller; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no activate/health/pause/resume/stop, B09, release, or Profile claim |
| `P5-T02/D02` | P5-T02 | activate a `registered` AgentInstance into exactly one current SidecarSession at a bumped fencing epoch, preserving register ≠ activate and rejecting protocol-digest drift without spawning a process or granting capability | `P5-T02/D01` registered instance | failure-first unregistered-root, protocol-digest mismatch, duplicate activation, and zero-capability negatives; management-session caller; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no pause/resume/stop/recover, process supervision, B09, release, or Profile claim |
| `P5-T02/D03` | P5-T02 | pause fences the current SidecarSession and clears the current pointer; resume creates a new epoch-bound session; stop quiesces active/paused instances; health returns redacted non-mutating facts with process_bound=false; recover restarts paused/stopped instances at a new epoch without capability, Effect, Task completion, or OS process adoption | `P5-T02/D02` active SidecarSession | failure-first stale-epoch pause/resume/stop/recover and zero-capability negatives; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; no process supervision, B09, release, or Profile claim |
| `P5-T03/D01` | P5-T03 | bind fixture MCP server package/manifest identity and transport-only initialize/capability negotiation that never grants CognitiveOS capability, Intent/Effect authority, or Task completion | completed P2-T05 native Tool registry; P2-T08 done | failure-first missing-identity, protocol-pin mismatch, and authority-writer/capability-grant negatives; exact native Linux focused module tests, Clippy, required CI; no B10/Gate/release/Profile claim |
| `P5-T03/D02` | P5-T03 | protocol/manifest digest drift and initialize timeout fail closed; MCP tool-list changes produce disabled candidates requiring re-qualification (no auto-enable) | `P5-T03/D01` | failure-first drift, timeout, and auto-enable negatives; exact native Linux focused tests, Clippy, required CI |
| `P5-T03/D03` | P5-T03 | direct endpoint bypass and unregistered MCP sandbox mediation negatives plus fixed-denominator non-claim qualification report | `P5-T03/D02` | failure-first bypass and authority-shaped claim rejection; exact native Linux focused tests, Clippy, required CI; B10 remains separate |
| `P5-T03/D04` | P5-T03 | final acceptance mapping over D01–D03 MCP adapter evidence | `P5-T03/D01-D03` | exact acceptance mapping, required CI, consistency, checkpoint, PR, lease, and branch closure; no B10/Gate/release/Profile/GMVP claim |
| `P5-T04/D01` | P5-T04 | bind fixture dynamic Tool package/manifest identity and discovery that produces disabled candidates requiring re-qualification (no auto-enable); authority-writer rejected | completed P5-T03 MCP adapter boundary | failure-first missing-identity, schema mismatch, auto-enable, and authority-writer negatives; exact native Linux focused module tests, Clippy, required CI; no B10/Gate/release/Profile claim |
| `P5-T04/D02` | P5-T04 | enable/disable/quarantine lifecycle plus TaskContract-scoped exposure of only enabled+healthy tools | `P5-T04/D01` | failure-first empty requalification, quarantine-blocks-enable, and out-of-contract exposure negatives; exact native Linux focused tests, Clippy, required CI |
| `P5-T04/D03` | P5-T04 | original-key unknown-outcome reconcile, composite child Intent/Effect retention, pure-read version-bound cache telemetry, and sandbox direct-bypass reject | `P5-T04/D02` | failure-first blind-retry, hidden-unknown-outcome, mutating-cache, and bypass negatives; exact native Linux focused tests, Clippy, required CI |
| `P5-T04/D04` | P5-T04 | ADR-0050 fixed-denominator B10 MVP matrix, tools non-claim harness, disposition, and final acceptance | `P5-T04/D01-D03` | exact native Linux matrix + Clippy; non-claim report; required CI; §2.3 ADR-0050 disposition; checkpoint, PR, lease, and branch closure; no GMVP-LINUX/release/Profile claim |
| `P5-T05/D01` | P5-T05 | daemon-owned process-bound SidecarSession: activate registers a fenced process-attempt identity; health reports `process_bound=true` only with that durable binding; pause/stop clear the binding without arbitrary PID attach, capability grant, Effect, or Task completion | completed P5-T02 SidecarSession foundation and P2-T06 supervisor boundary | failure-first unbound health, stale-epoch clear, and zero-capability negatives; exact native Linux focused validation, Clippy, and required Ubuntu/Windows CI; B09 remains `not-run` |
| `P5-T05/D02` | P5-T05 | upgrade/rollback and uninstall/quarantine paths preserve process-bound fencing and fail closed on pin/digest drift before activation | `P5-T05/D01` | exact native Linux focused validation and required CI; no B09 claim |
| `P5-T05/D03` | P5-T05 | recover/orphan and identity-separation negatives for AgentInstance/SidecarSession/process/Task; install ≠ permission | `P5-T05/D02` | exact native Linux focused validation and required CI; Gate campaign remains `not-run` until D04 |
| `P5-T05/D04` | P5-T05 | formal B09 campaign preregistration and supported execution with redacted evidence and disposition (owner affirm or §2.3 ADR-0047-class self-disposition after complete evidence) | `P5-T05/D01-D03` | preregistered campaign on supported env; disposition recorded; no non-Pi adapter qualification |
| `P2-T08/D01` | P2-T08 | preregistered Runtime Spine Gate suite harness and non-claim evaluator for the fixed B02/B04/B05/B12 observation set (projection/channel isolation, confirmation accounting, fault/OUTCOME_UNKNOWN/no-blind-retry, false-completion, ADR-0018 expiry) without allowing the runner to set Gate state | completed P2-T01..T07 Runtime Spine boundaries and UCR/B03 non-claim report patterns | failure-first incomplete observation, authority-shaped claim, wrong gate set, and confirmation-ceiling negatives; tools Node tests, consistency, and required Ubuntu/Windows CI; B02/B04/B05/B12 stay `not-run` |
| `P2-T08/D02` | P2-T08 | remove or replace the ADR-0018 local-native Provider secret development exception so only the daemon-owned Provider proxy path remains, and record residual-exception failure in the suite | `P2-T08/D01` suite harness; P1-T07 daemon Provider proxy | focused adapter/proxy negatives, exact native Linux or supported CI validation, required Ubuntu/Windows CI; no Gate pass claim |
| `P2-T08/D03` | P2-T08 | authority-path focused Runtime Spine negatives covering shell/daemon close, OUTCOME_UNKNOWN original-key reconcile, no-blind-retry, and false-completion self-check floor | `P2-T08/D01` and completed P2-T06/P2-T07 executor/verifier boundaries | exact native Linux focused validation, Clippy where applicable, required Ubuntu/Windows CI; Gate campaigns remain `not-run` until D04 |
| `P2-T08/D04` | P2-T08 | formal B02/B04/B05/B12 campaign preregistration and supported execution with redacted evidence, independent review, and explicit Gate disposition | `P2-T08/D01-D03` complete suite and ADR-0018 closure | preregistered campaign on a supported environment; complete denominator/threshold accounting; required CI for suite artifacts; owner interaction only if Provider/environment prerequisites are unavailable |
| `P9-T01/D01` | P9-T01 | repeat the P7-T04 governed-path stage observation on an immutable native revision, summarize stage tails, and apply a deterministic migration decision rule; preserve single-writer SQLite and keep the result hypothesis/non-claim | P7-T04/D02 collector and P8-T01 design baseline | exact native Linux observation, raw redacted report, static/consistency checks, and required CI; if async migration is selected, D02 must define a bounded stream-only migration slice; otherwise D01 closes the task with a conservative outcome |
| `P9-T05/D01` | P9-T05 | extract the daemon readiness wait that every Personal integration test copies into one shared `personal/apps/kernel-server/tests/common` implementation carrying today's exact fixed 2 s budget, so the confirmed fragility becomes directly assertable rather than a per-file accident | none; the existing `p1_t05_personal_readiness` fixture and its siblings | a failure-first case proving a healthy start slower than two seconds is failed by today's budget, plus a fail-closed case proving a readiness that never arrives still fails within its own budget and names the missing subject; no product code, contract, negative, or assertion is touched |
| `P9-T05/D02` | P9-T05 | replace the fixed budget with a bounded readiness poll — short interval, generous but finite ceiling, informative timeout diagnostic, and early failure when the spawned daemon has exited — and move every sibling wait in `personal/apps/kernel-server/tests` onto it, including the unbounded `loop` connect waits | `P9-T05/D01` | the D01 slow-start case passes and the fail-closed case still fails closed; a new negative proves an exited daemon is reported with its exit status instead of consuming the ceiling; required Ubuntu/Windows CI; every existing assertion and test semantic is unchanged and no product behavior changes |
| `P9-T05/D03` | P9-T05 | complete the P9-T05 acceptance mapping and closure | `P9-T05/D01-D02` | acceptance mapping, required CI green on both platforms at the merge revision, consistency and docs-sync gates, closure record, and PR/lease/branch/main reconciliation; test-harness reliability is not a Gate, release, Profile, or product claim |
| `P7-T07/D01` | P7-T07 | Windows Credential Manager production secret backend behind the frozen SecretStore port: Win32 credential calls run inside a fixed audited PowerShell helper started from the absolute system path, secret material transits only helper stdin/stdout, generic credentials persist local-only, the 2560-byte blob ceiling is enforced before write, and selection stays fail-closed with no plaintext fallback | P1-T02 frozen SecretStore/Provider-config boundary (done); ADR-0052 §1 | failure-first non-Windows fail-closed, real Credential Manager roundtrip/rotate/delete, oversized-rejection-with-nothing-stored, foreign/absent-ref, redaction, and script-embedding negatives; validation on `CI-WINDOWS-MSVC-01` plus `CI-UBUNTU-01`; no B01-W, install parity, Gate, release, or Profile claim |
| `P7-T07/D02` | P7-T07 | inspectable Windows bootstrap installer template and per-user least-privilege scheduled-task template mirroring the Linux install.sh/user-service contract: unrendered templates fail closed before any network or filesystem action, downloads are HTTPS-only and bounded through the absolute System32 curl.exe, and the digest-verified downloaded installer is the only delegation target | `P7-T07/D01`; P7-T01/P7-T02 release and lifecycle pipeline (done); ADR-0052 §2 | static required/forbidden-fragment checks on every platform plus behavioral unrendered, version-mismatch, malformed-digest, non-HTTPS, and extra-argument rejections and a least-privilege task-XML parse on `CI-WINDOWS-MSVC-01`; the download path itself stays inspectable-only until B01-W executes |
| `P7-T07/D03` | P7-T07 | author the dedicated B01-W gate: campaign policy, preregistration, environment requirements registration, and the operator procedure for a clean-Windows-VM install to first dialogue | `P7-T07/D02`; ADR-0039 B01 successor policy pattern | documentation/preregistration and consistency checks only; authoring creates no execution, parity, Gate, release, or Profile claim; execution additionally requires a provisioned clean Windows VM campaign environment |
| `P7-T07/D04` | P7-T07 | complete P7-T07 acceptance mapping and closure, or an honest blocked record for the owner-gated B01-W execution prerequisite | `P7-T07/D01-D03` | acceptance mapping, required CI, consistency, closure checkpoint, and PR/lease/branch closure or explicit blocked accounting; Windows install parity is never claimed without an executed B01-W |
| `P7-T05/D01` | P7-T05 | readiness, technical stack ADR, route/contract inventory, session/serving/CORS/CSRF decision, and threat model; no invented public contract | completed P2-T08 and P7-T03; existing daemon management/Task routes | ADR-0053 accepted; inventory negatives covering invented lifecycle, missing daemon route, Task-channel secrets, Web storage, and browser-direct targets; `clients/**` and `apps/cognitiveos-console` SPA forbidden; no Gate/release/Profile claim |
| `P7-T05/D02` | P7-T05 | SPA shell, session bootstrap, Home, Agent inventory/detail in `cognitiveos-clients/pc/web/` | `P7-T05/D01`; approved checkout `D:\cognitiveos-clients` | management/task channel isolation; redacted readiness; Agent identity separation |
| `P7-T05/D03` | P7-T05 | Provider account forms, SecretStore handoff, connectivity/model probes | `P7-T05/D02` | secret-leak negatives; endpoint policy errors; probe result semantics |
| `P7-T05/D04` | P7-T05 | Agent Provider binding and revision-aware confirmation | `P7-T05/D03` | fixed binding, CAS/idempotency, no-fallback negatives |
| `P7-T05/D05` | P7-T05 | Task/Run/Process/Effect/Evidence/Event views and typed lifecycle controls | `P7-T05/D04` | watch reconnect/gap; typed mutation preview; completion non-claim; unavailable/not-run for missing HTTP |
| `P7-T05/D06` | P7-T05 | Accessibility, CSP, security hardening, responsive layout, performance measurement | `P7-T05/D05` | keyboard/a11y, CSP, bounded latency, dependency review |
| `P7-T05/D07` | P7-T05 | Integration, supported validation, docs sync, and final acceptance | `P7-T05/D06` | exact revision validation; focused negatives; task docs and closure; no Gate/release/Profile claim |
| `P7-T05/D08` | P7-T05 | Live control-panel completion: correct Provider kinds/trust/key SecretStore handoff, binding CAS, and Task preview/admit/watch so the owner can enter an LLM key, bind an Agent, and start/observe a Task through the SPA | `P7-T05/D07`; P8-T13 typed Provider Control Plane; existing Task HTTP | focused negatives for kinds, trust confirmation, redaction, stale expected_revision, no fallback; SPA unit/DOM tests; no invented lifecycle HTTP; no Gate/release/Profile claim |
| `P7-T05/D09` | P7-T05 | Exact-revision `DEV-LINUX-NATIVE-01` UI driver: daemon-served `/ui/`, session, live key via approved SecretStore, probe, bind, admit/watch, redaction, cleanup | `P7-T05/D08` | Linux native evidence; live key never in Git/chat/argv/logs/evidence; claim ceiling hypothesis; no Gate/release/Profile claim |
| `P7-T05/D10` | P7-T05 | Refine the shipped Provider control WebUI in `agentkernel/cognitiveos-clients/pc/web/` into one coherent Apple-inspired operator composition: purposeful brand and navigation, restrained neutral depth, clear Provider list/detail/status/action hierarchy, and explicit loading/empty/error states without changing daemon calls or authority behavior | `P7-T05/D09`; owner visual direction 2026-08-24 | focused DOM/accessibility regression for shell identity and navigation; existing SPA tests; deterministic production build; desktop and narrow-viewport review; no new dependency, route, authority state, Gate, release, Profile, B01, EVAL, or Agent-benefit claim |
| `P7-T05/D11` | P7-T05 | Control Plane redesign W1 Foundation + W2 Providers per `clients/docs/design/01–41`: approved design tokens (system-following light/dark), the seven-category state system with verbatim task/effect vocabularies, the API normalization + route-whitelist + 200-stub-detection layer, the zero-dependency projection store, the app shell (status strip, seven-space navigation, session gate, designed 404), the component foundation, six honest placeholder spaces, then the Providers space end-to-end (five-section detail, key handoff, CAS binding folded from the retired Bindings page) | `P7-T05/D10` (merged via clients PR #5/#6); `clients/docs/design/39` waves + `clients/docs/design/40` first slice; frozen route inventory | all existing SPA tests green plus new focused tests for state mapping, normalization, stub detection, route whitelist, and the projection store; deterministic production build pass; keyboard/focus/ARIA basics; desktop and narrow review in both themes; no new dependency, no new route, no backend change, no invented capability, no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D12` | P7-T05 | Control Plane W3 Home attention surface per `clients/docs/design/13`/`39`: replace the Home placeholder with an attention surface — not a dashboard — composed of exactly four real regions (readiness, needs attention, current work, recent evidence), where critical changes render as the top group *inside* the attention queue rather than as a separate dashboard card, and waiting-on-owner items fold into the same attention queue instead of a parallel surface | `P7-T05/D11` (clients `001595b`, Draft PR [cognitiveos-clients#7](https://github.com/agentkernel/cognitiveos-clients/pull/7)); the D11 state system, normalization/route-whitelist/stub-detection layer, and projection store; frozen route inventory | Home consumes only daemon APIs already verified to exist in the frozen route inventory; the envelope-only task inventory carries a permanent BD-3 honesty line wherever task counts or work items are shown; existing SPA tests stay green plus new focused tests for region composition, critical-change grouping inside the attention queue, waiting-on-owner folding, and the BD-3 honesty line; deterministic production build pass; keyboard/focus/ARIA basics and a rendered light+dark desktop/narrow review; no new dependency, no new backend, no new API, no fake or non-functional control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D13` | P7-T05 | Control Plane W4 Work inventory + governed task creation per `clients/docs/design/14`/`39`: a Tier-1 Work inventory at `#/work` whose only sources are the envelope-only task list, this session's observed refs, and bounded per-ref evidence, plus the complete governed creation flow at `#/work/new` running the daemon's real chain objective → `/task/intent.record` → `/task/intent.interpret` → interpretation review → `/task/preview` → digest-bound review → `/task/admit` → admission receipt | `P7-T05/D12` (clients `2944945`, Draft PR [cognitiveos-clients#7](https://github.com/agentkernel/cognitiveos-clients/pull/7)); the D11 state system, normalization/route-whitelist/stub-detection layer and projection store; the existing `taskDraft.ts` chain helper; frozen route inventory | the browser never mints authority — every step is a daemon call through `taskDraft.ts`; ambiguities and information gaps are first-class review content and `clarification_required` is a normal branch, not an error; an unresolved material ambiguity blocks preview and admit; admit reuses the exact preview digest, `interpretation_id` and current principal; 403 states the principal/session cause and 409 never auto-retries but regenerates a preview for explicit reconfirmation; inventory merges and de-duplicates by `task_ref` with a stable sort, preserves selection across refresh, defaults to this session only, and carries the resident BD-3 envelope-only line; unknown lifecycle/agent/objective/age stay unknown; unsupported cancel/pause/retry render as class-C unavailable copy and never as clickable controls; the session's intent → interpretation → ephemeral preview → admission chain is retained in the in-memory projection store so W5 can compose it, with the preview labelled ephemeral and never persisted; the legacy `#/tasks` page stays reachable until W5 migrates its diagnostics; existing SPA tests stay green plus focused tests for the exact request order, the negative branches, selection/sort stability, the honesty footer and route-whitelist/stub/error normalization; deterministic production build; keyboard/focus/ARIA basics; no new dependency, no new backend, no new API, no invented task list/detail/control endpoint, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D14` | P7-T05 | Control Plane W5 Work detail + composed Run timeline per `clients/docs/design/15`/`39`: a per-task detail view at `#/work/:taskRef` composed of six continuous sections (Overview, Run, Effects, Evidence, Intent & Contract, Context) with no tabs and no accordion, where the Run timeline renders the authority lane (`GET /task/evidence` lifecycle transitions) and the bounded observation lane (`GET /task/observation`) as two visually separate lanes that are never merged | `P7-T05/D13` (clients `7a7c159`, Draft PR [cognitiveos-clients#7](https://github.com/agentkernel/cognitiveos-clients/pull/7)); the D11 state system, normalization/route-whitelist/stub-detection layer and projection store; the D13 Work inventory and its session intent/interpretation/preview/admission chain; frozen route inventory | an observation is never rendered as a state transition and detaching a watch never means cancelling a Task or stopping an Agent; an interval with no recorded fact reads `no recorded facts` and a bounded transition/effect set is marked bounded; no real-time streaming is claimed before W11 and an unattached watch reads unknown/not attached rather than live; `OUTCOME_UNKNOWN` and `VERIFY_FAILED` effects surface first and an empty effect set means no recorded external mutation, never success; `completed` is used only where a current acceptance record exists, a passing verification alone is never Task completion, a non-current verification is labelled, and evidence 404 reads `No terminal evidence recorded`; the ephemeral preview is stated as `Previews are ephemeral by design; the admitted contract is the durable record.` and missing historical fields read unavailable; Context shows real `GET /task/resource/v1/consumption` pins and names missing/truncated/stale/conflicting ones; Loop/WIA/Context detail with no HTTP exposure renders a named unavailable; an unknown `task_ref` renders a designed object-404 without fabricating detail; section deep links work and returning to Work preserves inventory selection and filter; existing SPA tests stay green plus focused tests for lane separation, ordering/gaps, effect failure priority, truncation, evidence 404, verification-pass-is-not-completion, current-acceptance-only completion, ephemeral-preview honesty, missing historical chain, context unavailability, unsupported controls, deep link and return selection, denied/disconnected/stub/error, route whitelist, keyboard/focus/ARIA and the absence of any call to a non-existent API; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D15` | P7-T05 | Control Plane W6 Agents dossier per `clients/docs/design/16`/`39`: replace the Agents placeholder with an inventory plus per-agent dossier (identity cards via kept `identities.ts`, binding section, capabilities, class-C lifecycle block, dsh runtime observation) over verified existing daemon reads only | `P7-T05/D14` (W5 rendered review pass at `main@b77a0243`; [D14 report](../checkpoints/20260825-personal-p7-t05-d14-w5-rendered-review-report.md)); D11 state system and route whitelist; frozen route inventory | no fake lifecycle control — BD-2 remains named unavailable; identity, binding and dsh facts are composed from existing reads and never invented; existing SPA tests stay green plus focused tests for dossier honesty, class-C unavailable copy, route whitelist and keyboard/focus/ARIA; deterministic production build; rendered review; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D16` | P7-T05 | Control Plane W7 Resources family hub per `clients/docs/design/18`/`39`: replace the Resources placeholder with a four-row family index (Memory, Skills, Tools, Context) whose counts come from real `list?family=` reads, Context points at Work, and envelope limits are labeled | `P7-T05/D15` (W6 rendered review pass at `633215d9`; [D15 report](../checkpoints/20260825-personal-p7-t05-d15-w6-rendered-review-report.md)); D11 state system and route whitelist | no card wall; Context is not a standalone browser; empty/denied/stub families are distinct; existing SPA tests stay green plus focused hub tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D17` | P7-T05 | Control Plane W7 Memory family page per `clients/docs/design/18`/`39`: replace hub-only Memory browse with an envelope-honest master+inspector over `list?family=memory` and inspect, labeled limit 64, BD-6 content search named unavailable, Remember/Forget as class-A on this page (not the hub) | `P7-T05/D16` (W7 hub rendered review pass at `6c33c94f`; [D16 report](../checkpoints/20260825-personal-p7-t05-d16-w7-rendered-review-report.md)); D11 state system and route whitelist | tombstones are not invented from the non-tombstoned list; unknown fields stay unknown; existing SPA tests stay green plus focused Memory tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D18` | P7-T05 | Control Plane W7 Skills family page per `clients/docs/design/18`/`39`: envelope-honest master+inspector over `list?family=skill` (bindings, not packages) and binding explain, with the standing content≠permission annotation and Import/Bind/Revoke as class-A on this page (not the hub) | `P7-T05/D17` (W7 Memory rendered review pass at `32119056`; [D17 report](../checkpoints/20260825-personal-p7-t05-d17-w7-memory-rendered-review-report.md)); D11 state system and route whitelist | list is skill bindings not packages; existing SPA tests stay green plus focused Skills tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D19` | P7-T05 | Control Plane W7 Tools family page per `clients/docs/design/18`/`39`: table (not cards) over `GET /management/resource/v1/tool` with operation, risk, lifecycle and execution-readiness, the standing registered/enabled ≠ production-wired caveat, and Enable/Disable/Quarantine/Revoke as class-A on this page (not the hub) | `P7-T05/D18` (W7 Skills rendered review pass at `27e454a3`; [D18 report](../checkpoints/20260825-personal-p7-t05-d18-w7-skills-rendered-review-report.md)); D11 state system and route whitelist | quarantined→enabled refused and revoke terminal are stated before confirm; existing SPA tests stay green plus focused Tools tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D20` | P7-T05 | Control Plane W8 Activity per `clients/docs/design/19`/`39`: replace the Activity placeholder with a time-ordered evidence stream over real sources, seven labeled kinds, a persistent BD-5 coverage banner, and alert ack inline — never a unified authority log | `P7-T05/D19` (W7 Tools rendered review pass at `f007f352`; [D19 report](../checkpoints/20260826-personal-p7-t05-d19-w7-tools-rendered-review-report.md)); D11 state system and route whitelist | no fabricated completeness; empty/partial/bounded are distinct; existing SPA tests stay green plus focused Activity tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D21` | P7-T05 | Control Plane W9 System per `clients/docs/design/20`/`39`: replace the System placeholder with readiness detail, doctor (honest placeholder sub-sections), stewardship backup/restore over existing routes, session facts, and about/diagnostics — no Gate/release/Profile claim in the copy | `P7-T05/D20` (W8 Activity rendered review pass at `acc5814e`; [D20 report](../checkpoints/20260826-personal-p7-t05-d20-w8-activity-rendered-review-report.md)); D11 state system and route whitelist | restore stays preview-first with named 409 classes; doctor sub-sections that have no HTTP stay named unavailable; existing SPA tests stay green plus focused System tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D22` | P7-T05 | Control Plane W10 command layer (⌘K) per `clients/docs/design/21`/`39`: palette over loaded projections — destinations, known-object search, class-A landings, class-B inline only; class-C/D absent; BD-6 honesty that the index is partial | `P7-T05/D21` (W9 System rendered review pass at `32ebbbe9`; [D21 report](../checkpoints/20260826-personal-p7-t05-d21-w9-system-rendered-review-report.md)); D11 state system and route whitelist | palette is not a mutation console; no server search; no secret-bearing input; existing SPA tests stay green plus focused command tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D23` | P7-T05 | Control Plane W11 watch streaming + refresh policy per `clients/docs/design/15`/`39`: wire the kept `watch`/`watchSse` controller to real `EventSource` on `GET /task/watch` from Work detail Run, with stale/gap/detach semantics and no fabricated completion | `P7-T05/D22` (W10 command palette rendered review pass at `5f4185fd`; [D22 report](../checkpoints/20260826-personal-p7-t05-d22-w10-command-rendered-review-report.md)); D11 state system and route whitelist; existing `/task/watch` | an observation is never a state transition; detach never cancels a Task or stops an Agent; unattached watch reads unknown/not attached rather than live; existing SPA tests stay green plus focused watch tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D24` | P7-T05 | Control Plane W12 accessibility / QA gate per `clients/docs/design/25`/`39`: keyboard-complete pass, measured contrast on final hues (OQ-4), reduced-motion/transparency, and the compact scenario suite re-run against the built product | `P7-T05/D23` (W11 watch streaming rendered review pass at `db599bfd`; [D23 report](../checkpoints/20260826-personal-p7-t05-d23-w11-watch-rendered-review-report.md)); D11–D23 surfaces | no new backend or API; existing SPA tests stay green; deterministic production build; contrast/keyboard/reduced-motion findings are fixed or honestly retained; no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D25` | P7-T05 | Retire the legacy `styles.css` / `#/tasks` governed-task diagnostics page now that W5–W12 surfaces exist, so the Control Plane CSS and route map are one composition | `P7-T05/D24` (W12 a11y/QA rendered review pass at `b30314f3`; [D24 report](../checkpoints/20260826-personal-p7-t05-d24-w12-a11y-rendered-review-report.md)); existing `#/work` and `#/work/new` | no invented task HTTP; `#/tasks` either redirects or is removed with a designed 404; `styles.css` no longer reaches Control Plane markup; existing SPA tests stay green plus focused retirement tests; deterministic production build; no new dependency, no new backend, no new API, no fake control, and no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P7-T05/D26` | P7-T05 | Complete P7-T05 acceptance mapping, required CI, Draft PR ready/merge, and lease/branch/main reconciliation after D01–D25 | `P7-T05/D25` (legacy `styles.css` / `#/tasks` retirement rendered review pass at `872074bf`; [D25 report](../checkpoints/20260826-personal-p7-t05-d25-legacy-tasks-retirement-rendered-review-report.md)) | required Ubuntu/Windows CI green at the merge head; HTTP cancel remains `not-run`; no Gate/release/Profile/B01/EVAL or Agent-benefit claim |
| `P2-T09/D01` | P2-T09 | separate registered/enabled from execution-ready in the daemon-private Tool projection, so a family with no assembled executor can never be presented as executable | completed P2-T05 registry and P2-T06 executor boundary | failure-first negatives proving an unassembled family reports `execution_ready=false`, that readiness cannot be forged from registry availability, and that descriptor digests are unchanged; exact-revision Linux focused tests, Clippy, and required CI; no new Tool family and no Gate/release/Profile claim |
| `P2-T10/D01` | P2-T10 | assemble a bounded `WorkspaceSearch` executor: a daemon-staged, digest-bound query resolved under the canonicalized approved root with fixed file/match/byte ceilings, redaction before retention, original-key idempotency, sink-side fencing and a queryable outcome | completed P2-T05 registry validators and the P2-T06 `WorkspaceRead` Effect boundary | failure-first negatives proving an unstaged or digest-drifted dispatch never reads the filesystem, a symlinked or traversing hit that resolves outside the approved root is refused, ceilings truncate rather than stream unbounded output, a stale fencing epoch is refused before I/O, and a durable `EXECUTING`-before-I/O plus original-key OUTCOME_UNKNOWN reconcile over real SQLite; exact-revision native Linux focused tests, Clippy, and required CI; no Task advancement and no Gate/release/Profile claim |
| `P2-T10/D02` | P2-T10 | assemble the `WorkspaceWrite` and `WorkspacePatch` mutation executor behind one expected-preimage/atomic-publish boundary: staging binds the expected preimage digest and the intended postimage digest, publication is same-directory temp write plus durable rename, and `query_outcome` reconciles by re-reading the target so the workspace is a genuinely queryable sink | `P2-T10/D01`; the P2-T06 durable Intent/Effect and reconciliation boundary | failure-first negatives for preimage mismatch (no write, no partial file), atomic publish (a reader never observes a partial target), interrupted publication (target unchanged and no staging residue), symlink and post-resolution path race, duplicate dispatch under one original key writing exactly once, and OUTCOME_UNKNOWN reconciled from the durable target state rather than from process memory; exact-revision native Linux focused tests, Clippy, and required CI; no existing negative or contract is weakened |
| `P2-T10/D03` | P2-T10 | assemble the `HttpFetchReadOnly` executor over the repository's single audited Rustls boundary: GET/HEAD only, credential-free HTTPS, no caller headers, no redirects, registered-origin allowlist, bounded timeout and bounded response retained under the original key | `P2-T10/D01`; the existing `cognitive-provider-transport` Rustls boundary and its hermetic loopback fixture pattern | failure-first negatives proving plaintext, credential-bearing, unregistered-origin and non-GET/HEAD targets never reach the network, that a redirect is returned rather than followed, that an oversize body is refused rather than truncated silently, and a real hermetic loopback HTTPS exchange with exactly one observed request per original key; exact-revision native Linux focused tests, Clippy, and required CI; no ambient credential ever enters the request |
| `P2-T10/D04` | P2-T10 | extend `ASSEMBLED_EXECUTOR_FAMILIES` to the newly assembled families and complete the task acceptance mapping so the daemon-private projection reports execution readiness that matches what this binary can actually dispatch | `P2-T10/D01-D03` | a failure-first negative proving readiness still derives from the assembled set and still changes no descriptor digest, the full tool_registry and tool_executor suites, exact-revision native Linux, Clippy, required CI, handbook sync of the three source-mapped page pairs, and closure; readiness parity is not a claim that an Agent can reach these tools — the scheduler-bootstrap and verifier gaps remain open and must stay recorded as such |
| `P2-T12/D01` | P2-T12 | make an admitted Task schedulable by creating all three missing prerequisites in the admission transaction — the scheduler work row, the contract-named Loop governed object in `START`, and the contract-named Budget row — and repair an admitted Task missing any of them at startup recovery instead of letting it stall | completed P2-T01 `TaskApplicationService`, the durable P2-T03 scheduler repository, `ProtocolStore::create_budget` and `LoopDriver::start_loop` (both defined, neither production-called) | failure-first negatives proving an unadmitted Task has none of the three, a duplicate admit creates no second row, a Task missing only the Loop or only the Budget is repaired rather than stalling, and the row is `Runnable` at the current contract epoch and no other; exact-revision native Linux focused tests, Clippy, and required CI; a schedulable Task is not an executed or completed Task |
| `P2-T12/D02` | P2-T12 | break the tick's circular precondition so a first execution is reachable: a task whose Intent set is empty must route into the pre-admission candidate branch instead of aborting the pass, and one bad row must not abort the whole tick | `P2-T12/D01`; `resolve_scheduler_work_for_task` and the pre-admission branch in `dispatch.rs` | failure-first negatives proving a freshly admitted Task with zero Intents reaches candidate admission rather than `MissingEffectBinding`, that a malformed row is skipped with the rest of the pass still running, and that the branch still refuses to consume worker authority in the same pass; exact-revision native Linux focused tests over real SQLite, Clippy, and required CI |
| `P2-T12/D03` | P2-T12 | replace the pre-bind startup tick with a bounded periodic scheduler tick: single-writer, non-reentrant, cancellable, clean on shutdown, holding its own scheduler handle, and never able to prevent daemon startup | `P2-T12/D02`; the existing `run_private_scheduler_tick_with_store` at `server.rs:128` | failure-first negatives proving a Task admitted after startup is observed by a later tick, an overlapping tick cannot double-lease one work key, shutdown leaves no running tick and no held lease, and a tick failure neither aborts startup nor poisons the store; exact-revision native Linux focused tests, Clippy, and required CI |
| `P2-T12/D04` | P2-T12 | dispatch the task's durable Tool Effect from the worker attempt through the assembled native executor selected by the persisted descriptor, so an admitted Task actually reaches a governed Tool execution | `P2-T12/D03`; the P2-T10 assembled sinks | failure-first negatives proving an unassembled family fails closed rather than closing the Effect, a stale or replaced scheduler lease cannot dispatch, a crash between dispatch and outcome recording reconciles through the original key without repeating the external operation, and a STOP/budget ceiling still stops before dispatch; exact-revision native Linux focused tests over real SQLite, Clippy, and required CI |
| `P2-T12/D05` | P2-T12 | complete the P2-T12 acceptance mapping and closure, and update the execution-chain-status gap record to whatever is then true | `P2-T12/D01-D04` | acceptance mapping, required CI, consistency, bilingual handbook sync, and closure; Task completion is explicitly still unreachable until P2-T13 and P2-T14, and no Gate/release/Profile claim is created |
| `P2-T13/D01` | P2-T13 | compose an `ArtifactStore` into the daemon at startup and persist the `FixedPostStateRow` plus `VerificationRequestRow` for a Task whose governed Tool Effect is closed and reconciled, committing the Loop `ACT -> VERIFY` edge | completed P2-T07 verifier persistence, P3-T03 Artifact CAS, and `P2-T12/D04` governed dispatch | failure-first negatives proving nothing is pinned while any Effect is open or unreconciled or under a stale epoch, that the pinned subject version is the one actually observed, and that the daemon fails closed rather than skipping verification when the artifact root is unavailable; exact-revision native Linux focused tests, Clippy, and required CI; pinning a post-state is not completing a Task |
| `P2-T13/D02` | P2-T13 | derive the verification request's criteria from the real contract — the `Acceptance` conditions and their `verifier_ref` — and add a production `IndependentVerifier` plus the registry that resolves a `verifier_ref` to it, then call `record_independent_verification` from the daemon | `P2-T13/D01` | failure-first negatives proving an unresolvable `verifier_ref` fails closed rather than defaulting, a report from the actor's own identity is refused, a report bound to a different fixed post-state is refused, and a missing or digest-invalid CAS evidence reference is refused before report persistence; exact-revision native Linux focused tests, Clippy, and required CI |
| `P2-T13/D03` | P2-T13 | emit the loop checkpoint and issue the continuation authorization after a passed report, closing `VERIFY -> CONTINUE -> OBSERVE` through the consumption path that is already production-wired | `P2-T13/D02`; `ProtocolStore::append_checkpoint` and `issue_continuation_authorization` (both defined, neither production-called) | failure-first negatives proving no continuation is issued without a checkpoint at the same fencing epoch, without a `passed` report, or at a stale loop version, and that a continuation is consumed exactly once; exact-revision native Linux focused tests over real SQLite, Clippy, and required CI |
| `P2-T13/D04` | P2-T13 | complete the P2-T13 acceptance mapping and closure, and record honestly that a closed verification loop still yields zero completed Tasks until P2-T14 | `P2-T13/D01-D03` | acceptance mapping, required CI, consistency, bilingual handbook sync, and closure; a verification report in tests is evidence of the mechanism, not a Gate, release, Profile or campaign result |
| `P2-T14/D01` | P2-T14 | obtain the owner and Lane-CTR decision on the `acceptance_decision` evidence object and the acceptance authority principal, then register whatever contract that decision produces | `P2-T13/D04`; the `task.transitions.json` acceptance edge, which requires `acceptance_decision` evidence that has no Rust type, schema binding or table | documentation, contract registration and consistency only; authoring creates no completion capability, and the task stays `blocked` while the decision is outstanding rather than substituting a private structure for a registered evidence object |
| `P2-T14/D02` | P2-T14 | implement `ACTIVE -> CANDIDATE_COMPLETE` and `CANDIDATE_COMPLETE -> COMPLETED` with the three acceptance guards genuinely derived rather than asserted | `P2-T14/D01` | failure-first negatives proving an open Effect blocks `CANDIDATE_COMPLETE`, a stale fixed post-state blocks acceptance, a superseded report blocks `verification_passed_and_current`, and a Tool receipt, Provider response, process exit and Pi `agent_end` each individually fail to complete a Task; exact-revision native Linux focused tests, Clippy, and required CI; a verified completion in tests is not a Gate, release or Profile claim |
| `P2-T15/D01` | P2-T15 | harden workspace search/mutation and every native sink against descriptor drift, symlink/reparse/path-swap TOCTOU, final-window overwrite, whole-directory enumeration, unbounded preimages, incomplete unified-diff newline semantics and hidden cleanup failure | completed P2-T10 sinks plus the owner-confirmed independent-review defect list | focused Linux/Windows swap/reparse, exact-descriptor-field × every-family, enumeration ceiling, final-window CAS, sparse/over-limit, orphan-cleanup and final-newline negatives; required Ubuntu/Windows CI; normative surface unchanged |
| `P2-T15/D02` | P2-T15 | make mutation and HTTP reconciliation durable and original-key-bound across restart/state loss, keep receipt state outside mutable workspaces, and resolve Provider readiness secrets from the already-loaded config snapshot | `P2-T15/D01`; completed P2-T11 readiness correction | same-postimage competitor, post-execution reversion, HTTP attempted/completed EffectProtocol restart, missing-state seen-key, linked state-root, unsafe receipt placement and deterministic config-swap fake-SecretStore negatives; bilingual mapped handbook sync; no secret material retained |
| `P2-T15/D03` | P2-T15 | complete exact-revision supported/native validation, final defect-first review, acceptance mapping and deterministic closure | `P2-T15/D01-D02`; closed `PERSONAL-PERF-EVAL-002` recovery lease | required Ubuntu/Windows CI and exact-revision `DEV-LINUX-NATIVE-01` focused/full tests + Clippy/fmt; one running report and final closure record; no findings; ready/merge/lease/branch/main closure; no Gate/release/Profile/B01/benchmark claim |
| `P2-T19/D01` | P2-T19 | register the isolated task and add the first failure-first production-path test proving an admitted Task cannot currently place an authorized Memory object and exact-pinned Skill binding into its governed Context | exact P2-T13 head plus completed P4/P8 authority stores | expected failing test committed on the isolated branch; required Rust execution routes to GitHub Ubuntu/Windows CI because local Windows GNU is static/fmt-only; completion enables the daemon resolver implementation without claiming consumption |
| `P2-T19/D02` | P2-T19 | extend the existing MemoryStore, SkillStore and ContextView pipeline with metadata-first eligibility, current authorization/lifecycle revalidation, exact digest/provenance binding and one append-only durable consumption record keyed to Task, epoch, request and ContextView | `P2-T19/D01`; existing P4 stores and P3/P2 governed Context resolver | failure-first cross-workspace/task/tenant, unauthorized-body-before-rank, wrong scope/purpose/epoch, forgotten/revoked/stale/digest-drift, forged record and partial-write negatives; exact-revision supported CI and, when needed, disposable native Linux validation |
| `P2-T19/D03` | P2-T19 | wire daemon-approved Memory/Skill fragments into the sealed ContextView before Pi, keep Pi candidate-only, and resume session 2 solely from durable Task/context/consumption facts while preserving exact Skill pins across supersession | `P2-T19/D02`; P2-T13 production loop and existing Pi candidate boundary | negatives for no-record Skill-use claims, prompt-injected pseudo-consumption, session-2 user-restatement requirement, superseded auto-drift and post-revoke/forget future load; restart integration proves no chat replay |
| `P2-T19/D04` | P2-T19 | make existing management lifecycle preconditions discoverable and complete valid remember→review→forget and import→inspect→bind→supersede→revoke client cycles without raw SQLite or test helpers | `P2-T19/D03`; existing resource API and management/task channel isolation | HTTP lifecycle integration plus malformed/partial residue and task-bearer management-mutation negatives; preserve existing DTOs/errors where possible and synchronize any explicit new public field/error through contract and bilingual handbook obligations |
| `P2-T19/D05` | P2-T19 | complete acceptance mapping, one incremental validation report, bilingual personal/handbook/fingerprints, docs-sync/consistency, independent defect-first self-review, then wait for P2-T14 on main before final rebase/validation/merge/reconciliation | `P2-T19/D01-D04`; P2-T14 merged to `main` | local fmt/static/Node; exact candidate GitHub Ubuntu/Windows CI; scoped native Linux only when needed; no findings after repair; Draft remains unmerged while dependency is absent; no Gate/release/Profile/B08/EVAL claim |
| `P2-T16/D01` | P2-T16 | establish the caller boundary and immutable daemon-owned check registry: `RegisteredCheckRunRequest` carries only `check_id`, while each descriptor fixes executable identity, exact argv template, cwd, minimal environment, timeout, output/process-tree/write-root/network bounds and its digest | exact P2-T13 branch head and completed P2-T15 hardened native Tool sinks | first failure-first test proves no `check_id`-only production request exists; then unknown check, descriptor/version drift, caller argv/env/cwd fields and shell metacharacters all deny before dispatch; local fmt/static plus required Ubuntu/Windows CI |
| `P2-T16/D02` | P2-T16 | route one resolved registered check through the existing daemon Tool/Effect authority path and bounded process-tree supervisor, persisting Intent/Effect and durable `EXECUTING` before dispatch and retaining original-key reconciliation across restart | `P2-T16/D01`; P2-T15 durable Tool attempts/receipts and P2-T12 scheduler worker | timeout, oversized output, orphan/process-tree escape, out-of-scope write, network attempt, duplicate dispatch, before/mid/after-dispatch crash, unresolved restart outcome and stale epoch/lease negatives; no general ProcessRun or ambient environment |
| `P2-T16/D03` | P2-T16 | publish bounded stdout/stderr/exit and diff facts into Artifact/CAS evidence, invoke the P2-T13 independent verifier path, and provide one deterministic registered check for the frozen EVAL-003 C2a repair fixture | `P2-T16/D02`; P2-T13 fixed-post-state, verifier and checkpoint path | exit 0/self-report without a current independent verifier never completes Task; C2a/A3/T6 fixed-oracle fixture passes on exact revision; all evidence remains implementation/non-claim |
| `P2-T16/D04` | P2-T16 | complete bilingual personal/handbook/fingerprint sync, one incremental validation report, dependency rebase, supported CI/native validation, defect-first self-review and deterministic task closure | `P2-T16/D01-D03`; P2-T14 merged on `main` | keep Draft while #210 or P2-T14 is absent from `main`; after both land, rebase on `origin/main`, run required Ubuntu/Windows CI and exact-revision disposable `DEV-LINUX-NATIVE-01`, fix every actionable review issue, then ready/merge/lease/branch/main reconcile without Gate/release/Profile/EVAL promotion |
| `P2-T17/D01` | P2-T17 | freeze the product-facing A7 observation interface with a failure-first restart test and implement the campaign-owned loopback fixture contract (mutate, original-key query, digest/count, bounded reset and cleanup) without modifying the prepared EVAL-003 assets | completed P2-T13/P2-T15 branch baseline; ignored EVAL-003 prep assets are read-only design input | the failure-first test must initially demonstrate the missing production observation path, then pass against fixture duplicate/conflict/bounds/reset/residue negatives; local Windows runs format/static only and supported Rust validation routes to Ubuntu/Windows CI |
| `P2-T17/D02` | P2-T17 | persist the campaign request carrier and Effect before dispatch, add campaign-only/default-off authorized fault points, publish bounded public observation, and reconcile restart state by the daemon-issued original idempotency key without dispatching again | `P2-T17/D01`; any separately registered mutation carrier required by the chosen production route | exact fault matrix for dispatch-before, mutation-after/receipt-before, receipt-after/close-before and verification-before; stage timings and key digest/reference only; query timeout/ambiguity remains Indeterminate; stale epoch/lease and unauthorized hook fail before external mutation |
| `P2-T17/D03` | P2-T17 | bind the reconciled post-state to P2-T13 fixed-post-state independent verification and close the complete exactly-once negative matrix while proving this task cannot complete a Task | `P2-T17/D02`; P2-T13 verification production path | mutation count >1, replacement key, blind retry, receipt/digest mismatch, open/unknown Effect completion, missing/tampered verifier evidence, duplicate restart worker, crash-at-every-point and cleanup-residue negatives; public evidence contains Effect/report references but no raw SQLite |
| `P2-T17/D04` | P2-T17 | synchronize the running report and bilingual handbook, run consistency/docs-sync, exact-revision native Linux and required Ubuntu/Windows CI, perform independent self-review, then rebase and close only after dependencies land | `P2-T17/D03`; P2-T13, P2-T14 and required carrier merged on `main` | all validation entries appended incrementally; Draft dependency is removed only after exact-base verification; implementation evidence may prepare EVAL-003 A7/T9/O5 but cannot make a campaign/Gate/release/Profile/B01 claim |
| `P2-T18/D01` | P2-T18 | replace bootstrap token derivation with a private OS-CSPRNG-backed 256-bit generator and fail closed before the bootstrap file exists | accepted P1-T04 local-auth boundary and existing private runtime path | failure-first source negative plus private-seam zero/short/failure/repeated-block cases; assert no bootstrap file and preserve Unix `0600`; exact-revision native Linux focused tests, fmt and required CI; no PID/time/hash fallback or token material in output |
| `P2-T18/D02` | P2-T18 | route session bearer minting through the same production OS RNG while preserving channel binding, constant-time bootstrap proof, expiry/revocation and redaction, then close the independent corrective | `P2-T18/D01` | no-session-on-entropy-failure, bounded uniqueness without statistical claim, wrong-channel/expired/revoked and Debug/serialization redaction regressions; exact-revision native Linux focused/full tests and Clippy, GitHub Ubuntu/Windows CI, consistency/docs-sync, bilingual security/limitations pages with refreshed fingerprints, independent defect-first review and deterministic merge/lease/branch/main closure |
| `P2-T20/D01` | P2-T20 | failure-first 证明：admitted Task 无法对 WorkspaceSearch（带 query）/WorkspaceWrite（带 preimage）/HttpFetchReadOnly（带 registered origin）做 production dispatch，sink 仍 test-only | P2-T12 worker loop、P2-T15 hardened sinks | 首个失败测试断言参数化请求在 Effect authorization 前 fail closed（无载体）；expected-fail on CI，随后按族接入真实 caller |
| `P2-T20/D02` | P2-T20 | wire the WorkspaceSearch production request carrier: the governed query carried in the persisted Intent (`parameters.query`) flows through the production router into the search sink, reusing the hardened Effect/authority path | `P2-T20/D01`; P2-T15 hardened search sink | focused positive + negative proves an admitted Task with a governed query reaches the search sink; missing/unparseable query fails closed before Effect authorization; required Ubuntu/Windows CI |
| `P2-T20/D03` | P2-T20 | wire the WorkspaceWrite/WorkspacePatch production request carrier: expected-preimage + payload flows through the production router into the mutation sink, reusing the hardened Effect/authority path | `P2-T20/D02`; P2-T15 hardened mutation sink | focused negative proves an admitted Task with a governed preimage reaches the mutation sink; missing/drifted/unknown preimage fails closed before Effect authorization; required Ubuntu/Windows CI |
| `P2-T20/D04` | P2-T20 | wire the ProcessCheck and HttpFetchReadOnly production request carriers: the bounded process check and the pinned HTTPS target flow through the production router into their sinks, reusing the hardened Effect/authority path | `P2-T20/D03`; P2-T15 hardened sinks | ProcessCheck stages the bounded process check and HttpFetchReadOnly stages the pinned HTTPS target; both fail closed before Effect authorization (fail-closed supervisor / empty origin allowlist); required Ubuntu/Windows CI |
| `P2-T21/D01` | P2-T21 | register bounded canonical parameter variants only for WorkspaceSearch, WorkspaceWrite, and WorkspacePatch; prove the existing candidate-to-Intent path loses the router-required fields, then daemon-canonicalize verified candidate data, recompute the digest, validate descriptor/family/epoch/fencing, and atomically bind bytes to the governed Intent | P2-T12 production Task loop; P2-T15 hardened sinks; P2-T20 request carriers | failure-first missing/malformed/non-canonical variant, digest mismatch, oversized query/payload, family/action mismatch, authority-shaped candidate field, stale epoch/fence, descriptor drift, duplicate admission, and no-external-I/O-before-denial negatives; generated contract drift check; Ubuntu supporting CI plus exact pushed-revision `linux-002`; Windows not-run by the owner-directed route |
| `P2-T21/D02` | P2-T21 | prove the real production caller path from public Task admission through private candidate, verified governed Intent, scheduler, and production router into WorkspaceSearch/WorkspaceWrite/WorkspacePatch sinks without test-only resolved dispatch | `P2-T21/D01`; P2-T12 scheduler loop; P2-T14 completion boundary | focused production-chain positives and denial-before-Effect negatives preserve preimage CAS, staging-plus-atomic-rename publish, containment/no-follow, persist-before-dispatch, original-key reconciliation, idempotency, budget, and fencing; no receipt, Pi `agent_end`, or router success completes a Task; Ubuntu supporting CI plus exact pushed-revision `linux-002`; Windows not-run by the owner-directed route |
| `P2-T21/D03` | P2-T21 | add authenticated task-channel read-only terminal evidence that rehydrates from authority/CAS rather than process-local watch history, returning only bounded redacted task state, opaque refs, reconcile class, verification/acceptance refs-digests-currentness, and terminal transition/cursor facts | `P2-T21/D02`; P2-T14 acceptance authority; existing task API/client channel isolation | successful accepted completion, open Effect, unknown/reconciled/indeterminate, failed/superseded/missing verification, missing CAS, cross-task/wrong-channel, restart durability, bounded response/cursor, and complete-redaction tests; CLI parity, generated/docs drift checks, Ubuntu supporting CI, exact pushed-revision `linux-002`, full acceptance mapping and task closure; Windows not-run by the owner-directed route |
| `P2-T22/D01` | P2-T22 | freeze TypeScript/Rust repair corpora and add failure-first public caller tests | P2-T21 and P2-T16 | no secret, deterministic reset/digest, hidden checks cannot be weakened; Ubuntu supporting CI |
| `P2-T22/D02` | P2-T22 | connect read/search, write/patch, RegisteredCheckRun, diff Artifact, verifier and acceptance as one governed journey | `P2-T22/D01` | real caller and durable authority outcome; focused mutation/check/verification negatives |
| `P2-T22/D03` | P2-T22 | run exact-revision `linux-002` restart/unknown-outcome/resource/secret/cleanup matrix and close the task | `P2-T22/D02` | one append-only report, Windows not-run by route, deterministic PR/lease/branch/main closure |
| `P2-T23/D01` | P2-T23 | failure-first public lifecycle and task consumption journey | P2-T19 and P2-T21 | valid Memory/Skill lifecycle plus pre-rank authorization negatives; Ubuntu supporting CI |
| `P2-T23/D02` | P2-T23 | persist bounded consumption and resume session 2 solely from durable state | `P2-T23/D01` | exact pins, zero user restatement, restart and forged-prompt negatives |
| `P2-T23/D03` | P2-T23 | execute exact-revision `linux-002` recall/isolation/lifecycle/cleanup matrix and close | `P2-T23/D02` | required recall/exposure counters, Windows not-run by route, deterministic closure |
| `P2-T24/D01` | P2-T24 | expose default-off authorized fixed fault profiles and bounded Effect history | P2-T17 and P2-T21 | unauthorized caller denied; no raw parameter/receipt; Ubuntu supporting CI |
| `P2-T24/D02` | P2-T24 | reconcile every fault by original key across restart and preserve Indeterminate/open non-completion | `P2-T24/D01` | mutation count 0/1, no replacement key/blind retry |
| `P2-T24/D03` | P2-T24 | execute exact-revision `linux-002` fault/restart/residue matrix and close | `P2-T24/D02` | one append-only report, Windows not-run by route, deterministic closure |
| `P2-T25/D01` | P2-T25 | connect lifecycle projection, Agent exposure and bounded selection receipt | P2-T21 and existing lifecycle stores | atomic propagation and least-tool selection negatives; Ubuntu supporting CI |
| `P2-T25/D02` | P2-T25 | provide immutable RegisteredCheckRun and pinned read-only HTTPS success paths | `P2-T25/D01`, P2-T16 | no ProcessRun; timeout/redirect/credential/oversize/drift negatives |
| `P2-T25/D03` | P2-T25 | execute exact-revision `linux-002` lifecycle/call/reconcile/cleanup matrix and close | `P2-T25/D02` | Windows not-run by route and deterministic closure |
| `P2-T26/D01` | P2-T26 | add authenticated bounded O2/O3/O4 collectors with active negative controls | P2-T23 and scheduler/context stores | no body/capability leakage; denominator-bound observations; Ubuntu supporting CI |
| `P2-T26/D02` | P2-T26 | add O5/O13 Effect and audit-chain cursor/replay/gap collectors | `P2-T26/D01`, P2-T24, P2-T25 | durable restart consistency and fail-closed cursor/digest handling |
| `P2-T26/D03` | P2-T26 | execute exact-revision `linux-002` concurrency/restart/redaction/cleanup matrix and close | `P2-T26/D02` | every observed zero controlled; Windows not-run by route; deterministic closure |
| `P2-T27/D01` | P2-T27 | implement public backup preflight/archive and transactional restore excluding secrets | P7-T02 and P2-T23 | tamper/version/missing-part/partial-state negatives; Ubuntu supporting CI |
| `P2-T27/D02` | P2-T27 | execute managed Pi install through recover lifecycle with bounded outcomes | `P2-T27/D01`, P5-T01/T02/T05 | state-transition, rollback and orphan negatives |
| `P2-T27/D03` | P2-T27 | execute exact-revision `linux-002` equality/migration/RTO/RPO/lifecycle/cleanup matrix and close | `P2-T27/D02` | Windows not-run by route and deterministic closure |
| `P2-T28/D01` | P2-T28 | freeze UJ1..UJ6 capability-truth matrix and failure-first missing-caller/oracle checks | P2-T22..P2-T27 | scope-excluded Web UI/Multi-Agent remain explicit; Ubuntu supporting CI |
| `P2-T28/D02` | P2-T28 | run all required public journeys including cold/warm, faults, resources and terminal evidence | `P2-T28/D01` | every row has caller, mechanical oracle, cleanup and bounded evidence |
| `P2-T28/D03` | P2-T28 | execute exact-revision aggregate `linux-002` matrix and deterministic product-train closure | `P2-T28/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T29/D01` | P2-T29 | advertise daemon-governed WorkspaceSearch/Write/Patch through the Pi Extension and adapter without opening bash/edit/write | P2-T21 | allowlist stays empty; native filesystem tools remain blocked; Ubuntu supporting CI |
| `P2-T29/D02` | P2-T29 | daemon-owned GovernanceSeed composer so public Memory remember succeeds without caller-minted sealed headers | P2-T23, `P2-T29/D01` | sealed envelopes still work; mixed/caller-header unsealed payloads fail closed |
| `P2-T29/D03` | P2-T29 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T29/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T30/D01` | P2-T30 | failure-first public admit → scheduler tick: Task stays DRAFT, `lease_acquired` 0, proposer/Pi calls 0 | P2-T12, P2-T21, P2-T29 | prove EVAL-005 skip class `scheduler_row_skip_before_lease`; Ubuntu supporting CI |
| `P2-T30/D02` | P2-T30 | persist daemon-owned Context authorization + revocation at public admit; first tick walks Loop START→DECIDE then spawns Workspace* Pi; later tick acquires lease and leaves DRAFT via existing activation | `P2-T30/D01` | no client capability channel; admission still must not complete the Task |
| `P2-T30/D03` | P2-T30 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T30/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T31/D01` | P2-T31 | failure-first live `kernel-server` HTTP admit: Task stays DRAFT, `lease_acquired` 0, stderr skip retained | P2-T30 | prove EVAL-006 skip on the production tick; Ubuntu supporting CI |
| `P2-T31/D02` | P2-T31 | live daemon tick acquires a lease and leaves DRAFT; Unix private-candidate transport is invoked (stub adapter allowed) | `P2-T31/D01` | no client capability channel; admission still must not complete the Task |
| `P2-T31/D03` | P2-T31 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T31/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T32/D01` | P2-T32 | failure-first public `cognitive daemon start` admit: Task stays DRAFT, `lease_acquired` 0, skip not a public fact while stderr is `/dev/null` | P2-T31 | prove EVAL-007 public-launcher skip class; Ubuntu supporting CI |
| `P2-T32/D02` | P2-T32 | public launcher retains scheduler logs, Unix-detaches, acquires a lease and leaves DRAFT; Unix private-candidate transport is invoked (stub adapter allowed) | `P2-T32/D01` | no client capability channel; admission still must not complete the Task |
| `P2-T32/D03` | P2-T32 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T32/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T33/D01` | P2-T33 | failure-first: long `--runtime-root` still binds a private completion socket; rejecting adapter stderr is a public `daemon.log` fact; private-candidate proxy accepts `role`+`content` and strips `tools` | P2-T32 | prove EVAL-008/009 skip classes; Ubuntu supporting CI |
| `P2-T33/D02` | P2-T33 | public launcher on a long root acquires a stub lease and leaves DRAFT; Linux candidate env allowlist; redacted adapter diagnostic retained | `P2-T33/D01` | stub ≠ real adapter C1/C2; no client capability channel |
| `P2-T33/D03` | P2-T33 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T33/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T34/D01` | P2-T34 | failure-first: JSON-fallback `sha256:parameters` is rejected; WorkspaceSearch parameters recompute the canonical digest | P2-T33 | prove EVAL-010 skip class; Ubuntu supporting CI |
| `P2-T34/D02` | P2-T34 | JSON-fallback with valid parameters overwrites a garbage digest; empty digest with parameters is recomputed rather than fail-empty | `P2-T34/D01` | stub ≠ real adapter C1/C2; no client capability channel |
| `P2-T34/D03` | P2-T34 | execute exact-revision `linux-002` matrix and deterministic product-train closure | `P2-T34/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T35/D01` | P2-T35 | failure-first negatives for omitted JSON-fallback digest, missing digest and parameters, unknown extra fields, usage/runtime exit separation, and retained/redacted adapter diagnostics | P2-T34 | prove EVAL-011 skip class; Ubuntu supporting CI |
| `P2-T35/D02` | P2-T35 | recompute omitted JSON-fallback digests, separate usage from runtime failures, preserve diagnostic tails with exit-code subclasses, and make the prompt state that the digest may be omitted | `P2-T35/D01` | adapter protocol, kernel-server bin tests, Clippy and fmt; no contract relaxation |
| `P2-T35/D03` | P2-T35 | execute exact-revision `linux-002` matrix and deterministic product-train closure; record whether live Pi skip disappears and classify any residual skip | `P2-T35/D02` | Windows not-run by route; no EVAL/Gate/release/Profile promotion |
| `P2-T36/D01` | P2-T36 | failure-first: the locked-down Pi Extension and adapter lack WorkspaceRead while Pi-native filesystem/shell tools must remain blocked | P2-T35, P2-T29 | Node/TypeScript regression plus adapter protocol negative; no Rust test on Windows GNU |
| `P2-T36/D02` | P2-T36 | add daemon-governed WorkspaceRead candidate extraction and compose it through the existing descriptor, scheduler, executor, verifier, and acceptance path with public `cognitive daemon start` | `P2-T36/D01` | candidate-only boundary, no private transport injection or SQLite fixture; exact pushed `DEV-LINUX-NATIVE-01` |
| `P2-T36/D03` | P2-T36 | complete supported Linux/CI validation, docs sync, acceptance mapping, and deterministic task closure | `P2-T36/D02` | no EVAL/B01/paired sample or performance claim; a later campaign must use a new preregistration |
| `P2-T37/D01` | P2-T37 | failure-first: public Pi launch omits WorkspaceWrite/Patch and public candidate mapping rejects or cannot represent their schema-bound mutation fields; Pi built-in filesystem/shell tools remain blocked | P2-T21, P2-T29, P2-T36 | Node/TypeScript plus adapter/kernel negatives; no Rust test on Windows GNU |
| `P2-T37/D02` | P2-T37 | add only daemon-governed WorkspaceWrite/Patch to the public launch allowlist and public candidate mapper, then prove separate public real-Pi mutation Tasks through existing daemon admission, scheduler, expected-preimage executor, independent verifier, and acceptance path | `P2-T37/D01` | candidate-only boundary, no private transport injection or SQLite fixture; no filesystem/shell built-ins; exact pushed `DEV-LINUX-NATIVE-01` |
| `P2-T37/D03` | P2-T37 | complete supported Linux/CI validation, docs sync, acceptance mapping, and deterministic task closure | `P2-T37/D02` | no EVAL/B01/paired sample or performance claim; a later campaign must use a new preregistration |
| `P2-T38/D01` | P2-T38 | failure-first: a production Patch whose expected preimage is the raw SHA-256 of `c2a-patch-v1` plus LF currently cannot reconcile; a fail-closed Patch must not request independent verification while the Effect is not RECONCILED | P2-T37 | kernel-server focused tests; no Rust test on Windows GNU |
| `P2-T38/D02` | P2-T38 | accept `digest:sha256:<raw file bytes>` as an equivalent CAS preimage to the domain-tagged workspace-image digest, and return `AwaitingReconciliation` instead of `begin_verification` when native dispatch leaves the Effect pending | `P2-T38/D01` | existing tagged-digest Patch still completes; wrong preimage still refuses the write; no replacement-bytes Patch payload |
| `P2-T38/D03` | P2-T38 | complete supported Linux/CI validation, docs sync, acceptance mapping, and deterministic task closure | `P2-T38/D02` | no EVAL/B01/paired sample or performance claim; a later campaign must use a new preregistration |
| `P9-T08/D01` | P9-T08 | amend the C1/C2 readiness programme so packages 6–14 are real delivery, rewrite confirmatory gates now that O-arm exists, reserve EVAL isolation, and make the environment checklist an exit criterion | P2-T36, P2-T37, P2-T23, P2-T24, P2-T14, P2-T21 | docs/consistency/diff; no B01 sample; no EVAL activation |
| `P9-T08/D02` | P9-T08 | deliver the secret-safe pure-Pi credential broker and equivalent WorkspaceRead/Search/Write/Patch fixture adapter for the same C1/C2 tasks | `P9-T08/D01` | exact-revision `DEV-LINUX-NATIVE-01` focused negatives plus one C1-class and one C2a-class fixture call; no B01 guest mutation |
| `P9-T08/D03` | P9-T08 | freeze runner, corpus, oracle, redactor, seeds, timeout, arm-order, digest ledger, and `retry=0`; encode the §2.3 fairness contract and prove the checker on non-B01 | `P9-T08/D02` | freeze ledger complete; non-B01 fairness observability proof; no counted sample |
| `P9-T08/D04` | P9-T08 | B01 guest procedure, secret-bind runbook, freezeable C1/C2 cell manifests, environment-checklist recording, leftover-branch housekeeping, and EVAL preregistration scaffolding with no samples | `P9-T08/D03` | section 8 exit table; reserved isolation recorded; no B0/B1/B2 sample; then ready/merge/lease/branch/main closure |
| `P9-T09/D01` | P9-T09 | failure-first: dry-run `system_task_prompt_bytes` must not be the shared placeholder `frozen-c1-c2-prompt-v1` | P9-T08 | `tools/test/c1_c2_paired_p_arm.test.mjs`; no B01 sample |
| `P9-T09/D02` | P9-T09 | freeze one UTF-8 prompt file, observe its byte length on both dry-run arms, and include the file in the freeze ledger | `P9-T09/D01` | existing equal-arm dry-run still passes; no B0 sample |
| `P9-T09/D03` | P9-T09 | complete supported validation, docs sync, acceptance mapping, and deterministic task closure | `P9-T09/D02` | no EVAL/B01/paired sample; live O-arm prompt injection may remain a later slice if not done here |
| `P9-T10/D01` | P9-T10 | failure-first: `cognitive pi launch` rejects relative, missing, or empty `--append-system-prompt`; live P/O command manifests must share the freeze file | P9-T09 | `apps/admin-cli` launch-plan tests plus `tools/test/c1_c2_paired_p_arm.test.mjs`; no B01 sample |
| `P9-T10/D02` | P9-T10 | accept an existing absolute non-empty prompt file, forward `--append-system-prompt` to Pi after `--print`, and keep `system_task_prompt_bytes` as that file's byte length on both arms | `P9-T10/D01` | without the flag, argv stays `--extension`/`--tools`/`--print` only; no B0 sample |
| `P9-T10/D03` | P9-T10 | complete supported validation, docs sync, acceptance mapping, and deterministic task closure | `P9-T10/D02` | no EVAL/B01/paired sample; replacement-bytes Patch remains later |
| `P9-T11/D01` | P9-T11 | failure-first: P-arm WorkspacePatch refuses replacement bytes and invalid unified diffs without writing | P9-T10 | `tools/test/c1_c2_paired_p_arm.test.mjs`; no B01 sample |
| `P9-T11/D02` | P9-T11 | apply daemon-compatible UTF-8 unified diffs, pin `workspace_patch_payload: unified-diff` in the freeze ledger, and include `fixtures/c2a/workspace-patch.unified.diff` | `P9-T11/D01` | Write remains replacement bytes; no B0 sample |
| `P9-T11/D03` | P9-T11 | complete supported validation, docs sync, acceptance mapping, and deterministic task closure | `P9-T11/D02` | no EVAL/B01/paired sample; private-candidate epoch-1 skips remain later |
| `P9-T12/D01` | P9-T12 | failure-first: secret-shaped env refuses; missing `--append-system-prompt` refuses; dry-run cannot be labeled counted B1/B2; fairness fail is retained with `counted_sample: false` and no arm spawn | P9-T11 | `tools/test/c1_c2_paired_p_arm.test.mjs`; injected `executeArm` only; no B01 sample |
| `P9-T12/D02` | P9-T12 | live `runLivePairedCell` uses frozen seeds, live P/O manifests, deterministic arm order, `retry=0`, and counts a sample only for b1/b2 when fairness passes and both arms exit 0 without timeout | `P9-T12/D01` | measurement-only; not a second authority writer; no live Provider spawn in unit tests |
| `P9-T12/D03` | P9-T12 | complete supported validation, docs sync, acceptance mapping, and deterministic task closure | `P9-T12/D02` | no EVAL/B01/live Provider cell; implementing the executor is not a counted sample |
| `P2-T20/D05` | P2-T20 | complete the task acceptance assessment, merge the Draft PR, close the lease, and reconcile branch/main | `P2-T20/D01-D04` | final acceptance mapping, required Ubuntu/Windows CI green, then ready/merge/lease/branch/main reconciliation; no Gate/release/Profile claim |
| `P2-T11/D01` | P2-T11 | make the `provider` readiness component reflect whether the configured `secret_ref` actually resolves, and stop `POST /management/resource/v1/skill/binding/revoke` being shadowed by the `skill/bind` prefix match | completed P2-T09 readiness-separation pattern; `PERSONAL-PERF-EVAL-002` evidence | failure-first negatives proving a dangling ref never reads ready, an unreachable backend blocks rather than assumes, and a discriminating revoke-route probe that only the revoke handler can satisfy; exact-revision Linux focused tests, Clippy, and required CI; readiness must emit no secret material and no Gate/release/Profile claim is created |
| `P9-T07/D01` | P9-T07 | a failure-first proof plus the Pi-side nested observation core: an opaque correlation id, a monotonic sequential stage recorder that cannot emit overlapping Pi-domain stages, a strict observation validator, an authorization resolver that is denied by default, and a bounded content-free sink a campaign runner can read | completed `P9-T04/D01` measurement envelope and the existing `packages/pi-cognitiveos` daemon client | failure-first negatives for malformed/duplicate correlation id, missing/overlapping stages, zero or negative duration, inconsistent usage counters, secret-shaped observation, raw body/header capture, unauthorized instrumentation, and authority-shaped sink target; Node package tests and `check:consistency`; the core alone performs no daemon request |
| `P9-T07/D02` | P9-T07 | wire the real Pi route: `daemon-provider` records preparation/dispatch/parse/event-delivery, `daemon-client` records the loopback wait and joins the daemon-reported nested stages by correlation id, and the Extension publishes one observation per run with the Provider usage already carried in the assistant message | `P9-T07/D01` | failure-first negatives proving an unauthorized run publishes nothing, an unechoed or mismatched correlation id degrades the daemon domain to `not_available` rather than fabricating it, and a nested daemon total exceeding the observed loopback wait is refused; loopback fake-daemon integration over a real socket; Node package tests and required CI |
| `P9-T07/D03` | P9-T07 | daemon-side nested stage: measure the preflight/SecretStore interval separately from the Provider network exchange, echo a valid opaque correlation id, and emit both only under explicit daemon authorization; malformed or duplicated correlation input is refused without changing the product response body | `P9-T07/D02` and the existing `provider_proxy` transport timing seam | failure-first Rust negatives for malformed/duplicate correlation header, unauthorized emission, zero-duration stage, and header-only effect (identical response body and untouched authority store); exact-revision native Linux or required Ubuntu/Windows CI |
| `P9-T07/D04` | P9-T07 | retain completed/cancelled/error attempts with exact terminal schema, bind immutable Provider usage evidence to one correlated publication, complete the running incremental validation report, bilingual handbook sync and deterministic closure | `P9-T07/D01-D03` | failure-first terminal/error/cancellation/concurrency/usage-provenance negatives; full Node suite, required CI, `check:consistency`, `check:handbook`, generator `--check`, docs-sync gate, and ready/merge/lease/branch/main closure; instrumentation existence is not an overhead attribution, a Gate, a release, a Profile or a campaign result |
| `P9-T06/D01` | P9-T06 | bind one SecretStore per readiness evaluation so secret probe and provider resolve share it, skip get when probe already proved the backend cannot answer, drop material immediately, and re-evaluate every request with no stale-ready TTL | completed P2-T11 honest resolve and P2-T15 one-snapshot config | failure-first counting store proving probe/get once per evaluation, two evaluations pay twice, unavailable probe skips get, and material never enters status/doctor JSON; required Ubuntu/Windows CI; no Gate/release/Profile claim |
| `P9-T04/D01` | P9-T04 | establish a daemon-client-safe, secret-free campaign measurement envelope for opaque correlation IDs, monotonic stage timing, and Provider usage availability; missing usage must remain `not_available`, and the envelope must not write authority state | P7-T04/D02 collector, P8-T05/P9-T03 measurement boundaries, and ADR-0051 preregistration | failure-first invalid correlation, partial/inconsistent Provider usage, and zero-duration stage negatives; exact-revision Linux focused tests and required CI; completion enables real daemon/Pi/Provider instrumentation, not benchmark execution or any claim |
| `P9-T04/D02` | P9-T04 | executable L0/L1 campaign runner: admit only registered measurement arguments, refuse secret-shaped process input before parsing, run the existing deterministic module benchmark in a cleared child environment, and emit one redacted hypothesis report bound to the D01 envelope | `P9-T04/D01` envelope and the existing `p7_t04_module_benchmark` binary | failure-first unregistered-argument, secret-shaped-environment, duplicate/missing-argument, out-of-bound sample, unredacted-observation, and self-promoted-claim negatives; exact-revision Linux focused tests, Clippy, and required CI; the runner stays a non-authority observer and performs no Provider call |
| `P9-T04/D03` | P9-T04 | decompose real loopback transport into disjoint connection-admission, request-read, header-admission, route-dispatch, and response-write stages on the daemon front door, so `effect_persistence` is never read as HTTP cost; observations retain durations and byte counts only | `P9-T04/D02` runner and the P7-T04/D02 governed-path collector | failure-first absorbed-attribution, fabricated-stage, duplicate-stage, omitted-with-duration, and self-promoted-claim negatives plus one real loopback request carrying a credential header; exact-revision Linux focused tests, Clippy, and required CI; transport timing is hypothesis-only and creates no Gate/release/Profile claim |
| `P9-T04/D04` | P9-T04 | bounded OS resource sampler for declared daemon/Pi/adapter roles that records CPU ticks, RSS, threads, descriptor count, and I/O bytes without opening `cmdline` or `environ`, resolving a descriptor target, or attaching to an arbitrary PID | `P9-T04/D03` transport decomposition | failure-first incomplete-facts, zero-clock-rate, unordered/empty series, cross-identity, PID-reuse (decreasing cumulative counter), and self-promoted-claim negatives plus a fixture asserting command-line and environment sentinels never reach a sample; exact-revision Linux focused tests, Clippy, and required CI; soak observations stay hypothesis-only |
| `P9-T04/D10` | P9-T04 | define the remaining `T2`-`T8` governed-Task scenarios on the D08/D09 harness, starting with `T2` controlled repair: a deterministic test oracle, a non-zero permitted-mutation budget, and out-of-bounds path/descriptor-drift/stale-epoch/budget-exhausted negatives | `P9-T04/D09` safety coverage | failure-first negatives per scenario; exact-revision Linux focused tests, Clippy, and required CI; scenario execution against a real Provider still requires the B01 start gate and operator Provider import |
| `P9-T04/D09` | P9-T04 | bind daemon-observed safety facts to each retained `L4` run so every hard campaign safety counter — unauthorized/stale Context exposure, duplicate external Effect, stale-epoch commit, unreconciled Effect, false completion, completion without independent acceptance, and scenario boundary violation — is reachable from a scenario judgment | `P9-T04/D08` harness | failure-first coverage negative asserting all seven counters are producible, plus a negative proving observed safety counts survive an otherwise verified run; exact-revision Linux focused tests, Clippy, and required CI |
| `P9-T04/D08` | P9-T04 | `L4` governed-Task scenario harness starting with the `T1` read-only admission scenario on the existing daemon authority path, with a fixed oracle, retained denominator, and no Provider dependency for its structure | `P9-T04/D07` route policy and the completed P2-T0x runtime spine | failure-first fabricated-completion, missing-oracle, and dropped-scenario negatives; exact-revision Linux focused tests, Clippy, and required CI; scenario execution against a real Provider still requires the B01 start gate and operator Provider import |
| `P9-T04/D07` | P9-T04 | executable `L3` Provider-route policy: zero retry budget, every started request retained as a classified outcome, no TTFT without real streaming timestamps, no measured usage on a failed request, and no cost without a preregistered pricing snapshot | `P9-T04/D06` offline execution and the D01 Provider usage envelope | failure-first discarded-failure, retried-completion, fabricated-TTFT, fabricated-cost, network-time-without-dispatch, implausible-complete-response, usage-on-failure, and self-promoted-claim negatives; exact-revision Linux focused tests, Clippy, and required CI; the policy admits no Provider traffic by itself |
| `P9-T04/D06` | P9-T04 | offline L0–L2 campaign execution on a registered non-Gate environment that emits one policy-validated evidence report with `L3`–`L5` recorded as `not_run`; the runner refuses to label its own output with the sole active B01 campaign guest | `P9-T04/D02-D05` | failure-first B01-environment, unregistered-environment, and missing-environment negatives plus a real exact-revision execution on `DEV-LINUX-NATIVE-01`; exact-revision Linux focused tests, Clippy, and required CI; the result is `hypothesis`/`tested-local` at most and is not B01, Gate, release, Profile, or Agent-benefit evidence |
| `P9-T04/D05` | P9-T04 | executable ADR-0051 campaign report policy: full L0–L5 layer coverage, every started sample retained in the denominator, hard safety accounting, cleanup facts, independent-verifier disposition, and a claim ceiling that cannot exceed the evidence | `P9-T04/D02-D04` measurement surfaces and the ADR-0051 preregistration | failure-first dropped-sample, safety-failure-with-claim, unverified/uncleaned promotion, benefit-claim-without-L5, evidence-without-execution, empty-completed-layer, missing-layer, and missing-non-claim negatives; exact-revision Linux focused tests, Clippy, and required CI; an incomplete campaign must still produce a complete non-claim report |

### Phase 6 - Multi-Agent

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P6-T01 | Post-1.0 Multi-Agent policy 与 scheduler 扩展 | P5-T05 | 默认关闭、budget/lease/isolation 正确 | not-started | — |
| P6-T02 | Post-1.0 mailbox 与 append-only findings | P6-T01 | 消息不可作为 authority；replay 可处理；默认关闭 | not-started | — |
| P6-T03 | Post-1.0 Reviewer/Verifier/Integrator 编排 | P6-T02 | child success 不可绕过 verifier；默认关闭 | not-started | — |
| P6-T04 | Post-1.0 B11 收益 Gate | P6-T03 | 仅在质量或速度收益达标时启用；NO-GO 并保持默认关闭是合法结果，不阻塞 GMVP-LINUX/RC | not-started | P7-T06 对本 RC 记录 `disabled-nogo`；本任务仍 not-started，不取消 Phase 6 |

### Phase 7 - 产品化与发布

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P7-T01 | Release pipeline、六资源 manifest、SBOM 与 attestation | P0-T03, P1-T08, P2-T08 | release manifest 固定六类 schema/version/digest，以及 sidecar/adapter/skill/tool pins；可验证 Linux artifact；production trust、SBOM/attestation、immutable actions/toolchain/environment | done | 2026-08-11；PR #184 merged at `main@3198614496571ac251821d2eff1f982274959f06`. D01–D04 closed. Closure: `docs/checkpoints/20260811-personal-p7-t01-release-pipeline-closure.md`. No Gate/release/Profile/GMVP-LINUX claim. |
| P7-T02 | Transactional lifecycle、Memory/Skill backup/restore | P1-T01, P1-T08, P2-T08, P7-T01 | update/rollback/uninstall；`cognitive backup`/`restore` 覆盖 Memory、Skill 与 bindings，排除 secret；migration preflight 与恢复证据 | done | 2026-08-11；`lease/personal/P7-T02/lifecycle-backup` on `personal/P7-T02-lifecycle-backup` / PR #185. D01–D04 closed. Linux `personal_backup` 15/15 + Clippy at `68abc82`; required CI `31449589853` on `8b388e8`. Closure: `docs/checkpoints/20260811-personal-p7-t02-lifecycle-backup-closure.md`. No Gate/release/Profile claim. |
| P7-T03 | 六资源 doctor、headless vault、sidecar/process/effect support | P1-T05, P2-T08, P7-T02 | 六类 health；desktop Secret Service 与 headless encrypted-vault locked/TTY/unattended paths；sidecar drift、process/effect/reconcile、migration；仅 redacted facts；stable error code 与可操作恢复路径 | done | 2026-08-11；`lease/personal/P7-T03/six-resource-doctor` on `personal/P7-T03-six-resource-doctor` / PR #186. D01–D04 closed. Linux through `749a0c3`; required CI `31451402260` on `a8db5cf`. Closure: `docs/checkpoints/20260811-personal-p7-t03-six-resource-doctor-closure.md`. No Gate/release/Profile claim. |
| P7-T04 | 完整性能 campaign 与回归地板 | P3-T06, P4-T05（原列 P5-T05 依赖仅约束 D03 之后的 sidecar 阶段计时；D01-D05 实际依赖已按 slice register 拆分，D05 经 owner 预注册批准执行） | 模块级确定性 benchmark、阶段级真实治理耗时、B06/B07 non-claim raw observation、固定环境 A/B 非劣化、性能回归地板与可审计阈值；不把 B06/B07 升级为 Linux 1.0 硬门禁 | done | 2026-08-10；closure checkpoint `20260810-personal-p7-t04-performance-governance-closure.md`; D01-D05 complete on PR #179 with fixed-native governance A/B report digest `sha256:b90b8452e5d7b833ada423fb6d9d8e6ae5db92830c22ebd2363d435e4fc4aad9`. B06/B07, Gate, release, Profile, and GMVP-LINUX remain non-claims. |
| P7-T05 | 非阻塞 Web UI | P2-T08, P7-T03 | 通过 clients gate；localhost 控制面板可设置 LLM API（含密钥进入 SecretStore）、绑定 Agent 的固定 account+provider+model，并经已有 Task preview/admit/watch 启动与观察 Agent 工作；Control Plane redesign waves（clients/docs/design）为 owner-directed 后续范围 | done | 2026-08-26 merged PR [#274](https://github.com/agentkernel/cognitive-os/pull/274) at `main@5996afbb`. Required CI [32942980183](https://github.com/agentkernel/cognitive-os/actions/runs/32942980183) **SUCCESS** at `b147711a`. D01–D26 accepted. Closure: [P7-T05 closure](../checkpoints/20260826-personal-p7-t05-control-plane-redesign-closure.md). Web UI path is this repo `clients/pc/web`. HTTP cancel / class-C Agent lifecycle `not-run`. Non-blocking for Linux 1.0；No Gate/release/Profile claim。 |
| P7-T06 | RC、文档、支持矩阵与声明范围内 B01-B12 | P7-T08, P7-T04, P5-T05 | clean VM suite 与 release claim evidence；P6 可为明确 NO-GO/disabled，不阻塞 RC | done | 2026-08-26；merged PR [#276](https://github.com/agentkernel/cognitive-os/pull/276) at `main@712a517d`. Required CI [32954890567](https://github.com/agentkernel/cognitive-os/actions/runs/32954890567) **SUCCESS** at `c75ffcd9`. Closure: [P7-T06 closure](../checkpoints/20260826-personal-p7-t06-rc-docs-support-matrix-closure.md). Composition `report_digest` `sha256:2c36e4594c4318fa64bfd7017299b7cd858f1e3e33c8f57ae3d99d601acc62c3`. P6 recorded `disabled-nogo` for this RC. No Profile / Windows B01-W / production GitHub Release / Gate promotion. |
| P7-T07 | Windows 安装面：credential 后端、installer/service 与 B01-W Gate | P1-T02, P7-T01, P7-T02 | Windows credential store 后端（同 fail-closed 边界，无明文 fallback）、可检查 installer/service、专门 B01-W Gate 编写并执行；不阻塞 Linux RC；未执行前不得声称 Windows install parity（ADR-0025） | blocked | 2026-08-12：D01-D03 complete under ADR-0052 on `personal/P7-T07-windows-install-surface` (Draft PR #200): Windows Credential Manager backend + failure-first negatives (real roundtrip on Windows CI), inspectable installer/scheduled-task templates + behavioral fail-closed negatives, and the authored B01-W gate with preregistration and `B01-W-DESKTOP-001` requirement registration. Required CI `31570126985` passed at `13e772a`. **Blocked on B01-W execution prerequisites**: Windows release artifacts (release-pipeline scope), `B01-W-DESKTOP-001` provisioning, operator for graphical hidden-input credential entry. Blocked-closure record `docs/checkpoints/20260812-personal-p7-t07-windows-install-closure.md`; lease released, branch/PR retained. No install parity, B01-W, Gate, release, or Profile claim. |
| P7-T08 | Public Linux 1.0 Gate（`GMVP-LINUX`） | P1-T09, P2-T08, P3-T06, P4-T06, P5-T01, P5-T02, P5-T05, P7-T01, P7-T02, P7-T03 | 汇合 Runtime Spine、Resource Value、Product Operability；promotion exact benchmarks 为 B01/B02/B03/B04/B05/B08/B09/B12；六类最小真实 slice、UCR-01 fixed-scenario assertions、desktop/headless SecretStore、Pi+sidecar 与 release operability 均有 evidence；B06/B07/B10/B11 不阻塞；不构成 Profile | done | 2026-08-11；`lease/personal/P7-T08/gmvp-linux` / PR #194. D01–D04 closed under ADR-0048/0049. B08 MVP `pass` at `65a736c` + CI `31479512940`; GMVP-LINUX MVP `pass` at `b3f4b88` + CI `31480604511`. Closure: `docs/checkpoints/20260811-personal-p7-t08-gmvp-linux-closure.md`. No Profile/Windows B01-W claim. |

### Phase 8 - 通用 Agent 适配与设计基线（post-1.0 能力列车）

Phase 8 承载"主流 agent 认知资源统一管理底座"的设计基线与通用适配实现。它不改变
Linux 1.0 的 `GMVP-LINUX` 组合与非阻塞边界；除 P8-T01 的文档基线外，各实现任务在
1.0 收敛路径（P2-T08、P5-T02..T05、P7）之后按依赖领取。每个新 agent 沿用 B09 模式
独立资格化，不继承 Pi 证据。

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P8-T01 | 文档体系重构与 2.0 设计基线 | — | 单一公理文档与治理规则收敛；现行白皮书（OS 定位正名、对标差距、context/harness/loop 三支柱、生态定位）；产品/架构设计扩展（通用 Agent Adapter Contract、多 agent 编排、性能架构、IoT 与企业多租户延伸余量章）；ADR-0041+；P8/P9 任务卡与权威台账修复。documentation-only，不改 core/specs/conformance 机器合同 | done | 2026-08-10；D01-D03 on `personal/P8-T01-doc-restructure`；PR #180；required CI run `31383446541`；closure `20260810-personal-p8-t01-doc-restructure-closure.md`；无 Gate/release/Profile claim |
| P8-T02 | 通用 Agent Adapter Contract 与 adapter framework 实现 | P8-T01, P5-T02 | AKP 为唯一适配协议的 adapter 能力声明/注册/生命周期实现；candidate-only 与通道隔离负例；对齐 A2A Agent Card 发现语义而不引入公网 listener；Lane-CTR 登记 `agent-adapter-manifest` 合同 | done | 2026-08-11；`lease/personal/P8-T02/agent-adapter-contract` on `personal/P8-T02-agent-adapter-contract` / PR #187. D01–D04 complete; Linux evidence through `791d5ff`; required CI `31453659735` on `f5e427f`. Closure: `docs/checkpoints/20260811-personal-p8-t02-agent-adapter-contract-closure.md`. No Gate/release/Profile claim. |
| P8-T03 | 首个非 Pi Agent 适配与独立资格化 | P8-T02, P5-T05 | 选定一个主流 CLI agent 走完整 acquisition/install/activation/rollback/uninstall 生命周期与 sidecar 资格化；证据独立于 Pi；B09 模式泛化 campaign | done | 2026-08-11；`lease/personal/P8-T03/non-pi-agent` on `personal/P8-T03-non-pi-agent` / PR #191. Selected: OpenAI Codex CLI fixture. D01–D04 complete; Linux evidence through `b41f06f`; required CI `31463130827` on `5d1c0c7`. Closure: `docs/checkpoints/20260811-personal-p8-t03-non-pi-agent-closure.md`. No Gate/release/Profile/Pi-transfer claim. |
| P8-T04 | Harness 扩展原语：确定性 hooks 与分级加载 | P8-T01, P2-T08 | daemon-owned 生命周期拦截点（admission/pre-dispatch/post-effect/verification 事件）与 owner 可编程确定性 hooks；Skill/规则按 context 成本分级加载；hooks 不得放松任何公理边界 | done | 2026-08-11；`lease/personal/P8-T04/harness-hooks` on `personal/P8-T04-harness-hooks` / PR #188. D01–D04 complete; Linux evidence through `bc3dacd`; required CI `31457314002` on `15e7200`. Closure: `docs/checkpoints/20260811-personal-p8-t04-harness-hooks-closure.md`. No Gate/release/Profile claim. |
| P8-T05 | Context compaction 与自适应预算 | P8-T01, P3-T04 | daemon-owned 会话/Context 压缩与汇总策略；预算自适应；压缩产物走 digest 绑定与显式损失记录；UCR-01 可复验收益观察 | done | 2026-08-11；`lease/personal/P8-T05/context-compaction` on `personal/P8-T05-context-compaction` / PR #189. D01–D04 complete; Linux evidence through `e15492a`; required CI `31459558236` on `1d2103e`. Closure: `docs/checkpoints/20260811-personal-p8-t05-context-compaction-closure.md`. No Gate/release/Profile claim. |
| P8-T06 | 跨 episode 学习闭环 | P8-T01, P4-T05 | agent 自产 Skill/Memory 候选经既有 candidate→deterministic admission 进入 Skill revision/Memory 生命周期；失败经验（Reflexion 谱系）沉淀为可解释、可撤销的治理事实；不引入自授权 | done | 2026-08-11；`lease/personal/P8-T06/learning-loop` on `personal/P8-T06-learning-loop` / PR #190. D01–D04 complete; Linux evidence through `b81414d`; required CI `31461384771` on `29399b7`. Closure: `docs/checkpoints/20260811-personal-p8-t06-learning-loop-closure.md`. No Gate/release/Profile claim. |
| P8-T07 | 独立双语 handbook 文档系统 | P8-T01 | 新建独立 `personal/handbook/`（en + zh-CN 平行树，user/developer/reference/AI 四类读者入口）；机器 manifest、frontmatter schema、source-map、tracked-source coverage、per-page source fingerprint 与 source-set 记录；CLI/HTTP/错误/配置/env/transition/schema/tool catalog reference 从实现与机器合同生成；独立 `check-handbook` 检查器带 focused negative fixtures（缺源、陈旧指纹、断链、未映射新文件、非法状态、生成漂移、缺双语、secret-shaped、动态状态复制、History 引用）；`.cursor/rules/20` 同步适配、根 `llms.txt` 与 AGENTS 单指针接入；不建立第二事实源，不复制 `PROGRESS.md` 动态状态，与基线相比现有文档除最小治理记账外字节不变。documentation-only + 工具，不改 core/specs/conformance 机器合同 | done | 2026-08-12；D01–D04 on `personal/P8-T07-independent-handbook` / PR #202。54 文档 ×2 语言 + 9 生成族；checker 负例 15/15、`check-handbook`/`generate --check`/`--diff-base`/consistency/tools 49/49 本地全绿；required CI 于 PR #202 收口头（内容头 run `31572262460` 除浅克隆 HB013 外全绿，收口头带修复）。阅读基线 `9fbd390`、实现基线 `6b637a5`。Closure：`docs/checkpoints/20260812-personal-p8-t07-independent-handbook-closure.md`。无 Gate/release/Profile claim |
| P8-T08 | Handbook 同步强制化（commit/push/merge 前置义务与机器门） | P8-T07 | 把"开发后、提交/推送/合并之前同步文档系统"从建议升级为义务与机器约束：`docs/standards/docs-sync-contract.md` 增补全档位通用的 handbook 联动义务（§2）、CI/本地机器门登记（§5）与作者自查项（§6）；`tools/src/docs-sync-gate.mjs` 条件门（`--staged`/`--push`/`--range` 路由 source-map、命中即运行 `check-handbook` + `generate --check`、映射源改动而手册未同步时 fail-closed，唯一逃生口为显式 `DOCS_IMPACT_NONE=<理由>` 且须记入 PR）；仓库内 `.githooks/` pre-commit/pre-push + `pnpm run hooks:install` 一次性注册 + `check:docs-sync` 聚合脚本；rule 10/20 与 AGENTS.md 检查点/收口协议同步纳入该义务；受影响手册页（sync-policy、ai.docs-impact、ai.validation-commands、contributing、coverage/source-map/allowlist）同批更新。义务语义唯一 owner 为 docs-sync-contract；不新建第二工作流，不改 core/specs/conformance 机器合同 | done | 2026-08-12；D01–D03 on `personal/P8-T08-docs-sync-enforcement` / PR #203。gate 路由/负例 7/7（tools 56/56）、gate 对自身暂存集自验绿、`check-handbook`/`generate --check`/consistency/`git diff --check` 全绿；required CI 于 PR #203 merge head。Closure：`docs/checkpoints/20260812-personal-p8-t08-docs-sync-enforcement-closure.md`。无 Gate/release/Profile claim |
| P8-T09 | DeepSeek Harness candidate-only AKP adapter | P8-T02, P8-T03, P2-T36, P2-T37 | Pin exact dsh revision `528c682e061696f5a160f363f236ecbf53cbd006` and the AKP request-envelope schema digest; fence sessions and sequences; reject authority-shaped and secret-shaped payloads; JSONL/HTTP bounded transport; daemon `POST /task/akp/dsh` maps Workspace* candidates onto existing public candidate admission; linux-002 real DeepSeek Flash read/search/mutation through Intent/Effect/verification/acceptance. dsh is never an authority writer and a dsh response never completes a Task. | done | 2026-08-22 PR [#254](https://github.com/agentkernel/cognitive-os/pull/254). Jump-host Rust pass at `9e239b75`. linux-002 real dsh Workspace* + Flash **pass** 2/2 at `5b1c2279`. Shim E2E + live fail-closed matrix pass. Clean-runtime restart `INACTIVE` pass. Dirty-runtime restart fail (scheduler lease conflict; out of adapter slice). Required CI `32499728681` pass at docs HEAD `abff112c`. Report: [P8-T09 validation](../checkpoints/20260821-dsh-akp-linux-002-validation.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |
| P8-T10 | Install dsh as the Personal product agent path | P8-T09 | `cognitive dsh configure` pins exact dsh revision `528c682e061696f5a160f363f236ecbf53cbd006` and a candidate-only adapter digest; `cognitive dsh launch` is the installed Path B after daemon-owned ready state (Pi may stay `not_configured`). linux-002: real WorkspaceRead/Search and disposable Write through Intent/Effect/verification/acceptance, plus same-host Path A vs Path B timing (n≥5). Path A is measurement-only. A dsh response never completes a Task. | done | 2026-08-22 PR [#256](https://github.com/agentkernel/cognitive-os/pull/256). linux-002 installed Path B Workspace* `COMPLETED` + Flash `pong` at `4fbffc24`. Same-host n=5 Path A p50 10628 ms vs Path B p50 10800 ms (overhead 172 ms). Cleanup pass. Required CI `32522937342` at `a4b0ad09`. Report: [P8-T10 report](../checkpoints/20260822-personal-p8-t10-dsh-installed-agent-report.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |
| P8-T11 | Provider streaming, dsh OS runtime inspect, real-task A/B | P8-T10 | Public `POST /provider/v1/chat/completions` forwards `stream:true` as SSE and must not wait for a unary JSON body; Path B no longer uses an SSE-to-unary bridge; Pi and private-candidate stay unary. `GET /personal/dsh/runtime` + `cognitive dsh status` observe session/process/lifecycle. linux-002: raw Provider vs Path A vs installed Path B, real-task corpus (read/search/write + non-`pong` LLM), n≥5 retained. Fail-closed version/digest/session/secret/authority pins unchanged. A dsh response never completes a Task. | done | 2026-08-22 PR [#257](https://github.com/agentkernel/cognitive-os/pull/257). linux-002 at `4b191740`: P8-T10 ~10.5 s was tsx-from-source on the 2 vCPU guest; compiled-lib Path A p50 3181 ms vs Path B 3654 ms; raw 1056 ms. Runtime inspect ACTIVE/CRASHED/INACTIVE. Cleanup pass. Required CI `32551050984` at `849c01a5`. Report: [P8-T11 report](../checkpoints/20260822-personal-p8-t11-provider-stream-dsh-runtime-report.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |
| P8-T12 | Resource Manager common envelope | P2-T02, P2-T05, P4-T05, P2-T25 | Informative product/architecture pairing; management `GET/POST /management/resource/v1/{list,inspect,bind,unbind,enable,disable,revoke}`; task-channel and generic create/install/execute/complete fail closed; list/inspect from authority stores without invented rows; mutating dispatcher onto existing Skill/Tool sinks; `cognitive resource` caller; focused negatives; exact-revision `DEV-LINUX-NATIVE-01` 真机实测. Watch stays on `/resource/v1/watch`. No public generic Resource DTO. | done | 2026-08-22 PR [#258](https://github.com/agentkernel/cognitive-os/pull/258). Linux `DEV-LINUX-NATIVE-01` at `1adbdd13`: focused kernel-server **3/3**, admin-cli parse **1/1**, Clippy `-D warnings`, fmt. Required CI `32561124182` passed Ubuntu, Windows, and required-ci. Report: [P8-T12 report](../checkpoints/20260822-personal-p8-t12-resource-manager-report.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |
| P8-T13 | Owner-local LLM Provider Control Plane | P1-T02, P1-T03, P1-T04, P1-T06, P1-T07, P8-T09, P8-T10 | Named OpenAI/Anthropic/OpenAI-compatible accounts; Secret Store opaque refs; explicit endpoint trust; discovered+manual catalog; fixed pi/dsh bindings; usage/cost/audit; observe-only budgets/alerts; `cognitive provider`, `cognitive agent binding`, `cognitive usage`, `cognitive budget`, `cognitive alerts`, and `cognitive audit` callers. Maps design cards PCP-T01..PCP-T07. No fallback/routing/OAuth/UI. Pi and DeepSeek harness are independent adapter paths. | done | 2026-08-23 PR [#259](https://github.com/agentkernel/cognitive-os/pull/259). Linux `DEV-LINUX-NATIVE-01` at `6256f723`: endpoint_trust 1/1, store 8/8, kernel-server 3/3, admin-cli parse 1/1, workspace Clippy `-D warnings`, fmt. Required CI `32582360429` passed Ubuntu, Windows, and required-ci. Report: [P8-T13 report](../checkpoints/20260822-personal-p8-t13-provider-control-plane-report.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/provider-quality/Agent-benefit claim. Live Secret Store rotate/remove and live Pi/dsh qualification **not-run**. |
| P8-T14 | Provider Control Plane operator usage docs | P8-T13 | Bilingual handbook operator usage for the shipped daemon API and `cognitive` CLI (no Web/desktop panel); source-map/manifest; related user/reference pointers. Documentation-only; does not reopen P8-T13. | done | 2026-08-23 PR [#260](https://github.com/agentkernel/cognitive-os/pull/260) merged at `main@a2b8ddb3`. Required CI `32609687956` at `7694f747` passed Ubuntu, Windows, and required-ci. Closure: [P8-T14 closure](../checkpoints/20260823-personal-p8-t14-provider-control-plane-docs-closure.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/provider-quality/Agent-benefit claim. |
| P8-T15 | Native dsh Web UI control panel | P8-T10, P8-T11, P8-T14 | `cognitive dsh web` starts native `dsh --profile web --no-open` on loopback `http://127.0.0.1:3080` (not Personal `/ui/`); frontend dist fail-closed; Path B AKP+SecretStore preserved; headless `--print` unchanged. linux-002 `ss` + `GET /` HTML. Apply Cos binding to running web (`op=apply` + remap). Native Models follow dsh-bound catalog. | done | 2026-08-23 owner delivery; merged PR [#265](https://github.com/agentkernel/cognitive-os/pull/265) at `main@562d2a5d` (2026-08-24). Lease closed. Closure: [P8-T15 closure](../checkpoints/20260824-personal-p8-t15-dsh-native-web-panel-closure.md). Claim ceiling `hypothesis`. No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |

### Phase 9 - 性能与结构演进（工程健康候选池）

Phase 9 是实现层演进候选池：不新增产品能力与 Gate，出口以 P7-T04 已建立的模块
回归地板与治理路径 stage 计时为前后对照证据。

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P9-T01 | 异步事件底座演进 | P8-T01 | 以 P7-T04/D02 治理路径 stage 计时区分"治理税/实现税"为决策门：若连接/open/锁竞争主导 p95，分阶段将 HTTP/watch/sidecar 流层迁移到异步 runtime，权威 SQLite 写路径保持单写者语义；否则记录保守优化结论 | done | 2026-08-12；D01 produced the hypothesis-only `conservative-no-migration` decision: the aggregate authority `effect_persistence` stage is not separable transport evidence. Exact native Linux focused tests passed at `826745c`; CI recovery added bounded socket reads and serial loopback-daemon execution after a Windows test stall. Final CI `31540218963` passed on PR #197 head `498852f`, which merged at `main@af64b85`. Closure: `docs/checkpoints/20260811-personal-p9-t01-async-decision-gate-closure.md`. No async migration, Gate, release, or Profile claim. |
| P9-T02 | 权威路径结构债拆分 | — | 拆分 `scheduler_authority.rs`、`sqlite.rs`、`tool_executor.rs` 超大模块并外移内嵌测试；行为不变，以既有 focused tests 与回归地板做前后对照 | done | 2026-08-11；`lease/personal/P9-T02/structure-debt` on `personal/P9-T02-structure-debt` / PR #192. D01–D04 closed. Linux evidence through `a11d0bd`; required CI `31470278984`. Closure: `docs/checkpoints/20260811-personal-p9-t02-structure-debt-closure.md`. No Gate/release/Profile claim. |
| P9-T03 | 存储访问与组合根优化 | P9-T02 | 消除每请求 `SqliteAuthorityStore::open` 的长生命周期 store（保持单写者与 fail-closed 语义）；Personal 垂直逻辑从 `kernel-server` 组合根下沉；以 stage 计时对照验证 | done | 2026-08-11；`lease/personal/P9-T03/store-composition` on `personal/P9-T03-store-composition` / PR #193. D01–D04 closed. Linux evidence through `648e69f`; required CI `31476761080` on `64f89cd`. Closure: `docs/checkpoints/20260811-personal-p9-t03-store-composition-closure.md`. No Gate/release/Profile claim. |
| P9-T04 | 全面性能与真实 Task campaign | P7-T04, P8-T05, P9-T01, P9-T03 | measurement-only correlation/timing/usage/transport/resource/evidence runner；在单一 preregistered B01 campaign 中完整执行 L1--L5，保留完整分母、独立 verifier、报告和 cleanup。不得建立第二 authority writer、伪造 TTFT 或 Provider usage；L5 不达标也必须完成 non-claim report 和收口 | done | 2026-08-12；D01–D09 完成，`L0`–`L3` 全部执行，`L4` `T1` admission 部分执行，`L5` 经 owner disposition 关闭为 `not-run`。closure/final report：`docs/checkpoints/20260812-personal-p9-t04-performance-campaign-closure.md`；preregistration 记录全部 cell digest。全部安全计数为 0，claim 上限 `hypothesis`，`verifier=not_reviewed`，无 Gate/release/Profile/B01/Agent-benefit claim。 |
| P9-T05 | daemon 集成测试就绪等待可靠性 | — | 已确认的 required-CI 可靠性缺陷：`p1_t05_personal_readiness` 等 daemon 集成测试用各自复制的固定 2 s（100×20 ms）预算等待 bootstrap secret，一次约 2.2 s 的 GitHub Windows runner 启动即把 required check 判红（同 run 151 个单元测试全过，同 revision 重跑即过），且该脆弱性会持续误伤所有并行分支。收敛为唯一的有界就绪轮询：短间隔、宽裕但有界的上限、真超时 fail closed 并给出可诊断信息，同时新增"daemon 已退出"早停，避免抬高上限反而掩盖真实挂死。测试断言与语义不得削弱，产品行为不得改变 | done | 2026-08-14；`personal/P9-T05-ci-readiness-wait` / PR #213。D01 failure-first CI `31717595506` 在 Ubuntu/Windows 均按设计证明固定 2 s 无法容纳 2.5 s 健康启动；D02 将八个复制/无界等待收敛为 20 ms 间隔、60 s 明确 deadline 的共享 poll，并在 child 提前退出时立即报告真实 exit status。慢启动、secret 永不发布、端口永不监听、进程提前退出四类负例在 exact `543f3f49e39b982dc1a98d440933064d05b93286` 的 required CI `31728481070` 双平台通过；Windows Rust workspace test step 通过。`cargo fmt --check`、consistency、docs-sync 与 diff check 通过。仅测试夹具可靠性；无产品、Gate、release、Profile、B01 或性能改善声明。运行报告：`docs/checkpoints/20260813-personal-p9-t05-readiness-wait-validation.md`。 |
| P9-T07 | Pi route 嵌套 per-request stage 计时与 Provider usage 暴露 | P9-T04 | 单次授权 Pi 尝试产出一条精确 schema 观测：成功请求含七个 joined、单调、互不重叠的阶段；取消/错误请求保留实际测得的 Pi 阶段前缀、终态、失败类别与固定 `non_streaming` 模式。opaque correlation id 按请求连接 Pi/daemon 并隔离并发；Pi 使用 Node 单调时钟、daemon 使用 Rust `Instant`，禁止跨域相减。Provider usage 仅由认证 daemon 响应解析器产生，绑定 correlation、冻结且全进程单次发布；缺失/不一致/自报/重放一律 `not_available` 或拒绝。instrumentation 默认关闭、content-free、非权威、不成为第二 writer，不为测量增加网络请求 | done | 2026-08-14；corrective renumber 保留 PR #216 与 legacy branch，不改写历史。Accepted implementation `20f2636ea34619e3d01d870326dc8ac5e8678265`：本地 Pi 90/90；exact native daemon `554c6cf9` route 9/9、timing 1/1、front door 2/2、fmt/Clippy；required CI [31732673976](https://github.com/agentkernel/cognitive-os/actions/runs/31732673976) Ubuntu/Windows pass；personal/handbook/generator/consistency/docs-sync green。Closure：[20260813-personal-p9-t07-pi-nested-stage-timing-closure.md](../checkpoints/20260813-personal-p9-t07-pi-nested-stage-timing-closure.md)。`EVAL-003/PI-NESTED` `not-run`（无冻结计划/runner/report），无 +1.8 s 归因、Gate/release/Profile/B01/benchmark claim。 |
| P9-T06 | readiness SecretStore 一次评估内合并 | P2-T11, P2-T15 | 一次 `status`/`doctor` 评估只绑定一次 SecretStore：secret 探针与 provider `secret_ref` 解析共用该后端，探针已证明不可答时不再 `get`，材料立即丢弃，且不跨请求缓存就绪（无 stale-ready TTL）。不得缓存 secret 材料。不改变公开错误码与 fact 词汇 | done | 2026-08-14 owner-directed corrective：评测实测 status/doctor 在 P2-T11 真实解析后约 1.8 s，根因是一次评估内重复 `select`/`probe`/`get`。Merged via PR #218 at `main@56e5b2d6`，required CI 双平台绿。无 Gate/release/Profile claim。 |
| P9-T08 | C1/C2 paired-benchmark readiness instruments | P2-T36, P2-T37, P2-T23, P2-T24, P2-T14, P2-T21 | Deliver the remaining conditions to start complete real-machine paired C1+C2: secret-safe pure-Pi P-arm (broker + equivalent Workspace* fixture adapter), frozen runner/corpus/oracle/redactor/seeds/`retry=0`/digest ledger, checkable B0 fairness contract, B01-Desktop-Linux-002 isolation procedure, `--reuse-existing-secret-binding` runbook, rewritten C1/C2 confirmatory gates, environment-checklist exit, leftover-branch housekeeping, and a new EVAL preregistration with no samples. Instruments are measurement-only and must not become a second authority writer. If P-arm schemas cannot be cloned without daemon authority, fail closed and register a P2 task rather than daemon-proxy as pure Pi. Out of scope: live EVAL B0/B1/B2 samples (separate owner-activated campaign). | done | 2026-08-20 merged PR [#247](https://github.com/agentkernel/cognitive-os/pull/247) at `main@f24c282e`. Linux Secret Service get at `7dc8c999`; required CI `32316092401`. EVAL-012 reserved, not active. Navigation: [PERSONAL-C1-C2-READINESS-DELIVERY-PLAN.md](PERSONAL-C1-C2-READINESS-DELIVERY-PLAN.md). No Gate/release/Profile/B01/EVAL/Agent-benefit claim. |
| P9-T09 | C1/C2 P/O system_task_prompt_bytes fairness (EVAL-012 Priority 2) | P9-T08 | EVAL-012 B0 fairness failed only `system_task_prompt_bytes` (P short campaign instruction vs O CognitiveOS Extension session). Dry-run must not forge equality with placeholder `frozen-c1-c2-prompt-v1`. Both arms must observe the byte length of one frozen UTF-8 prompt file, and that file must be the prompt P-arm sends. Live O-arm injection of the same bytes remains required before a new EVAL. Out of scope: B0 samples, replacement-bytes Patch, private-candidate epoch-1 skips. | done | Merged PR [#249](https://github.com/agentkernel/cognitive-os/pull/249) at `main@bf6d5ed0`. Local `c1_c2_paired_p_arm` **15/15**. Required CI `32335402680` at `e9bdd070`; docs-head `32341567321` attempt 2 at `692c6e8a` (Windows A7 flake rerun). Live O-arm injection **not-run**. Report: [P9-T09 validation](../checkpoints/20260820-personal-p9-t09-system-prompt-fairness-report.md). Claim ceiling `hypothesis`. |
| P9-T10 | Live C1/C2 P/O `--append-system-prompt` injection (EVAL-012 Priority 2 remainder) | P9-T09 | P9-T09 froze dry-run observation of `frozen-system-task-prompt.txt`. Live O-arm `cognitive pi launch` must forward `--append-system-prompt <absolute existing non-empty file>` to Pi after `--print`; live P-arm uses the same flag and file without loading the CognitiveOS Extension. Relative, missing, and empty files fail closed. File bytes are not printed. Out of scope: a new EVAL, B0 samples, replacement-bytes Patch, private-candidate epoch-1 skips. | done | Merged PR [#250](https://github.com/agentkernel/cognitive-os/pull/250) at `main@30c4e16a`. Local `c1_c2_paired_p_arm` **16/16**. Linux exact `745c60d8` admin-cli `append_system_prompt` **3/3**, print-mode **1/1**, Clippy `-D warnings`, fmt. Required CI `32350029054` at `745c60d8`; docs-head `32352242187` at `36fe0029`. Report: [P9-T10 validation](../checkpoints/20260820-personal-p9-t10-live-prompt-injection-report.md). Claim ceiling `hypothesis`. |
| P9-T11 | Comparable C1/C2 P/O WorkspacePatch unified-diff payload (EVAL-012 Priority 3) | P9-T10 | EVAL-012 C2a P-arm Patch passed via replacement bytes while O-arm production Patch applies a strict unified diff. P-arm must apply the same UTF-8 unified-diff `input_b64` the daemon would accept, refuse replacement bytes and invalid diffs, pin `workspace_patch_payload: unified-diff` in the freeze ledger, and include `fixtures/c2a/workspace-patch.unified.diff`. Out of scope: a new EVAL, B0 samples, private-candidate epoch-1 skips, changing the daemon patch applier. | done | Merged PR [#251](https://github.com/agentkernel/cognitive-os/pull/251) at `main@49b66200`. Local `c1_c2_paired_p_arm` **19/19**; tools suite **83/83**. Required CI `32359826035` at `6fe018cf` passed Ubuntu, Windows, and required-ci. Report: [P9-T11 validation](../checkpoints/20260820-personal-p9-t11-c2a-patch-unified-diff-report.md). Claim ceiling `hypothesis`. |
| P9-T12 | Live C1/C2 paired executor (EVAL-013 Priority 1) | P9-T11 | Closed measurement on `evaluation/EVAL-013-freeze` left B1/B2 `not-run` because `paired-runner.mjs` is dry-run fairness only (execution plan §2.5 forbids cobbling B0 shell into a formal paired campaign). Freeze a campaign-only live paired executor: injected `executeArm`, required `--append-system-prompt` absolute freeze file, deterministic arm order, `retry=0`, fairness-before-count, `counted_sample` only for b1/b2 when both arms complete. Dry-run cannot be labeled counted. Out of scope: a new EVAL, B01 samples, live Provider cells, changing freeze default binds. | done | Merged PR [#252](https://github.com/agentkernel/cognitive-os/pull/252) at `main@39cf8019`. Local `c1_c2_paired_p_arm` **24/24**; tools suite **88/88**. Required CI `32370316101` at `a7b09edd` passed Ubuntu, Windows, and required-ci. Report: [P9-T12 validation](../checkpoints/20260820-personal-p9-t12-live-paired-executor-report.md). Claim ceiling `hypothesis`. |

### Phase 10 - Personal 2.0 desktop Control Plane 与 MCP family

Phase 10 按“先语义、再合同兼容、再 authority integration、最后 desktop experience”
推进。它不新增 Gate，不改变 finalized Personal 1.0 六 family 或任何既有 evidence。

| ID | 工作项 | 依赖 | 验收摘要 | 状态 | 证据/备注 |
|---|---|---|---|---|---|
| P10-T01 | Personal 2.0 desktop + MCP semantic adoption | owner-accepted scope；ADR-0037/0043/0055 | 接受 ADR-0056/0057；ADR-0037 partial backlink；desktop IA / candidate-only global Shell / vendor conversation capability matrix / native Agent app coexistence / MCP seventh-family routing / ADR-0055 `Requires-backend` 在 canonical product、plan、trace、support、version 与 current snapshot 一致；documentation-only，不改 machine contract | in-progress | `P10-T01/D01` 与 `D02` done；`P10-T01/D03` validation/docs-sync/acceptance closure active on `personal/P10-T01-desktop-mcp-semantics` with `lease/personal/P10-T01/desktop-mcp-semantics`。无 implementation/Gate/release/Profile claim |
| P10-T02 | Lane-CTR contract and compatibility decision | P10-T01 | 决定 MCP family 与 common conversation projection 的 public/private contract boundary、identity/version/capability/binding/health/quarantine compatibility、P5-era migration 与 older-client fail-closed；需要的 schema/generated bindings/negatives 只在 Lane-CTR 决策后同批交付 | not-started | unclaimed；不创建 Gate |
| P10-T03 | MCP family authority and product integration | P10-T02, P5-T03, P5-T04, P8-T12 | daemon-only seventh-family identities/lifecycle；Tool candidate、Context/Skill admission、SecretStore、Intent/Effect、fencing、quarantine/requalification/reconcile 与 focused negatives；不得建立 generic Resource authority | not-started | unclaimed；既有 P5/B10 evidence 不等于本任务实现 |
| P10-T04 | Desktop Control Plane experience | P10-T02, P10-T03, P7-T05, P8-T13 | desktop primary entry；Home/Agents/Work/Library/Activity/Settings；Providers/System under Settings；global Agent Shell candidate-only；vendor-specific conversation adapters behind honest common capability matrix；native Agent apps 可用；缺 backend/import 显示 `Requires-backend` | not-started | unclaimed；现有 Web UI/Agent evidence 不自动构成 Personal 2.0 support |

Phase 10 只使用上述 task acceptance；**不登记 G10、B13 或任何新 Gate**。

## 5. Gate 与证据要求

| Gate | 必需结果 | 最低证据 |
|---|---|---|
| G0 | 基线、决策和 PoC 已完成或明确 NO-GO | ADR、PoC report、支持矩阵、handoff |
| B01 | 安装、初始化、Provider、daemon、Pi 首次响应 | successor `002` 固定 N=6 clean Linux VM outcomes；全部六个 counted outcomes 入 denominator；至少 5 次成功；zero critical；aggregate median/p95/CI；affirmative independent verifier |
| B02/B04/B05/B12 | 管理、Task/Tool、恢复、unknown outcome 闭环 | ADR-0046 MVP fixed matrix + owner affirm; see P2-T08 closure |
| B03 | Context correctness | scope-before-ranking、required fail-closed、explicit loss、revocation/cache/Artifact access negatives；收益不是 pass 前提 |
| B06/B07 | Context/Loop benefit observations | delta/stable-prefix/cache/telemetry raw evidence；采集但不阻塞 Linux 1.0 |
| B08 | Memory + Skill lifecycle 与 actual consumption | provenance/freshness/conflict/forget、package/revision/binding，以及同一 Task trace 的真实 Context/Task consumption |
| B09 | Managed Pi + sidecar lifecycle qualification | exact Agent/sidecar package与 protocol/adapter/instance pins；acquire/install/register/health/activate/pause/resume/upgrade/rollback/stop/uninstall/recover；identity isolation；只支持 Pi |
| B10 | Post-1.0 MCP/dynamic Tool qualification | MCP/dynamic package/manifest、drift/timeout、enable/disable/quarantine/reconcile、sandbox/bypass negatives；不阻塞 Linux 1.0 |
| B11 | Post-1.0 Multi-Agent benefit | 相同模型/预算/任务的 single-Agent baseline；默认关闭且不阻塞 Linux 1.0 |
| GMVP-LINUX | Personal `1.0.0` Linux x86_64 可发布 | benchmark composition **精确为 B01+B02+B03+B04+B05+B08+B09+B12**；P7-T08 acceptance 另汇合 production trust/native systemd/desktop-headless SecretStore/六资源 manifest/UCR-01 fixed-scenario assertions/lifecycle/backup/doctor evidence；不新增第二 release Gate |
| RC | 完整发布声明 | CI、SBOM、attestation、升级/卸载、支持矩阵 |

### UCR-01 cross-Gate preregistered workload（不是新 Gate）

[UCR-01](../evaluation/personal-unified-cognitive-resource-workload.md) 在同一个 Task trace 中
使用 Memory、Skill、Tool、Context、Task、Runtime 六类最小真实资源。它可贡献 B02、
B03、B04、B05、B08、B09、B12，但仅当对应 Gate **分别**绑定自己的 preregistration、
qualified environment、threshold、failure accounting、evidence collector 与 independent
verifier。一次 UCR-01 run 不自动 pass 多个 Gate，某 Gate 的证据也不向其他 Gate 继承；
UCR-01 不创建 B13、第二个 release Gate 或新的 task ID。

其中 required recall、unauthorized/stale exposure、Skill reuse、duplicate Effect、false
completion、stale epoch，以及 stable/changed Context 相对 full replay 的 `>=20%` 重复输入
token 降低且 verified completion 不下降，是该固定 workload 的 P7-T08 acceptance assertions。
这不等于 B06/B07 Gate 阻塞，也不能据此产生跨 W1/W2 的一般 Agent-benefit claim。

### Formal campaign preregistration minimum

B01、B02/B03/B04/B05/B08/B09/B12、GMVP-LINUX 与后续产品 Gate 的 preregistration 必须在
attempt 开始前固定：formal-plan revision/digest、campaign/Gate ID、exact OS image 与
reset、source/artifact/signature/SBOM/attestation、Node/Pi/package/SRI/adapter digest、
SecretStore/operator opt-in、workload/attempt denominator/threshold、所有失败计入规则、
evidence collector/redaction/cleanup、operator 与 independent verifier、允许 claim 和
non-claims。Handoff/attempt ledger 不得覆盖本表 threshold；不一致必须 fail closed 并走
`product-semantic` 修订。环境明细见 [PERSONAL-TEST-ENVIRONMENTS.md](PERSONAL-TEST-ENVIRONMENTS.md)。

B01 的历史统计解释见
[2026-08-02 addendum](../checkpoints/20260802-personal-p1-t09-b01-statistical-interpretation-addendum.md)。
该 addendum 只约束已保留的失败 campaign `001`（其历史 N=20、成功率 ≥90% 与
zero-critical 口径）。现行 B01 结论由 ADR-0039 的 successor `002` 政策（固定六个
counted outcomes、至少 5 次成功、zero critical、完整统计与 affirmative independent
verifier）决定，并已按上方 Gate 表记为 `pass`；`001` 的口径不再适用于任何新 claim。

Windows install parity 声明需要 P7-T07 的专门 B01-W Gate 与已执行证据；在此之前，
RC 与支持矩阵中的安装声明仅覆盖 Linux bundle（Windows 仅为 daemon/CLI 产品路径，
按 ADR-0025 表述）。

## 6. 实施时的文档联动清单

- [ ] 更新本文件的任务状态、日期和证据/阻塞说明。
- [ ] 更新 `docs/plan/PROGRESS.md` 的全局状态（不将 planning 任务误报为已实现）。
- [ ] 如有合同、schema、vector 或 generated binding 改动，遵循 Lane-CTR 与 docs-sync 流程。
- [ ] 如有开放风险或漂移，更新 `docs/traceability/findings-ledger.md`。
- [ ] 写入 `docs/checkpoints/` handoff，记录完成项、未完成项、测试、证据、风险与下一步。
- [ ] 在 PR/提交中关联 `PERS-*` 计划 ID，并在适用时关联真实 REQ-ID。
- [ ] 如发生计划结构变化（新增/取消任务、验收或依赖变化），在同一修订批中同步 `docs/plan/plan.md` 任务卡与依赖图、`personal-trace.yaml` 映射，保持无孤儿任务。
