# Former 17-item AUDIT blocker reclassification

| Prior matrix item(s) | Ordinary Core ruling | Evidence / next gate |
|---|---|---|
| 1–5 record, port, terminal fields, minimization | Core candidate required | frozen here; real tracer/product consumer closes the former “no consumer” premise only for Core |
| 7–10 receipt ordering, persistence failure, digest rules | Core candidate required | durable adapter, release gate, repository-computed digests |
| 15 real consumer | partially closed for Core | public audited management API and `admin-cli`; not an external third-party consumer |
| 16 error responsibility | Core candidate required | narrow `STATE_STORE_UNAVAILABLE` reuse ruling; registry unchanged |
| 17 final bytes/digests | Core candidate required | exact review payload manifest; isolated final-byte technical review accepted for Ordinary Core only |
| 3, 6, 11–14 stream graph, partition, checkpoints/signing, retention/redaction/export, DAG/freeze epoch | High-Assurance deferred or later registration | not required to block this Core candidate freeze |

The file adapter does not prove independent deployment, third-party consumption,
cryptographic non-repudiation, or full AUDIT-family registration.
