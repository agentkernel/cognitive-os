# P13-T02 Hosted DSH real Attempt loop — running report

- Task: `P13-T02` / slices `P13-T02/D01` → `P13-T02/D02`
- Change class: `implementation-only` (daemon-owned stdio broker + real child spawn in `cognitive-runtime`; v36 Attempt / frame / artifact-fact ledger in `cognitive-store`; management HTTP `dsh.hosted.attempt.*` routes; minimal `runs` read; no `core/specs`, no Lane-CTR, no new first-level chrome, no Pi Member engine)
- Product: CognitiveOS Personal 2.0.0
- Lease: `lease/personal/P13-T02/hosted-dsh-attempt`
- Branch: `personal/P13-T02-hosted-dsh-attempt` (worktree `D:\agent-kernel-wt-p13-t02`; original `d:\agent-kernel` untouched, A8 protected)
- Base: `origin/main@a04656531b280d75ce4058022466fb0234c07083`
- Claim ceiling: `hypothesis` (A7: local / CI / Linux native is not Gate / release / Profile; Linux real spawn closes "implementation exists" only; Windows sandbox / ACL / supply-chain cells stay `not-run` until `P13-T13`)
- Evaluation routing: **OFF**

## Unique next action

Closure: formal plan / PROGRESS / trace / plan.md → `P13-T02` `done` (D01, D02 done), closure doc, required CI green on the to-be-merged HEAD, `gh pr ready 310` → merge → lease closure on `main`. After merge the dependents `P13-T04/D01`, `P13-T05/D01`, `P13-T06/D01` (with `P13-T03/D01`) and the non-mutex `P13-T08/D02` engine facts become claimable.

## Identifier

Hidden engine pin `cognitiveos.personal.hidden-hosted-dsh/0.1`; exact artifact `528c682e061696f5a160f363f236ecbf53cbd006` (= `DSH_PACKAGE_REVISION`); Path B proxy `POST /provider/v1/dsh/chat/completions` remains the only secret-bearing path. Reused, not rebuilt: P11-T07 `p11_hosted_dsh_child` identity + `runtime_binding_ref`; P2-T06 supervisor seam shape (PID ownership / timeout / orphan); P11-T14 persist-before-dispatch ledger shape.

## Failure-first (D01)

| ID | Negative | Surface |
|---|---|---|
| N1 | process death ≠ completion (exit 0, non-zero, signal/kill, timeout all land as `terminal_kind` ≠ success; `completion_claimed = 0`; `verification_status = not-run`) | runtime broker + store attempt ledger |
| N2 | unknown / free-text / `ok` / `{"status":"success"}` child output ≠ success (unknown lines counted, never promoted) | runtime broker + store |
| N3 | secret never in child env / argv (secret-shaped key/value, `sk-`, bearer, `ssv1:` refused before spawn) | runtime broker pre-spawn checks |
| N4 | child direct Provider refused (Provider host / base-URL / `--api-key-file` / `--provider-path a` in launch plan refused; child `provider_request` / non-loopback URL frames rejected and recorded) | runtime broker |
| N5 | native MCP / base tool / HMR / home patch refused | runtime broker pre-spawn checks |
| N6 | bounded Context payload (oversize refused; digest bound into request frame) | runtime broker |
| N7 | heartbeat frames never write authority; observation frames never advance Task state | store attempt ledger (frames are observations) |
| N8 | task channel cannot run / observe attempts (403); artifact digest mismatch refused (422); GNU fence recorded | kernel-server routes |
| N9 | persist-before-dispatch: attempt row exists before spawn; crash-shaped `persisted`-not-dispatched row reconciles to `unknown-outcome`, never success | store |

## Incremental validation log (TEST-REPORT-INCREMENTAL-01)

Units are appended **immediately** after each finishes. `not-run` is never pass.

| Time | Unit | Result | Env | Revision | Notes |
|---|---|---|---|---|---|
| 2026-09-03 | Lease claim + PROGRESS / formal plan / plan.md status → `P13-T02` in-progress, `P13-T02/D01` in-progress | recorded | docs-only (`DEV-WIN-GNU-01`) | worktree, uncommitted | `git fetch origin` failed twice (local proxy TLS handshake, `github.com` → fake-ip `198.18.0.10`); worktree created from local `origin/main@a0465653` (== local `main`); will re-fetch before first push |
| 2026-09-03 | Resume after aborted worker: inherited edits reviewed (broker, lease row, plan/PROGRESS claim); lease writable paths trimmed to the real change set; `git fetch origin` / `gh auth status` / `ssh git@github.com` all fail on this host (proxy fake-ip `198.18.0.10`) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | Network fault is recoverable, not a blocker: implementation continues; push/PR retried later (fallback: exact-revision bundle pushed from `DEV-LINUX-NATIVE-01`, which reaches GitHub) |
| 2026-09-03 | D01 failure-first tests written: store `p13_t02_hosted_dsh_attempt.rs` (9 tests: artifact facts health/update/rollback, unhealthy refuses spawn, persist-before-dispatch → terminal, process death ≠ completion incl. schema CHECKs, unknown output ≠ success, frames never write authority, crash-shaped rows → unknown-outcome, bounded/secret-free context, task channel/unseated/Pi refused); runtime `p13_t02_hosted_dsh_broker.rs` (7 tests with **real `node` children**: stdin Context + allowlisted env, exit 0/7/timeout-kill/spawn-failed ≠ completion, unknown output, direct Provider refused pre-spawn and in-stream, secret env/argv/native escape never spawn (marker file), bounded stdout, artifact observe/resolve health classes) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | `cargo test` is `not-run` here (`RUST-LINK-DEV-WIN-GNU-01`); first execution is CI + `DEV-LINUX-NATIVE-01` |
| 2026-09-03 | `node --test scripts/hosted-attempt-child.test.mjs` (product child script; fake pinned dsh CLI + fake loopback daemon) | **pass** 12/12 | `DEV-WIN-GNU-01` (Node 22) | worktree, uncommitted | Refusals: Path A / `--api-key-file` / `--mcp` / `--direct-base-url` exit 2 before spawn; non-request frame exit 2; `--revision` ≠ pin exit 3; pin-file drift exit 3; dsh CLI missing exit 4 (`dsh-cli-missing`, response `failed`); non-loopback origin / absent bootstrap / closed port exit 5. Full run: `child.started` (context digest) → `dsh.cli.compiled-lib` → `provider.proxy.bound` (model from `agent-bindings`) → `dsh.spawned` → observations → one `DeliverableDraft` candidate → `response done`, exit 0, bearer/bootstrap never on stdout, patch `baseURL` = loopback `/provider/v1/dsh`, dsh env has no secret-shaped keys. Slow dsh killed at budget → `failed / timed-out` exit 6; dsh exit 2 → `failed / dsh-exit-2`, text stays a candidate. First attempt hung: `spawnSync` blocked the fake daemon's event loop — test harness fixed to async `spawn` (test defect, not product) |

| 2026-09-03 | D01 + D02 implementation written: store v36 `hosted_dsh_attempt.rs` (artifact facts, Attempt Intent → dispatched → terminal, frames, unknown-outcome reconcile), `hosted_dsh.rs` `observe_spawn`, runtime broker (`env_clear` + allowlist, stdin request frame with `timeout_ms`, Unix process-group kill, `HostedDshArtifact::observe/resolve` + child-script digest, `ledger_frames`), kernel-server `hosted_dsh_attempt.rs` routes (`attempt.run/list/detail`, `artifact.check/facts`; task aliases 403) + startup reconcile in `serve_personal_loopback`, product child `hosted-attempt-child.mjs` | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | vertical: management HTTP → store Intent → v31 child identity → real spawn → observations → daemon terminal |
| 2026-09-03 | `cargo fmt --all -- --check` | **pass** | `DEV-WIN-GNU-01` | worktree, uncommitted | after `cargo fmt --all`; formatting only, no link |
| 2026-09-03 | `cargo build/test/clippy` (store `p13_t02_hosted_dsh_attempt`, runtime `p13_t02_hosted_dsh_broker`, kernel-server `hosted_dsh_attempt::tests`) | **not-run** | `DEV-WIN-GNU-01` | worktree, uncommitted | `RUST-LINK-DEV-WIN-GNU-01`; routed to required CI + exact-revision `DEV-LINUX-NATIVE-01` |
| 2026-09-03 | `node tools/src/generate-handbook.mjs` (10 new `dsh.hosted.attempt.*` / `dsh.hosted.artifact.*` route annotations; `hosted_dsh_attempt.rs` added to definition sources) + `fill-handbook-fingerprints` (22 pages) | **pass** | `DEV-WIN-GNU-01` (Node) | worktree, uncommitted | `http-api` both locales regenerated; store-migrations (v36), daemon-and-http, agent-and-pi-lifecycle, capability-status hand-updated in en + zh-CN |
| 2026-09-03 | `node tools/src/check-handbook.mjs` | **pass** (58×2, 9 generated) | `DEV-WIN-GNU-01` (Node) | worktree, staged | first run reported HB006 for untracked new sources → new files `git add`ed (explicit paths), then OK |
| 2026-09-03 | `node tools/src/generate-handbook.mjs --check` | **pass** (18 pages byte-identical) | `DEV-WIN-GNU-01` (Node) | worktree, staged | — |
| 2026-09-03 | `pnpm run check:consistency` | **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | trimmed `lease/personal/P13-T02/hosted-dsh-attempt` row accepted; Personal plan/Gates/leases verified |
| 2026-09-03 | `pnpm -r build` / `pnpm -r test` | **pass** / **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | all packages `Done`; adapter suite includes the 12 hosted-attempt-child tests |
| 2026-09-03 | `git diff --check` (worktree + staged) | **pass** | `DEV-WIN-GNU-01` | worktree, staged | no whitespace errors |
| 2026-09-03 | Checkpoint commit + push; Draft PR [#310](https://github.com/agentkernel/cognitive-os/pull/310) opened | recorded | GitHub | `6e5caae4eba476589e97f1b6be5f9d55fe06d268` | network recovered; pre-commit docs-sync gate OK after regenerating fingerprints post-`cargo fmt` |
| 2026-09-03 | Required CI run [33671450531](https://github.com/agentkernel/cognitive-os/actions/runs/33671450531): `verify (ubuntu-latest)` + `verify (windows-latest)` | **fail** (1 test) | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `6e5caae4` | Rust workspace **built** on both platforms; runtime broker 6/7 pass, `p13_t02_child_direct_provider_is_refused_and_recorded` failed because the test's inline `node -e` argv carried `api.deepseek.com` / `Bearer ` literals and `validate_launch_plan` refused the spawn (`SecretMaterial{argv}`) — the product fail-closed check worked; test defect, fixed in `d2612bb5` (child assembles the strings at runtime) |
| 2026-09-03 | `cargo test -p cognitive-store --test p13_t02_hosted_dsh_attempt --locked` | **pass** 9/9 | `DEV-LINUX-NATIVE-01` `~/cognitiveos-personal-worktrees/p13-t02-6e5caae4` | `6e5caae4eba476589e97f1b6be5f9d55fe06d268` | artifact facts health/update/rollback; unhealthy refuses spawn; persist-before-dispatch → daemon terminal; process death ≠ completion (+ CHECK constraints reject `success`/`completion_claimed=1`/`verification_status='passed'`/DELETE); unknown output → `unknown`; frames never write authority (Employee/Project/stage-test/acceptance unchanged; append-only trigger); crash-shaped rows → `unknown-outcome`; context bounds + secret shape; task channel / assistant / unseated / stale revision / Pi refused. Worktree fetched from GitHub after one `curl 28` timeout retry (host-side transient) |
| 2026-09-03 | Test fix commit pushed | recorded | GitHub | `d2612bb590becef3e5ae140c16604794767fdf9c` | test-only; docs-sync gate `skip` |
| 2026-09-03 | Resume after a second aborted worker: `gh pr checks 310` → **no checks** on `d2612bb5` and PR `mergeable: CONFLICTING` (origin/main moved: P13-T12/D01 PR #308 `main@3680b742`, lease closure `main@84188aac`, docs-only, no migration) | recorded | GitHub / `DEV-WIN-GNU-01` | `d2612bb5` | GitHub could not build the PR merge commit, so no `pull_request` CI ran for the test fix; resolved by merging `origin/main` into the task branch (no rebase / no force push) |
| 2026-09-03 | `git merge origin/main` — 4 docs conflicts resolved (PERSONAL-DEVELOPMENT-PLAN status line + Phase 13 / 合计 counts → P13-T02 **and** P13-T12 in-progress: 167/132/2/1/16; PROGRESS snapshot heading, Active task lease row, Layer 1 table 167/132/2/1/16, Layer 1 prose; handbook `architecture-overview` en + zh-CN fingerprints regenerated); `fill-handbook-fingerprints` (2 pages) → `check-handbook` **pass** (58×2) → `generate-handbook --check` **pass** (18 byte-identical) → `check:consistency` **pass** → `git diff --check` **pass** | **pass** | `DEV-WIN-GNU-01` (Node) | merge commit (below) | sibling ledger rows integrated per PARALLEL-LANES rule 8; no sibling migration landed, v36 number kept |
| 2026-09-03 | Merge checkpoint pushed | recorded | GitHub | `053d25fa4f9b105319d6b06991ee0cb24432c0fd` | Draft PR #310 now mergeable; `pull_request` CI re-triggered |
| 2026-09-03 | Linux worktree moved to the pushed revision: `~/cognitiveos-personal-worktrees/p13-t02-053d25fa` (`git fetch` + `git checkout --detach`, `git rev-parse HEAD` = `053d25fa…`; cargo cache retained) | recorded | `DEV-LINUX-NATIVE-01` | `053d25fa` | Earlier remote exit 127 root cause: PowerShell 5.1 expands `$HOME` / `$PATH` inside a double-quoted `ssh … "…"` argument before ssh sees it, corrupting the remote PATH (`tail: command not found`). Mechanism now: remote command passed in **single quotes** so nothing expands locally |
| 2026-09-03 | `cargo test -p cognitive-runtime --test p13_t02_hosted_dsh_broker --locked` | **fail** 6/7 | `DEV-LINUX-NATIVE-01` | `053d25fa` | Real `node` children: stdin Context + allowlisted env, exit 0/7/timeout-kill/spawn-failed ≠ completion, unknown output ≠ success, bounded stdout, secret env/argv/native escape never spawn, artifact observe/resolve **pass**. `p13_t02_child_direct_provider_is_refused_and_recorded` still failed: the inline `node -e` script carried the literal `Authorization`, which `validate_launch_plan` refuses as secret-shaped argv (`SecretMaterial{argv}`) — product fail-closed check correct; test defect (token now assembled at runtime), re-run at the next pushed revision |
| 2026-09-03 | Test-only fix pushed; Linux worktree moved to `~/cognitiveos-personal-worktrees/p13-t02-f82bd437` (`git rev-parse HEAD` verified) | recorded | GitHub / `DEV-LINUX-NATIVE-01` | `f82bd4373657983b0744f4fd38e43bac0c9098c8` | docs-sync gate `skip` (test + report only) |
| 2026-09-03 | `cargo test -p cognitive-runtime --test p13_t02_hosted_dsh_broker --locked` | **pass** 7/7 | `DEV-LINUX-NATIVE-01` | `f82bd437` | real `node` children; direct-Provider frames (`provider_request`, Provider URL in candidate payload, `provider_direct:true`) all rejected and recorded as `child-direct-provider`; bearer redacted in the surviving observation; Path A / `--direct-base-url` / non-loopback origin refused pre-spawn |
| 2026-09-03 | `cargo test -p cognitive-store --test p13_t02_hosted_dsh_attempt --test p1_t01_layout_migrations --locked` | **pass** 9/9 + 8/8 | `DEV-LINUX-NATIVE-01` | `f82bd437` | v36 migrates from the previous fixture and from empty; replay-safe; digest-mismatch refuses later versions. Remote mechanism: local script → `(Get-Content … -Raw) -replace CRLF | ssh … "bash -s"` |
| 2026-09-03 | `cargo test -p kernel-server hosted_dsh_attempt --locked` | **pass** 2/2 | `DEV-LINUX-NATIVE-01` | `f82bd437` | HTTP handler → artifact health fact → Attempt Intent persisted → v31 child identity bound → **real `node` child spawned** → 7 ledger frames (2 rejected: `child-direct-provider`, `child-cannot-emit-authority-frame`; 1 unknown line `success` never promoted) → daemon terminal `exited/0`, `response_status=done`, `completion_claimed=false`, `verification_status=not-run`; crash → `exited/9`, `response_status=unknown`; hang → `timed-out` at 800 ms; task-channel aliases 403; pin drift → `mismatch` + `HOSTED_ARTIFACT_UNHEALTHY` 422; restore → history shows update + rollback; missing child script → 422; bearer / bootstrap bytes absent from every response |
| 2026-09-03 | **Live-daemon E2E setup** at the exact revision: `cargo build -p kernel-server -p admin-cli --locked`; disposable runtime root `~/cognitiveos-personal-worktrees/p13-t02-f82bd437-runtime` (0700); fixture seed (scratch crate **outside** the repo, depends on the worktree's `cognitive-store` by path, run **before** the daemon starts, daemon not running): G1 project → plan revision (2 stages) → roster → researcher seated; product CLI `cognitive dsh configure --runtime-root … --dsh-root ~/agent-kernel-worktrees/p8-t09-pins/dsh --adapter-root <worktree>/personal/packages/dsh-akp-adapter --revision 528c682e…` → `dsh.json` `candidate_only:true`, `lifecycle_state:active`; `kernel-server --personal --bind 127.0.0.1:48782 --runtime-root …` listening, bootstrap file mode 600 | **pass** | `DEV-LINUX-NATIVE-01` | `f82bd437` | Pinned dsh checkout = git HEAD = pin file = `528c682e061696f5a160f363f236ecbf53cbd006`, compiled lib present. Seeding is fixture setup only because no product HTTP/CLI path applies a PlanRevision yet (roster needs one) — gap noted for P13-T05/T06, not P13-T02 scope |
| 2026-09-03 | Live U1–U4: `artifact.check` → `health:pinned`, `kind:health-check`, `admits_spawn:true`, script digest 64 hex, `windows_supply_chain_e2e:not-run`; task-channel alias `POST /task/project/v1/dsh.hosted.attempt.run` → **403** `HOSTED_ATTEMPT_CHANNEL_FORBIDDEN`; no bearer → **401** `LOCAL_SESSION_UNAUTHORIZED`; `sk-live…` in bounded_context → **422** `HOSTED_ATTEMPT_INVALID`, value not echoed | **pass** 4/4 | `DEV-LINUX-NATIVE-01` (live daemon over loopback HTTP) | `f82bd437` | driver: Node script using the adapter's `daemon-task.mjs` `issueToken`/`httpJson`; bootstrap read from the file, never printed |
| 2026-09-03 | Live U5 **real Attempt**: `POST /management/project/v1/dsh.hosted.attempt.run` (seated researcher, 72-byte Context, `timeout_ms 90000`, `wait:true`) → daemon spawned the **product child** `hosted-attempt-child.mjs` → child minted a management session from the bootstrap *path*, wrote a 0600 credentials file in a private `DSH_HOME`, spawned the **real pinned dsh CLI (compiled-lib)** with the daemon proxy as `baseURL` → dsh called `POST /provider/v1/dsh/chat/completions` → Provider **unbound** in this disposable root → dsh stderr `provider proxy request was not completed`, dsh exit 1 → child `response failed / dsh-exit-1`, exit 6 → **daemon terminal** `exited/6`, `response_status=failed`, `completion_claimed=false`, `verification_status=not-run`, `intent_persisted=true`, `dispatched_at`+`terminal_at` set, `pid` cleared at terminal, `child_id dshchild-…`, `runtime_binding_ref hosted-dsh:…`; wall 17.5 s | **pass** (loop) / **partial** (Provider leg: unbound by design of this run) | `DEV-LINUX-NATIVE-01` | `f82bd437` | 8 frames all `authority_written=false`: `child.started`, `dsh.cli.compiled-lib`, `provider.proxy.bound`, `dsh.spawned`, 3 heartbeats (5 s cadence), `response failed`; `candidates=0` (dsh produced no text); `stdout_bytes=777`; `engine cognitiveos.personal.hidden-hosted-dsh/0.1`, `secret_bearer daemon-proxy-only`, `receipt_is_not_completion=true`, `windows_sandbox_e2e=not-run`. No Provider key was imported (owner boundary: new SecretStore entry) — a bound Provider would exercise the `done` leg; the loop itself is closed either way |
| 2026-09-03 | Live U6–U9: `attempt.detail` 8 frames redacted/bounded; `attempt.list?project_id=…` → 1 attempt, `any_success=false`, `receipt_is_not_completion=true` (the `runs` read); `artifact.facts` history 3 (`pinned`), `admits_spawn=true`; secret scan of every response body against the live bootstrap / management / task tokens and `Bearer <token>` → **0 leaks**; no leftover `p13t02-hosted-attempt-*` work dirs, no orphan child | **pass** | `DEV-LINUX-NATIVE-01` | `f82bd437` | pre-existing unrelated P8-T11 dsh web session (pid 2855824, other root) left untouched |
| 2026-09-03 | Live U10–U11 **daemon crash → reconcile**: `attempt.run wait:false` → row `persisted` (Intent before spawn) → child spawned → daemon `SIGKILL` 2 s later → restart same revision/root → startup log `1 hosted DSH attempt(s) reconciled to unknown-outcome` → `attempt.detail` → `state=unknown-outcome`, `terminal_kind=unknown-outcome`, `response_status=unknown`, `completion_claimed=false`, `pid=null`, `terminal_at` set, `frames=0` | **pass** | `DEV-LINUX-NATIVE-01` | `f82bd437` | **Known bounded limitation (recorded, not claimed):** the child runs in its own process group, so a daemon *crash* (not a timeout) leaves it alive until its own budget (`timeout_ms − 3 s`) or the daemon-unreachable failure; it was killed manually here (`pkill -f hosted-attempt-child.mjs`). Timeout path kills the whole group (no orphan). Cross-crash reaping is a supervisor concern (P2-T06 seam) not closed by this task |
| 2026-09-03 | Live E2E cleanup: daemon stopped, `…-runtime` and `…-seed` removed, `/tmp` has no `p13t02*` residue; worktree `p13-t02-f82bd437` retained until closure | **pass** | `DEV-LINUX-NATIVE-01` | `f82bd437` | secret-free records; no baseline / service / credential change on the host |
| 2026-09-03 | Required CI run [33676373077](https://github.com/agentkernel/cognitive-os/actions/runs/33676373077): `resolve validation route`, `verify (ubuntu-latest)` 3m53s, `verify (windows-latest)` 12m48s, `required-ci` | **pass** | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `f82bd437` | full workspace build/test/clippy incl. the three P13-T02 suites on both platforms (run 33675965648 on the merge commit `053d25fa` was superseded/cancelled by this push) |

| 2026-09-03 | Closure docs pushed as `c766007b` (acceptance mapping, live E2E units, closure record, plan/PROGRESS/trace/plan.md; `DOCS_IMPACT_NONE` acknowledged for the plan-status-only handbook mapping) → PR `CONFLICTING` again: `origin/main` moved to `21f34434` (`DOC-P13-DRIFT-FIX` PR #309 at `main@474505a7`, docs + `check-consistency` tightening). Second `git merge origin/main`: PROGRESS Active-task-lease row (kept ours + added the `DOC-P13-DRIFT-FIX` done entry) and 4 handbook fingerprint conflicts resolved; `fill-handbook-fingerprints` (4) → `check-handbook` **pass** → generator `--check` **pass** → `check:consistency` **pass** (with main's new checks) → `check:rules` **pass** → `git diff --check` **pass**; no conflict markers | **pass** | `DEV-WIN-GNU-01` (Node) | merge commit (below) | sibling ledger rows integrated (PARALLEL-LANES rule 8); no code or migration change on main |

## Acceptance mapping (P13-T02 card → implementation → negative → evidence at `f82bd437`)

| Card sentence (关闭门 / acceptance) | Implementation | Focused negative | Evidence |
|---|---|---|---|
| 一个已就位 Member 对一个 Task 的 Attempt 经隐藏托管 DSH **真实 spawn** | `hosted_dsh_attempt.rs` `attempt_run` → `HostedDshArtifact::resolve` → `run_hosted_child` (`Command::spawn`, `env_clear`, own process group) with the product child `hosted-attempt-child.mjs` | unseated / task-channel / stale revision / Pi refused (store N8); unhealthy artifact refuses spawn | store 9/9, kernel-server 2/2 (Linux), live U5 real spawn of product child → real pinned dsh |
| 有界 Context payload | `HostedContextPayload::validate` (64 KiB, non-empty, loopback origin) + v36 CHECK `context_bytes <= 65536`; digest bound into the request frame and the Attempt row | oversize / empty / secret-shaped context refused (N6, live U4) | runtime 7/7, store 9/9, live U4 |
| 完整 stdio broker（request/response/observation 帧、超时、bounded redacted output） | single stdin request frame with `timeout_ms`; NDJSON `observation`/`candidate`/`heartbeat`/`response`; 256 KiB stdout cap with drain; 2 KiB redacted stderr tail; 512-frame cap; 1024-char redacted frame text; group kill on timeout | unknown lines never promoted (N2); bounded stdout still drains; hang → `timed-out` | runtime 7/7, kernel-server 2/2, live U5/U6 (8 frames, heartbeats, redacted) |
| candidates/observations 流 | frames persisted as v36 `p13_hosted_dsh_attempt_frame` rows, `authority_written` CHECK = 0, append-only trigger | heartbeat / observation frames never move Employee/Project/stage/acceptance (N7) | store `p13_t02_frames_never_write_authority`, live U6 `authority_written_any=false` |
| 经 Intent/Effect persist-before-dispatch | `persist_intent` (state `persisted`) **before** `resolve`/`spawn`; `mark_dispatched` in `on_spawn`; startup `reconcile_unknown_outcomes` | crash-shaped `persisted`/`dispatched` rows → `unknown-outcome`, never success (N9) | store 9/9; live U10–U11 daemon SIGKILL → restart → `unknown-outcome` |
| 终态观察写 Attempt（只由 daemon） | `record_terminal` from the broker thread with the observed `terminal_kind`; `completion_claimed` CHECK = 0; `verification_status` CHECK = `'not-run'`; no `success` terminal kind exists | process death (exit 0 / 7 / 9 / kill / timeout / spawn-failed) ≠ completion (N1); `{"status":"success"}` / `response done` ≠ completion | runtime, store (schema CHECKs reject `success` / `completion_claimed=1` / `passed` / DELETE), kernel-server, live U5 |
| artifact health/update/rollback 事实可查 | `HostedDshArtifact::observe` → `record_artifact_observation` (`health-check` / `update` / `rollback` kinds, `previous_fact_id` chain) → `dsh.hosted.artifact.check` / `.facts` | pin drift → `mismatch` + spawn refused 422; restore → `rollback` fact; script missing → refused | store, kernel-server, live U1/U8 (`pinned`, history 3) |
| secret 不进 child env/argv | `validate_launch_plan` (secret-shaped keys/values, `sk-`/`bearer `/`ssv1:`), `env_clear` + allowlist, redaction before anything durable | secret env/argv never spawns (marker file untouched) (N3); live secret scan | runtime 7/7 (incl. two CI-caught test defects where the product check refused literal shapes), live U9 `leaks=0` |
| child 直连 Provider 被拒 | Provider hosts / base-URL / `--api-key-file` / Path A refused pre-spawn; `provider_request` / Provider URL frames rejected & recorded; child patch `baseURL` = daemon `/provider/v1/dsh` only | N4 pre-spawn + in-stream | runtime `p13_t02_child_direct_provider_is_refused_and_recorded`, kernel-server (`child-direct-provider` frame), child script 12/12 |
| native MCP / base tool / HMR / home patch 被拒 | `NativeHarnessEscape` pre-spawn; child `REFUSED_FLAGS` exit 2 | N5 | runtime 7/7, child script tests |
| Attempt 历史可被 `runs` 读取（D02） | `GET dsh.hosted.attempt.list?project_id` / `.detail` (management only, redacted, `receipt_is_not_completion`) | task-channel 403; redaction guard 500 on secret shapes | kernel-server 2/2, live U2/U6/U7 |
| 引擎无权威、无 secret、无完成权；Pi 不是 Member 引擎 | engine pin `cognitiveos.personal.hidden-hosted-dsh/0.1`, `pi_member_engine:false`, `installed_agent:false` in responses; child is not a `runtime_binding_ref` writer | assistant / Pi identity refused as hosted engine (store N8) | store 9/9, live U5 response fields |

Cells that stay **not-run** (by design, A7 / hard gate (6)): Windows sandbox / ACL / supply-chain E2E (`DEV-WINDOWS-NATIVE-OPC-01`, backfilled by `P13-T13/D02`); `DEV-WIN-GNU-01` cargo (`RUST-LINK-DEV-WIN-GNU-01`, fenced route `HOSTED_DSH_WIN_GNU_FENCE`); live Provider `done` leg with a bound key (no new SecretStore entry created — owner boundary); B01 guest run (optional, not needed for this task's evidence).

## Explicit non-claims

Not Gate, release, Profile, B01, Windows OPC qualification, Agent-benefit. Not `P13-T04` CAS/verifier/outputs, not `P13-T05` Routine arming, not `P13-T08` Settings chrome (engine facts only become readable). Not Installed Agent chrome, not native DSH UI in `/ui/`, not Pi as Member engine. Linux Path B / Linux real spawn ≠ Windows sandbox / ACL / supply-chain qualification. Evaluation routing OFF.
