# P12-T03 Project four submenus — running report

Incremental log per `TEST-REPORT-INCREMENTAL-01`. Append each finished unit immediately. `not-run` is never pass. Claim ceiling `hypothesis`. A7: local/CI is not Gate.

- Task: `P12-T03` / slice `P12-T03/D01`
- Branch: `personal/P12-T03-project-submenus`
- Lease: `lease/personal/P12-T03/project-submenus`
- Change class: `implementation-only` (daemon-served `/ui/` Project four submenus; no new authority writer; no `core/specs`)
- Unique next: rebase onto `origin/main`, push Draft PR, required CI; then claim `P12-T04`

Product origin is daemon-served `/ui/`. Vite/canvas is not the product. NVDA/200%/host-theme remain hung. Native UI E2E = `DEV-WINDOWS-NATIVE-OPC-01` / `not-run`. `DEV-WIN-GNU-01` cargo is `not-run` (`RUST-LINK-DEV-WIN-GNU-01`). Not T04 eight-tab / add-member. Not T06 HITL Confirm. Not T15.

## Units

| Unit | Result | Env | Revision | Notes |
|---|---|---|---|---|
| Claim `lease/personal/P12-T03/project-submenus` | **pass** | `DEV-WIN-GNU-01` | worktree `D:/agent-kernel-wt-P12-T03` stacked on `origin/personal/P12-T02-wizard` @ `36186ff9` | T02 PR [#295](https://github.com/agentkernel/cognitive-os/pull/295) **merged** at `main@23646a84`; required CI [33375302320](https://github.com/agentkernel/cognitive-os/actions/runs/33375302320) **SUCCESS** on `36186ff9`. DOC-REFRAME retained. Evaluation routing OFF. |
| Dual Track TS four submenus (`projectWork` + `projectSubmenus` + `opcIa` + `normalize`) | **pass** | `DEV-WIN-GNU-01` | worktree | personal-web-ui **358/358** (`projectSubmenus` 9/9, `projectWork` 5/5). `#/projects/:id` `{,/members,/runs,/outputs}` against GET `detail`/`axis`/`roster`. Empty/denied/missing stay distinct. No fake Activate/Install/Add/Confirm. Not T04 eight-tabs. Native UI E2E **not-run**. NVDA/200%/host-theme **not-run**. GNU cargo **not-run**. |
