# P13-T03 Hidden Pi Assistant real inference — closure

- Task: `P13-T03` **done** / slice `P13-T03/D01` **done** (on the PR head the formal plan / PROGRESS read `in-progress → closing` because the active lease bound `P13-T03/D01`; this lease-closure commit on `main` records `done`, following the P13-T02 closure precedent)
- Change class: `implementation-only` (no `core/specs`, no Lane-CTR, no new SQLite migration, no contract or negative weakened)
- Lease: `lease/personal/P13-T03/pi-inference` → PARALLEL-LANES §3.1 (closed 2026-09-03 in this closure delivery on `main`)
- Branch / PR: `personal/P13-T03-pi-inference` (remote deleted after merge; worktree `D:\agent-kernel-wt-p13-t03` removed) → PR [#311](https://github.com/agentkernel/cognitive-os/pull/311) merged at `main@7f9b4115`
- Required CI: closure head [33699388684](https://github.com/agentkernel/cognitive-os/actions/runs/33699388684) **SUCCESS** at `b043aa9c` (resolve 3s, ubuntu 4m6s, windows 11m43s, required-ci 2s); merged head of the live-validated code [33682946140](https://github.com/agentkernel/cognitive-os/actions/runs/33682946140) **SUCCESS** at `93e6ed01`; first green implementation run [33676327318](https://github.com/agentkernel/cognitive-os/actions/runs/33676327318) at `7e6157f1`
- Running report: [P13-T03 report](2026-09-03-personal-p13-t03-pi-inference-report.md)
- Claim ceiling: `hypothesis`. Linux native + ordinary CI evidence closes "the implementation exists" only (Phase 13 hard gate 6). No Gate / release / Profile / B01 / Windows / Agent-benefit claim.
- Validated implementation revision: `347ab54a` (all live units: four turn kinds bound, unbound 409 + status, ambient tool 403) and `93e6ed01` (merged head = `347ab54a` + `origin/main`, no task-owned code change: focused store / runtime / adapter / kernel-server tests + clippy on `DEV-LINUX-NATIVE-01`, required CI [33682946140](https://github.com/agentkernel/cognitive-os/actions/runs/33682946140) **SUCCESS**). Live units were not re-run at `93e6ed01`. Commits after `93e6ed01` (`a370a986` closure docs, `b043aa9c` merge of `origin/main` with P0-T09) are documentation / sibling-ledger only.

## 1. Acceptance mapping (formal plan P13-T03 card + Delivery Slice `P13-T03/D01`)

| Acceptance item | Implementation | Focused negative(s) | Evidence |
|---|---|---|---|
| `assistant.turn` explain / navigate / research / propose **really call** exact Pi (daemon side; Provider via daemon proxy) | `kernel-server/src/personal/assistant_inference.rs` `DaemonAssistantRuntime::infer` spawns `pi-agent-adapter assistant-turn` (exact pin check, `--no-builtin-tools --no-extensions --no-skills --no-context-files --no-session --no-approve --mode rpc`, only the daemon-private completion provider extension) with the one-shot `PrivateCompletionSocket`; `accepted()` → `provider_round_trips`; `run_turn` refuses `provider_round_trips == 0` as echo | store `turn_without_provider_round_trip_is_refused_as_echo`; HTTP `prose_echo_fabricated_source_and_zero_round_trip_register_nothing`; failure-first A/B: pre-task `main@84188aac` echoes a 200 candidate with no Provider bound, task revision refuses 409 | Linux live re-run #5 at `347ab54a`: four turn kinds HTTP 200, `provider_round_trips: 1`, `model_id: deepseek-v4-flash`, 10–26 s wall time each; `daemon.log` and outputs secret-free |
| Bounded Context in T10 `inject_order` | `cognitive-runtime/src/pi_inference.rs` `assemble_bounded_context` (`CONTEXT_INJECT_ORDER`, 16 KiB, tail drop); daemon builds `task-contract` (draft facts) → `fixed-decision` (Project facts) → `sourced-excerpt` (fetched research) → `summary` (archive index refs) | runtime `bounded_context_follows_inject_order_and_drops_from_the_tail`; `request_frame_rejects_unknown_protocol_layer_or_oversize` (layer outside the order, oversize, bearer-shaped field) | Linux runtime 7/7; live responses report `context.inject_order_ref = CONTEXT_INJECT_ORDER`, `layers[0].layer = task-contract` |
| Candidate object chain BusinessBrief / ResearchRun / Charter / Axis / Roster / Recipe with typed provenance (`sources[]` / owner-stated / assistant-assumption); **no provenance → refused** | `cognitive-store/src/assistant.rs` `validate_inferred_object_chain` (single validator: closed kinds in chain order, one per kind, `{value, provenance}` per field through the P11-T06 provenance validator, closed schema) | store `candidate_field_without_provenance_is_refused`, `chain_outside_closed_kinds_order_or_schema_is_refused`; runtime `object_chain_parser_accepts_fenced_json_and_refuses_everything_else` | Linux store 10/10; live chains carry owner-stated and assistant-assumption fields per object |
| Fabricated `sources[]` refused | `validate_cited_sources`: a cited uri must be fetched by the daemon or owner-supplied | store `fabricated_sources_are_refused`; HTTP fabricated-source case | Linux store + kernel-server tests; live research turn cites nothing because nothing was fetched |
| Registered candidate + optional preview (research / propose) | `run_turn` → v26 `register_candidate` (author `assistant`, typed provenance) → `request_preview("activation")`, re-announcing an existing pending preview instead of failing | store `inferred_turn_registers_the_chain_not_the_echo`, `second_propose_on_the_same_draft_reannounces_the_pending_preview` | Live: `propose` mints `preview-01a063e0-…`; `research` re-announces the same id; `explain` / `navigate` announce none |
| Provider not bound → create-page chat renders a "go to Settings to connect" pointer (not a fake chat box) | `GET assistant.status` (`ready` / `provider_unbound` / `pi_unavailable`, derived from daemon facts only); `POST assistant.turn` → 409 `ASSISTANT_PROVIDER_UNBOUND` + `provider_unbound_guidance()`; web `CreateAssistantChat` renders the Settings link and no textarea; `RailCanvasWrite` points at Settings on 409 | HTTP `provider_unbound_turn_points_at_settings_and_registers_nothing`; web `createAssistantChat.test.tsx` (pointer instead of chat box; daemon 409 flips to pointer), `railWrite.test.tsx` | Linux live: unbound runtime → status `provider_unbound`, turn 409, no Pi process; web vitest 56 files / 433 tests |
| Still candidate-only — no authority / Secret / archive / Memory writes | unchanged P11-T06 fail-closed `write_archive` / `write_secret` / `write_memory` / `confirm_preview` / `apply_candidate` / `grant_capability`; candidate ops digest only in SQLite | store `assistant_direct_writes_to_authority_secret_archive_memory_stay_refused`; HTTP responses never carry `preview_digest` or `Approve` | Linux store 10/10; live responses `observation_only: true`, draft `base_seq` untouched (candidates not applied) |
| Ambient tool refused | `AssistantPlane::admit_turn_request` runs before any binding lookup or Pi spawn; adapter refuses any `tool_execution_*` event (Workspace* included) | store `ambient_tool_is_refused_before_any_inference`; HTTP `ambient_tool_and_missing_draft_are_refused_before_pi_spawns`; adapter `assistant_events_refuse_any_tool_attempt_including_workspace_families` | Linux live: `tools: ["bash"]` → 403 before Pi; adapter 8/8 |
| Research fetch only through the `HttpFetchReadOnly` read-only family | `validate_research_target` = registered `validate_read_only_http_fetch` against pinned origins for `task://personal/assistant-research` (default empty); Rustls read-only GET; refused targets reported, never fetched | runtime `research_targets_are_refused_unless_https_and_pinned`; HTTP `research_targets_outside_pinned_origins_are_never_fetched` (scripted transport records zero fetches) | Linux live research: `research.refused = [{"reason":"HTTP origin is not registered"}]`, `fetched: []` |
| `run_turn` no longer echoes the client payload (关闭门 / drift negative) | `AssistantTurnSpec.inference` is mandatory; ops carry `chain` + labelled `owner_payload`; HTTP no longer builds a candidate from the body | store echo test; A/B observation above | Linux A/B at `84188aac` vs task revision |

关闭门 (plan.md card), sentence by sentence: (1) four turn kinds really reach exact Pi through the daemon and produce typed-provenance candidate object chains — **true** on `DEV-LINUX-NATIVE-01` at `347ab54a` (live re-run #5; Windows Pi route **not-run**, `P13-T13`); (2) Provider unbound points at Settings — **true** (live unbound runtime: `assistant.status: provider_unbound`, turn 409, no Pi process; web renders the pointer, no textarea); (3) `run_turn` no longer only echoes the client payload — **true** (`inference` mandatory, `provider_round_trips == 0` refused; A/B against `main@84188aac`). Live Provider inference used the host's pre-existing Linux Secret Service binding (`--reuse-existing-secret-binding`); no key was created, read or printed.

Drift negatives from the card, all refused with nothing registered: direct SecretStore/DB/Provider access (assistant plane fail-closed; Provider reached only through the daemon socket), fabricated `sources[]`, unprovenanced candidate, ambient tool, preview bypass (announcement only, chat has no Approve), Provider-unbound "pretend answer" (409 + pointer), completion self-report (`observation_only: true`; candidate is never applied by the assistant).

## 2. Validation summary

| Environment | Result |
|---|---|
| `DEV-WIN-GNU-01` | `cargo fmt --all -- --check`, `clients/pc/web` `pnpm test` (56 files / 433 tests) + `pnpm build`, `check:consistency`, `check:rules`, handbook check set, docs-sync gate — **pass**; cargo build/test/clippy **not-run** (`RUST-LINK-DEV-WIN-GNU-01`) |
| `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | run [33674312344](https://github.com/agentkernel/cognitive-os/actions/runs/33674312344) **fail** at `3280880d` (dead-code under `-D warnings`, recovered); run [33676327318](https://github.com/agentkernel/cognitive-os/actions/runs/33676327318) **SUCCESS** at `7e6157f1`; merged head run [33682946140](https://github.com/agentkernel/cognitive-os/actions/runs/33682946140) **SUCCESS** at `93e6ed01` (resolve 3s, ubuntu 3m49s, windows 12m26s, required-ci 4s); closure head [33699388684](https://github.com/agentkernel/cognitive-os/actions/runs/33699388684) **SUCCESS** at `b043aa9c` (resolve 3s, ubuntu 4m6s, windows 11m43s, required-ci 2s) |
| `DEV-LINUX-NATIVE-01` (exact pushed revisions, cleanable worktree `~/cognitiveos-personal-worktrees/p13-t03-3280880d`, cleanable runtimes `~/cos-wt/p13-t03-rt{,-unbound}`) | store 10/10 + 6/6, runtime 7/7, adapter 21/21 + 8/8 + 7/7 + 5/5 + 4/4, kernel-server assistant 7/7, clippy touched crates clean (at `347ab54a` and re-run at the merged head `93e6ed01`); failure-first A/B at `84188aac`; **live** (at `347ab54a`): bound runtime (existing Linux Secret Service entry re-bound via `--reuse-existing-secret-binding`; no key created, read, or printed) → all four turn kinds registered typed-provenance candidates through exact Pi 0.81.1 + daemon proxy; unbound runtime → Settings pointer + 409; ambient tool 403 |
| `DEV-WINDOWS-NATIVE-OPC-01` | **not-run** (not provisioned); Windows Pi route backfilled by `P13-T13`; Linux Pi qualification does not transfer |
| `B01-Desktop-Linux-002` | **not-run** (optional per PERSONAL-TEST-ENVIRONMENTS §7/§10; not needed for this card) |

Live-inference caveats recorded honestly: the model (`deepseek-v4-flash`) needed three parser/prompt hardening rounds (prose around the object, raw control characters inside strings, a dropped closer) before all four turn kinds landed; each hardening is structural only and every attempt still ends in the closed-schema validator. Pinned research origins were empty on the runtime, so the research turn's fetch path was exercised only as a refusal (`HTTP origin is not registered`); the fetch transport itself is the registered `HttpFetchReadOnly` Rustls path and is covered by the scripted HTTP test.

## 3. Non-claims

Not P13-T02 (hosted DSH), not P13-T06 group chat, not P13-T10 Skill/MCP acquisition. Not Installed Agent, second scheduler, engine store, chat Approve, or mixing draft-apply with authority-approve. No Gate / release / Profile / B01 / Windows / Agent-benefit / conversation-quality claim. Linux Pi inference is not a Windows OPC qualification.

## 4. Cleanup

Linux: both daemons stopped (`cognitive daemon stop`), management header files removed, private completion directory empty; runtime roots `~/cos-wt/p13-t03-rt`, `~/cos-wt/p13-t03-rt-unbound`, the worktree `~/cognitiveos-personal-worktrees/p13-t03-3280880d` and the A/B worktree `p13-t03-base-84188aac` removed in this closure (see the report's final row). The pre-existing Secret Service entry was neither created nor deleted by this task. Local: `D:\agent-kernel-wt-p13-t03` removed after merge; local + remote task branches deleted; `main` fast-forwarded to `7f9b4115`.

## 5. Unique next

`P13-T06/D01` (group chat / manager routing) now has its `P13-T03/D01` prerequisite; `P13-T10/D01` likewise. `P13-T04/D01` and `P13-T05/D01` are claimable behind `P13-T02/D02` (done). Independent leases / worktrees each.
