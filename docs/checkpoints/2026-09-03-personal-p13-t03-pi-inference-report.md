# P13-T03 Hidden Pi Assistant real inference — running report

- Task: `P13-T03` / slice `P13-T03/D01`
- Change class: `implementation-only` (daemon/store/runtime/adapter/HTTP candidate path + `clients/pc/web` create-page assistant chat; no `core/specs`, no Lane-CTR, no new SQLite migration)
- Product: CognitiveOS Personal 2.0.0
- Lease: `lease/personal/P13-T03/pi-inference`
- Branch: `personal/P13-T03-pi-inference` (worktree `D:\agent-kernel-wt-p13-t03`, cut from `origin/main@a0465653`)
- Sibling: `P13-T02` (hosted DSH real Attempt loop) runs concurrently on its own lease/worktree; shared registration files are edited additively
- PR: Draft PR [#311](https://github.com/agentkernel/cognitive-os/pull/311) (Draft until every acceptance item is mapped below; ready + merge recorded in the final rows)
- Claim ceiling: `hypothesis` (A7: local/CI/Linux-native evidence is not Gate/release/Profile; Pi Linux qualification does not transfer to Windows)
- Evaluation routing: **OFF**

## Identifier

Hidden engine pin: `cognitiveos.personal.hidden-pi-assistant/0.1` (P11-T06, unchanged).
New daemon-side inference protocol: `cognitiveos.personal.assistant-inference/0.1`
(request/response frame between the daemon and the `pi-agent-adapter assistant-turn`
verb; not a Core contract, not a second candidate schema — the candidate still lands
through v26 `register_candidate` / `request_preview`).

Reused, not rebuilt: exact Pi `0.81.1` (`PiCompatibilityPin`), the daemon-supervised
private completion socket + Provider proxy (`pi_runtime.rs`, P8-T13 `agent://personal/pi`
binding), `HttpFetchReadOnly` pre-validator + Rustls read-only transport (P2-T05/P2-T10),
T10 `CONTEXT_INJECT_ORDER`, T05 conversation archive index (read-only), T11 closed
candidate schema + typed provenance validators.

## Incremental validation log (TEST-REPORT-INCREMENTAL-01)

Units are appended **immediately** after each finishes. `not-run` is never pass.

| Time | Unit | Result | Env | Revision | Notes |
|---|---|---|---|---|---|
| 2026-09-03 | worktree + lease claim | recorded | docs-only | uncommitted | `git worktree add -b personal/P13-T03-pi-inference D:\agent-kernel-wt-p13-t03 origin/main` at `a0465653`; lease row added to PARALLEL-LANES §3; PROGRESS Active task lease / `P13-T03/D01` in-progress; plan Phase 13 counts 1 in-progress |
| 2026-09-03 | worker resume after abort | recorded | `DEV-WIN-GNU-01` | uncommitted | Previous worker aborted mid-flight with uncommitted store/runtime/adapter edits and new test files. Reviewed `git diff`: store `run_turn` now requires a daemon-observed `AssistantInferenceRecord`; runtime `pi_inference.rs` (frame + bounded Context + prompt + chain parser); adapter `assistant-turn` verb + `extract_assistant_text_from_pi_events`; `pi_runtime.rs` exposes the one-shot completion socket + `accepted()` fact. Kept all of it; the kernel-server `assistant_inference.rs` module referenced by `mod.rs` did not exist yet — written in this session. |
| 2026-09-03 | failure-first negatives authored (store / runtime / adapter / HTTP) | authored (Rust execution routed) | `DEV-WIN-GNU-01` (`RUST-LINK-DEV-WIN-GNU-01`: cargo test forbidden locally) | uncommitted | Store `p13_t03_assistant_inference.rs`: echo (zero Provider round trips) refused; field without provenance refused; fabricated `sources[]` refused; chain outside closed kinds/order/schema refused; ambient tool refused before inference; inferred turn registers the chain not the echo; Provider-unbound guidance is a Settings pointer; assistant direct writes to authority/Secret/archive/Memory refused. Runtime `pi_inference.rs` unit tests: prompt names closed schema + forbids tools; request frame refuses unknown protocol/layer/oversize/bearer field; bounded Context follows inject order and drops from the tail; chain parser accepts fenced JSON only; response frame protocol-bound; research targets refused unless HTTPS + pinned. Adapter `p13_t03_assistant_turn.rs`: exactly one final text; any `tool_execution_*` (Workspace* included) refused; multiple/missing/errored finals refused. HTTP `assistant_inference.rs` tests with a scripted runtime: unbound → 409 Settings pointer + nothing registered; ambient tool / missing draft / closed schema / unlabeled provenance refused before Pi spawns; prose echo, fabricated source, zero round trips → 422 nothing registered; adapter failure 502, Pi missing 503; inferred turn registers chain with bounded context and no Approve; unpinned research targets never fetched. Because local cargo is forbidden, the "fails before" half of failure-first is observed on `DEV-LINUX-NATIVE-01` as an A/B: at pre-task `main@84188aac` the existing `assistant_turn_registers_candidate_and_omits_approve` unit passes by **echoing** the client payload as a 200 candidate with no Provider bound (the P13-T01 gap record); at this task's revision that same request is refused 409 and the new negatives pass (unit below). |
| 2026-09-03 | `clients/pc/web` `pnpm test` (vitest) | **pass** | `DEV-WIN-GNU-01` | uncommitted | 56 files / 433 tests pass, including new `createAssistantChat.test.tsx` (Settings pointer instead of chat box when unbound; honest `pi_unavailable`; no chat box when status unavailable; explain turn on lazily created research draft renders typed chain; research targets posted with `HttpFetchReadOnly` only for research turns; empty / secret-shaped text refused before POST; daemon 409 flips to Settings pointer; refused candidate renders no reply), `railWrite.test.tsx` additions (unbound → Settings pointer, nothing applied; inferred reply + chain kinds rendered), `assistant.test.ts` additions (chain projection drops unprovenanced fields; status mapping opens input only on explicit daemon `ready`; unbound detection). |
| 2026-09-03 | `clients/pc/web` `pnpm build` (`tsc --noEmit` + `vite build`) | **pass** | `DEV-WIN-GNU-01` | uncommitted | 164 modules; pre-existing >500 kB chunk warning unchanged. |
| 2026-09-03 | `cargo fmt --all -- --check` | **pass** (after `cargo fmt --all`) | `DEV-WIN-GNU-01` | uncommitted | Formatting only; not build/test evidence. |
| 2026-09-03 | `pnpm run check:consistency` / `pnpm run check:rules` | **pass** | `DEV-WIN-GNU-01` | uncommitted | First run failed on Layer 1 counts (167/132/1/1/17/35 vs plan 2 in-progress) and on writable-path overlap with the still-active `lease/personal/P13-T12/visual-spec` row; resolved by fast-forwarding onto `origin/main@84188aac` (T12 lease closed there) and updating counts to 167/132/2/1/16/35. Rules: 4 rules, 88 path references, 5 known local-only warnings. |
| 2026-09-03 | handbook sync (`generate-handbook`, `fill-handbook-fingerprints`, `check-handbook`, `generate-handbook --check`) | **pass** | `DEV-WIN-GNU-01` | uncommitted | New route annotations `assistant.status` (management + forbidden task alias) and rewritten `assistant.turn` annotation; generated `ref.http-api` regenerated; hand-written bilingual updates: `dev.daemon-and-http`, `dev.store-migrations`, `dev.agent-pi-lifecycle`, `user.pi-shell`, `user.known-limitations`, `ai.code-map`; fingerprint-only refresh where the checker required it. 58 × 2 documents OK; 18 generated pages byte-identical. |
| 2026-09-03 | `git diff --check` | **pass** | `DEV-WIN-GNU-01` | uncommitted | no whitespace errors |
| 2026-09-03 | `DEV-WIN-GNU-01` cargo build / test / clippy | **not-run** | `RUST-LINK-DEV-WIN-GNU-01` | — | Environment boundary, not a product failure; routed to required CI + `DEV-LINUX-NATIVE-01`. |
| 2026-09-03 | Windows Pi route (native Pi inference on Windows) | **not-run** | `DEV-WINDOWS-NATIVE-OPC-01` (not provisioned) | — | Backfilled by `P13-T13`; Linux Pi qualification does not transfer. |
| 2026-09-03 | checkpoint commit + push + Draft PR | **pass** | GitHub | `3280880d` | Branch fast-forwarded onto `origin/main@84188aac` (T12/D01 closure) before committing. Draft PR [#311](https://github.com/agentkernel/cognitive-os/pull/311). |
| 2026-09-03 | required CI run [33674312344](https://github.com/agentkernel/cognitive-os/actions/runs/33674312344) | **fail** (recovered) | `CI-WINDOWS-MSVC-01` | `3280880d` | `Build Rust workspace` with `-D warnings`: `InferenceFailure::Failed` never constructed on non-unix; store-only `handle` / `UnconfiguredAssistantRuntime` only used by tests. Fixed in `7e6157f1` (`cfg_attr(not(unix), allow(dead_code))`; `#[cfg(test)]` on the store-only entry) plus clippy `doc_lazy_continuation` on the store test module doc. Not a product behavior failure. |
| 2026-09-03 | `DEV-LINUX-NATIVE-01` exact-revision worktree | **pass** | `DEV-LINUX-NATIVE-01` (`hal9000`, Linux 6.8 x86_64, Rust 1.97.1, Node 22.19.0, Pi `0.81.1` at `~/.local/share/pnpm/bin/pi`) | `3280880d` → `7e6157f1` | `/home/wuz/cognitiveos-personal-worktrees/p13-t03-3280880d` (objects seeded from a sibling clone, task ref fetched from GitHub through the host's local proxy after direct GitHub access timed out; `git status` clean). The fix-up commit `7e6157f1` was transferred over SSH from the same pushed object (SHA equal to `origin/personal/P13-T03-pi-inference`) after five GitHub TLS handshake failures; checked out detached, clean. |
| 2026-09-03 | store `p13_t03_assistant_inference` + `p11_t06_assistant` | **pass** 8/8 + 6/6 | `DEV-LINUX-NATIVE-01` | `3280880d` | `cargo test -p cognitive-store --test p13_t03_assistant_inference --test p11_t06_assistant --locked -- --test-threads=1` |
| 2026-09-03 | runtime `pi_inference` unit tests | **pass** 6/6 | `DEV-LINUX-NATIVE-01` | `3280880d` | `cargo test -p cognitive-runtime --lib --locked pi_inference` |
| 2026-09-03 | `pi-agent-adapter` all tests (incl. `p13_t03_assistant_turn` 4/4, `daemon_candidate_protocol` 21/21) | **pass** | `DEV-LINUX-NATIVE-01` | `3280880d` | `cargo test -p pi-agent-adapter --locked` |
| 2026-09-03 | kernel-server assistant unit tests (`assistant_inference::tests` ×6 + `project_aggregate::tests::assistant_turn_registers_candidate_and_omits_approve`) | **pass** 7/7 | `DEV-LINUX-NATIVE-01` | `3280880d` | `cargo test -p kernel-server --locked --bin kernel-server assistant -- --test-threads=1`; unbound 409 Settings pointer registers nothing; ambient tool / missing draft refused before Pi; prose / fabricated source / zero round trips 422; adapter failure 502; Pi missing 503; inferred turn registers chain with `task-contract` first; unpinned research targets never fetched. |
| 2026-09-03 | clippy touched crates at `3280880d` | **fail** (recovered) | `DEV-LINUX-NATIVE-01` | `3280880d` | Same findings as CI (`doc_lazy_continuation`, dead code); fixed in `7e6157f1`, re-run recorded below. |
| 2026-09-03 | `cargo build -p kernel-server -p pi-agent-adapter -p admin-cli --locked`; `pnpm install --frozen-lockfile`; `@cognitiveos/pi-cognitiveos` build | **pass** | `DEV-LINUX-NATIVE-01` | `3280880d` | binaries + `dist/index.js` ready for the live E2E. |
| 2026-09-03 | clippy touched crates (`--all-targets --locked -- -D warnings`) at the fix-up revision | **pass** | `DEV-LINUX-NATIVE-01` | `7e6157f1` | `kernel-server`, `cognitive-store`, `cognitive-runtime`, `pi-agent-adapter` clean. |
| 2026-09-03 | store 8/8 + 6/6, runtime 6/6, adapter (4/4 + 21/21 + …), kernel-server assistant 7/7 at the fix-up revision | **pass** | `DEV-LINUX-NATIVE-01` | `7e6157f1` | same commands as above, all green. |
| 2026-09-03 | **failure-first A/B**: pre-task `assistant_turn_registers_candidate_and_omits_approve` at `main@84188aac` | **pass = old echo confirmed** | `DEV-LINUX-NATIVE-01` | base worktree `84188aac` | The pre-task handler answers `propose` with HTTP 200 + `candidate_digest` from the client payload with no Provider bound (echo). At `7e6157f1` the identical request is refused 409 `ASSISTANT_PROVIDER_UNBOUND` (kernel-server test above). |
| 2026-09-03 | live runtime provisioning on a cleanable root (`~/cos-wt/p13-t03-rt`) | **pass** (no key created) | `DEV-LINUX-NATIVE-01` | `7e6157f1` | `cognitive init --reuse-existing-secret-binding --provider deepseek` bound the host's **existing** Linux Secret Service entry (`secret_ref_resolves: true`, `secret_material_exposed: false`); first attempt with `--model-id deepseek-chat` failed closed (`alias_drift`: not in the discovered catalog) and left no selected model; second attempt selected catalog model `deepseek-v4-flash`. `cognitive pi configure` wrote `pi.json` (pinned Pi `~/.local/share/pnpm/bin/pi` 0.81.1, built extension, `pi-agent-adapter`, private candidate provider). `cognitive daemon start --bind 127.0.0.1:48493`; redacted doctor: system/database/secret/provider/daemon/pi **ready**, `first_conversation_ready: true`. |
| 2026-09-03 | live `GET assistant.status` (bound) | **pass** | `DEV-LINUX-NATIVE-01` | `7e6157f1` | `status: ready`, `chat_input: true`, `model_id: deepseek-v4-flash`, `binding_source: selected-model`, `research_origins_pinned: 0`, `installed_agent: false`. |
| 2026-09-03 | live `GET assistant.status` + `POST assistant.turn` on an **unbound** runtime (`~/cos-wt/p13-t03-rt-unbound`, port 48494) | **pass** | `DEV-LINUX-NATIVE-01` | `7e6157f1` | status `provider_unbound`, `chat_input: false`, `settings_route: #/settings`; turn → HTTP 409 `ASSISTANT_PROVIDER_UNBOUND`, `candidate_registered: false`, no Pi process. The same result was observed on the first (not yet model-selected) bound-root daemon. |
| 2026-09-03 | live ambient tool (`tools: ["bash"]`) | **pass** | `DEV-LINUX-NATIVE-01` | `7e6157f1` | HTTP 403 `PROJECT_FORBIDDEN: ambient tool/shell rejected` before any Pi process. |
| 2026-09-03 | live `propose` / `explain` / `navigate` through exact Pi 0.81.1 + daemon Provider proxy (`deepseek-v4-flash`) | **partial** (inference real; candidate refused) | `DEV-LINUX-NATIVE-01` | `7e6157f1` | Each turn really ran Pi and a Provider round trip (45 s / 11 s / 36 s wall time) and came back HTTP 422 `ASSISTANT_CANDIDATE_REFUSED: assistant final message is not a JSON candidate object` — the model wrapped or replaced the JSON with prose; fail-closed worked (nothing registered) but no candidate landed. `daemon.log` shows only startup lines (no Provider material). |
| 2026-09-03 | live `research` through exact Pi with an unpinned target | **partial** (chain parsed; registration refused) | `DEV-LINUX-NATIVE-01` | `7e6157f1` | HTTP 422 `PROJECT_INVALID: secret-shaped material is rejected at registration`: the model's chain parsed and validated, then the v26 registration guard matched `sk-` inside an ordinary hyphenated word (e.g. `risk-…`). Root cause is the substring guard, not Provider material. |
| 2026-09-03 | fix-up for the two live findings | **pass** (committed `5a4b0d8d`) | `DEV-WIN-GNU-01` | `5a4b0d8d` | (1) `parse_assistant_object_chain` takes the first balanced top-level JSON object even when surrounded by prose or a fence-with-preamble (new runtime test); prompt made compact and repeats "ONLY the JSON object" after the untrusted context. (2) Store `looks_like_secret` treats `sk-` as a key prefix only at a token start (new store test: `risk-based … desk-side … task-contract` registers; `sk-abcdefghijklmnopqrstuvwxyz` still refused). (3) 422 `ASSISTANT_CANDIDATE_REFUSED` carries a bounded, key-scrubbed `assistant_text_excerpt` (HTTP test + create-page chat render). Web tests 28/28 on the three touched files. |
| 2026-09-03 | live re-run #2 (`explain` / `navigate` **registered**; `propose` / `research` refused) | **partial** | `DEV-LINUX-NATIVE-01` | `5a4b0d8d` | clippy clean; store 9/9; kernel-server 7/7. Live: `explain` → 200 candidate `business-brief` (owner-stated question + assistant-assumption fields), `navigate` → 200 candidate `axis`; `propose` / `research` → 422 with the excerpt showing valid-looking JSON — the two longer chains carried raw control characters inside string values (strict JSON refuses them). |
| 2026-09-03 | fix-up: raw control characters inside JSON strings | **pass** (committed `79917a0b`) | `DEV-WIN-GNU-01` | `79917a0b` | `escape_raw_controls_in_strings` (string-literal-only; structure untouched) as a last parse attempt; truncated objects stay refused (tests). Excerpt bound raised to 1000 chars. Also fixed the `{{reply, objects}}` assertion format string clippy caught on Linux. |
| 2026-09-03 | live re-run #3 (all four turns refused) | **fail** (diagnosed) | `DEV-LINUX-NATIVE-01` | `79917a0b` | clippy clean; focused tests green. Every turn 422 `not a JSON candidate object`; the full `navigate` excerpt shows the model dropped the object's own `}` before `]` (`…"provenance":{"kind":"assistant-assumption"}}}]}`), i.e. a structural slip by `deepseek-v4-flash`, not truncation and not a daemon/adapter defect (`provider_round_trips: 1` every time). |
| 2026-09-03 | fix-up: close a dropped closer, keep truncation refused | **pass** (committed `5dcd263a`) | `DEV-WIN-GNU-01` | `5dcd263a` | `close_unbalanced_json`: a mismatched `]`/`}` first closes the open brackets; open brackets at end of text are closed; text ending inside a string or closing nothing is refused. Only closers are inserted; values untouched; the closed-schema validator still decides. Tests: the live sample repairs to `axis`; cut-inside-string refused; cut-before-provenance refused by the schema. |
| 2026-09-03 | live re-run #4 (`propose` / `explain` / `navigate` **registered**; `research` conflict) | **partial** | `DEV-LINUX-NATIVE-01` | `5dcd263a` | clippy clean; focused tests green. `propose` → 200 `charter` + `preview_id`; `explain` → 200 `business-brief`; `navigate` → 200 `axis`; `research` → 409 `PROJECT_CONFLICT: pending preview already exists for subject` — the P11-T06 preview announcement failed after the candidate was registered because `propose` had already minted the draft's pending activation preview. |
| 2026-09-03 | fix-up: re-announce the pending preview | **pass** (committed `347ab54a`) | `DEV-WIN-GNU-01` | `347ab54a` | `run_turn` reuses the draft's pending activation preview id on `Conflict` (store test: second research/propose on the same draft returns the same `preview_id`, two candidates, one preview). |
| 2026-09-03 | **live re-run #5 — all four turn kinds through exact Pi 0.81.1 + daemon Provider proxy** | **pass** | `DEV-LINUX-NATIVE-01` | `347ab54a` | clippy clean; store 10/10 + 6/6, runtime 7/7, adapter 21/21 + 8/8, kernel-server 7/7. Bound runtime (`deepseek-v4-flash`, existing Secret Service entry, `secret_material_exposed: false`): `propose` → HTTP 200, chain `["charter"]` (fields `purpose`/`scope`/`cadence` **owner-stated**, `name`/`deliverable`/`success_criteria` **assistant-assumption**), `preview_id` minted, `provider_round_trips: 1`, 12 s; `explain` → 200 `["business-brief"]` (owner-stated `request`, assistant-assumption `answer`/`audience`/`cadence`), no preview, 26 s; `research` with unpinned target `https://example.com/report-format` → 200 `["research-run"]`, `research.refused: [{"reason":"HTTP origin is not registered"}]`, `fetched: []` (never fetched), same `preview_id` re-announced, 10 s; `navigate` → 200 `["axis"]` (owner-stated `dimension`/`question`, assistant-assumption `options`/…), 18 s. Every response: `installed_agent: false`, `observation_only: true`, `context.layers[0].layer = task-contract` (the log redactor masks it as `task-<redacted>`), `context_refs: []`, `model_id: deepseek-v4-flash`, `binding_source: selected-model`. `daemon.log` carries only lock/bootstrap-path/listening lines; `private-completions/` left empty (one-shot sockets cleaned); no Provider material in any output. |
| 2026-09-03 | merge `origin/main` (P13-T02 PR #310 + its lease closure, DOC-P13-DRIFT-FIX PR #309) into the task branch | **pass** | `DEV-WIN-GNU-01` | `223b825f`, `93e6ed01` | Sibling ledger rows integrated (PARALLEL-LANES rule 8); handbook front-matter conflicts resolved toward `main` and fingerprints refilled; Phase 13 counts reconciled to plan 13/2/2/0/9 and Layer 1 167/133/2/1/15/34; `check:consistency` OK (leases verified after the T02 lease closed on `main`). No SQLite migration renumbering needed (P13-T03 adds none; T02's v36 untouched). |
| 2026-09-03 | required CI run [33682946140](https://github.com/agentkernel/cognitive-os/actions/runs/33682946140) on the merged head | **pass** | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `93e6ed01` | `resolve validation route` 3s, `verify (ubuntu-latest)` 3m49s, `verify (windows-latest)` 12m26s, `required-ci` 4s; PR `mergeStateStatus: CLEAN`, still Draft. |
| 2026-09-03 | worker resume after IDE crash (closure phase) | recorded | `DEV-WIN-GNU-01` | `93e6ed01` + uncommitted | Previous worker was killed while writing the closure docs. Uncommitted: this report (fix-up rows `5a4b0d8d` … `93e6ed01`), the closure doc, plan / PROGRESS closing rows. Reviewed against the rows above and kept; the Linux worktree was found checked out at `93e6ed01` with no recorded result, so the focused set was re-run there (next row) instead of trusting an unrecorded run. |
| 2026-09-03 | focused Rust tests + clippy at the merged head | **pass** | `DEV-LINUX-NATIVE-01` (`~/cognitiveos-personal-worktrees/p13-t03-3280880d`, clean) | `93e6ed01` | store `p13_t03_assistant_inference` 10/10 + `p11_t06_assistant` 6/6; runtime `pi_inference` 7/7; `pi-agent-adapter` 8/8 + `daemon_candidate_protocol` 21/21 + `launch_policy` 5/5 + `p0_t06_compatibility` 7/7 + `p13_t03_assistant_turn` 4/4; kernel-server `assistant` 7/7; `cargo clippy -p kernel-server -p cognitive-store -p cognitive-runtime -p pi-agent-adapter --all-targets --locked -- -D warnings` clean. Live turn units are **not** re-run at this head; they stand at `347ab54a` (re-run #5), the merge added no task-owned code. |
| 2026-09-03 | closure docs authored (acceptance mapping, 关闭门 / drift negatives, validation summary, non-claims, cleanup, unique next; plan / PROGRESS / plan.md / trace closing rows) | recorded | `DEV-WIN-GNU-01` | uncommitted → closure checkpoint | [closure](2026-09-03-personal-p13-t03-pi-inference-closure.md). Every acceptance line of the P13-T03 card maps to an implementation symbol, a focused negative and an evidence row above; 关闭门 sentences all **true** on Linux (`347ab54a`); Windows Pi route **not-run** (`P13-T13`). On the PR head the formal plan / PROGRESS read `in-progress → closing` because the active lease binds `P13-T03/D01` (checker `CURRENT_SNAPSHOT_LEASE_MISMATCH`); `done` lands with the lease-closure commit on `main`, following the P13-T02 precedent. |
| 2026-09-03 | closure-head local gates: `pnpm run check:consistency` → `pnpm run check:handbook` → `node tools/src/generate-handbook.mjs --check` → `pnpm run check:rules` → `git diff --check` | **pass** | `DEV-WIN-GNU-01` (Node) | uncommitted → closure checkpoint | consistency OK (275 requirements, leases verified: Layer 1 167/133/2/1/15/34, Phase 13 13/2/2/0/9); handbook 58 × 2 OK; 18 generated pages byte-identical; rules 4 / 88 path refs / 5 known local-only warnings; no whitespace errors. |

## Implementation shape (this checkpoint)

- **Store** (`cognitive-store/src/assistant.rs`): `AssistantTurnSpec.inference: &AssistantInferenceRecord` is mandatory; `validate_inference_record` (protocol, bound `model_id`, `provider_round_trips ≥ 1`, bounded reply); `validate_inferred_object_chain` is the single chain validator (closed kinds in chain order, one per kind, `{value, provenance}` per field, typed provenance via the P11-T06 validator, `sources[]` uris only from fetched/owner-supplied, closed schema); ops carry `chain` / `owner_payload` (labelled) / `reply_digest` / `model_id` / `provider_round_trips` / `allowed_source_uris` / `inject_order_ref`; `admit_turn_request` for pre-spawn refusal; `provider_unbound_guidance()`; `candidate_count` read-only helper.
- **Runtime** (`cognitive-runtime/src/pi_inference.rs`): request/response frames (`deny_unknown_fields`), `assemble_bounded_context` (T10 `CONTEXT_INJECT_ORDER`, 16 KiB, tail drop), `render_assistant_prompt`, `parse_assistant_object_chain`, `validate_research_target` (registered `validate_read_only_http_fetch`).
- **Adapter** (`pi-agent-adapter`): `assistant-turn` verb (exact pin check, `--no-builtin-tools --no-extensions --no-skills --no-context-files --no-session --no-approve --mode rpc`, only the private completion provider extension, prompt over RPC stdin, one final text, tool events refused).
- **Daemon HTTP** (`kernel-server/src/personal/assistant_inference.rs`): `AssistantRuntime` trait; `DaemonAssistantRuntime` (binding from P8-T13 `agent://personal/pi` else legacy carrier; `pi.json` adapter/extension presence; pinned origins for `task://personal/assistant-research`; Rustls read-only GET; unix-only adapter spawn with allowlisted env + one-shot `PrivateCompletionSocket`, `accepted()` → `provider_round_trips`); `handle_turn` (admit → draft → binding → Context → research → infer → parse → `run_turn`); `handle_status`; `project_aggregate::handle_with_assistant` wired from `server.rs` with `layout.config_dir()/data_dir()`; store-only `handle` uses `UnconfiguredAssistantRuntime` (unbound).
- **Web** (`clients/pc/web`): `assistant.ts` (status/turn/chain projections, unbound detection, turn text gate), `CreateAssistantChat.tsx` embedded in `CreateWizardPage` (status-gated: Settings pointer / Pi-unavailable note / chat; lazily creates a research draft; renders reply + typed chain; research targets only for research turns), `RailCanvasWrite.tsx` (Settings pointer on 409; reply + chain kinds), `normalize.ts` route.

## Unique next action

Closure checkpoint (this report + closure doc + ledger rows) → required CI green on the
to-be-merged HEAD → `gh pr ready 311` → merge → docs-only lease-closure commit on `main`
(`P13-T03` / `P13-T03/D01` `done`, merge revision, closure-head CI) → remove worktrees.
Then claim `P13-T04/D01` / `P13-T05/D01` / `P13-T06/D01` on separate leases.

## Non-claims

Not P13-T02 (hosted DSH), not P13-T06 group chat, not P13-T10 Skill/MCP acquisition.
Not Installed Agent, second scheduler, engine store, chat Approve, or mixing draft-apply
with authority-approve. No Gate/release/Profile/B01/Agent-benefit claim. Linux Pi
inference is not a Windows OPC qualification (P13-T13 backfills; Windows Pi route `not-run`).
