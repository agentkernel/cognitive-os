# v0.1 Re-review (M6 Exit)

- Date: 2026-07-21
- Baseline tip: `24842bb83a0395264e4d5467883e5ac841ceff84` (merge #34 onto main)
- CI: run `29801433518` success (ubuntu + windows)
- Reviewer lane: Lane-DOC
- Verdict: **GO-with-explicit-non-claim**

## 1. Entry check

| Check | Result |
|---|---|
| M6 delivery (install/sandbox/OOB/readiness/PERF builder/RC manifest) | pass (实现已提供；部分测试已执行) |
| Pins | **pass 55 / not-run 29 / self-check ≥36** (CI honesty gate on tip) |
| Contract freeze D-020/D-021 | honored (no new transition/readiness carriers) |
| F-017 vs release claim set | **closed-for-release-claim-set** — [f017-platform-matrix.md](../traceability/f017-platform-matrix.md) |
| Exit plan | [M6-EXIT-PLAN.md](../plan/M6-EXIT-PLAN.md) |

## 2. Acceptance matrix (exit)

| ID | Result | Notes |
|---|---|---|
| F017-MATRIX | **pass** | three linux_native deny digests pinned; reproduce commands recorded |
| F017-NO-MERGE | **pass** | `refuse_cross_platform_merge` unit |
| V01-REGRESS | **pass** (retained) | F-011 three negatives + AGENT-INSTALL/BYPASS/OOB |
| V01-MANIFEST | **pass** (honesty) | RC ≤ experimental；implemented = 0 |
| V01-PERF | **non-claim** | builder + ungoverned baseline field only；no full HW campaign digest |
| V01-D018 | **non-claim** | exchange-surface risk；governance object ports residual |
| V01-REVIEW | **pass** | this document |
| M6-A1 | pass | AGENT-INSTALL-001 |
| M6-A2 | pass (linux) | AGENT-BYPASS-002；Windows native unsupported |
| M6-A3 | pass (unit) + **durable non-claim** | in-process ledger；not KRN InstallationStore |
| M6-A4 | pass | AGENT-OOB-001 |
| M6-A5 | pass (milestone) | D-021；not registry conformance |
| M6-A6 | **partial + non-claim** | see V01-PERF |
| M6-A7 | pass (honesty) | implemented = 0 |

## 3. Explicit non-claims (must appear in any release note)

1. **Windows-native sandbox** — unsupported
2. **WSL2 guest sandbox** — not_tested（若日后测试仅代表 Linux guest）
3. **Durable install authority** — in-process ledger only；not KRN SQLite `InstallationStore`
4. **REQ-PERF-004 full HW campaign** — not executed；sample/builder ≠ campaign
5. **REQ-PERF-005 agent benefit** — not emitted
6. **Profile implemented** — zero profiles
7. **D-018 governance object ports** — residual；exchange-surface non-claim
8. **R2/R3, distributed, clients/Console/Agent Hub, M7+ surfaces** — out of v0.1

## 4. Honesty audit

- Sample manifest remains all-`planned` where used；RC binds report digest；profiles ≤ `experimental`
- No installation transition table or readiness carrier invented
- Expanding F-017 claims beyond the freeze table re-opens F-017 → NO-GO until new digests

## 5. Conclusion

**GO-with-explicit-non-claim** for v0.1 single-node R0/R1 reference release **documentation and tag readiness**, provided merged `main` CI is green at the tip that contains this review and the claim-freeze digests.

Not a claim that every profile is implemented, nor that Windows-native sandbox or durable install authority is proven.
