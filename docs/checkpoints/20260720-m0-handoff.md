# 20260720 M0 Handoff

## 1. 本次会话完成

- **任务 A**：Rust workspace（8 crate + kernel-server/admin-cli）+ pnpm workspace（contracts-ts/sdk-ts/agent-shell/tools）+ tests/tools/artifacts 目录 + 根 README 四区导航 + 工程配置（rust-toolchain 1.97.1、.gitignore、.gitattributes eol=lf）。提交 `2c3938c`。
- **任务 A/E3**：canonical JSON（RFC 8785）+ `CognitiveOS-Digest-V1` 域分离 digest/签名前缀，Rust（`cognitive-contracts::canonical`）与 TS（`@cognitiveos/contracts-ts`）双实现；`tests/golden/canonical-json-fixtures.json`（12 正例 + 8 负例）双侧执行 + emit_golden 跨语言逐字节对比通过。同批提交。
- **任务 B**：`.cursor/rules/` 9 份（00/01/02 alwaysApply + 10~15 globs）`13e2d4e`；AGENTS.md；docs/README.md 文档地图 + 8 份新标准 + ADR-0001/0002/0003/0006 + checkpoints/TEMPLATE.md `d0122a6`；findings-ledger（F/IMP 逐条核验 + 漂移 D-001~006）+ matrix.yaml（273 条派生）`42dcc4c`。
- **任务 C+F**：DEVELOPMENT-PLAN（M0~M11 + Console 九组依赖台账 + 映射表 + 风险 + 待决项 P-1~P-4）、PROGRESS、PARALLEL-LANES `c1747aa`；docs/prompts/ 14 份接续提示词（本 handoff 同批提交）。
- **任务 D+E**：tools 一致性检查（schema 元校验/$ref、双向孤儿、断链、孤儿 REQ 引用、matrix 完整性与新鲜度、台账完整性）、validate-manifest、CI 两 OS 矩阵、PR 模板、docs-sync-contract；最小漂移修正 D-005（迁移表 schema version enum）`0758bc0`。
- **注入演练**（docs-sync-contract §5）：分支 `drill/docs-sync-injection` 注入孤儿 REQ 引用（PROGRESS）+ 断链（docs/README）→ 检查器退出码 1 并逐条指出 `docs/plan/PROGRESS.md: orphan requirement reference` 与 `docs/README.md: broken relative link`（完整输出见 §3）→ 回滚删支，主区复绿。

## 2. 未完成 / 进行中

- 无 M0 范围内未完成项。M1 起工作见 §5。
- CI 远端未验证（仓库无 remote，待决项 P-3）；本地已逐步执行 ci.yml 全部命令等价序列。

## 3. 测试与证据状态

- Rust：19 测试通过（单元 + golden 集成）；clippy -D warnings 绿；fmt 绿。
- TS：14 测试通过（contracts-ts 10 / sdk-ts 1 / agent-shell 1 / tools 2）。
- 一致性检查：`check-consistency: OK (273 requirements, 55 error codes, 56 schemas, 74 vectors, ...)`；`gen-matrix --check` 新鲜。
- runner：74 向量全 `not-run`（15 层 + contract-traceability 18 条跨切片）；样例 manifest 13 Profile 全 `planned` 且过 schema 校验。证据在 `artifacts/evidence/conformance/`（gitignore，按需再生成）。
- 跨语言 golden：`GOLDEN MATCH`（digest map 逐字节一致）。
- 注入演练输出摘录：`check-consistency: 2 violation(s)`——`docs/README.md → broken relative link: standards/no-such-standard.md`；`docs/plan/PROGRESS.md → orphan requirement reference`（演练注入了一个 registry 中不存在的 CTX 域 999 号 REQ 编号，被逐字指出；此处不复写该编号本体，避免台账文档自身触发孤儿检查）。

## 4. 未决风险与漂移

- 漂移台账：D-001/D-004/D-006 开放（排 M1）；D-002/D-003/D-005 已闭合。开放 P0：F-003（阻断 M1 出口）；开放 P1：F-011/F-014/F-023/F-017（+F-015 持续）。
- **并行会话提醒**：本会话进行期间，工作区出现另一会话的未提交改动，且仍在推进——36+ 份 schema 的单轨迁移（metadata/strongRef → governed-object-header/object-reference，即 F-003 方向）、`specs/registry/requirements.yaml` 的 owner_spec 改指、两个向量调整、`tools/check_metadata_shape.py`/`migrate_governed_header.py` 两个脚本、`apps/cognitiveos-console/` 产品文档大改与新增 `docs/` 子目录。**均已保护，未混入本会话任何提交**（一次误吸收的 matrix 重生成已还原）。接续 Lane-CTR 的会话必须先与该并行工作对齐（其改动即 M1 F-003 任务本体），避免双头迁移。
- **已知本地现象**：只要上述 registry 改动保持未提交，本地 `gen-matrix --check`（含 `pnpm -r test` 中 tools 的用例）会如实报 matrix 陈旧——这是检查器在正确工作（worktree registry ≠ 已提交 matrix）。对本会话已提交内容，干净检出下全部检查为绿。Lane-CTR 提交 registry 变更时随批 `gen-matrix` 再生成即消除。
- 待决项 P-1~P-4（许可证/发布渠道/CI 远端/pnpm 版本）见 DEVELOPMENT-PLAN §6。

## 5. 下一步入口

- 建议：启动 **Lane-CTR（M1）**，提示词 `docs/prompts/lane-ctr.md`（统筹式用 `docs/prompts/milestone-m1.md`）；可同时开 **Lane-CFR** 会话（`docs/prompts/lane-cfr.md`）并行 runner 执行能力。
- 工作分支：`lane/ctr` / `lane/cfr`（从 main 新建）。
- 第一个动作：核对工作区并行迁移改动与 F-003 任务边界（见 §4），再按提示词清点双轨 schema。

## 6. 快照

- PROGRESS 已更新：是（M0 done，M1 not-started 入口开启）。
- 本次提交：`2c3938c`（A）→ `13e2d4e`（B1/B2）→ `d0122a6`（B3 文档）→ `42dcc4c`（B3 追溯）→ `c1747aa`（C+F 计划）→ `0758bc0`（D+E 工具与 CI）→ 本 handoff 批次（prompts + checkpoints，哈希见 git log）。
