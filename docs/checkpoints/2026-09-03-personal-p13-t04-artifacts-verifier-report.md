# P13-T04 Independent verification + openable outputs + publication package — running report

- Task: `P13-T04` / slices `P13-T04/D01` → `P13-T04/D02`
- Change class: `implementation-only` (Attempt artifacts enter the existing P3-T03 daemon CAS; a new registered independent verifier identity under the P2-T07/P2-T13 verifier seam; StageTestPassed derived from durable evidence through the existing P11-T03 `derive_stage_test_passed`; `run-acceptance` / `external-send` join the P11-T09 ApprovalPreview subject kinds via an additive v37 authority migration; management HTTP routes; `outputs` page on daemon `/ui/`; no `core/specs`, no Lane-CTR, no new first-level chrome, no chat Confirm, no Publish button)
- Product: CognitiveOS Personal 2.0.0
- Lease: `lease/personal/P13-T04/artifacts-verifier`
- Branch: `personal/P13-T04-artifacts-verifier` (worktree `D:\agent-kernel-wt-p13-t04`; `d:\agent-kernel` untouched, A8 protected)
- Base: `origin/main@964dd29b` (P13-T02 lease closure; PR #310 merged at `main@d8a002ea`)
- Claim ceiling: `hypothesis` (A7: local / CI / Linux native is not Gate / release / Profile; host file-open E2E stays `not-run` until `P13-T13`)
- Evaluation routing: **OFF**

## Unique next action

Closure (this checkpoint): acceptance mapping below → ledgers `in-progress → closing` on the branch → required CI on the final head → PR #313 ready + merge → lease-closure commit on `main` (`done`) → worktree / Linux cleanup → **owner-directed pause** (no new claim; see the closure file). Superseded earlier wording (kept for the record): D01 failure-first negatives → vertical chain → D02 `outputs` / publication on the same lease — both slices are implemented and validated per the log below.

## Identifier

Verifier identity `verifier://personal/attempt-artifact` `v1` (principal `principal://personal/independent-verifier`, the same principal the P2-T13 production verifier uses; deterministic checks only). Artifact source `hosted-dsh-child:candidate:DeliverableDraft`. CAS = `ArtifactStore` at `<data_dir>/artifacts` (single daemon CAS, P3-T03); openable exports under Personal Home `data/` = `<data_dir>/projects/<project_id>/outputs/`. Reused, not rebuilt: v36 Attempt / frame ledger (P13-T02), `derive_stage_test_passed` + `p11_stage_test_fact` (P11-T03), `request_preview` / `confirm_preview` (P11-T09), P11-T14 preview → persist-before-dispatch ledger shape for external-send.

## Failure-first (D01)

| ID | Negative | Surface |
|---|---|---|
| N1 | model text ≠ completion: a `DeliverableDraft` whose text says "task complete / success" ingests as an artifact with `verification_status = not-run`; nothing on the Attempt row changes (`completion_claimed = 0`, `verification_status = 'not-run'` CHECK) | store |
| N2 | HTTP 200 receipt / `response done` / exit 0 / `agent_end` ≠ completion: an Attempt with `response_status = done`, exit 0 and no evidence cannot derive StageTestPassed and cannot mint a run-acceptance preview | store + kernel-server |
| N3 | intermediate-ring acceptance refused: a current StageTestPassed on a non-last stage still cannot mint `run-acceptance`; `confirm` of a stale preview after the plan moved is refused | store |
| N4 | file-as-authority refused: a `file://` / filesystem path is never accepted as an artifact reference; CAS bytes tampered on disk → verifier `failed` (digest mismatch), StageTestPassed refused; an exported copy under `data/` is never re-read as authority | store + kernel-server |
| N5 | verifier independence: evidence can only be written by the verifier path (append-only; no caller-supplied `passed`); the Attempt's own `response_status` never feeds the disposition | store |
| N6 | freshness: a newer artifact for the same task supersedes the older one; a superseded artifact cannot back a StageTestPassed | store |
| N7 | task channel cannot ingest / verify / accept / open (403); artifact of another Project cannot be accepted (cross-project refused) | kernel-server |
| N8 | secret shape never enters an artifact or evidence (refused / redacted) | store |

## Incremental validation log (TEST-REPORT-INCREMENTAL-01)

Units are appended **immediately** after each finishes. `not-run` is never pass.

| Time | Unit | Result | Env | Revision | Notes |
|---|---|---|---|---|---|
| 2026-09-03 | Lease claim: PARALLEL-LANES §3 row `lease/personal/P13-T04/artifacts-verifier`; PROGRESS heading / snapshot / Active task lease row / P13-T04 row / Layer 1 (167/133/2/1/15/34) / Layer 2 `P13-T04/D01` in-progress; formal plan Phase 13 + 合计 counts and P13-T04 row `in-progress`; plan.md card status | recorded | docs-only (`DEV-WIN-GNU-01`) | worktree from `origin/main@964dd29b`, uncommitted | worktree `D:\agent-kernel-wt-p13-t04` created fresh (no prior branch / worktree existed) |
| 2026-09-03 | `pnpm run check:consistency` after the lease claim | **pass** | `DEV-WIN-GNU-01` (Node) | worktree, uncommitted | first run failed only because the fresh worktree had no `node_modules` (`pnpm install --frozen-lockfile` fixed it; environment, not a violation) |
| 2026-09-03 | D01 failure-first tests written **before** the implementation: store `tests/p13_t04_attempt_artifacts.rs` (7 tests: N1 model text / `response done` / exit 0 ≠ completion; N5 verifier writes CAS-backed evidence, ignores `response_status`, evidence append-only, foreign verifier identity refused by CHECK; N4 file / `file://` / `artifact://` refs refused, payload not bound to the observed frame refused, observation frame refused, tampered CAS bytes → `failed` + StageTestPassed refused; N3 StageTestPassed derived from evidence, cross-slot refused, intermediate-ring acceptance refused, last-ring preview → confirm → append-only acceptance fact, second acceptance refused; N6 newer artifact supersedes, stale acceptance preview cannot confirm; N8 secret-shaped deliverable refused; external send previewed → planned, `published` unrepresentable) and kernel-server `attempt_artifacts.rs::tests` (2 tests: task-channel 403 + forged refs 404 + no-fact refusal; **real `node` child** → CAS ingest → auto-verify → `outputs` / `outputs.open` / `outputs.detail` / `outputs.export` (copy edited by hand changes nothing) → stage-test wrong slot / intermediate ring refused → last-ring preview → stale digest refused → `confirm` → acceptance → packet → external-send preview → `confirm` → `planned`, `published:false`) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | `cargo test` is `not-run` here (`RUST-LINK-DEV-WIN-GNU-01`); first execution is required CI + `DEV-LINUX-NATIVE-01`. Failure-first is authored-first; the implementation landed in the same checkpoint because a compile-failing red checkpoint would prove nothing on CI |
| 2026-09-03 | D01 implementation written: store v37 `attempt_artifacts.rs` (`p13_attempt_artifact` / `p13_artifact_evidence` / `p13_run_acceptance` / `p13_external_send` + `p11_approval_preview` rebuild for `run-acceptance` / `external-send`; `ingest_candidate`, `verify_artifact`, `derive_stage_test`, `request_run_acceptance`, `publication_packet`, `request_external_send`; locked helpers `accept_run_locked` / `external_send_locked` called from `ProjectAggregateStore::confirm_preview`), runtime broker retains the canonical candidate payload in memory (`HostedFrame::payload_canonical`, ≤256 KiB, candidate frames only), kernel-server `attempt_artifacts.rs` (11 management routes + task aliases 403; `ingest_terminal_run` called from the P13-T02 broker thread right after `record_terminal`), `server.rs` dispatch, `personal_db.rs` v37, migration test bumped to 37, `Cargo.lock` dependency-list line for `serde_json_canonicalizer` in `cognitive-store` (hand-edited; the crate is already resolved for `cognitive-runtime`) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | vertical chain: hosted Attempt terminal → CAS put → verifier evidence → StageTestPassed → last-ring `run-acceptance` preview → `confirm` → acceptance fact; publication packet → `external-send` preview → `confirm` → `planned` |
| 2026-09-03 | `cargo fmt --all -- --check` (after `cargo fmt --all`) | **pass** | `DEV-WIN-GNU-01` | worktree, uncommitted | first attempt failed to parse: a `r#"…"#` raw string in the kernel-server test contained `"# Report` — switched to `r##"…"##`; formatting only, no link |
| 2026-09-03 | `cargo build/test/clippy` (store `p13_t04_attempt_artifacts`, `p1_t01_layout_migrations`; runtime `hosted_dsh_broker::tests`; kernel-server `attempt_artifacts::tests`) | **not-run** | `DEV-WIN-GNU-01` | worktree, uncommitted | `RUST-LINK-DEV-WIN-GNU-01`; routed to required CI + exact-revision `DEV-LINUX-NATIVE-01` |
| 2026-09-03 | Handbook: 22 route annotations (11 management + 11 task 403) in `http-routes.json`; `attempt_artifacts.rs` added to the http-api definition sources; `store-and-migrations` (v37 map row + narrative), `daemon-and-http`, `capability-status`, `execution-chain-status` hand-updated in en + zh-CN; `node tools/src/generate-handbook.mjs` regenerated `http-api` (both locales); `fill-handbook-fingerprints` (22 pages) → `check-handbook` **pass** (58×2, 9 generated) → `generate-handbook --check` **pass** (18 byte-identical) | **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | fingerprint-only pages added to the lease writable paths |
| 2026-09-03 | `pnpm run check:consistency` / `pnpm run check:rules` / `git diff --check` | **pass** / **pass** (5 known local-only warnings) / **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | — |
| 2026-09-03 | D01 checkpoint committed + pushed; Draft PR [#313](https://github.com/agentkernel/cognitive-os/pull/313) opened | recorded | GitHub | `292dc288979cd5a1a48b06e53a26972f9daf77d4` | pre-commit docs-sync gate OK (check-handbook 58×2, generator `--check` 18 byte-identical) |
| 2026-09-03 | `cargo test -p cognitive-store --test p13_t04_attempt_artifacts --test p1_t01_layout_migrations --locked` | **pass** 7/7 + 8/8 | `DEV-LINUX-NATIVE-01` `~/cognitiveos-personal-worktrees/p13-t04-292dc288` (`git rev-parse HEAD` = `292dc288…`; `CARGO_TARGET_DIR` reused from the retained `p11-t14-cf94a8d/target` because the host disk is at 93 %) | `292dc288` | first execution of the failure-first suite: model text / `response done` / exit 0 ≠ completion; CAS-backed evidence + append-only + foreign verifier identity refused by CHECK; file / `file://` / `artifact://` refs refused; payload not bound to the frame refused; observation frame refused; tampered CAS → `failed` + StageTestPassed refused; cross-slot refused; intermediate ring refused; last-ring preview → confirm → acceptance fact; superseded artifact refused; stale preview refused; secret shape refused; external send previewed → planned, `published` unrepresentable; v37 migrates from empty and from the previous fixture, replay-safe |
| 2026-09-03 | D02 web: `data/projections/outputs.ts` (+ 5 unit tests), `loadOpcReads` loaders, `KNOWN_ROUTES` (4 GET + 4 POST), `ProjectOutputsPage` select-then-view (artifact listbox → detail with evidence criteria / verifier identity / acceptance / export state; Open from CAS via `outputs.open` + client-side download; Export copy to `data/` labelled not authority; Derive stage test only for a verified current artifact from the Member's responsible stages; 「验收，回 Today」 only on the last ring as a `run-acceptance` preview deep-linked to the HITL canvas; AUTONOMY packet panel `planned` / `not published` / `chat has no Confirm`; Request external-send preview deep-linked to the canvas; no Publish button); `projectOutputs.test.tsx` 6 tests; the P12-T03 outputs case in `projectSubmenus.test.tsx` follows the artifact-based semantics | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | fake-action regex in the tests now also refuses `publish` |
| 2026-09-03 | `clients/pc/web` `pnpm test` / `pnpm build` (tsc + vite) | **pass** 428/428 (57 files) / **pass** | `DEV-WIN-GNU-01` (Node 24) | worktree, uncommitted | `pnpm install --frozen-lockfile` first (fresh worktree) |
| 2026-09-03 | D02 web checkpoint committed (`DOCS_IMPACT_NONE` acknowledged: client-side rendering of routes documented in the D01 handbook sync; `clients/docs` is its own governance tree) | recorded | `DEV-WIN-GNU-01` | `024ad1aa` | — |
| 2026-09-03 | Draft PR #313 `CONFLICTING` before any CI ran: `origin/main` moved to `3d66d66d` (P13-T03 PR #311 merged at `main@7f9b4115`, P0-T09 PR #312 merged at `main@8badb83c`, both lease closures). `git merge origin/main`: 24 conflicts — `mod.rs` / `server.rs` (both `assistant_inference` and `attempt_artifacts` kept), PROGRESS (heading, snapshot prose, Active task lease row = this lease + P13-T03 / P0-T09 closures, Layer 1 167/135/2/1/13/32, Layer 2 P13-T03/D01 done + P13-T04/D01 in-progress), formal plan (Phase 13 3/2/0/8, 合计 135/2/1/13; P13-T03 done row + P13-T04 in-progress row), 20 handbook pages (all fingerprint-only or generated) → regenerate + `fill-handbook-fingerprints` (18) → `check-handbook` **pass** → generator `--check` **pass** → `check:consistency` **pass** (main's new tracked-only link + build-order edge-set checks) → `check:rules` **pass** → `cargo fmt --check` **pass** → `git diff --check` **pass** | **pass** | `DEV-WIN-GNU-01` (Node) | merge commit `582e0377` | sibling ledger rows integrated (PARALLEL-LANES rule 8); no sibling migration landed, v37 kept |
| 2026-09-03 | `clients/pc/web` `pnpm test` after the merge (P13-T03 web changes + P13-T04) | **pass** 444/444 (58 files) | `DEV-WIN-GNU-01` (Node 24) | `582e0377` | 433 (main) + 11 (this task) |
| 2026-09-03 | Merge checkpoint pushed; required CI run [33704422347](https://github.com/agentkernel/cognitive-os/actions/runs/33704422347) triggered (`resolve validation route` pass; ubuntu / windows pending) | recorded | GitHub | `582e0377` | — |
| 2026-09-03 | Linux stage B (`~/cos-wt/p13t04-stageB.sh`) at `582e0377` | **not-run** (fetch) | `DEV-LINUX-NATIVE-01` | `582e0377` | `git fetch` from GitHub aborted (`Operation too slow. Less than 1000 bytes/sec`) — host-side transient (P13-T03 saw the same); re-run at the next pushed revision |
| 2026-09-03 | Required CI run [33704422347](https://github.com/agentkernel/cognitive-os/actions/runs/33704422347) `verify (ubuntu-latest)`: Rust workspace **built**; `Test Rust workspace` 397 passed / **1 failed** — `attempt_artifacts::tests::p13_t04_real_attempt_enters_cas_is_verified_and_accepts_only_on_the_last_ring` at the s1 stage-test step: `422 ATTEMPT_ARTIFACT_REJECTED superseded artifact cannot back a stage test` | **fail** (real defect, fixed) | `CI-UBUNTU-01` | `582e0377` | **Design defect found by the real-child test:** freshness was keyed by (project, task ref) only, so the researcher's later deliverable on the same task ref superseded the manager's ring-one artifact. Freshness is now per Member (`n.employee_id = a.employee_id` in the newer-artifact subquery); a store assertion pins "another Member's deliverable never supersedes". Not a test-shape fix — the product rule changed. All other Rust suites passed (store 7/7 + 8/8 included) |
| 2026-09-03 | Per-Member freshness fix committed + pushed; required CI run [33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112) triggered on the new head | recorded | GitHub | `84b2467e38b09051c9c19b7f8d4c36252ebf7088` | worker aborted (connection error) after the push; this row and the two below were written by the resuming worker from the retained Linux logs (`~/cos-wt/p13t04-stageB.log`) — append-only, nothing rewritten |
| 2026-09-03 | Linux stage B (`~/cos-wt/p13t04-stageB.sh`) at the pushed fix: worktree `~/cognitiveos-personal-worktrees/p13-t04-292dc288` moved to `git rev-parse HEAD` = `84b2467e…` (fetch succeeded on retry); `cargo test -p cognitive-store --test p13_t04_attempt_artifacts --test p1_t01_layout_migrations --locked` **pass** 7/7 + 8/8 (incl. the new per-Member assertion "another Member's deliverable never supersedes"); `cargo test -p cognitive-runtime --lib hosted_dsh_broker --locked` **pass** 6/6 (touched: `payload_canonical` retention); `cargo clippy -p cognitive-store -p cognitive-runtime -p kernel-server --all-targets --locked -- -D warnings` **pass** (Finished, no warnings) | **pass** | `DEV-LINUX-NATIVE-01` (node v22.19.0, cargo 1.97.1; `CARGO_TARGET_DIR` reused from `p11-t14-cf94a8d/target`) | `84b2467e` | — |
| 2026-09-03 | Linux stage B kernel-server step: `cargo test -p kernel-server --bin kernel-server attempt_artifacts hosted_dsh_attempt …` | **not-run** (script defect) | `DEV-LINUX-NATIVE-01` | `84b2467e` | libtest refused the second positional filter (`error: unexpected argument 'hosted_dsh_attempt' found`); no test executed. Re-run as stage B2 with one filter per invocation (row below) |
| 2026-09-03 | Required CI run [33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112): `resolve validation route` 3s, `verify (ubuntu-latest)` 3m51s, `verify (windows-latest)` 13m21s, `required-ci` 3s | **pass** | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `84b2467e` | full workspace build / test / clippy on both platforms including the real-child kernel-server test that failed at `582e0377`; first green required CI for the task code |
| 2026-09-03 | Linux stage B2 (`~/cos-wt/p13t04-stageB2.sh`, one libtest filter per invocation): `cargo test -p kernel-server --bin kernel-server attempt_artifacts --locked -- --test-threads=1` **pass** 2/2 (**real `node` child**: task-channel 403 + forged `file://` / traversal / bare-digest refs 404 + no-fact refusal; manager ring → researcher ring → CAS ingest → auto-verify → `outputs` / `open` / `detail` / `export` (hand-edited copy changes nothing) → intermediate ring 422 "last ring" → wrong slot 422 → last-ring preview → stale digest refused → `confirm` → `run_accepted` → packet `planned`/`published:false` → external-send preview → `confirm` → `planned`, `connector none-qualified`); `hosted_dsh_attempt` **pass** 2/2 (touched: ingest hook after `record_terminal`); `project_aggregate` **pass** 19/19 (touched: `confirm_preview` subject kinds); store touched suites `p11_t03_project_aggregate` **pass** 19/19, `p11_t09_hitl_canvas` **pass** 11/11, `p13_t02_hosted_dsh_attempt` **pass** 9/9 | **pass** | `DEV-LINUX-NATIVE-01` (worktree `git rev-parse HEAD` = `84b2467e…`, `dirty=0`) | `84b2467e` | the per-Member freshness fix is what makes the manager's ring-one artifact stay `current` next to the researcher's later deliverable |
| 2026-09-03 | Resume (third worker): recovery tuple verified — worktree `D:\agent-kernel-wt-p13-t04` on `personal/P13-T04-artifacts-verifier` at `84b2467e`, in sync with origin, only this report uncommitted; Draft PR #313 checks `required-ci` / `resolve validation route` / `verify (ubuntu-latest)` / `verify (windows-latest)` all **pass** ([33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112)); `origin/main` still `3d66d66d`; Linux worktree `~/cognitiveos-personal-worktrees/p13-t04-292dc288` at `84b2467e`, `dirty=0`; three unrelated `kernel-server` processes on the host (deployment `:48181`, P8-T09 `:39245`, P11-T08 `:48681`) left untouched | recorded | `DEV-WIN-GNU-01` + `DEV-LINUX-NATIVE-01` | `84b2467e` | remaining: live daemon E2E → local gates → handbook / closure docs → merge → lease closure |
| 2026-09-03 | **Live-daemon E2E setup** (Linux stage C, `~/cos-wt/p13t04-stageC.sh`, log `p13t04-stageC.log`) at the exact revision: `cargo build -p kernel-server -p admin-cli --locked` **pass**; scratch seed crate **outside** the repo (`~/cos-wt/p13t04-seed`, depends on the worktree's `cognitive-store` by path, run **before** the daemon starts) seeded G1 project `project-01a06550-d893…` → PlanRevision `plan-01a06550-d89c…` (s1 `manager`, s2 `researcher`; both stages confirmed) → roster → manager `employee-…53fb` + researcher `employee-…756d` seated; disposable runtime root `~/cos-wt/p13-t04-rt` (0700): `cognitive init --provider deepseek --model-id deepseek-v4-flash --reuse-existing-secret-binding` → `bound_existing_secret_ref`, `secret_ref_redacted:true` (host's **pre-existing** Linux Secret Service entry, same as P13-T03; no key created / read / printed); Provider control-plane **account row seeded as a fixture** pointing at that same opaque `secret_ref` (`acct-p13t04-deepseek`, `openai_compatible`, `https://api.deepseek.com`; the P13-T08 Settings write path does not exist yet); `cognitive dsh configure --dsh-root ~/agent-kernel-worktrees/p8-t09-pins/dsh --adapter-root <worktree>/personal/packages/dsh-akp-adapter --revision 528c682e…` → `configured`, `candidate_only:true` (pin file = git HEAD = `528c682e…`); `cognitive daemon start --bind 127.0.0.1:48784` → doctor system / database / secret (`secret_material_exposed:false`) / provider (`secret_ref_resolves:true`) / daemon **ready**, pi `not_configured` (not needed); **product routes**: `cognitive provider models refresh --account-id acct-p13t04-deepseek` → live catalog discovery through the existing secret returned 3 models (`deepseek-v4-flash`, `deepseek-v4-flash-vision-exp`, `deepseek-v4-pro`); `cognitive agent binding set --agent dsh --account-id acct-p13t04-deepseek --model-id deepseek-v4-flash` → binding `agent://personal/dsh` revision 1 `active` | **pass** | `DEV-LINUX-NATIVE-01` (worktree `HEAD` = `84b2467e…`, `dirty=0`; `CARGO_TARGET_DIR` reused from `p11-t14-cf94a8d/target`; node v22.19.0, cargo 1.97.1, pnpm 10.33.2) | `84b2467e` | Seeding is fixture setup only: no product HTTP/CLI path applies a PlanRevision (P13-T02 gap, owned by P13-T05/T06) and no product path yet creates a Provider account from an existing Secret Service entry (P13-T08). The web bundle build failed in this stage only because `clients/pc/web` is its own pnpm project (`tsc: not found` — environment, not a violation); re-done in stage D. Unlike P13-T02's live run, the dsh leg is now **bound** so the real pinned dsh can produce a deliverable through the daemon proxy |
| 2026-09-03 | Live stage D setup: `clients/pc/web` `pnpm install --frozen-lockfile` + `pnpm build` (tsc + vite, `base /ui/`) on Linux, `dist/` copied into `<data_dir>/ui/` (worktree stays `dirty=0`) | **pass** | `DEV-LINUX-NATIVE-01` | `84b2467e` | daemon serves `/ui/` from disk; no restart needed |
| 2026-09-03 | **Live U0–U2** (driver `~/cos-wt/p13t04-e2e.py`, loopback HTTP; bootstrap read from file, token in memory, both scanned-for and never printed): management session minted; `dsh.hosted.artifact.check` → `fact.health: pinned`, `kind: health-check`, `admits_spawn: true`, `windows_supply_chain_e2e: not-run`; task channel with a management bearer → **403** `SHELL_CHANNEL_BINDING_MISMATCH` (front door), with a real task bearer → **403** `ATTEMPT_ARTIFACT_CHANNEL_FORBIDDEN`; `outputs` without bearer → **401**; `outputs` before any Attempt → `artifacts: []`, `files_are_authority: false`, `chat_can_confirm: false`, `host_file_open_e2e: not-run`; `outputs.open` with `file:///etc/passwd` / `..%2F..%2Fauthority.sqlite` / bare `sha256:000…` → **404** each; `run.acceptance.request s2` → **422** "last ring has no current StageTestPassed" | **pass** 8/8 | `DEV-LINUX-NATIVE-01` (live daemon `127.0.0.1:48784`) | `84b2467e` | the driver's first pass flagged U1 / U2a only because it read `health` at the top level and expected this module's error code on a management bearer; the probe (`p13t04-probe.sh`) recorded the exact bodies above — refusals in both cases |
| 2026-09-03 | **Live U3–U4 real Attempts**: `POST dsh.hosted.attempt.run` (manager, ring one, `timeout_ms 180000`, `wait:true`) → daemon spawned the **product child** `hosted-attempt-child.mjs` → child minted a management session from the bootstrap *path* → **real pinned dsh `528c682e` (compiled-lib)** → `POST /provider/v1/dsh/chat/completions` through the **bound** control-plane binding (`deepseek-v4-flash`, existing Secret Service entry) → dsh answered → child emitted **one `DeliverableDraft` candidate** → daemon terminal `exited/0`, `response_status: done`, `completion_claimed: false`, `verification_status: not-run`, `candidate_count: 1`, `receipt_is_not_completion: true`, wall 4.9 s (`dshattempt-01a06552-f044…`, child `dshchild-01a06552-f04b…`); researcher (last ring) likewise: `exited/0`, `done`, `completion_claimed: false`, `candidate_count: 1`, wall 5.5 s (`dshattempt-01a06553-034d…`); `attempt.detail` 17 frames (`observation` / `heartbeat` / `candidate` / `response`), `authority_written=false` for every frame | **pass** | `DEV-LINUX-NATIVE-01` | `84b2467e` | this is the Provider `done` leg P13-T02 could not exercise: a **real** model deliverable entered the loop; `done` + exit 0 + `DeliverableDraft` text still never move `completion_claimed` / `verification_status` on the Attempt row |
| 2026-09-03 | **Live U5–U8 CAS + outputs + open + export**: `outputs?project_id` → 2 artifacts; researcher artifact `artifact-01a06553-18bb…`: `source hosted-dsh-child:candidate:DeliverableDraft`, `format text/markdown`, `freshness current`, `verification_status passed`, `accepted_at null`, `cas_ref sha256:d1b0ebec…` (70 bytes); manager artifact `artifact-01a06553-0339…` stays `current` + `passed` next to the later deliverable (**per-Member freshness**); CAS file `<data_dir>/artifacts/d1b0ebec…` exists and re-hashes to its digest; `outputs.detail` → 1 evidence row `passed` by `verifier://personal/attempt-artifact` / `principal://personal/independent-verifier`, criteria `attempt-terminal-observed pass`, **`attempt-response-status not-used`**, `source-frame-bound pass`, `source-payload-cas pass`, `cas-bytes-match-digest pass`, `deliverable-non-empty pass`, `no-secret-shape pass`, `format-parses pass`, report bytes in the same CAS (`report_cas_ref sha256:7c78b5c1…`); `outputs.open` → 200 `text/markdown; charset=utf-8`, body sha256 == `cas_ref`; `outputs.export` → copy at `<data_dir>/projects/<project>/outputs/<artifact>.md` (sha == digest), `is_authority: false`, `host_file_open_e2e: not-run`; copy overwritten by hand → `open` still serves the CAS bytes, `detail.export = {exists: true, is_authority: false}`, artifact still `passed` | **pass** 8/8 | `DEV-LINUX-NATIVE-01` | `84b2467e` | deliverable text is the real model's last assistant line (product child behaviour from P13-T02) — content is irrelevant to the verifier, which never reads the Attempt's `response_status` |
| 2026-09-03 | **Live U9–U10 last-ring acceptance**: `run.acceptance.request s2` → 422 (no fact); `attempt.artifact.stage-test` manager artifact + `s1` → 200 `derived_from [seating, independent-verifier-evidence, cas-re-read, attempt-terminal]`, `stage_test_is_not_acceptance: true`; `run.acceptance.request s1` → **422** "run acceptance is only offered on the last ring"; manager artifact + `s2` → **422** "the Attempt's Member is not responsible for this stage"; researcher artifact + `s2` → 200; `run.acceptance.request s2` → 200 `subject_kind run-acceptance`, `chat_can_confirm: false`, `preview-01a06553-18ec…`, `confirm_route /management/project/v1/confirm`; `pending-previews?subject_ref=<project>` lists it; `confirm` with a zero digest → **409** `PROJECT_STALE`; `confirm` with the real digest → 200 `result: run_accepted`, `runaccept-01a06553-18f3…`, `receipt_ref cas:f50134fd…`; `run.acceptance?project_id` → 1 row `s2`, `last_ring: true`, bound to the researcher artifact; second `run.acceptance.request s2` → **409** `ATTEMPT_ARTIFACT_CONFLICT` "this StageTestPassed fact was already accepted"; `outputs.detail` → `accepted_at` set, `run_acceptance` (`stage_position 1 / stage_count 2`, `evidence_id`, `stage_test_fact_id`, `acceptance_decision_ref`) | **pass** 12/12 | `DEV-LINUX-NATIVE-01` | `84b2467e` | the driver expected 422 for the double acceptance; 409 is the correct conflict class — refused either way |
| 2026-09-03 | **Live U11–U12 publication packet + external send**: `publication.packet?project_id&artifact_id` → `planned: true`, `published: false`, `chat_can_confirm: false`, `autonomy_packet` = `{memory_of_actions, observable, outcome_verify, override, preview, tiered_authority, yield}`, `outcome_verify {accepted: true, verified: true, verifier_ref, evidence_id, run_acceptance_id}`; `publication.external-send.request` (2 recipients) → 200 `subject_kind external-send`, `send.state previewed`, `published: false`; `confirm` → 200 `result: external_send_planned`, `send-01a06553-1904…`, `receipt_ref receipt:external-send:…`; `publication.sends` → 1 row `planned`, `published: false`, `connector none-qualified`, `published_any: false` | **pass** 4/4 | `DEV-LINUX-NATIVE-01` | `84b2467e` | planned ≠ published on a live daemon; no connector is qualified, nothing left the machine |
| 2026-09-03 | **Live U13 tamper (file is never authority)**: manager artifact's CAS file appended on disk → `attempt.artifact.verify` → 200 evidence `disposition failed` (`checked_cas_ref sha256:72bd47d9…` ≠ artifact digest); `attempt.artifact.stage-test s1` → **422** "completion requires current verification"; `outputs.open` → **409** `ATTEMPT_ARTIFACT_DIGEST_MISMATCH` "deliverable bytes on disk no longer hash to the artifact digest"; `outputs.detail` → `verification_status failed`, evidence history `[failed, passed]` (append-only) | **pass** 4/4 | `DEV-LINUX-NATIVE-01` | `84b2467e` | the earlier `passed` evidence is superseded, not rewritten |
| 2026-09-03 | **Live U14–U15**: daemon `GET /ui/` → 200 index; `/ui/assets/index-CpGKXUv_.js` → 200 and the bundle contains `outputs.open`, `publication.packet`, `publication.external-send.request`, `run.acceptance.request` (the `ProjectOutputsPage` routes at this revision); `dsh.hosted.attempt.list?project_id` → 2 rows, every row `response_status done` / `completion_claimed false` / `verification_status not-run`, `receipt_is_not_completion: true`; secret scan of **every** response body against the live bootstrap secret and management token → **0 leaks**; no leftover `p13t02-hosted-attempt-*` work dirs in `/tmp`; `daemon.log` tail secret-free | **pass** | `DEV-LINUX-NATIVE-01` | `84b2467e` | `/ui/` bundle presence is not a rendered-browser review (that is `P13-T12/D02`); host file-open E2E stays **not-run** (`P13-T13`) |
| 2026-09-03 | Live E2E cleanup (stage E): `cognitive daemon stop` → `stopped`; port 48784 free; no `p13-t04` kernel-server, no orphan `hosted-attempt-child`; runtime root `~/cos-wt/p13-t04-rt` and seed crate `~/cos-wt/p13t04-seed` removed; `/tmp` has no `p13t02-hosted-attempt-*` residue; the three unrelated kernel-server processes (`:48181`, `:39245`, `:48681`) untouched; worktree `p13-t04-292dc288` retained at `84b2467e` (`dirty=0`) until closure. The pre-existing Secret Service entry was neither created, read, printed nor deleted by this task | **pass** | `DEV-LINUX-NATIVE-01` | `84b2467e` | — |
| 2026-09-03 | Local gates at the closure candidate (`84b2467e` + this report): `cargo fmt --all -- --check` **pass**; `git diff --check` **pass**; `pnpm run check:consistency` **pass** (tracked-only links + Phase 13 build-order edge set); `pnpm run check:handbook` **pass** (58×2, 9 generated); `node tools/src/generate-handbook.mjs --check` **pass** (18 byte-identical); `pnpm run check:rules` **pass** (5 known local-only warnings, 0 failures); `pnpm -r build` **pass**; `pnpm -r test` **pass** (repo-tools, contracts-ts, sdk-ts, pi-cognitiveos, agent-shell 13/13); `clients/pc/web` `pnpm test` **pass** 444/444 (58 files) + `pnpm build` **pass** | **pass** | `DEV-WIN-GNU-01` (Node 24, cargo fmt only) | `84b2467e` + uncommitted report | cargo build/test/clippy **not-run** locally (`RUST-LINK-DEV-WIN-GNU-01`); covered by required CI + `DEV-LINUX-NATIVE-01` above |
| 2026-09-03 | Resume (fourth worker, after an abort): recovery tuple re-verified — worktree at `84b2467e`, only this report uncommitted, `git fetch origin --prune` → `origin/main` still `3d66d66d` (already merged in `582e0377`; no new sibling ledger rows, no sibling migration; `personal/P13-T05-routine-runs` and `personal/P0-T01-D02-toolchain` exist remotely, `P13-T06` not yet pushed); Linux host re-read over a fresh SSH session: worktree `p13-t04-292dc288` `HEAD` = `84b2467e…`, `dirty=0`; `p13t04-stageB.log` / `p13t04-stageB2.log` `test result:` lines re-read = 7/7, 8/8, 6/6 and 2/2, 2/2, 19/19, 19/19, 11/11, 9/9 (all `0 failed`, matching the rows above); `p13t04-stageD.log` re-read: 44 `[PASS]` lines and the 3 driver-expectation `[FAIL]` lines (U1 top-level `health` key, U2a front-door 403 code, U10g 409-vs-422) already dispositioned above as refusals; port 48784 free; only the three unrelated kernel-server processes remain | recorded | `DEV-WIN-GNU-01` + `DEV-LINUX-NATIVE-01` | `84b2467e` | the earlier transport-level failure to read a store-test log did not recur; nothing was re-run because every unit already has a pass at this exact revision. Two task-coherent paths were found outside the lease's explicit list (`personal/crates/cognitive-store/src/hosted_dsh_attempt.rs`: one `pub(crate) fn conn_arc`; `clients/pc/web/src/views/opc/projectSubmenus.test.tsx`: the P12-T03 outputs case follows the artifact semantics) — added to the lease row in this checkpoint rather than left implicit |
| 2026-09-03 | Closure docs staged (this report + closure file; PROGRESS heading / prose / Active task lease row / P13-T04 row / Layer 1 prose / Layer 2 `D01` done + `D02` closing; formal plan header / Phase 13 counts row / milestone rows / P13-T04 row; plan.md status; trace PERS-PR-049 comment; lease row led by `P13-T04/D02` + two paths added): `pnpm run check:consistency` **pass** (first run failed only because the new closure file was untracked — tracked-only link rule; staged, re-run OK); `pnpm run check:handbook` **pass** (58×2, 9 generated); `node tools/src/generate-handbook.mjs --check` **pass** (18); `pnpm run check:rules` **pass** (5 known local-only warnings); `git diff --check --cached` **pass**; `cargo fmt --all -- --check` **pass** | **pass** | `DEV-WIN-GNU-01` (Node) | `84b2467e` + staged docs | docs-only checkpoint; no code changed since `84b2467e`, so Rust / web suites are not re-run here (required CI re-runs on the pushed head) |

## Acceptance mapping (closure)

Formal plan P13-T04 card (三栏 + 关闭门 + 漂移检测负例) and Delivery Slices `P13-T04/D01` / `P13-T04/D02`, mapped at the candidate head `84b2467e` (code) — the closure checkpoint on top of it is documentation only.

| Acceptance item | Implementation | Focused negative(s) | Evidence (env / revision) |
|---|---|---|---|
| Attempt 产物入 CAS 并有 digest / format / 来源 / 新鲜度 | store `attempt_artifacts.rs` `ingest_candidate` (v37 `p13_attempt_artifact`: `cas_ref` sha256, `format`, `source` = `hosted-dsh-child:candidate:DeliverableDraft` + source frame binding, `freshness` `current`/`superseded` per Member); kernel-server `ingest_terminal_run` called from the P13-T02 broker thread right after `record_terminal`; runtime `HostedFrame::payload_canonical` retains the observed candidate bytes | store `p13_t04_model_text_and_done_receipt_are_never_completion` (N1/N2), `p13_t04_file_as_authority_is_refused_and_tampered_cas_fails_verification` (N4: `file://` / `artifact://` / path refs refused, payload not bound to the frame refused, observation frame refused), `p13_t04_newer_artifact_supersedes_and_stale_preview_cannot_confirm` (N6 incl. per-Member freshness) | Linux stage B 7/7 at `84b2467e`; CI [33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112); **live** U5a–U5d: two real deliverables, `source` / `format text/markdown` / `freshness current` / `cas_ref` 70 bytes, CAS file re-hashes |
| `outputs` select-then-view 显示真实产物并可打开/下载（Personal Home `data/`） | kernel-server `outputs` / `outputs.detail` / `outputs.open` (CAS re-read, digest-checked, `Content-Type` from format) / `outputs.export` (copy under `<data_dir>/projects/<project>/outputs/`, `is_authority:false`); web `data/projections/outputs.ts`, `ProjectOutputsPage` (artifact listbox → detail; Open from CAS + client download; Export copy labelled not authority) | kernel-server `p13_t04_task_channel_is_forbidden_and_files_are_never_artifact_refs` (403 + forged refs 404 + no-fact refusal), real-child test (hand-edited copy changes nothing); web `projectOutputs.test.tsx` 6 + `outputs.test.ts` 5 (fake-action regex refuses `publish`) | Linux B2 2/2; web 444/444 (`DEV-WIN-GNU-01`); **live** U7 `open` 200 sha == `cas_ref`, U8a–c export copy sha == digest, overwritten copy ignored, `export.is_authority:false`; U14 daemon `/ui/` bundle carries the routes; host file-open **not-run** (`P13-T13`) |
| 独立 verifier 对业务产物出 evidence | `verify_artifact`: `verifier://personal/attempt-artifact` v1 (principal `principal://personal/independent-verifier`), deterministic criteria (`attempt-terminal-observed`, `attempt-response-status` **not-used**, `source-frame-bound`, `source-payload-cas`, `cas-bytes-match-digest`, `deliverable-non-empty`, `no-secret-shape`, `format-parses`), append-only `p13_artifact_evidence`, report bytes in the same CAS; auto-run at ingest, re-runnable via `attempt.artifact.verify` | store `p13_t04_independent_verifier_writes_cas_backed_evidence_and_ignores_response_status` (N5: append-only, no caller `passed`, foreign verifier identity refused by CHECK), tampered CAS → `failed` (N4), `p13_t04_secret_shape_never_enters_artifact_or_evidence` (N8) | Linux B 7/7; **live** U6 evidence `passed` with `attempt-response-status not-used`, `report_cas_ref`; U13a–d tamper → `failed`, history `[failed, passed]` append-only |
| run 验收只在末环且需 StageTestPassed | `derive_stage_test` (P11-T03 `derive_stage_test_passed` from seating + current verifier evidence + CAS re-read + terminal Attempt; never caller booleans); `request_run_acceptance` mints a P11-T09 ApprovalPreview `run-acceptance` only for the last stage with a current fact; `confirm_preview` → `accept_run_locked` → append-only `p13_run_acceptance`; v37 rebuild of the `p11_approval_preview` subject-kind CHECK | store `p13_t04_stage_test_is_derived_from_evidence_and_acceptance_only_on_last_ring` (N2/N3: no evidence → no fact; cross-slot refused; intermediate ring refused; stale preview refused; second acceptance refused); kernel-server real-child test | Linux B 7/7 + B2 2/2 + `p11_t03_project_aggregate` 19/19 + `p11_t09_hitl_canvas` 11/11; **live** U9a–d (422 no fact / 422 "only on the last ring" / 422 wrong slot), U10a–h (preview → 409 stale digest → `run_accepted` → 409 double acceptance) |
| publication package = 完整 AUTONOMY packet 画布预览，planned ≠ published，聊天无 Confirm | `publication_packet` (`memory_of_actions` / `observable` / `outcome_verify` / `override` / `preview` / `tiered_authority` / `yield`; `planned` vs `published:false`, `chat_can_confirm:false`); web packet panel `planned` / `not published` / `chat has no Confirm`, no Publish button | store `p13_t04_external_send_is_previewed_then_planned_never_published` (`published` unrepresentable); web tests refuse `publish` actions | **live** U11 packet `planned:true`, `published:false`, `chat_can_confirm:false`, `outcome_verify` bound to the evidence + acceptance ids |
| external-send 走 T09 ApprovalPreview | `request_external_send` → ApprovalPreview `external-send` → `confirm_preview` → `external_send_locked` → `p13_external_send` `planned` (P11-T14 persist-before-dispatch shape; `connector none-qualified`) | store external-send test; kernel-server real-child test (`confirm` → `planned`, `published:false`) | **live** U12a–c `previewed` → `external_send_planned` → `publication.sends` `planned`, `published_any:false`; nothing left the machine |
| 完成 = 独立 verifier + daemon acceptance；产物 = CAS 引用 (card acceptance) | Attempt row keeps `completion_claimed = 0` / `verification_status = 'not-run'` (v36 CHECK); completion lives only in evidence → fact → acceptance | N1/N2/N5 | **live** U3/U4/U15a every Attempt row `done` / `completion_claimed false` / `verification_status not-run`, `receipt_is_not_completion:true` |

漂移检测负例 (card), each refused on a live daemon at `84b2467e`: 模型文本 / HTTP 200 / `agent_end` 当完成 (U3/U4/U15a); file-as-authority (U2e forged refs 404, U8b hand-edited copy ignored, U13 tamper → `failed` / 422 / 409); 中间环验收 (U9c 422); 无验证即 published (U13b 422 "completion requires current verification"; `published` unrepresentable); 聊天 Confirm (`chat_can_confirm:false` on every preview; confirm only via `/management/project/v1/confirm` with the preview digest); receipt-as-completion (U15a); 假 Publish 按钮 (web tests refuse `publish`; no such control).

关闭门 sentence by sentence: (1) 真实 Attempt 产物入 CAS 且 `outputs` 可选看/打开 — **true** on `DEV-LINUX-NATIVE-01` at `84b2467e` (real pinned dsh deliverable → CAS → `outputs` / `open` / `export`; daemon `/ui/` serves the page bundle); the **host** file-open cell (opening the exported `data/` copy in a native file manager / app) stays **not-run** until `P13-T13`. (2) 独立 verifier 出 evidence — **true** (U6, U13). (3) 末环验收需 StageTestPassed — **true** (U9/U10). (4) 发布包画布预览且 planned ≠ published — **true** (U11/U12; rendered-browser review of the canvas is `P13-T12/D02`, bundle presence only here).

Slice reconciliation: `P13-T04/D01` and `P13-T04/D02` both meet Operating Model §2.1 items 1–4 at `84b2467e` (durable authority facts + user-visible `outputs` path; failure-first + supported validation ran and passed on required CI and `DEV-LINUX-NATIVE-01`; evidence / non-claims / revision recorded here; no unowned helper-only TODO — the host file-open cell is owned by `P13-T13/D02`). On this branch the ledgers read `in-progress → closing` (the active lease binds the slices); `done` lands in the lease-closure commit on `main` (P13-T02 / P13-T03 precedent).
