# P13-T05 Routine/Trigger arming, occurrence ledger, `runs` + Today — running report

- Task: `P13-T05` / slices `P13-T05/D01` → `P13-T05/D02`
- Change class: `implementation-only` (Routine arming + scheduler-driven occurrence ledger in `cognitive-store` / kernel-server; `runs` + Today projections in `clients/pc/web`; no `core/specs`, no Lane-CTR, no new first-level chrome, no second scheduler; additive authority migration)
- Product: CognitiveOS Personal 2.0.0
- Lease: `lease/personal/P13-T05/routine-runs`
- Branch: `personal/P13-T05-routine-runs` (worktree `D:\agent-kernel-wt-p13-t05`; original `d:\agent-kernel` untouched, A8 protected)
- Base: `origin/main@3d66d66d` (P13-T03 lease closure)
- Claim ceiling: `hypothesis` (A7: local / CI / Linux native is not Gate / release / Profile; Linux native evidence closes "implementation exists" only; clock / sleep / restart host E2E stays `not-run` until `P13-T13`)
- Evaluation routing: **OFF**

## Unique next action

Push the D02 + routing-fix checkpoint; required CI at that head; `DEV-LINUX-NATIVE-01` focused Rust tests + clippy + live daemon E2E (arm → tick fires → Intent → hosted Attempt → ledger with overlap / queue-latest / missed / coalesced → `routine.runs` / `today.overview`); then closure.

## Scope decision recorded at claim (PlanRevision-apply gap)

P13-T02 found that no product HTTP / CLI path applies a PlanRevision, records a stage test, or reaches G2 acceptance (`roster.register` needs `plan_revision_id`; `apply_plan_revision`, `record_stage_test` and the `acceptance` preview subject are store-only). Arming after G2 needs all three facts (an `active` accepted Project, a current plan stage, a seated responsible Member). Decision: **not widened into this card**. Arming fails closed before G2 (`ROUTINE_ARM_BEFORE_G2`), unit tests and the live E2E seed those facts as a fixture exactly as P13-T02 did (scratch seed run before the daemon starts), and the gap stays owned by `P13-T04` (stage test / last-ring acceptance) and `P13-T06` (`@manager` plan revision). Recorded in PROGRESS Layer 2 and the plan.md card.

## Failure-first (D01)

| ID | Negative | Surface |
|---|---|---|
| N1 | second scheduler refused: the only dispatcher of `task://personal/routine/*` rows is the daemon scheduler tick; a second lease holder is fenced (`acquire_eligible_lease` epoch / owner CAS) and an occurrence can never be dispatched twice | store + kernel-server tick |
| N2 | overlap refused: one Routine never has two `active` occurrences; a schedule firing while one is active queues (latest only) and never spawns a second Attempt | store + tick |
| N3 | silent drop forbidden: a schedule firing while the host is paused / offline lands as `missed` with a reason; a manual trigger on an un-armed Routine lands as `missed` (`not-armed`), never vanishes; coalesced occurrences keep `coalesced_by` | store + tick |
| N4 | checkpoint ≠ completion: `record_checkpoint(complete = true)` refused; an occurrence reaches `attempted` only through a daemon-observed Attempt terminal, and `completion_claimed` stays false | store |
| N5 | arming before G2 refused (`creating` Project → `ROUTINE_ARM_BEFORE_G2`); arming with an unseated / wrong-slot Member refused; stale Routine revision refused | store + HTTP |
| N6 | running-prompt injection refused: a new instruction revision never changes the `context_digest` of the active occurrence's Attempt; `continue` applies from the next occurrence, `pause` stops new occurrences, `restart` queues a new-revision occurrence behind the active one | store + tick |
| N7 | process exit ≠ cancel / complete: Attempt terminal `exited/0`, `failed`, `timed-out`, `unknown-outcome` all land as occurrence outcome facts with `verification_status = not-run` | store + tick |
| N8 | task-channel aliases 403; secret-shaped declaration refused; no fake Start: manual trigger is the existing Intent path `routine.trigger` | HTTP |

## Incremental validation log (TEST-REPORT-INCREMENTAL-01)

Units are appended **immediately** after each finishes. `not-run` is never pass.

| Time | Unit | Result | Env | Revision | Notes |
|---|---|---|---|---|---|
| 2026-09-03 | Worktree `D:\agent-kernel-wt-p13-t05` created from `origin/main@3d66d66d`; lease row + PROGRESS / formal plan / plan.md → `P13-T05` in-progress, `P13-T05/D01` in-progress; report skeleton | recorded | docs-only (`DEV-WIN-GNU-01`) | worktree, uncommitted | `git worktree list` / `git branch --list "personal/P13-T05*"` showed no prior branch or worktree |
| 2026-09-03 | Claim committed (`72ddfe7e` report, `18bff3c2` ledgers); `pnpm run check:consistency` **pass** after the report was tracked (P0-T09 tracked-only link check) | **pass** | `DEV-WIN-GNU-01` (Node) | `18bff3c2` | docs-sync gate `DOCS_IMPACT_NONE` acknowledged for status-only plan edits |
| 2026-09-03 | Failure-first A/B read of `main@3d66d66d`: P11-T08 `admit_trigger` upserts a `runnable` `scheduler_entries` row `task://personal/routine/<occ>`, and the daemon tick (`scheduler_authority/dispatch.rs`) fails every such row with `MissingContract` and skips it — no Attempt is ever driven, nothing arms a Routine, and no route knows G2. The D01 green-path tests below (`routine_tick_drives_a_hosted_attempt_and_writes_the_occurrence_ledger`, `p13_t05_overlap_never_dispatches_twice_and_queue_latest_promotes`) cannot pass on that revision because `RoutineArmingStore` / `routine.arm` / `run_routine_tick` do not exist there | recorded (baseline fact) | code read (`DEV-WIN-GNU-01`) | `3d66d66d` | honest A/B: the negative surface is "the ledger row sits `runnable` forever" |
| 2026-09-03 | D01 failure-first tests written: store `p13_t05_routine_arming.rs` (7 tests: arming before G2 refused / after G2 armed; unseated / non-responsible / unknown stage / stale revision / task channel / invalid or secret declaration refused; second scheduler fenced by lease CAS + `bind_attempt` lease check, one scheduler row per occurrence, no temporal table; overlap never dispatches twice + queue-latest coalesce + promotion; silent drop forbidden — `not-armed` missed, close-window pause → `host-unavailable:close-paused` missed, offline segment reason, internal missed resumes; checkpoint / process exit ≠ completion incl. schema CHECK refusals of `completion_claimed=1` and `attempt_outcome='success'`; instruction continue / pause / resume / restart at a safe point with the running occurrence untouched; Today overview counts and periods); kernel-server `routine_runs::tests` (2 tests: task aliases 403 + `ROUTINE_ARM_BEFORE_G2` 409 + un-armed manual trigger → tick marks `missed/not-armed` + honest empty Today; full tick loop with a real `node` child: arm → trigger → lease + launch → second trigger queued and never launched → terminal reconciled to `attempted/done` → lease released → queued promoted and dispatched → pause instruction leaves the running Attempt's context digest untouched → third waits `waiting-paused` → resume dispatches → Today row) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | `cargo test` is `not-run` here (`RUST-LINK-DEV-WIN-GNU-01`); first execution is CI + `DEV-LINUX-NATIVE-01` |
| 2026-09-03 | D01 implementation written: store v37 `routine_arming.rs` (`p13_routine_arming`, `p11_routine_occurrence` rebuilt with dispatch / outcome columns and the `attempted` disposition; `RoutineDeclaration` parser; `arm` / `apply_instruction` / `resume_arming`; tick helpers `host_dispatch_availability` (P11-T02 daemon state) / `due_schedule_armings` / `fire_schedule` / `dispatchable_occurrences` / `in_flight_occurrences` / `bind_attempt` (lease-checked) / `mark_not_armed` / `record_attempt_terminal` (no `success`) / `promote_queued`; `list_project_ledger`; `today_overview`), kernel-server `routine_runs.rs` (routes `routine.arm` / `routine.instruction` / `routine.arming.resume` / `routine.armings` / `routine.runs` / `today.overview` + `run_routine_tick`), `hosted_dsh_attempt.rs` additive refactor: `attempt_run` core extracted into `launch_hosted_attempt` (same refusals, same persist-before-dispatch order) so the tick and the HTTP route share one launch path, `server.rs` (route dispatch + the routine step inside the existing `PeriodicSchedulerWorker` closure), `scheduler_authority/dispatch.rs` (generic pass skips `task://personal/routine/*` rows), `personal_db.rs` v37, `lib.rs` exports, `p1_t01_layout_migrations.rs` version lists | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | vertical: HTTP arm → daemon tick → Intent (`persist_intent`) → real child → ledger → `routine.runs` / `today.overview` |
| 2026-09-03 | `cargo fmt --all -- --check` | **pass** | `DEV-WIN-GNU-01` | worktree, uncommitted | after `cargo fmt --all`; formatting only, no link |
| 2026-09-03 | `cargo build/test/clippy` (store `p13_t05_routine_arming`, `p1_t01_layout_migrations`, kernel-server `routine_runs::tests`, `hosted_dsh_attempt::tests`) | **not-run** | `DEV-WIN-GNU-01` | worktree, uncommitted | `RUST-LINK-DEV-WIN-GNU-01`; routed to required CI + exact-revision `DEV-LINUX-NATIVE-01` |
| 2026-09-03 | `node tools/src/generate-handbook.mjs` (12 new route annotations; `routine_runs.rs` added to the http-api definition sources and to the `daemon-http` source-map rule) + `fill-handbook-fingerprints` (24 pages) → `check-handbook` **pass** (58×2) → `generate-handbook --check` **pass** (18 byte-identical) | **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | `http-api` both locales regenerated; `store-and-migrations` (v37), `daemon-and-http`, `capability-status`, `tasks-and-execution` hand-updated in en + zh-CN; first `check-handbook` run reported HB006/HB015 for untracked new sources → new files `git add`ed (explicit paths) |
| 2026-09-03 | `pnpm run check:consistency` / `pnpm run check:rules` / `git diff --check` | **pass** / **pass** (3 known local-only warnings) / **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | lease row heartbeat added `tools/src/generate-handbook.mjs` (definition-source list only) |
| 2026-09-03 | `pnpm -r build` / `pnpm -r test` | **pass** / **pass** | `DEV-WIN-GNU-01` (Node) | worktree, staged | all packages `Done`; 0 failing tests |
| 2026-09-03 | D01 checkpoint pushed (`6eae57f6` implementation, `f61d9b63` lease heartbeat); Draft PR [#315](https://github.com/agentkernel/cognitive-os/pull/315) opened | recorded | GitHub | `f61d9b634c52a0d2746bb764c467a2cf2da38c4e` | Draft until full acceptance |
| 2026-09-03 | Required CI run [33706254500](https://github.com/agentkernel/cognitive-os/actions/runs/33706254500): `verify (ubuntu-latest)` + `verify (windows-latest)` | **fail** (compile) | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `f61d9b63` | `cognitive-store` compiled; `kernel-server` E0631 in `routine_runs::bump` (`get_mut(..).and_then(Value::as_i64)`); one-line fix (`get`) in the next checkpoint |
| 2026-09-03 | Linux worktree `~/cognitiveos-personal-worktrees/p13-t05-f61d9b63` (seeded from a sibling clone, task branch fetched from GitHub through the host proxy after a direct `curl 28` timeout; `git rev-parse HEAD` = `f61d9b63…`, dirty=0; Rust 1.97.1, Node 22.19.0) | recorded | `DEV-LINUX-NATIVE-01` | `f61d9b63` | remote scripts piped over stdin (`bash -s`), never `$HOME`/`$PATH` in a double-quoted ssh argument |
| 2026-09-03 | `cargo test -p cognitive-store --test p13_t05_routine_arming --test p1_t01_layout_migrations --test p11_t08_routine --locked -- --test-threads=1` | **pass** 8/8 + 8/8 + 7/7 | `DEV-LINUX-NATIVE-01` | `f61d9b63` | v37 migrates from empty and from the previous fixture, replay-safe; P11-T08 behaviour unchanged after the occurrence-table rebuild; all P13-T05 negatives (before-G2, unseated / non-responsible / stale / task-channel / invalid / secret, second scheduler fenced, overlap, silent drop with `not-armed` / `host-unavailable:close-paused` / `offline:sleep`, checkpoint + schema CHECK refusals of `completion_claimed=1` and `success`, instruction never touches the running occurrence, Today periods) green |
| 2026-09-03 | `cargo test -p kernel-server routine_runs` / clippy / `cargo build` | **not-run** (blocked by the same E0631) | `DEV-LINUX-NATIVE-01` | `f61d9b63` | re-run at the next pushed revision |
| 2026-09-03 | Required CI run [33707129052](https://github.com/agentkernel/cognitive-os/actions/runs/33707129052) at `7491a574` (E0631 fixed): `verify (ubuntu-latest)` 396/398, `verify (windows-latest)` 387/388 | **fail** (2 tests) | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `7491a574` | (a) `routine_runs::tests::routine_tick_drives_a_hosted_attempt_and_writes_the_occurrence_ledger` on **both** runners: every step up to the pause instruction passed with a real `node` child (arm → trigger → lease + launch → overlap queued → terminal reconciled → queued promoted and dispatched → pause leaves the running Attempt's context digest untouched → third occurrence `waiting-paused`); the `routine.arming.resume` call then got `400 ROUTINE_ARM_FIELD_REQUIRED` because `parse_route` used `starts_with` and `routine.arm` is a prefix of `routine.arming.resume` — routing bug, not a scheduler fact. (b) ubuntu only: pre-existing `project_aggregate::tests::http_standing_policy_missing_expires_at_is_rejected` raced the clock by 1 ms (`now + MAX_TTL + 1` vs the handler's later `now_ms`) |
| 2026-09-03 | Fix: `parse_route` matches the path part exactly (query stripped); regression assertions added to `routine_runs_task_channel_forbidden_…` (`arming.resume` with `{}` → `400 ARMING_ID_REQUIRED`; `routine.runs-extra` does not match); `http_standing_policy_missing_expires_at_is_rejected` margin 1 ms → 60 s (same 7-day boundary, no longer racing the handler clock); lease row heartbeat names `normalize.ts` / `normalize.test.ts` (three GET whitelist entries) and the test-only `project_aggregate.rs` line | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | `cargo fmt --all -- --check` **pass** |
| 2026-09-03 | D02 written: `clients/pc/web` projections `routineRuns.ts` (runs view: host / scheduler / armings / occurrence ledger with `dispatch_state`, `coalesced_by`, `miss_reason`, Attempt binding, `completion_claimed`, `verification_status`; Attempt history from `dsh.hosted.attempt.list`; counts daemon-stated, unknown never 0) and `todayOverview.ts` (period-validated view: created / live / blocked counts + one row per live Project: status / `attempts_done` / current stage / duration / queued–missed / failed–unknown); `loadOpcReads.ts` loaders (`loadRoutineRuns`, `loadAttemptHistory`, `loadTodayOverview[ForLiveProject]` — creating-only / empty homes never fetch the overview); `normalize.ts` three GET whitelist entries (no `routine.arm` / `routine.instruction` / `routine.trigger` / `attempt.run` POST from the UI); `ProjectRunsPage.tsx` = ledger summary + armed Routines + occurrence table + Attempt history + the P12-T03 axis, no Start / Approve / Complete control; `TodayPage.tsx` = period switch (today / week / month, `aria-pressed`) + counts + overview rows (`Runs` deep link) + packet block as `<details>` collapsed when nothing is pending while the overview stays | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | product-truth sources: user-journeys §2 / §3, long-running-operations §3 / §7, v9 checklist rows `M-TODAY` / `M-LIVE-PROJECT` |
| 2026-09-03 | `clients/pc/web` `pnpm test` (60 files / 454 tests incl. new `routineRuns.test.ts` 5, `todayOverview.test.ts` 5, `projectRuns.test.tsx` 5, `todayOverview.test.tsx` 5, `normalize.test.ts` +1) + `pnpm build` | **pass** / **pass** | `DEV-WIN-GNU-01` (Node) | worktree, uncommitted | negatives green: no Start / Approve / Complete / arm / pause / resume / restart control on `runs`; denied ledger not converted to empty; missing counts → `unknown` never 0; creating-only home never fetches `today.overview`; packet block collapsed with nothing pending and open with a pending preview while the overview stays; period switch refetches `?period=week` with no POST; task-channel paths never called |
| 2026-09-03 | `cargo fmt --all -- --check` / `pnpm run check:consistency` / `check:handbook` (after `generate-handbook` + `fill-handbook-fingerprints`, 2 pages) / `generate-handbook --check` / `check:rules` / `git diff --check` | **pass** ×6 | `DEV-WIN-GNU-01` (Node) | worktree, staged | first `check-handbook` run failed HB010: the generator scanned the test's negative route string `routine.runs-extra` as an unannotated literal → the test now builds it at runtime |
| 2026-09-03 | D02 + routing-fix checkpoint pushed as `bafb3c9c` (`feat(P13-T05/D02)…`); Draft PR #315 updated | recorded | GitHub | `bafb3c9c56d2757dfb9921cb4b188444cb1c28c8` | required CI pending at push time |
| 2026-09-03 | Linux worktree moved to the pushed revision (`~/cognitiveos-personal-worktrees/p13-t05-bafb3c9c`, renamed from `…-f61d9b63` to reuse its 2.3 GB `target/`; `git rev-parse HEAD` = `bafb3c9c…`, dirty=0; Rust 1.97.1, Node 22.19.0; host disk 94 % full) | recorded | `DEV-LINUX-NATIVE-01` | `bafb3c9c` | scripts piped over stdin as LF (`bash -s`), never `$HOME`/`$PATH` in a double-quoted ssh argument |
| 2026-09-03 | `cargo test -p cognitive-store --test p13_t05_routine_arming --test p1_t01_layout_migrations --test p11_t08_routine --locked -- --test-threads=1` | **pass** 7/7 + 8/8 + 8/8 | `DEV-LINUX-NATIVE-01` | `bafb3c9c` | unchanged from `f61d9b63` (store code untouched by the checkpoint) |
| 2026-09-03 | `cargo test -p kernel-server --bin kernel-server --locked -- --test-threads=1 routine_runs hosted_dsh_attempt project_aggregate` | **pass** 23/23 | `DEV-LINUX-NATIVE-01` | `bafb3c9c` | `routine_tick_drives_a_hosted_attempt_and_writes_the_occurrence_ledger` green end-to-end with real `node` children (arm → trigger → lease + launch → overlap queued → terminal `attempted/done` reconciled + lease released → queued promoted and dispatched → pause leaves the running Attempt's `context_digest` untouched → third `waiting-paused` → `arming.resume` → dispatched → Today row `running`, `attempts_done=2`, stage `s1`); `routine_runs_task_channel_forbidden_…` incl. the new exact-route assertions; `http_standing_policy_missing_expires_at_is_rejected` with the 60 s margin; hosted_dsh_attempt 2/2 unchanged |
| 2026-09-03 | `cargo clippy -p kernel-server -p cognitive-store --all-targets --locked -- -D warnings`; `cargo build -p kernel-server -p admin-cli --locked` | **pass** / **pass** | `DEV-LINUX-NATIVE-01` | `bafb3c9c` | 0 warnings; binaries for the live E2E |
| 2026-09-03 | Required CI run [33711917159](https://github.com/agentkernel/cognitive-os/actions/runs/33711917159) at `bafb3c9c`: resolve 3s, `verify (ubuntu-latest)` 4m15s, `verify (windows-latest)` 15m31s, `required-ci` 4s | **pass** | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | `bafb3c9c` | first fully green required run for this task (the two earlier runs failed on E0631 and the `routine.arm` prefix-routing bug, both fixed in this head); resumed worker verified with `gh pr checks 315` |
| 2026-09-03 | **Live-daemon E2E setup** at the exact revision (worktree `~/cognitiveos-personal-worktrees/p13-t05-bafb3c9c`, `git rev-parse HEAD` = `bafb3c9c…`, dirty=0): disposable runtime root `…/p13-t05-bafb3c9c-runtime` (0700); fixture seed = scratch crate `~/cos-wt/p13t05-seed` **outside** the repo (path-depends on the worktree's `cognitive-store`, built offline into the worktree `target/`, run **before** the daemon starts): G1 → `apply_plan_revision` (s1 manager / s2 researcher) → roster → manager seated → both stages `StageTestPassed` → acceptance preview confirmed (**G2**, `active`), plus one G1-only `creating` Project; product CLI `cognitive dsh configure --runtime-root … --dsh-root ~/agent-kernel-worktrees/p8-t09-pins/dsh --adapter-root <worktree>/personal/packages/dsh-akp-adapter --revision 528c682e…` → `dsh.json` `candidate_only:true`, `lifecycle_state:active`; `kernel-server --personal --bind 127.0.0.1:48785 --runtime-root …` listening; bootstrap file mode 600 | **pass** | `DEV-LINUX-NATIVE-01` | `bafb3c9c` | pinned dsh checkout git HEAD = pin file = `528c682e061696f5a160f363f236ecbf53cbd006`. Seeding is fixture setup only because no product HTTP/CLI path applies a PlanRevision / stage test / G2 acceptance yet (scope decision above; P13-T04 / P13-T06). Other tasks' daemons on the host (ports 48181 / 39245 / 48681) untouched |
| 2026-09-03 | Live driver (Node, product HTTP only: `routine.revision` → `routine.arm` → tick → `routine.trigger` → `routine.instruction` / `arming.resume` → `routine.runs` / `today.overview` / `attempt.list`) first run at `bafb3c9c`: **4/11 units** — `routine.runs?project_id=…`, `today.overview?period=…`, `routine.trigger` (P11-T08), `attempt.list`, secret scan behaved; but `POST routine.arm` / `routine.instruction` / `arming.resume` and the task-channel alias `POST /task/project/v1/routine.arm` all answered **HTTP 200 with no `arming` / no `code`**, no arming ever existed, the interval schedule never fired (38 snapshots, 0 occurrences for the armed Routine), and the manual trigger landed `missed/not-armed` | **fail** (routing defect found by the live E2E) | `DEV-LINUX-NATIVE-01` (live daemon, loopback HTTP) | `bafb3c9c` | Root cause: the live server dispatches `method_path` as `"<METHOD> <path> "` (trailing space from `parse_request_line`); the exact-match `parse_route` introduced at `bafb3c9c` compared the query-stripped string **untrimmed**, so no P13-T05 route matched on a real daemon and `POST /management/...` fell through to the generic channel echo (200). Query-bearing GETs matched by accident (`split('?')` also dropped the space). Every in-process test passes untrailed strings, which is why CI and the Linux focused suites were green. Not a scheduler fact; the daemon tick itself ran (no error lines) with nothing to dispatch. Daemon stopped; runtime root discarded before the re-run |
| 2026-09-03 | Fix: `parse_route` trims the path part after stripping the query; new failure-first test `routine_runs_routes_match_the_live_request_line_shape` pins the live shape for all six management routes and three task aliases (403 through `handle`), keeps exactness (`routine.arm` ≠ `routine.arming.resume`; suffixed / truncated paths built at runtime do not match), and asserts `routine.arm ` → 400 `ROUTINE_ARM_FIELD_REQUIRED`, `arming.resume ` → 400 `ARMING_ID_REQUIRED`, `today.overview ` → 200 (not the channel echo) | recorded | `DEV-WIN-GNU-01` | worktree, uncommitted | `cargo fmt --all -- --check` **pass** after `cargo fmt --all`; cargo test `not-run` here (`RUST-LINK-DEV-WIN-GNU-01`) → CI + `DEV-LINUX-NATIVE-01` at the next pushed revision |
