# P2-T36 running validation report

- Task: `P2-T36` - C1 WorkspaceRead/Search public production path
- Branch: `personal/P2-T36-c1-public-production-path`
- Lease: `lease/personal/P2-T36/c1-public-production-path`
- Classification: product-semantic + implementation; no normative contract change
- Claim ceiling: hypothesis/non-claim; no EVAL, Gate, release, Profile, B01,
  or Agent-benefit promotion

## Running results

Results are appended immediately after each completed validation unit.

| Unit | Environment | Result | Evidence |
|---|---|---|---|
| D01 failure-first Pi extension registration | `DEV-WIN-GNU-01` Node/TypeScript allowlist | fail (expected) | After adding the C1 expectation, `pnpm --filter @cognitiveos/pi-cognitiveos build` passed, then `node --test --test-name-pattern "registration queues" "packages/pi-cognitiveos/dist/extension.test.js"` failed: the actual registered tools are `WorkspacePatch`, `WorkspaceSearch`, and `WorkspaceWrite`; `WorkspaceRead` is absent. This proves the C1 Read Pi-visible tool gap before implementation. No Rust build/test was run on Windows GNU. |
| D01 Pi extension regression suite | `DEV-WIN-GNU-01` Node/TypeScript allowlist | pass | After adding the I/O-free daemon-governed WorkspaceRead tool and preserving the empty Pi-native allowlist, `pnpm --filter @cognitiveos/pi-cognitiveos build` and `pnpm --filter @cognitiveos/pi-cognitiveos test` passed. This is implementation evidence only; it does not prove a Rust scheduler, verifier, acceptance, real Pi, Provider, C1, B01, or paired benchmark path. |
| D01 Rust formatting | `DEV-WIN-GNU-01` Rust formatting allowlist | pass | `cargo fmt --all` completed after the adapter protocol update. Rust compilation and tests remain routed to exact pushed `DEV-LINUX-NATIVE-01`. |
| D01 plan and handbook checks | `DEV-WIN-GNU-01` static/documentation allowlist | pass | After registering P2-T36/D01-D03 and synchronizing both handbook locales, `pnpm run check:consistency`, `node tools/src/check-handbook.mjs`, and `node tools/src/generate-handbook.mjs --check` passed. These checks do not execute the C1 runtime path. |
| D01 staged documentation gate | `DEV-WIN-GNU-01` static/documentation allowlist | pass | `node tools/src/docs-sync-gate.mjs --staged` passed. The mapped `pi-shell` pages and fingerprints are synchronized; this gate does not execute the C1 runtime path. |
| D01 exact-revision push | local Git transport | partial | Checkpoint `6e7e4197` committed, but `git push -u origin HEAD` could not connect to GitHub through the configured loopback proxy. No supported Linux runtime validation has started because it must consume a pushed exact revision. Retry the normal push path before invoking `DEV-LINUX-NATIVE-01`; no force push or alternate source copy is permitted. |
| D01 transport recovery retry | local Git transport | partial | A second normal `git push -u origin HEAD` and a one-command no-config-override retry (`git -c http.proxy= -c https.proxy= push -u origin HEAD`) both failed with the same connection refusal via `127.0.0.1:443`. The current session has no `*PROXY` environment variables and no repository-level `http.proxy`/`https.proxy` setting. The fault remains external to task-owned code. |
| D01 exact-revision delivery recovery | GitHub HTTPS | pass | The normal `git push -u origin HEAD` succeeded after connectivity was restored. `origin/personal/P2-T36-c1-public-production-path` now contains product checkpoint `ae6fd828`; the next supported validation must check out that exact product commit from Git rather than copy local files. |
| D01 exact-worktree provisioning | `DEV-LINUX-NATIVE-01` | pass | Direct full and HTTP/1.1 clone attempts were transiently unavailable or below Git's transfer floor. A clean shallow Git clone of the pushed branch then completed at `/home/wuz/p2-t36-c1`; `git rev-parse HEAD` was `15557d18d5efa5a38e5c2948545742e07d53db81`, matching the pushed task head. The pre-existing `/home/wuz/agent-kernel` source directory has no `.git` metadata and was not used. |
| D01 adapter WorkspaceRead protocol | `DEV-LINUX-NATIVE-01` exact `15557d18` | pass | `cargo test -p pi-agent-adapter --test daemon_candidate_protocol --locked` completed **21/21**. This includes `pi_print_events_accept_one_daemon_governed_workspace_read` and the existing duplicate/mixed candidate, Pi-built-in, unknown digest, missing digest, authority-field, and oversized-context negatives. It proves adapter protocol parsing only, not public daemon admission, scheduler lease, dispatch, verification, acceptance, real Provider/Pi execution, C1, B01, or a paired benchmark. |
| D01 supporting CI | `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` exact `15557d18` | partial | Ubuntu verification passed. Windows failed only in existing unrelated `personal::p2_t17_a7_failure_first::post_dispatch_fault_points_reconcile_without_redispatch_or_task_acceptance` (`Indeterminate` observed where `ReconciledExecuted` was asserted); required-ci therefore failed. The failed job was rerun before any task-owned code change. This is not evidence for the pending real Pi public path. |
| D02 real-adapter prerequisites | `DEV-LINUX-NATIVE-01` exact `15557d18` | partial | `pnpm install --frozen-lockfile`, `pnpm --filter @cognitiveos/pi-cognitiveos build`, and `cargo build -p pi-agent-adapter --locked` passed in the exact Git worktree. The final non-secret `command -v pi` probe found no Pi executable on the host PATH, so no `cognitive pi configure`, daemon start, Provider readiness, or real Pi candidate attempt began. Locate or install only the pinned Pi `0.81.1` through an approved non-secret package path before configuring the disposable product runtime. |
| D02 pinned Pi environment recovery | `DEV-LINUX-NATIVE-01` | partial | `pnpm setup` registered the user-level PNPM home in `/home/wuz/.bashrc`; no repository file or secret path changed. The first global install was rejected because that bin directory was absent from the noninteractive PATH. A corrected `PNPM_HOME`/PATH install of `@earendil-works/pi-coding-agent@0.81.1` downloaded its pinned dependency graph but made no progress after link completion for more than 60 seconds, so the stalled SSH command was stopped. No Pi executable/version confirmation, `pi configure`, daemon, Provider, or candidate execution occurred. Retry from the installed package state with a bounded noninteractive verification before any public product attempt. |
| D02 Pi runtime configuration | `DEV-LINUX-NATIVE-01` exact `15557d18` | pass | A bounded retry confirmed `/home/wuz/.local/share/pnpm/bin/pi` reports pinned version `0.81.1`. On the same exact Git worktree, `cargo build -p admin-cli --locked` passed and public `cognitive pi configure` created a cleanable non-B01 runtime's non-secret `pi.json`, referencing the pinned Pi binary, built Extension, and built candidate adapter. This configures no Provider material and does not launch Pi or the daemon. |
| D02 exact-worktree refresh | `DEV-LINUX-NATIVE-01` | pass | The cleanable non-B01 Git worktree fetched `origin/personal/P2-T36-c1-public-production-path` with HTTP/1.1, detached at `979e52e4c6681d0fc8c6431c965e3267a7a0d917`, and reported a clean worktree. This replaces the previous `15557d18` checkout for subsequent D02/D03 units; no source files were copied and no Provider material was inspected. A pre-existing daemon for the disposable runtime was detected and will be stopped through the public CLI before rebuilding and restarting from this exact revision. |
| D02 exact-revision runtime rebuild | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | pass | Public `cognitive daemon stop --runtime-root /home/wuz/p2-t36-runtime` returned `already_stopped`; the earlier process observation had no live lock for this disposable product layout. `cargo build -p admin-cli -p kernel-server -p pi-agent-adapter --locked` completed from the exact Git checkout, and public `cognitive pi configure` returned success while rewriting only the non-secret Pi configuration with the current paths. No Provider configuration, SecretStore item, or secret material was read or printed. |
| D02 public daemon readiness | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | partial | Public `cognitive daemon start` started the exact-revision daemon at disposable non-B01 loopback endpoint `127.0.0.1:48436`; public status reported a live lock and process. The redacted public doctor projected `system`, `database`, `secret`, `daemon`, and `pi` ready (Pi `0.81.1`), but `provider` blocked with `provider_config_missing`, so `first_conversation_ready` is false. No Provider material was read, searched, printed, logged, or passed through argv/environment/configuration. Consequently real Pi launch, WorkspaceRead candidate validation, scheduler lease, dispatch, verification, and acceptance are `not-run`; no substitute private transport, SQLite edit, mock authority, fixture, or closed-EVAL asset was used. |
| D02 Provider readiness recovery | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | pass | The updated owner-designated key file was streamed through an SSH process with PTY explicitly disabled into the public `cognitive init --api-key-file - --rotate-key` path for the independent P2 runtime. The redacted result selected `deepseek-v4-flash`, reported `secret_ref_redacted: true`, and made no secret material available to argv, environment, config, SQLite, logs, or evidence. No EVAL-011 SecretStore item or root was reused. |
| D02 public daemon readiness after recovery | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | pass | Public daemon started at `127.0.0.1:48436`; status reported a live process and lock. Redacted doctor reported system/database/secret/provider/daemon/Pi ready, Provider SecretRef resolution and selected-model digest match, Pi `0.81.1`, and `first_conversation_ready: true`. This is readiness only and does not prove C1/C2 execution. |
| D02 public Pi launch gate | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | partial | Public `cognitive pi launch --runtime-root /home/wuz/p2-t36-runtime` passed the daemon-owned readiness gate and returned redacted `action: spawned`, `authority_side_effects: false`, and child PID `987278`. The child exited immediately because the non-interactive SSH invocation supplied no conversation input. No Pi candidate, WorkspaceRead/Search call, scheduler lease, Tool executor, verifier, acceptance, C1, C2, Gate, release, Profile, B01, or Agent-benefit result occurred. Next step must use a bounded, non-secret interactive conversation route; do not infer success from the spawn response. |
| D02 bounded interactive launch attempt 1 | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | partial / environment blocked | A PTY-bearing SSH session carried only a non-secret WorkspaceRead prompt; the public launch returned `spawned`. Pi attempted its runtime helper setup, downloaded `fd`, but could not download `ripgrep` (`fetch failed`) and produced no candidate or lifecycle evidence before the bounded session ended. No Provider material was passed through the PTY, and no authority, database, EVAL, or B01-Clean resource was changed. |
| D02 ripgrep dependency recovery | `DEV-LINUX-NATIVE-01` | pass | The missing runtime helper was installed through the host's approved noninteractive package path (`apt-get install -y ripgrep`). No repository, runtime database, SecretStore, Provider material, EVAL asset, or B01-Clean resource was modified. |
| D02 bounded interactive launch attempt 2 | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | not-run / no lifecycle evidence | After the helper recovery, a PTY-bearing SSH session carried only the same non-secret WorkspaceRead prompt and public launch returned `spawned`, but no WorkspaceRead response, candidate, scheduler lease, dispatch, verification, or acceptance appeared within the bounded window. This attempt produces no C1 result and is not retried as a benchmark sample. One final delayed-input diagnostic is permitted solely to distinguish Pi startup timing from a product caller/runtime integration issue. |
| D02 delayed-input public launch diagnostic | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | not-run / runtime integration blocker | A final bounded PTY session waited 30 seconds before sending the same non-secret WorkspaceRead prompt and kept the public launch shell alive. No candidate, tool call, scheduler lease, dispatch, verification, acceptance, or redacted Pi response appeared; the wrapper was terminated at its bounded cleanup boundary. Public daemon stop then returned `stopped` with confirmed stale-lock removal. The exact revision therefore proves daemon readiness and public Pi spawn only; it does not provide the required real Pi candidate caller. No further interactive retries are authorized in this task slice. |
| D02 disposable daemon cleanup | `DEV-LINUX-NATIVE-01` exact `979e52e4c6681d0fc8c6431c965e3267a7a0d917` | pass | Public `cognitive daemon stop --runtime-root /home/wuz/p2-t36-runtime` returned `stopped` and removed the confirmed stale lock for PID `959545`. Its shell reported that PID already absent during the stop operation; no process, B01 guest, EVAL root, SecretStore item, or Provider material was inspected or altered. |
| D02 public non-interactive caller repair | local Git transport / `DEV-WIN-GNU-01` static allowlist | pass | Exact revision `33437b06b69d26d3887d3492996be2a286035769` accepts the public `cognitive pi launch --print` flag, forwards the official `--print` argument to pinned Pi while retaining stdin for a non-secret prompt, and waits for Pi completion. `cargo fmt --all -- --check`, `check-handbook`, generated-handbook byte check, `pnpm run check:consistency`, `git diff --check`, and staged docs-sync all passed. The revision was pushed to `origin/personal/P2-T36-c1-public-production-path`; no Rust compilation occurred on Windows GNU. This implementation and static validation do not prove a real C1 lifecycle. |
| D02 exact-revision rebuild after caller repair | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | pass | The cleanable Git worktree fetched the pushed branch, checked out the exact revision, reported no worktree changes, and `cargo build -p admin-cli -p kernel-server -p pi-agent-adapter --locked` completed successfully. No source copy, private transport, Provider material, EVAL root, or B01 guest was used. |
| D02 public `--print` WorkspaceRead launch | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | not-run / daemon startup fault | After a public stop attempt, public `cognitive daemon start` reported that kernel-server exited before readiness; no endpoint document was published. Consequently public doctor and the bounded stdin-fed `cognitive pi launch --print` invocation both failed closed because the daemon endpoint was absent. No Pi process, Provider request, candidate, scheduler lease, executor dispatch, verification, acceptance, private transport, database edit, EVAL resource, or B01 guest was used. The daemon log was not inspected in this unit to avoid expanding the evidence surface. |
| D02 daemon startup fault diagnosis | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | partial / recoverable environment fault | The redacted daemon startup marker was `bind refused: Address already in use (os error 98)` for the disposable runtime. A subsequent non-mutating socket-owner probe found no listener at `127.0.0.1:48436`, so there is no unknown process to terminate. Retry one public daemon start; do not inspect Provider data or use a private route. |
| D02 public daemon-start retry | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | not-run / repeat startup fault | A second public `cognitive daemon start` again exited before readiness, so doctor and public `--print` launch remain unavailable. No listener was identified before this retry, no mutation beyond the daemon's normal startup attempt occurred, and no C1 lifecycle element started. Inspect the bounded startup diagnostics only to identify a supported recovery; do not retry the conversation path until readiness succeeds. |
| D02 daemon startup diagnostic expansion | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | partial / recoverable environment fault | The bounded startup log contains only normal lock/bootstrap-path/listening entries from earlier daemon starts and the two redacted bind refusals; it contains no Provider material. A non-mutating socket-state probe remains empty for `48436`. The fault is consistent with a short-lived residual socket race after the prior public stop. After waiting beyond that boundary, permit one final public daemon-start retry before declaring this environment fault blocking. |
| D02 alternate-loopback public readiness recovery | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | pass | After the default-port attempts remained unavailable, public `cognitive daemon start --bind 127.0.0.1:48437` started the same disposable runtime and published its dynamic endpoint. Redacted public doctor reported system, database, secret, provider, daemon, and pinned Pi `0.81.1` ready with `first_conversation_ready: true`. This confirms only ready state; it does not prove a Provider conversation, C1, C2, or lifecycle outcome. |
| D02 public `--print` WorkspaceRead conversation | `DEV-LINUX-NATIVE-01` exact `33437b06b69d26d3887d3492996be2a286035769` | partial / extension-tool exposure blocker | The official non-interactive public command completed after consuming its non-secret stdin prompt and returned `action: completed`. Pi reached the daemon-backed conversation route, but reported only its standard `read`, `bash`, `edit`, and `write` tools; it did not expose the required `WorkspaceRead` tool. Therefore no daemon candidate, scheduler lease, executor dispatch, verifier result, or acceptance exists. The public completion action is not a C1 claim. Next recovery is to inspect the extension's supported tool-registration path and add a focused failure-first regression before another real public conversation. |
| D02 native-tool containment recovery | `DEV-LINUX-NATIVE-01` exact `3557c279d86b00987c43edab78710f33c549e9a7` | pass | Public daemon stop returned `stopped`, the cleanable Git worktree fetched and detached at the pushed revision, and `cargo build -p admin-cli -p kernel-server -p pi-agent-adapter --locked` passed. The focused regression `cargo test -p admin-cli --lib personal_cli::pi::tests::launch_preparation_disables_pi_native_tools_and_preserves_print_mode --locked` passed 1/1. The launch plan now uses the configured Extension, `--no-builtin-tools`, and `--print`; this proves argument construction only, not Pi extension loading or a C1 lifecycle. |
| D02 public containment launch attempt | `DEV-LINUX-NATIVE-01` exact `3557c279d86b00987c43edab78710f33c549e9a7` | partial / result not retained | Public daemon start on disposable loopback `127.0.0.1:48438` and redacted doctor both succeeded. A bounded stdin-fed public `cognitive pi launch --print` invocation was started with a non-secret WorkspaceRead prompt and returned host success, but its Pi response was truncated by the controller after the full doctor projection. No lifecycle inference is made from that host result; retain this as a started partial attempt and run one bounded Pi-only inspection to obtain the response before deciding the next recovery. |
| D02 public containment response inspection | `DEV-LINUX-NATIVE-01` exact `3557c279d86b00987c43edab78710f33c549e9a7` | partial / extension registration blocker | A Pi-only bounded public `--print` invocation completed and honestly reported that `WorkspaceRead` was unavailable with no tools available. `--no-builtin-tools` successfully removed native bypass tools, but no daemon-governed extension tools were registered in the active Pi session. No WorkspaceRead candidate, scheduler lease, dispatch, verifier outcome, or acceptance occurred. The next recovery must trace why the explicit Extension lacks its runtime daemon-layout context; do not re-run this unchanged public conversation. |

## Remaining

WorkspaceRead extension registration and adapter extraction are implemented,
the exact Git worktree and cleanable non-B01 Pi configuration are prepared, and
Draft PR [#244](https://github.com/agentkernel/cognitive-os/pull/244) exists.
The independent P2 runtime now has public Provider readiness and passed the
public Pi launch gate, but its old non-interactive child exited before a
conversation. The exact pushed caller repair supplies Pi's official `--print`
mode while keeping the prompt on stdin. The real C1 lifecycle remains not-run:
no candidate validation, scheduler lease, dispatch, verification, or
acceptance was observed. Do not use a private transport, SQLite edit, test
caller, mock authority, manual database write, or any EVAL root/item to bypass
this state. The next action is to rebuild the exact revision and run one bounded
non-secret WorkspaceRead conversation through the public `--print` path, then
record its separate lifecycle evidence before considering WorkspaceSearch.
