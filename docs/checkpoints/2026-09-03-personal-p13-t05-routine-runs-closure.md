# P13-T05 Routine/Trigger arming, occurrence ledger, `runs` + Today — closure

- Task: `P13-T05` **done** / slices `P13-T05/D01` **done**, `P13-T05/D02` **done** (on the PR head the formal plan / PROGRESS read `D01 done`, `D02 in-progress → closing` because the active lease bound `P13-T05/D02`; the lease-closure commit on `main` records `done`, following the P13-T02 / P13-T03 precedent)
- Change class: `implementation-only` (no `core/specs`, no Lane-CTR, additive authority migration v37 `p13_routine_arming` + `p11_routine_occurrence` rebuild with dispatch / outcome columns, no contract or negative weakened, no new first-level chrome, no second scheduler)
- Lease: `lease/personal/P13-T05/routine-runs` → PARALLEL-LANES §3.1 (closed in the closure delivery on `main`)
- Branch / PR: `personal/P13-T05-routine-runs` (worktree `D:\agent-kernel-wt-p13-t05`) → PR [#315](https://github.com/agentkernel/cognitive-os/pull/315)
- Required CI: [33714624207](https://github.com/agentkernel/cognitive-os/actions/runs/33714624207) **SUCCESS** at `ecd35ab0` (resolve 3s, ubuntu 4m7s, windows 11m36s, required-ci 4s) — the live-validated code revision; [33711917159](https://github.com/agentkernel/cognitive-os/actions/runs/33711917159) **SUCCESS** at `bafb3c9c` (D02 head before the live-routing fix); the closure-head run is recorded in the running report and the lease-closure commit
- Running report: [P13-T05 report](2026-09-03-personal-p13-t05-routine-runs-report.md)
- Claim ceiling: `hypothesis`. Linux native + ordinary CI evidence closes "the implementation exists" only (Phase 13 hard gate 6). No Gate / release / Profile / B01 / Windows / Agent-benefit claim. Clock / sleep / restart **host** E2E stays `not-run` until `P13-T13`.
- Validated implementation revision: `ecd35ab0` (store 7/7 + 8/8 + 8/8, kernel-server 24/24 incl. the live-shape routing pin, clippy clean, **live daemon E2E 11/11** on `DEV-LINUX-NATIVE-01`; required CI green). Commits after `ecd35ab0` are documentation / sibling-ledger only.

## 1. Acceptance mapping (formal plan P13-T05 card + Delivery Slices `P13-T05/D01`, `P13-T05/D02`)

| Acceptance item | Implementation | Focused negative(s) | Evidence |
|---|---|---|---|
| Routine/Trigger armed **after G2** from the ③「周期与触发」declaration (cadence `manual` / `interval`, `interval_ms ≥ 1000`, `bounded_context`, `attempt_timeout_ms`), bound to a confirmed plan stage and its seated responsible Member | store `routine_arming.rs` `RoutineArmingStore::arm` (`load_project_gate` requires `active` + `accepted_at`; `load_stage`; `require_responsible_seated`; `RoutineDeclaration::from_body_json`; v37 `p13_routine_arming`), HTTP `POST routine.arm` | store `p13_t05_arming_before_g2_is_refused_and_after_g2_is_armed`, `p13_t05_arming_requires_seated_responsible_member_and_current_revision` (unseated / non-responsible slot / unknown stage / stale revision / cross-project / task-channel / invalid or secret-shaped declaration); HTTP `routine_runs_task_channel_forbidden_arm_before_g2_refused_…` | Linux store 7/7; live L1 (`creating` Project → 409 `ROUTINE_ARM_BEFORE_G2`, task alias 403), L2 (unseated researcher 422, seated manager → `armed`, `armed_after G2`) |
| **Daemon scheduler is the only dispatch authority** (no second scheduler / Temporal) | `routine_runs::run_routine_tick` inside the existing `PeriodicSchedulerWorker` closure (250 ms) reuses P11-T08 `scheduler_entries` rows `task://personal/routine/<occ>`; `acquire_eligible_lease` epoch / owner CAS (`personal-daemon-scheduler`) before any launch; `bind_attempt` is lease-checked; generic dispatch pass skips `task://personal/routine/*` | store `p13_t05_second_scheduler_is_fenced_and_cannot_bind_twice` (second lease holder fenced by CAS, `bind_attempt` under a foreign lease refused, one scheduler row per occurrence, no temporal table) | Linux store; live L3: 7 schedule fires came only from tick reports in `daemon.log`, `scheduler daemon-tick-only` in every `runs` read, `fenced 0` |
| Manual trigger through **Intent** (no Start button) | existing P11-T08 `POST routine.trigger` (`admit_trigger` → `persist_intent` row + `scheduler_entries` upsert); `routine.arm` answers `manual_trigger_path` | HTTP negatives in `p11_t08_routine_trigger_negatives_…`; web `projectRuns.test.tsx` "offers no Start, Approve, Complete, arm, pause, resume or restart control" | live L4 (`routine.trigger` while running → `queued`, still 1 `active`) |
| Occurrence ledger **really produced** by the scheduler driving hosted Attempts: no-overlap, queue-latest, missed, coalesced | `fire_schedule` → `admit_trigger` (active → `queued`, existing queued → `coalesced` + `coalesced_by`); `dispatchable_occurrences` → `launch_hosted_attempt` (P13-T02 persist-before-dispatch, real product child) → `bind_attempt`; `in_flight_occurrences` → `record_attempt_terminal` → `promote_queued` | store `p13_t05_overlap_never_dispatches_twice_and_queue_latest_promotes`; HTTP `routine_tick_drives_a_hosted_attempt_and_writes_the_occurrence_ledger` (real `node` children) | live L3: max 1 `active` per Routine in 38 snapshots, 4 → 6 distinct Attempts bound, `queued` seen, 3 `coalesced` rows with `coalesced_by`, `reconciled: 1, dispatched: 1, promoted: 1` ticks; L9 sqlite cross-check |
| Silent drop forbidden: missed facts carry reasons | `fire_schedule` with host unavailable → `missed` + `host-unavailable:<reason>`; `mark_not_armed` → `missed / not-armed`; `record_attempt_terminal("spawn-failed")` on refusals; `host_dispatch_availability` from P11-T02 `p11_windows_host_daemon` / offline segments (`close-paused`, `offline:<cause>`) | store `p13_t05_silent_drop_is_forbidden_missed_facts_carry_host_and_arming_reasons` | Linux store; live L5 (`missed/not-armed`, `missed/host-unavailable`, no Attempt for an un-armed Routine); host-derived reasons live **not-run** (Linux root cannot `home.admit`; P13-T13) |
| **Checkpoint ≠ completion; process exit ≠ cancel / complete** | `record_checkpoint(complete=true)` refused (P11-T08); `ROUTINE_ATTEMPT_OUTCOMES` has no `success`; schema CHECK `completion_claimed = 0`; outcome = daemon-observed Attempt terminal (`done` / `failed` / `blocked` / `timed-out` / `signaled` / `spawn-failed` / `unknown-outcome` / `unknown`), `verification_status = not-run` | store `p13_t05_checkpoint_and_process_exit_are_not_completion` (incl. schema CHECK refusals of `completion_claimed=1` and `attempt_outcome='success'`; `exited/0` → `unknown`, never done) | live L3 / L9: 6 terminals `exited/6/failed` → `attempted/failed`, `completion_claimed false`, `verification_status not-run` on every row and Attempt |
| Safe-point **continue / pause / restart never inject a running prompt** | `apply_instruction`: supersede arming → new arming (`armed` / `paused`), `restart` queues a new-revision occurrence through `admit_trigger` (`instruction-restart`); the active occurrence and its Attempt row are never written; `resume_arming` only from `paused` | store `p13_t05_instruction_applies_at_safe_point_and_never_injects_running_prompt`; HTTP tick test (pause leaves the running Attempt's `context_digest` untouched; third occurrence `waiting-paused`; resume dispatches) | live L6 (`running_prompt_injected false`, `context_digest` identical, fires 7 → 7, `waiting-paused`), L7 (resume → running), L8 (stale revision 409, restart → `queued` revision-2 occurrence behind the still-running one, armings history 1 arm / 2 pause / 3 resume / 4 restart) |
| Close-window background-or-pause ↔ offline / missed / resume facts | `host_dispatch_availability` reads P11-T02 daemon state (`paused` → `close-paused`; `offline` / `recovering` → open offline segment cause); tick `waiting` while unavailable; `runs` reports `host {available, reason}` and `dispatch_state waiting-host`; `routine.resume` (P11-T08) resumes internal missed occurrences | store silent-drop test (`host-unavailable:close-paused`, `offline:sleep`, internal missed resumes) | Linux store; live `host {available true}`; host-state cells **not-run** on Linux (P13-T13) |
| `runs` shows the **real occurrence ledger + Attempt history** | HTTP `GET routine.runs` (armings, ledger with derived `dispatch_state`, host, summary, `attempt_history_path` → `dsh.hosted.attempt.list` / `.detail`); web `routineRuns.ts` projection + `loadRoutineRuns` / `loadAttemptHistory`; `ProjectRunsPage.tsx` = ledger summary + armed Routines + occurrence table + Attempt history + P12-T03 axis | web `projectRuns.test.tsx` (ledger facts rendered; Attempt receipts never completion; no Start / Approve / Complete / arm / pause / resume / restart control; honest empty; denied ledger not converted to empty) | web 60 files / 454 tests; live L9 (6 Attempts listed = 6 bound, `receipt_is_not_completion`, 8 redacted frames) |
| Today: **one row per live Project** (status / 今日完成次数 / current stage / duration) + created / live / blocked counts + period switch; packets collapse with the overview retained; **no KPI wall** | store `today_overview` (`today` / `week` / `month`, UTC; `creating` → created, `active` → live, `attention` / `paused` → blocked; per-live-Project row from ledger + Attempt facts); HTTP `GET today.overview`; web `todayOverview.ts` + `TodayPage.tsx` (period switch `aria-pressed`, counts, rows with `Runs` deep link, packet `<details>` collapsed when nothing pending) | store `p13_t05_today_overview_counts_live_projects_and_periods`; web `todayOverview.test.tsx` (row per live Project; collapse / open with overview retained; `?period=week` refetch with no POST; creating-only home never fetches; missing counts → `unknown` never 0) | live L10 (`created 1 / live 1 / blocked 0`, row `paused`, `attempts_total 6`, `failed 6`, `stage s1 / Manage`, `duration_ms 103 956`; `week` / `month` 200; `year` 422; `kpi_wall false`, `cost unknown`) |
| Task-channel aliases 403; secret-shaped declaration refused; routes match the **live** request line | `parse_route` exact match on the trimmed path part; `handle_routine_runs_route` validates the bearer before the 403 | HTTP `routine_runs_routes_match_the_live_request_line_shape` (failure-first pin of the `bafb3c9c` defect), task-channel assertions | live L1; the run-1 defect (every route fell through to the channel echo) is recorded in the report and fixed at `ecd35ab0` |

关闭门 (plan.md card), sentence by sentence: (1) `runs` shows the real occurrence ledger + Attempt history — **true** (live L3 / L9 + web tests); (2) Routine armed from the ③ declaration after G2 — **true** (live L1 / L2, store negatives); (3) manual trigger through Intent — **true** (live L4, P11-T08 path, no Start); (4) safe-point continue / pause / restart — **true** (live L6 / L7 / L8 + store test; `continue` covered by the store test and by the live 409 for a stale revision on `apply:continue`); (5) Today overview rows + counts + period switch — **true** (live L10 + web tests). The G2 fixture behind (2) is seeded by a scratch crate because no product HTTP / CLI path applies a PlanRevision, records a stage test or reaches acceptance yet — that gap was **recorded, not resolved, in this scope** (owned by `P13-T04` stage test / last-ring acceptance and `P13-T06` `@manager` plan revision; see the report's scope decision and PROGRESS Layer 2).

Drift negatives from the card, all refused / never produced: second scheduler (only the daemon tick dispatches; foreign lease fenced), overlap (max 1 `active` per Routine, live and in tests), silent drop (`missed` rows with reasons; `not_armed` tick count), checkpoint-as-completion (refused; schema CHECK), running prompt injection (`context_digest` untouched under pause / restart), process exit = cancel / complete (`exited/0` → `unknown`, `exited/6` → `failed`, never `done` without `response done`, never completion), KPI wall (`kpi_wall false`, `cost unknown`), fake Start (no such control; `manual_trigger_path` points at the Intent route).

## 2. Validation summary

| Environment | Result |
|---|---|
| `DEV-WIN-GNU-01` | `cargo fmt --all -- --check`, `pnpm -r build`, `pnpm -r test`, `clients/pc/web` `pnpm test` (60 files / 454 tests) + `pnpm build`, `check:consistency`, `check:rules`, handbook check set, docs-sync gate, `git diff --check` — **pass**; cargo build/test/clippy **not-run** (`RUST-LINK-DEV-WIN-GNU-01`) |
| `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | [33706254500](https://github.com/agentkernel/cognitive-os/actions/runs/33706254500) **fail** at `f61d9b63` (E0631, fixed); [33707129052](https://github.com/agentkernel/cognitive-os/actions/runs/33707129052) **fail** at `7491a574` (`routine.arm` prefix routing + a pre-existing 1 ms TTL clock race, both fixed); [33711917159](https://github.com/agentkernel/cognitive-os/actions/runs/33711917159) **SUCCESS** at `bafb3c9c`; [33714624207](https://github.com/agentkernel/cognitive-os/actions/runs/33714624207) **SUCCESS** at `ecd35ab0` (live-validated revision) |
| `DEV-LINUX-NATIVE-01` (exact pushed revisions, cleanable worktree `~/cognitiveos-personal-worktrees/p13-t05-<sha>`, disposable runtime roots) | at `f61d9b63` / `bafb3c9c` / `ecd35ab0`: store `p13_t05_routine_arming` 7/7 + `p1_t01_layout_migrations` 8/8 + `p11_t08_routine` 8/8; at `bafb3c9c` / `ecd35ab0`: kernel-server `routine_runs` + `hosted_dsh_attempt` + `project_aggregate` 23/23 → 24/24 (real `node` children), clippy `-D warnings` clean, binaries built; **live daemon E2E** at `bafb3c9c` **fail** 4/11 (routing defect: every P13-T05 route fell through to the generic channel echo on the live request-line shape) → fixed → at `ecd35ab0` **pass 11/11**: arm after G2 → 250 ms daemon tick fires the 10 s interval 7× → Intent → 6 real hosted Attempts (product child → pinned dsh `528c682e` → daemon proxy → Provider unbound → honest `exited/6 failed`) → ledger with no-overlap / queue-latest / 3 coalesced / 2 missed (`not-armed`, `host-unavailable`) → pause / resume / restart at safe points with the running Attempt's `context_digest` untouched → `runs` + `attempt.list` (6 = 6) + Today (`created 1 / live 1`, row `paused`, `attempts_total 6`, stage `s1`) → 0 secret hits, 0 orphan children, 0 residue |
| `DEV-WINDOWS-NATIVE-OPC-01` | **not-run** (not provisioned); clock / sleep / restart host E2E and the P11-T02 host-state-derived missed reasons (`close-paused`, `offline:<cause>`) are backfilled by `P13-T13/D02`; Linux native evidence does not transfer |
| `B01-Desktop-Linux-002` | **not-run** (optional per PERSONAL-TEST-ENVIRONMENTS §7 / §10; not needed for this card; no guest `/ui/` deploy in this task) |

Cells that stay **not-run** by design (A7 / Phase 13 hard gate 6): Windows clock / sleep / restart host E2E; daemon-derived `host-unavailable:close-paused` / `offline:*` missed reasons on a live host (store-tested only); a live Provider **`done`** leg for a Routine-driven Attempt (no new SecretStore entry was created — owner boundary; the loop is closed with the honest `failed` terminal); `DEV-WIN-GNU-01` cargo (`RUST-LINK-DEV-WIN-GNU-01`); rendered browser review of `runs` / Today on daemon `/ui/` (that is `P13-T12/D02`).

## 3. Non-claims

Not P13-T04 (CAS / independent verifier / `outputs`), not P13-T06 group chat, not P13-T11 reflection, not P13-T13 Windows qualification. No Inbox L1, no second scheduler / Temporal, no Start / Approve / Complete button, no KPI wall, no completion claim anywhere (`completion_claimed = false`, `verification_status = not-run` on every occurrence and Attempt). No Gate / release / Profile / B01 / Windows / Agent-benefit / conversation-quality claim. The PlanRevision-apply / stage-test / G2-acceptance product HTTP gap remains open and owned by P13-T04 / P13-T06.

## 4. Cleanup

Linux: E2E daemon stopped, runtime roots `…/p13-t05-{bafb3c9c,ecd35ab0}-runtime` removed; worktree `~/cognitiveos-personal-worktrees/p13-t05-ecd35ab0`, scratch seed `~/cos-wt/p13t05-seed` and logs `~/cos-wt/p13t05-*` removed in the closure delivery (see the report's final row); other tasks' daemons (48181 / 39245 / 48681) and the pre-existing P8-T11 dsh web session never touched; no baseline / service / credential change. Local: untracked `.remote-*.sh` / `.remote-e2e-*.mjs` driver files deleted before closure; `D:\agent-kernel-wt-p13-t05` removed after merge; local + remote task branches deleted; `main` fast-forwarded to the merge.

## 5. Unique next

Owner-directed pause: after `P13-T04`, `P13-T05`, `P13-T06` and `P0-T01/D02` close, **no new claims**. Ready cards per the Phase 13 build order when the owner resumes: `P13-T07`, `P13-T08`, `P13-T09`, `P13-T10`, `P13-T11` (after T04 / T05). Do not claim `P11-T15`.
