# P13-T04 Independent verification + openable outputs + publication package — closure

- Task: `P13-T04` **done** / slices `P13-T04/D01` **done**, `P13-T04/D02` **done** (on the PR head the formal plan / PROGRESS read `in-progress → closing` because the active lease bound the slices; the lease-closure commit on `main` records `done`, following the P13-T02 / P13-T03 precedent)
- Change class: `implementation-only` (Attempt artifacts enter the existing P3-T03 daemon CAS; a registered independent verifier identity under the P2-T07/P2-T13 verifier seam; StageTestPassed through the existing P11-T03 `derive_stage_test_passed`; `run-acceptance` / `external-send` join the P11-T09 ApprovalPreview subject kinds via the additive v37 authority migration; 11 management HTTP routes + 11 task-channel 403 aliases; `outputs` page on daemon `/ui/`; no `core/specs`, no Lane-CTR, no contract or negative weakened, no new first-level chrome, no chat Confirm, no Publish button)
- Lease: `lease/personal/P13-T04/artifacts-verifier` → PARALLEL-LANES §3.1 (closed in the lease-closure delivery on `main`)
- Branch / PR: `personal/P13-T04-artifacts-verifier` (worktree `D:\agent-kernel-wt-p13-t04`) → PR [#313](https://github.com/agentkernel/cognitive-os/pull/313) (merged SHA recorded in the lease-closure commit)
- Required CI: implementation head [33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112) **SUCCESS** at `84b2467e` (resolve 3s, ubuntu 3m51s, windows 13m21s, required-ci 3s); the closure head run is recorded in the lease-closure commit. Earlier run [33704422347](https://github.com/agentkernel/cognitive-os/actions/runs/33704422347) at `582e0377` **failed** on a real defect (freshness keyed by task ref only) that was fixed as a product rule, not a test-shape change.
- Running report: [P13-T04 report](2026-09-03-personal-p13-t04-artifacts-verifier-report.md) (incremental log, failure-first table, live E2E U0–U15, acceptance mapping)
- Claim ceiling: `hypothesis`. Linux native + ordinary CI evidence closes "the implementation exists" only (Phase 13 hard gate 6). No Gate / release / Profile / B01 / Windows / Agent-benefit claim.
- Validated implementation revision: `84b2467e` (all Rust suites on `DEV-LINUX-NATIVE-01` and required CI; live daemon E2E U0–U15). Commits after it are documentation / ledger only.

## 1. Acceptance mapping (formal plan P13-T04 card + Delivery Slices `P13-T04/D01`, `P13-T04/D02`)

| Acceptance item | Implementation | Focused negative(s) | Evidence |
|---|---|---|---|
| Attempt 产物入 CAS，有 digest / format / 来源 / 新鲜度 | store `attempt_artifacts.rs` `ingest_candidate` → v37 `p13_attempt_artifact` (`cas_ref` sha256, `format`, `source` `hosted-dsh-child:candidate:DeliverableDraft` bound to the source frame, `freshness` per Member); kernel-server `ingest_terminal_run` runs on the P13-T02 broker thread right after `record_terminal`; runtime broker retains `payload_canonical` for candidate frames | store N1/N2 (model text, `response done`, exit 0 ≠ completion), N4 (`file://` / `artifact://` / path refs, unbound payload, observation frame refused), N6 (newer artifact supersedes; another Member's deliverable never supersedes) | Linux store 7/7 + migrations 8/8 at `84b2467e`; CI 33705526112; live U5a–d: two real dsh deliverables in CAS, files re-hash |
| `outputs` select-then-view 显示真实产物并可打开/下载（Personal Home `data/`） | routes `outputs` / `outputs.detail` / `outputs.open` (CAS re-read, digest-checked) / `outputs.export` (copy under `<data_dir>/projects/<project>/outputs/`, never authority); web `projections/outputs.ts` + `ProjectOutputsPage` (listbox → detail; Open from CAS + download; Export labelled not authority) | kernel-server task-channel 403 + forged refs 404; hand-edited copy changes nothing; web tests refuse `publish` | Linux kernel-server 2/2 (real `node` child); web 444/444; live U7 / U8a–c / U14; host file-open **not-run** (`P13-T13`) |
| 独立 verifier 对业务产物出 evidence | `verify_artifact`: `verifier://personal/attempt-artifact` v1 under `principal://personal/independent-verifier`, deterministic criteria, `attempt-response-status` explicitly **not-used**, append-only `p13_artifact_evidence`, report bytes in the same CAS | N5 (verifier-only writer, no caller `passed`, foreign identity refused by CHECK), tampered CAS → `failed`, N8 secret shape refused | Linux store 7/7; live U6, U13a–d (`failed` supersedes `passed`, history append-only) |
| run 验收只在末环且需 StageTestPassed | `derive_stage_test` from seating + current evidence + CAS re-read + terminal Attempt (P11-T03 oracle; no caller booleans); `request_run_acceptance` mints ApprovalPreview `run-acceptance` only on the last stage; `confirm_preview` → `accept_run_locked` → append-only `p13_run_acceptance` | N2/N3: no evidence → no fact; cross-slot refused; intermediate ring refused; stale preview 409; second acceptance 409 | Linux store + kernel-server + `p11_t03` 19/19 + `p11_t09` 11/11; live U9a–d, U10a–h |
| 发布包 = 完整 AUTONOMY packet 画布预览，planned ≠ published，聊天无 Confirm | `publication_packet` (7 AUTONOMY sections, `planned` vs `published:false`, `chat_can_confirm:false`); web packet panel, no Publish button | `published` unrepresentable in the ledger; web fake-action regex refuses `publish` | live U11 |
| external-send 走 T09 ApprovalPreview | `request_external_send` → ApprovalPreview `external-send` → `confirm` → `p13_external_send` `planned` (P11-T14 persist-before-dispatch shape, `connector none-qualified`) | store external-send test; kernel-server real-child test | live U12a–c: `previewed` → `external_send_planned` → `published_any:false`; nothing left the machine |
| 完成 = 独立 verifier + daemon acceptance；产物 = CAS 引用 | Attempt row stays `completion_claimed = 0` / `verification_status = 'not-run'` (v36 CHECK); completion exists only as evidence → fact → acceptance | N1/N2/N5 | live U3 / U4 / U15a (`receipt_is_not_completion:true` on every row) |

关闭门 (plan.md card), sentence by sentence: (1) 真实 Attempt 产物入 CAS 且 `outputs` 可选看/打开 — **true** on `DEV-LINUX-NATIVE-01` at `84b2467e` (real pinned dsh `528c682e` deliverable through the bound daemon Provider proxy → CAS → `outputs` / `open` / `export`; daemon `/ui/` serves the page bundle); the **host** file-open cell stays **not-run** until `P13-T13`. (2) 独立 verifier 出 evidence — **true**. (3) 末环验收需 StageTestPassed — **true**. (4) 发布包画布预览且 planned ≠ published — **true** (rendered-browser review of the canvas is `P13-T12/D02`; bundle presence only here).

漂移检测负例, all refused on the live daemon: 模型文本 / HTTP 200 / `agent_end` 当完成; file-as-authority (forged refs 404, hand-edited copy ignored, tamper → `failed` / 422 / 409); 中间环验收 (422); 无验证即 published (422; `published` unrepresentable); 聊天 Confirm (`chat_can_confirm:false`, confirm only through `/management/project/v1/confirm` with the preview digest); receipt-as-completion; 假 Publish 按钮 (no such control; tests refuse `publish`).

Design finding recorded (not a claim): the real-child CI test at `582e0377` exposed that freshness keyed by (project, task ref) let the researcher's later deliverable supersede the manager's ring-one artifact; the product rule is now per Member (`84b2467e`), pinned by a store assertion.

## 2. Validation summary

| Environment | Result |
|---|---|
| `DEV-WIN-GNU-01` | `cargo fmt --all -- --check`, `pnpm -r build`, `pnpm -r test`, `clients/pc/web` `pnpm test` 444/444 (58 files) + `pnpm build`, `check:consistency`, `check:handbook` (58×2, 9 generated), `generate-handbook --check` (18), `check:rules` (5 known local-only warnings), `git diff --check`, docs-sync gate — **pass**; cargo build/test/clippy **not-run** (`RUST-LINK-DEV-WIN-GNU-01`) |
| `CI-UBUNTU-01` / `CI-WINDOWS-MSVC-01` | [33704422347](https://github.com/agentkernel/cognitive-os/actions/runs/33704422347) **fail** at `582e0377` (real freshness defect, fixed); [33705526112](https://github.com/agentkernel/cognitive-os/actions/runs/33705526112) **SUCCESS** at `84b2467e` (ubuntu 3m51s, windows 13m21s); closure head run recorded in the lease-closure commit |
| `DEV-LINUX-NATIVE-01` (pushed exact revision `84b2467e`, cleanable worktree `~/cognitiveos-personal-worktrees/p13-t04-292dc288`, disposable runtime `~/cos-wt/p13-t04-rt`) | store `p13_t04_attempt_artifacts` 7/7 + `p1_t01_layout_migrations` 8/8 + `p11_t03_project_aggregate` 19/19 + `p11_t09_hitl_canvas` 11/11 + `p13_t02_hosted_dsh_attempt` 9/9; runtime `hosted_dsh_broker` 6/6; kernel-server `attempt_artifacts` 2/2 (real `node` child) + `hosted_dsh_attempt` 2/2 + `project_aggregate` 19/19; clippy `-D warnings` on store / runtime / kernel-server clean; **live daemon E2E** U0–U15 (seeded G1 project + confirmed 2-stage PlanRevision; runtime bound to the host's pre-existing Secret Service entry via `--reuse-existing-secret-binding`, no key created / read / printed; real pinned dsh `528c682e` through the daemon Provider proxy produced two `DeliverableDraft`s; CAS / verifier / stage-test / last-ring acceptance / packet / external-send / tamper / `/ui/` bundle; 0 secret leaks in every response body) |
| `DEV-WINDOWS-NATIVE-OPC-01` | **not-run** (not provisioned); host file-open E2E backfilled by `P13-T13/D02` |
| `B01-Desktop-Linux-002` | **not-run** (optional per PERSONAL-TEST-ENVIRONMENTS §7/§10; guest `/ui/` not touched by this task) |

Caveats recorded honestly: the live driver flagged three units on expectation shape only (U1 read `health` at the top level, U2a expected this module's 403 code on a management bearer where the front-door binding check fires first, U10g expected 422 where the daemon answers 409 conflict) — the probe bodies show refusals in all three; seeding used a scratch crate outside the repo because no product HTTP/CLI path applies a PlanRevision yet (gap owned by `P13-T05` / `P13-T06`) and no product path yet creates a Provider account from an existing Secret Service entry (`P13-T08`).

## 3. Non-claims

Not P13-T05 runs / Routine arming, not P13-T06 group chat, not P13-T08 Settings connections, not P13-T09 lifecycle / export, not P13-T11 reflection. No connector is qualified: `external-send` reaches `planned` and never leaves the machine. No host file-open, no rendered-browser / NVDA / 200% review, no Windows evidence. No Gate / release / Profile / B01 / Agent-benefit claim. Linux live E2E does not transfer to Windows.

## 4. Cleanup

Linux: daemon stopped, port 48784 free, runtime root `~/cos-wt/p13-t04-rt` and seed crate `~/cos-wt/p13t04-seed` removed, no orphan child, `/tmp` residue-free (report stage E); the worktree `~/cognitiveos-personal-worktrees/p13-t04-292dc288` and the `~/cos-wt/p13t04-*` scripts / logs are removed in the lease-closure step; the three unrelated kernel-server processes (`:48181`, `:39245`, `:48681`) were never touched. The pre-existing Secret Service entry was neither created nor deleted. Local: `D:\agent-kernel-wt-p13-t04` removed after merge; task branch deleted; `main` fast-forwarded.

## 5. Unique next

**Owner-directed pause.** After `P13-T04`, `P13-T05`, `P13-T06` and `P0-T01/D02` close, no new task is claimed (owner instruction 2026-09-03). When the owner resumes: ready cards in build order are `P13-T07`, `P13-T08`, `P13-T09`, `P13-T10`, `P13-T11` (after T04 + T05); `P13-T12/D02` after `P13-T04/D02` (done here), `P13-T05/D02`, `P13-T07/D01`, `P13-T08/D02`; do not claim `P11-T15`.
