// @generated by contracts-codegen v0.2.1. DO NOT EDIT.
// source: core/specs/schemas/task-admit-request.schema.json
// schema_digest: sha256:c7fa6c8fb1baa16fc0235a2456d64da0ee3b2984bb676b203e8233345ad52e27 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Digest, UriRef } from "./common-defs.js";

import type { UuidV7 } from "./object-reference.js";

import type { TaskContractDraft } from "./task-preview-request.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "task-admit-request.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:c7fa6c8fb1baa16fc0235a2456d64da0ee3b2984bb676b203e8233345ad52e27";

export interface TaskAdmitRequestAcceptance {
  accepted_by: UriRef;
  accepted_digest: Digest;
  interpretation_id: UuidV7;
}

export type TaskAdmitRequestSchemaVersion = "cognitiveos.task-admit-request/0.1";

/** Client request for the daemon-owned task.admit operation. The caller returns the server-issued preview digest and explicit interpretation acceptance; the daemon recomputes the draft digest and performs the authoritative acceptance and epoch-CAS checks before any mutation. */
export interface TaskAdmitRequest {
  acceptance: TaskAdmitRequestAcceptance;
  expected_current_epoch: number;
  preview_digest: Digest;
  schema_version: TaskAdmitRequestSchemaVersion;
  task_contract_draft: TaskContractDraft;
}
