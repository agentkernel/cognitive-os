// @generated by contracts-codegen v0.2.1. DO NOT EDIT.
// source: core/specs/schemas/task-preview-request.schema.json
// schema_digest: sha256:683f0f6109630741619d9f8d071d6cdcc3ceea8043a2ab025be486cdc6560cd9 (canonical bytes, domain schema-bundle/0.1)
// policy: docs/adr/0006-code-generation-policy.md
//
// Shape-level bindings: conditionals, const refinements and cross-field
// constraints remain enforced by JSON Schema validation.

import type { Budget, UriRef } from "./common-defs.js";

import type { UuidV7 } from "./object-reference.js";

/** Source schema file name (== its `$id`). */
export const SCHEMA_ID = "task-preview-request.schema.json";

/**
 * Canonical content digest of the source schema (canonical bytes,
 * domain `schema-bundle/0.1` — the schema-bundle manifest per-asset
 * digest); the envelope `schema_digest` pin for this payload.
 */
export const SCHEMA_DIGEST = "sha256:683f0f6109630741619d9f8d071d6cdcc3ceea8043a2ab025be486cdc6560cd9";

export type TaskContractDraftConditionItemKind = "acceptance" | "stop" | "escalation" | "wait" | "constraint";

export interface TaskContractDraftConditionItem {
  description: string;
  id: string;
  kind: TaskContractDraftConditionItemKind;
  verifier_ref?: UriRef;
}

export interface TaskContractDraftScope {
  in_scope: string[];
  out_of_scope: string[];
}

export interface TaskContractDraft {
  allowed_state_domains: string[];
  allowed_tools: UriRef[];
  budget: Budget;
  budget_id: UuidV7;
  conditions: TaskContractDraftConditionItem[];
  deadline: string;
  loop_object_id: UuidV7;
  max_iterations: number;
  max_retries: number;
  objective: string;
  scope: TaskContractDraftScope;
  task_ref: UriRef;
}

export type TaskPreviewRequestSchemaVersion = "cognitiveos.task-preview-request/0.1";

/** Client composition input for the daemon-owned task.preview operation. This is a pre-admission draft, not a TaskContract and not authority state. The authenticated daemon derives governance facts and the contract identity; callers cannot provide a governed header, contract epoch, acceptance fact, or preview digest. */
export interface TaskPreviewRequest {
  schema_version: TaskPreviewRequestSchemaVersion;
  task_contract_draft: TaskContractDraft;
}
